# 直接依赖。版本对齐 2026-09-23 实际运行并通过全量回归的那一组
# （候选 candidate-final-clockfix-20260923T035414Z，994 项通过）。
#
# **这里是两类改动，不是一类**：
#
# 一、三条 == 原本写错，对齐为真话（严格改善）
#     fastapi   ==0.115.12 → ==0.119.0
#     uvicorn   ==0.34.2   → ==0.37.0
#     pydantic  ==2.11.4   → ==2.12.3
#     要害不是"不准"，是**旧的那组 == 从来没人验证过**：今天所有回归都跑在 0.119.0 上，
#     没有任何人在 0.115.12 上跑过。旧文件不是"一把不准的锁"，是一句没人核对过的断言。
#
# 二、另四条从 >= 改成 ==，这是**钉法变更（浮动 → 锁定）**，不是"把谎言改成真话"
#     python-multipart >=0.0.9   → ==0.0.20
#     httpx[http2]     >=0.27.0  → ==0.28.1
#     PyJWT[crypto]    >=2.8.0   → ==2.13.0
#     psycopg[binary]  >=3.1.18  → ==3.3.4
#     本机无影响（装的就是这几版），但**干净环境的解析结果会变**：
#     原先允许装更新的兼容版本，现在只许这一版。谁在干净环境装出与预期不同的版本集时，
#     该看到的是这一段，而不是"没有依赖变更"。
#
# 包名与 extras 一个没增没减；执行这次修改时**本机没有任何安装、升级或降级**。
#
# **新的这组 == 同样没有在干净环境里验证过。** 它忠实描述了本机，
# 不等于"按它在干净环境装出来能跑通全量"。Linux 复现仍然是零。
#
# 这也不是完整的依赖锁：传递依赖（starlette / pydantic-core / anyio / h11 等）没有钉住。
# 完整闭包（21 个）的提案见窗口日志，落地前要在目标平台重算——
# colorama 只在 Windows 出现、tzdata 在 Linux 通常由系统提供，换平台闭包会变。

fastapi==0.119.0
uvicorn[standard]==0.37.0
pydantic==2.12.3
python-multipart==0.0.20
httpx[http2]==0.28.1
PyJWT[crypto]==2.13.0
psycopg[binary]==3.3.4
