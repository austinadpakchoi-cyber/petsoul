# 给 Claude 框架窗口的执行提示词

适用窗口：`claude-20260922-014933-307b`。用户已指定该窗口先搭整体框架，后续模块由用户分配。以下整段可直接发送；不代表已由本任务自动启动 Claude。

```text
你现在开始执行 PetSoul 2.0 的整体框架任务。你的现有窗口 ID 是：
claude-20260922-014933-307b

请继续使用这个 ID 和现有 WINDOW 日志，不另建窗口身份。

项目：E:\petsoul-audit\petsoul
唯一总方案：E:\petsoul-audit\petsoul\docs\product\PETSOUL-2.0-MASTER-PLAN.md（当前 v1.5，新增意图判断可替换边界/Jev 候选验证，保留接待/寻味/交通/地图音符电视）
新增特色专题：E:\petsoul-audit\petsoul\docs\product\FOOD-DISCOVERY-ENGINE.md
交通同行专题：E:\petsoul-audit\petsoul\docs\product\TRANSPORT-COMPANION-SYSTEM.md
注册接待专题：E:\petsoul-audit\petsoul\docs\product\RECEPTION-ONBOARDING-MEMORY.md
意图判断专题：E:\petsoul-audit\petsoul\docs\product\INTENT-DECISION-LAYER.md

这是明确的本地实施任务。我要你先把可以让其他窗口并行接入的整体框架搭好；完成后，我会给不同窗口分配业务模块。不要替我安排其他窗口，也不要让其他 agent 自动领取任务。不要只输出计划或等我再次批准普通本地操作。

一、开始前
1. 读取 AGENTS.md、总方案全文、FOOD-DISCOVERY-ENGINE.md、TRANSPORT-COMPANION-SYSTEM.md、RECEPTION-ONBOARDING-MEMORY.md、INTENT-DECISION-LAYER.md、docs/coordination/WINDOW-START-PROMPT.md，以及全部 WINDOW-*.log.md。若此前按 v1.4 开工，补读 v1.5 第 5.8 节和意图专题：仅新增可替换判断接口/关闭开关/离线样例，不要求 R0 接通 Jev。登记范围/契约增量，不重初始化工程、覆盖在建代码或重置总时钟；保持接待用途隔离和地图音符/电视入口。
2. 核实目录、实际分支/HEAD/git status，不假设文档里的 SHA 永远是最新。现有未提交产品文档不是你的代码，不覆盖或清理。
3. 在 WINDOW-claude-20260922-014933-307b.log.md 追加 START 或 SCOPE_CHANGE；如果仍 WAITING 就更新为框架任务，已在运行则补充新增范围和运行资源。再检查一次最新日志，无冲突再写。
4. 在现有仓库里开发，不创建额外工作副本/worktree，不接触 E:\petsoul-audit\_recover_backup。允许建立 PetJourneyWeb 等正常源码目录和已登记的本地测试运行文件。

二、必须保持的产品前提
- 手机网页，96 小时总预算，目标香港部署；总时钟不能因你启动而重置。
- 首页是共同的家；主人守家、种菜与互偷支持宠物旅行，宠物在家保护菜园。
- 所有宠物都专属，包含原创和真实原型，同一只不能发给多个家庭；宠物永远安全。
- 注册中由 AI 接待角色倾听主人对宠物的交代，再生成可编辑《入住叮嘱》；用户确认内容与用途后才交给宠物，私人倾诉隔离，可跳过/补充/纠错。接待员不是平台管理员；角色名可配置，不用“忘记主人”或资料完整度逼用户多说。
- 接待/通讯预留可替换意图层：多意图、否定、时间与使用限制分开，模型输出不等于许可/已执行。Jev 是候选且默认关闭，中文和完整链路收益待评测；明确按钮不调用它判断，不让新服务阻塞主线。
- 真实商家旅行：国内高德、国外 Google；扫街榜接口仍未确认，不能假装已接入。
- 宠物进店喝咖啡需要可见场景与操作；地图、场景、通讯、照片和动态共用一次到访状态。
- 寻味是新增特色：具体分店+菜品证据，宠物有自己的口味、也记得主人偏好。两种模式分别匹配，品质/口味/本次价值/证据把握分开；虚拟吃饭、AI 图、游戏点赞不得作为真实口碑。
- 交通覆盖飞机/火车/轮船/驾车等统一结构；现实提供运行日/路线/时长，宠物世界使用喵航 Cat222 等稳定独立身份。首版默认核验同日时刻表，驾车为道路预计耗时；真实支持按线路留证，不随机编班次、不压缩车程迁就下一站。
- 交通主界面就是地图：汽车/飞机/火车/轮船沿路线移动，听歌显示车辆旁音符，看剧显示小电视，点击打开对应播放 UI。不要设计舱室式空间页。一起听看需作品版本、媒体锚点和实际播放；交通独立计时，到站保存，驾驶禁视频，外链不能冒充同步。
- 宠物有自己的公开主页，其他宠物能点赞评论；真实玩家/NPC/主人身份要区分，私密通讯保持私密。
- 统一账本和物资，稀有种子/物资连接旅行、家园与后续市场。
- 英雄冒险、公益领养、影视 IP 和文旅都在总方案里；本轮做可扩展接入点，不同时实现整张长期路线图。

三、本轮交付范围：总方案第 7–10 节的 R0
默认 React + TypeScript + Vite，继续使用现有 FastAPI/SQLite 与既有引擎。你可以基于实际代码做常规实现调整，但要留痕；不要为搭框架整体重写后端、iOS 或数据库。

必须完成：
1. 可启动、可构建的 PetJourneyWeb；npm lockfile、版本记录和 README 运行说明。
2. 家 / 旅途 / 星球圈 / 通讯四个主入口，注册/领养/接待/叮嘱确认/店内/寻味详情/宠物主页/收藏等子路由插槽；刷新、前后导航和错误边界可用。旅途地图内预留车辆活动图标和播放器面板，深链接定位同一地图；不增加舱室路由或底部主 Tab。
3. 统一设计 tokens、基础组件、安全区、手机布局和必要动效。至少能看出家园、旅途和店内的体验结构，不能只是后台表格或空文件夹。
4. 统一会话入口、API 客户端、远端状态与错误处理、共享 DTO、显式 fixture/live 模式、地图/媒体/社交适配边界。不让每个模块各写一套 fetch、钱包或世界状态。
5. 一个真实本地 Web→FastAPI 读取链路及错误路径；业务 fixture 有清楚来源。尚未完成真实注册就如实标明，不能用本地假登录冒充完整账号能力。
6. docs/contracts/WEB-CONTRACT-v0.1.md：路由、字段、枚举、权限、错误、幂等、可见性、fixture 样例，与实际代码类型一致。
7. docs/contracts/MODULE-MAP.md：各模块目录、导出/接入方式、共享依赖、消费/提供接口、可写范围、验收命令、可开放状态。后续负责人留空，由我分配。
8. 持久化/版本迁移/任务处理的统一接入方式，保持旧 iOS API 兼容；记录旧路由权限与媒体问题必须覆盖，不能仅保护新入口留下旧入口绕过。
9. 独立本地测试配置、环境变量示例、可公开构建与部署接入说明。配置不能默认连接生产数据或触发真实供应商；密钥不得进入客户端。
10. docs/coordination/FOUNDATION-HANDOFF.md：实际启动命令、版本、端口/PID、验证结果、哪些是 fixture、哪些已接真本地接口、共享文件、已开放模块与剩余依赖。
11. food_discovery 独立模块接入点与 FoodDiscovery 服务边界：定义宠物虚拟探索/主人现实用餐两种模式、偏好/推荐/反馈 DTO、分店和菜品、理由/出处/未知项及数据来源状态。用明确的清淡/浓郁两组 fixture 展示推荐卡差异；私人饮食限制不进入公开动态。这一项只要求框架与示例，不要求此时研发完整评分、抓评论、取得全城菜单或调用付费模型。
12. transport / companion_media 独立模块与 Transport / CompanionMedia 服务边界：真实交通参考、动物世界班次、门到门 JourneyLeg、途中活动、作品/版本/权限能力、播放锚点、加入/控制/退出/参与 DTO。统一冻结时间依据、时区、数据版本、幂等、控制权、资料未知/陈旧、独立回听和播放失败语义。
13. 四类交通显式 fixture（飞/铁/船/驾），通过 VehicleMarker / ActivityBadge / MediaSheet 展示车辆沿地图移动、音符/电视随实际活动出现，点击打开对应音频/视频面板；含无活动、暂停/失败/恢复/到站与驾驶禁视频状态。素材可加载，面板展开/收起时交通继续。测试样例不冒充真实班次或已验收同步。
14. Journey→FoodDiscovery 的时间契约：可行到达、可停留窗口、行程版本；行程变化使旧推荐待复核。主人现实攻略用主人自己的日期/位置，不能套用宠物当前行程。MediaJob 生图与 CompanionSession 听看分开，私密通讯复用原服务。
15. reception 模块与 Reception / MemoryPolicy 接入边界：私有会话草稿、原文有依据的候选、习惯/往事/愿望/推测/私人内容、主人逐项确认和用途、跳过/恢复/保存失败/冲突/更正/撤回。明确确认版本和幂等，私人内容在检索前过滤，不直接拼进宠物提示词。
16. 自己的宠物与领养两组接待 fixture，人物/对话插槽、可编辑便笺与“交给 TA / 只留这里 / 不保存”；确认后的 HomeWelcome 用同一版本体现一个已支持的称呼/细节，未确认和 owner_private 候选不进入投影。R0 只做边界/契约/示例；真实保存、模型理解和删除传播在后续阶段按证据验收。
17. IntentContext / IntentAssessment / DecisionEvidence 与 ActionProposal / ActionOutcome 的最小接入契约（或统一等价命名），复用 OwnerIntentBrain 调用位置；判断与业务执行分离，兼容旧接口。独立 off/shadow/assist 或等价开关，默认禁用新增远端调用；以拒绝拍照、想念不改路、历史+当前约束、愿望非历史、不转达私人部分、判断不可用六类离线样例说明分支。复用已有结构即可，不要求现在接 SDK、跑付费评测或改完旧引擎。

寻味后续实现独立交给我安排的模块窗口，MODULE-MAP 中负责人仍留空。不要将现有 street_rank 的地点分直接改名为餐食品质：它当前包含照片/距离等因素，且本层算分后未真正重新排序。把差异记入交接，本轮不顺便接管所有旧选店业务。Google 返回的少量精选评论不是全量评价；扫街榜/分析权/真实菜品数据未具备时保留显式 fixture 或未知，不能靠生成文案补造。

交通和影音后续也由我分配窗口。本轮记录旧代码差异：transport_schedule 的 WebSearch 名称不代表实际联网；world_simulation 存在按下一站时间压短移动的逻辑；中转节点有插值坐标；原始承运人/班次直接进展示。R0 建好替换/适配边界，不把完整交通重构、全球时刻采集、热门影视接入或版权采购塞进框架阶段。内容/供应商缺失不阻塞本地框架，标明 fixture 与未验证能力。

接待后续负责人同样留给我安排。现有 PetDNA/MemoryRecord 能复用，但不等于已有用途授权；update_pet_dna 会记录记忆，未确认候选不能直接调用。旧 create_pet 会立即开启旅行，网页门面应明确注册/归属→可选接待与确认→入住/生活激活，不能一边接待一边自动把宠物送到外地。保留 iOS 兼容，不在 R0 全面重写存储。接待模型缺失可以做明确引导的便笺填写；语音克隆、深层心理分析和新动作动画不进入本轮框架任务。

建议在总预算内 4–6 小时形成 R0，先发布稳定契约和模块接入点。遇到字体/素材/城市/预算等尚未给定信息，按总方案的可替换默认值继续；不要为这些等待而让整个框架停工。

意图后续负责人仍由我安排。旧 OwnerIntentBrain 和 owner_interaction 后备均有字符取模回应，should_write_memory 会触发整段原文记忆；thought/event/trace 也要按用途审查。仅替换分类器不足以实现隐私隔离，不能把未升级旧规则当高影响动作的安全回退。具体修复留后续模块，本轮记录差异/边界，不抢工扩大范围。真实中文对照/影子运行另按调用范围与预算执行；失败保留明确控件或澄清，不无声换付费供应商。

四、协作边界
框架阶段由你登记并维护必要的共享入口：Web app/shared/package/lockfile、后端装配与新网页路由注册、统一配置/认证入口、迁移登记、契约。业务目录的初始插槽由你建立，稳定后逐项交接释放，不能长期笼统占用整个项目。
模块窗口只能在我分配后工作；你不提前改写他人日志，不创建虚假的领取/完成记录，也不自行给他们派活。
依赖安装和共享配置更改串行进行；数据库/上传路径/端口登记且独立，不停别人的服务。同一工作副本不能靠切分支隔离；不自行 pull/checkout/reset/clean/stash/merge/commit，不 git add .。
本地读写、安装、构建、测试和启动服务已授权，直接执行。生产变更、真实数据、付费调用和公开发布仍按具体授权处理；缺少这些条件不阻止本地框架工作。

五、验证和交接
至少跑 typecheck、build、必要的契约/模块接入检查和浏览器冒烟；修改后端则跑相关 unittest 和对应仓库门禁。不要删测试或隐藏失败来制造通过。
手机尺寸至少覆盖 320/390/430，检查导航、刷新、滚动、安全区及加载/失败状态；模拟尺寸不等于 iPhone/Android 真机证据。
证明两个独立模块能够通过固定导出接入，不需要模块作者重复修改核心 App 文件。至少一个本地真实 API 读取与错误处理有实际证据。
寻味的双模式与 fixture 接入检查只证明契约和展示；未实现筛选/排序、未核实资料或没有实际用餐反馈时，不能报告寻味算法或现实推荐效果已经通过。
交通/影音验收包括地图缩放/平移时音符/电视锚定正确车辆、点击只开对应播放器、活动结束后更新图标、收起面板后交通继续；现实参考与虚构身份、交通时钟与媒体锚点相互分离，寻味能消费到达上下文。测试素材能播放不等于同步、真实班次或公开权限，后续分别验收。
接待验收包括跳过、用途确认、便笺编辑/失败状态、私人候选不进入宠物投影、确认细节在 HomeWelcome 中可见；区分 fixture 演示与真实持久化/检索隔离。不要凭接待员回复“记好了”报告保存成功。
意图验收只核对候选接口、关闭/不可用分支与上述离线样例；没有调用真实 Jev 就明确未验证。高置信输出不能绕过确认/权限；有 fixture 不代表旧调用链已修复或中文准确率已经提升。
完成后报告 FRAMEWORK_READY 仅代表框架；逐项列明真实实现/fixture/未验证。给出文件、命令、结果和证据路径；未提交就写未提交。
在自己的日志追加 HANDOFF 和明确范围的 RELEASE，并在 FOUNDATION-HANDOFF 中列出我现在可分配给其他窗口的模块、可写路径与共享依赖。仍需维护的共享入口要明确保留，不能含糊释放。

现在开始，先留痕，再实施，直到 R0 具备可审查、可运行、可交接的结果。
```
