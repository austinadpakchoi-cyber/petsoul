# R7K · PetSoul Web 0.4.1 手机体验切片状态

窗口：`codex-20260922-131657-petsoul-visual-r7k`。本表按 2026-09-23 当前源码、生成契约、独立 18764 本地 API 和 390×844 浏览器实际页面核对；旧交接与设计图不是完成证明。`已有且可用` 仅表示本轮已在相应环境操作验证，不等于生产部署。接口本身存在不等于页面已接入。

| 用户流程 / 页面 | 审查结论 | 当前证据与明确缺口 | 批次 |
| --- | --- | --- | --- |
| 欢迎 / 三种入口 | 已有且可用（real-local） | 390×844 欢迎片使用用户新版影片、静音与暂停；带自己的宠物、访客看居民、家人邀请直达各有入口。邀请卡只提示从真实专属链接进入，不生成假链接。 | 1 |
| 访客浏览星球与居民列表 | 已有且可用（real-local） | `/world` 无登录读取 `/public/world`；居民数和条目来自实时返回，独立测试领养一位后列表 8→7。生成插画仅为虚构背景和物件，不代替居民身份。 | 1 |
| 访客看居民详情 / 动态 | 已有且可用（real-local） | `/world/residents/:petId` 读取公开档案与真实动态；无 `avatar_url` 时写明“暂无公开照片”，不贴通用猫。当前居民暂无公开动态。 | 1 |
| 基础注册 / 登录 | 已有且可用（real-local） | 独立账号真实注册、退出/登录；注册提交 `entry`，选中居民和邀请在表单有原对象摘要，返回操作保留路径。登录带原链接时恢复选择。 | 1 |
| 预选伙伴 / 邀请恢复及确认 | 已有且可用（real-local，有令牌边界） | 居民预选注册后读取 `pending_adoption`，两次明确点击后才调用原子领养；邀请公开预览→注册→两次点击后才接受。服务端只存邀请编号，不返原令牌：若丢失原链接，仅恢复邀请摘要并提示重新打开，不能凭摘要接受。 | 1 |
| 自己的宠物上传 / 领养 | 已有且可用（real-local） | 首只可带照片上传或领养；已入住家庭管理员能把第二只上传到相同 `household_id`，重走接待/入住。家庭成员无添宠入口，服务端仍判管理员权限；把第二只原创居民领养进已有家尚未接 UI。 | 1 / 2 / 4 |
| 接待对话 / 便笺确认 | 已有且可用（单宠） | 独立 0.4.1 浏览器真实建宠、接待一回合、逐项确认用途、保存成功；慢请求/重试/跨宠隔离仍未证明。 | 1 |
| 默认入住 | 已有且可用（单宠） | 同一 real-local 流程从确认便笺进入入住，点击后到 `/home`，真实快照显示测试宠物与 20 旅费。 | 1 |
| 居住环境选择 | 已有且可用（real-local） | 入住页读取 `/home/place`，仅开放 `open=true` 的海边/城市可选；明确默认香港·中环。隔离测试账号选海边并提交 `habitat`/`pet_id` 后，服务端 `/home` 返回香港·西贡的海边、`chosen=true`。其余环境不冒充开放。 | 1 |
| 家园 / 菜地 / 信箱 | 已有且可用（real-local 读与点击）；写操作待复验 | 实际手机页庭院/屋内可切换，当前宠物照片或明确“暂无照片”；三地块按真实 `crop_key`/stage 展示独立植株，点开选种、收获面板，旅行种子缺货时禁用。信箱未读随切宠变化。种植/收获的 live 写入本切片尚未实测；宠物本体仍是照片肖像而非身份保真的动作角色。 | 2 |
| 家庭与多宠切换 | 已有且可用（real-local 核心） | 真实独立账号同家第二只狗入住；`/households` 选可访问且已入住宠物，`/home?pet_id=` 分键，切换时取消并移除旧远端查询；真实手机页切换后姓名/照片状态/未读/各宠旅费不同，慢请求单测通过。家庭改名/成员权限页和跨账号浏览器实测尚未完成。 | 2 / 4 |
| 旅途地图 / 车 / 音符电视 | 已有且可用（local 部分能力） | 真实本地咖啡馆步行行程从地图进入，到店前定位随时间推进；音符打开已有媒体面板，播放内容明确为测试素材。抽象纸纹仅铺在示意底图下，不改动态线路/投影。TV 没有对应 live 事件；出租车/船等未开放能力不画作已发生。 | 3 |
| 出发建议与宠物决定 | 已有且可用（local 建议/陪同） | `/journey/destinations/plan/suggestions/suggest/depart` 显式带当前 `pet_id`；真实手机页分别提交主人建议（返回 pending）与主人陪同立即出发（实际扣 8 星币）；未验证自主建议最终被 TA 接受，不将 pending 写成出发。 | 3 |
| 到店活动 / 咖啡馆 | 已有且可用（local 活动） | 旅途中 `/visits/:id` 显示在路上并锁活动，到店后 8 秒刷新开锁；真实依次操作坐下、游戏饮品出现、纸质留念卡与居民事件。用户真实照片作为独立肖像层呼吸/移到椅子上；还不是完整身份保真的动态动物身体。无生成自拍时明确“没有照片”，不制造合影。 | 3 |
| 私聊 / 家庭频道 / 待回复 | 已有且可用（local 频道与条件关联） | 私聊/家庭分开；家庭频道读到同一行程的纸质明信片与冒险勋章；`source_event_id` 只有在本宠物真实指南或收藏匹配时才出现跳转，`reply_to` 仅在原消息已载入时引用。延迟回复、权限突变和离线重试的整条浏览器链尚未覆盖。 | 3 |
| 攻略手账 / 明信片关联 | 页面已有，真实内容未产出 | 手账列表/详情、真实地址核验标记、复制文字和生成图状态均已接；本地两次咖啡馆行程没有 guide，页面如实空态。纸质明信片可从家庭频道读到，但没有生成自拍或收藏里的明信片条目；不能称全链路已完成。 | 3 |
| 星球圈 / 收藏 / 寻味 | 部分接入 | 收藏显式按当前 `pet_id`；家庭事件按完全相同的 `source_event_id` 跳到实际小侦探勋章，另一只宠物为空。星球圈当前功能沿用，未完成跨账号实测；寻味 live 推荐 503 NOT_CONFIGURED，显示能力缺口，不回退演示商家。 | 3 |
| 家庭管理 / 邀请 / 称呼 | 已有且可用（local 核心） | `/households/manage` 读取实际成员/权限/两只宠物，服务端管理员才能编辑家名、开关与生成 72 小时共同照顾者链接，原始令牌只在创建响应显示一次；当前宠物称呼真实保存并回读。未实测角色变更/移除成员；付费照片设置只读，不给开启入口。 | 4 |
| 银行卡流水 / 工作 / 证件 | 已有且可用（local 读） | `/life` 用 `/jobs?pet_id` 和 `/credentials?pet_id`；这只猫无工作记录如实空态，三张已签发证件和未获证件来自服务端。星球银行卡明细显示真实 4 星币及 +20/-8/-8，与家园同账户；ref_kind/ref_id 不猜，若存在才显示。切到另一只宠物后旧卡直达被归属校验挡住。 | 4 |
| 爪爪驾校 | 部分接入 | `/life` 连原 `/school`，不重写；背景 `GET /driving?pet_id` 已修多宠 409。报名/考试/历史写链未做多宠 live 联调，不能声称驾校全链可用。 | 4 |
| 主人照片申请 / 结果 / 显式重画（0.4.5） | 已接入；禁供应商真实 HTTP 空态可用，四态由隔离替身验证 | 家园/列车/明确虚构飞行按服务端事实开放；咖啡馆继续原到店活动。列表纯读，`task_id=null` 不排队不假轮询；unknown 独立、只 failed/unknown 能显式重画。丢响应保留同一幂等键和 payload。本人 18766 手机浏览器验证 200/null 与禁用列车；c84a 固定前端副本真实 HTTP + 图片替身复验丢回执同键/同 task，最终回执提示修正后需 root 再捕获最终副本。真实供应商成图、权限突变整条浏览器链未证明。 | 0.4.5 |

## 第一批切片交付 · 2026-09-23 07:10 +08:00

- 已完成：正式访客 `/welcome`→`/world`→公开居民详情；新视频 25 秒竖幅压缩到 576×1024/约 1.14 MB、静音、暂停、减少动态回退。用户真实照片只作当前宠物头像；ImageGen 产物是无动物场景与钥匙/手账/邀请信物件。注册/登录保存预选宠物或邀请直达；领养与加入家庭都在登录后单独二次确认。接待对话、便笺用途确认/跳过、入住公开范围与服务端开放片区选择已接。
- real-local 操作：18764 独立本地库 + 5291 手机页，访客选“小岚”后注册并确认领养，原 `pet_id` 保留且候选从可领养改为不可领养；另一个账号建宠并选海边入住，`/home/place` 返回 `habitat=seaside/chosen=true`；独立家人邀请创建、访客预览、注册后 `pending_invite=pending`，确认加入后角色为 `caregiver`、pending 解除。无付费供应商；不触碰 I 的 E2 账号、库、进程。
- 验证：390×844 浏览器实操和截图见 `PetJourneyWeb/output/playwright/web-041-*`；首批收尾时 `npm run typecheck`、13 文件 78 测试、`npm run build` 通过。前端 build 是本地可运行，不是部署。若原邀请令牌丢失，后端只有邀请摘要，不能免令牌接受；页面明确要求重新打开链接。
- 缺口/下一步：首只宠物闭环可用；第二批多宠、真实身份与菜地视觉见下。邀请失效/权限变化、断网慢请求的整条页面链仍需专门复验；第三/四批旅途关联与经济/证件未完成。

## 第二批切片交付 · 2026-09-23 08:53 +08:00

- 已完成：同一家有多只已入住宠物时，顶部显示真正的当前宠物切换；现有家庭管理员从家园入口上传第二只并沿用接待/入住，`household_id` 由当前 `/households` 给出，服务端再次校验权限。切换后所有已识别的 home 快照消费者使用唯一 `useActiveHome`，`user_id`/`pet_id` 分键并取消旧请求。新入住宠物由用户级 `sessionStorage` 选中，但展示前必须在服务端家庭列表中重新验证。缺照片不再画通用猫。家园与菜地以分层场景、独立作物植株与真实成熟状态呈现；豌豆/萝卜透明植株由内置 ImageGen 生成，番茄沿用现有素材。
- real-local 操作：一只上传真实参考照片的猫与一只无照片的狗住进同一隔离测试家庭；第二只入住时 `/home/place` 返回先前选定的海边，不产生第二个家；在 390×844 浏览器切换，当前猫显示原照片和 20 旅费/1 未读，狗显示“暂无照片”和 0 旅费/0 未读（数值来自各自实际快照，不共享或伪造）。第二只加入后修复驾校背景读取遗漏 `pet_id` 的 409；重新加载和切换时浏览器 console error 0。
- 截图：`PetJourneyWeb/output/playwright/web-041-home-real-pet-390.png`、`web-041-multipet-after-390.png`、`web-041-home-room-second-390.png`；菜地 `web-041-home-pea-ripe-390.png`、`web-041-pea-sheet-390.png`、`web-041-crop-picker-icons-390.png`。首批已有的 `web-041-home-seaside-390.png` 与新图同为 390×844，可看无照片身份与多宠前后；截图是本地运行而非设计稿。
- 验证：`npm run typecheck`、14 文件 80 测试、`npm run build` 均通过；新增慢请求单测证明切宠后旧请求 AbortSignal 触发，旧响应晚到也不覆盖新宠。`npm run contract:check` 当前失败：并行后端改动使 `generated.ts` 与 `docs/contracts/generated/web-contract.schema.json` 落后，生成物由 I 持有，r7k 未手改；需 I 在冻结后重生/核验。该失败不提升成代码通过证明。
- 缺口/下一步：多宠真实页面切换成立，但尚未完成跨账号慢速浏览器操作、权限变化/断网重试整条流程；家园中真实照片目前以肖像层呈现，不能称身份保真动作动画。家人管理、第二只领养居民、驾校写链、旅途/通讯/攻略/明信片与经济证件仍在第三/四批。农场 live 写入本切片未操作。

## 第三批切片交付 · 2026-09-23 11:20 +08:00

- 已完成：出发计划与建议/立即陪同分开；真实地图与正在移动的步行标记、音符对应播放器面板；到店状态开锁、坐下/饮品/纸质卡片/居民事件；家庭频道与私聊语义分开；只把确实存在的手账或收藏链接到相同 `source_event_id`。生成的示意纸纹只铺地图背景，不提供地理信息。未开放交通/媒体不冒充 live。
- real-local：当前猫从 20 星币两次真实咖啡馆步行各扣 8，银行卡余额后来可见 4；建议返回 pending 不扣出发费用。到店留念没有生成自拍，服务端发纸质卡片并产生小侦探勋章，家庭频道按事件链接到该勋章。切另一只狗收藏为空。攻略列表对该咖啡馆行程为空，不假填目的地。
- 手机截图（390×844）：`PetJourneyWeb/output/playwright/web-041-departure-source-aware-390.png`、`web-041-journey-paper-map-390.png`、`web-041-journey-music-sheet-390.png`、`web-041-cafe-seated-390.png`、`web-041-cafe-drink-390.png`、`web-041-cafe-keepsake-no-photo-390.png`、`web-041-family-verified-links-390.png`、`web-041-event-linked-collection-390.png`。
- 验证：15 文件 81 测试、typecheck、build、`contract:check`（111 枚举/213 模型/120 路由）通过；真实页面 390×844 操作，未部署。未证明 TV live 事件、真实地图供应商、手账生成、回复等待/权限变化/离线完整链。当前寻味推荐 503 NOT_CONFIGURED 是后端能力缺口，不回退 fixture。

## 第四批切片交付 · 2026-09-23 11:46 +08:00

- 已完成：家庭档案读取成员/宠物/权限和设置，当前宠物专属称呼可写；管理员可改非付费设置和生成/撤销家庭邀请，付费写实照片开关只读不动。`/life` 接工作列表与证件列表，卡片详情/星球银行卡明细按当前宠物校验归属；保留原驾校并提供入口。
- real-local：隔离账号两只宠物家庭在浏览器显示一致；给当前猫保存“星球家人 / 家人”后重新加载可见。猫的银行余额 4 星币来自服务端，与两次 -8 出行、+20 入住流水一致；服务端 `/jobs` 空列表，页面不编职业。切狗后直达猫的银行卡详情显示“这不是当前宠物的证件”。未测试邀请新令牌实发/成员权限变更，也未测试驾校写链。
- 手机截图：`PetJourneyWeb/output/playwright/web-041-family-overview-real-390.png`、`web-041-family-manage-real-390.png`、`web-041-life-record-real-390.png`、`web-041-bank-ledger-real-390.png`。同为 390×844 实际页面。
- 验证：typecheck、15 文件 81 测试、build、`contract:check`（111/213/120）通过。独立 18764/5291、禁用供应商、本地数据；不是 E2/真实付费/生产。后续按照 `COORD-FRONTEND-PHOTO-20260923` 转入 0.4.5 照片 UI，不继续扩散旧视觉项。

## 0.4.5 照片切片交付 · 2026-09-23 12:52 +08:00

- 已完成：`/photos` 连接 0.4.5 三种申请命令、纯读结果和显式重画；家园、旅途、到店入口连接，咖啡馆仍用到店活动。按用户/宠物隔离未确认申请与展示；响应丢失后保留原键/原内容，确认结论后新拍照才换键；处理/完成不冒充新尝试，`task_id=null` 不列队。玩家文案只说照片状态与下一步。
- 证据：作者 16 文件 88 测试通过；最终针对回执提示 8/8、独立 typecheck、build、contract:check 通过。c84a 较早固定副本真实 HTTP 响应丢失反例 14 项通过，最终回执提示变更仍需其新副本复验。本人 5291→18766 0.4.5（供应商关闭）手机 390×844 实际注册/入住/照片操作，家园与虚构飞行 POST 均 200/null、列表 200 空、列车按钮因不在车上禁用；三张照片页截图见 `PetJourneyWeb/output/playwright/web-045-photo-*-18766-390.png`。
- 缺口：真实付费生图/身份质量、权限变化整条浏览器链、最终版本跨账号断网视觉收口和生产部署未验证；不把替身结果图或旧截图当真实成图。当前 18764 API 仍为本人旧 0.4.1 数据，5291 预览现代理独立 18766；不影响 18763/E2 或 root 的 18767/5295。

## 证据分层（收口口径）

- 设计：用户参考与入口影片只说明视觉方向。
- fixture：旧场景的样板交互；不得称为正式数据。
- real-local：本轮隔离的 `PetJourneyBackend/data/web-r7k-041/`、18764、Vite 5291；访客预选/注册/领养、家人邀请确认、接待与开放片区入住、多宠同家切换、出发与到店活动、家庭频道事件收藏关联、家庭称呼、银行读链已在浏览器操作。慢请求仅隔离单测证明；真实付费生图与生产行为未验证。
- 生产：未部署、未验证。
