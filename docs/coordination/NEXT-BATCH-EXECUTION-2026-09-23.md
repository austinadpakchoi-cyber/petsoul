# PetSoul 下一批续工单

准备时间：2026-09-23，依据截至 04:38 +08:00 的黑板与源码核对。

**状态：用户已在本对话确认向窗口发送续工指令。** 各窗口接单及文件交接以自身最新 START / CLAIM / RELEASE 为准，不据“已发送”推定全部已经开工。文件存在、黑板刷新本身不会唤醒窗口；本文不替任何窗口 RELEASE。

目标：让前端体验、生图可靠性、核心提交修复、独立验收并行推进；不再把所有修复和交接累积到 I。

## 启动次序

1. 先给 I 发送文末的通用续工消息。I 第一项动作是精确交接下列前端和生图范围，然后继续核心修复。
2. 同一条消息可发给 Q、前端、A、B。Q 和 B 可立即在自己的范围开工；A、前端先做不重叠部分，看到 I 明确 RELEASE 后追加 CLAIM 接手相应共享文件。
3. C 已完成当前版本复验，不必为了所有窗口都忙而再跑一遍。I 给出新提交边界版本后，再向 C 发同一条消息做定向复验。
4. E2 保留既有等待安排；本批开发不等 E2。不得重启或改写 18763 的取证环境。

## 按已有窗口 ID 领取

| 窗口 | 本批任务 | 首个可检查交付 |
|---|---|---|
| I：`claude-20260922-014933-307b` | 先交接修改范围，再修最终提交与认知调度 | RELEASE 清单；随后给出一个可复验的小批版本 |
| A：`claude-20260922-234337-4bef` | 接手生图结果未知与重试恢复，维护任务/额度边界 | 超时、崩溃恢复、显式重画的定向回归 |
| B：`claude-20260922-234415-fed3` | 复验第11批的三个调度缺口，交付现有策略的准确接入条件 | 三项各自结论、当前指纹与不可覆盖的输出 |
| C：`claude-20260922-234408-91f2` | I 修复后定向复验提交边界及所在地时区 | 旧提案被拒、正常路径成功；不重复已验模块 |
| Q：`claude-20260922-234631-4d18` | 现在编写缺失的独立故障合同 | 第一批能抓住缺陷的合同，后续按稳定版本关闭 |
| 前端：`codex-20260922-131657-petsoul-visual-r7k` | 完成手机端访客到入住的真实接口闭环 | 独立本地 API 上可复跑的浏览器路径 |

### I：先解除交接阻塞

先停止写入准备移交的具体文件，在自己的日志追加精确 RELEASE、当前指纹、接口注意事项，指明接手窗口；不要等下一次全量回归后才交接。

建议交给前端 r7k：

- `PetJourneyWeb/src/shared/services/types.ts`
- `PetJourneyWeb/src/shared/services/registry.tsx`
- `PetJourneyWeb/src/shared/query/queryClient.ts`
- `PetJourneyWeb/src/shared/session/onboarding.ts`
- `PetJourneyWeb/src/app/router.tsx`、`RootLayout.tsx` 仅限本批入口和恢复流程所需改动。

`src/shared/contracts/generated.ts` 仍由 I 按生成器维护，前端不手改生成契约。前端按已核验的后端接口实现适配，不自己发明响应结构。

建议交给 A：

- `PetJourneyBackend/app/web_journey/illustrations.py`
- `PetJourneyBackend/app/web_providers/images.py`
- `PetJourneyBackend/tests/test_web_illustration_settlement.py`

A 原持有的任务、租约、额度、计量实现范围保持原归属。其他消费者、schema、迁移、组合根不随上述三文件自动转移；A 如需配合，应给出最小接口改动，由 I 处理或明确再交接。

I 继续独占核心装配文件，避免 A/B/C 同时修改 `brain_life.py`、`brain_wiring.py`、`runtime_view.py`、旅程 `service.py`、UoW、ticker 与迁移：

1. 修 CR-C1 / CR-A4：外部解析在事务外；最终短写事务用同一个连接校验授权、提案版本、此刻有效期、领域条件和适用租约。出发、留家都覆盖；不要只测 UoW 辅助函数。
2. 接入 B 最新三项：shadow 成功记录尝试/复查但不伪造世界决定；区分硬性 `not_before` 与只读 `check_at`；异常路径在可能调用模型之前占用本轮名额并进入退避。
3. 接入 C 的 `timezone_of`，让在外地的宠物使用实际所在地钟点。
4. 按“最终提交”“调度”“前端契约配合”等小批交付路径、指纹、测试与未解决项；只暂停修改正在复验的小批，不要求整个仓库等待。

### A：生图未知结果与恢复

I RELEASE 前，只读核对并在原范围准备必要的任务/额度接口；RELEASE 后 CLAIM 上述三文件并实施。

- 已发送的调用按实际结果和单位记账。`unknown` 不能展示成确认“没画成”；让 API 消费者能区分结果未确认与明确失败。
- 超时不自动重发。注入“展示状态写入后、任务终态写入前”退出，再恢复任务，检查不会再次调用替身供应商；如需队列事务接口，使用 A 原持有范围完成。
- 用户显式重画才创建新的尝试；上一笔 unknown 记录保留，不能回写成未发送。
- 保持 A1 部分发送计量正确、A5 重画两次写入共同提交/回滚；用故障注入检查，不能仅断言传了 `conn`。
- 测试用一次性库和假供应商，保留当前失败与修复后的版本证据。旧历史失败原因未明不需要再循环写报告。

### B：三项定向复验，不等待 E2

I 已登记第11批收口，可以基于实际磁盘指纹跑受影响的三个探针分支：shadow 成功、额度硬恢复时间、异常上限。运行前后记录指纹；若相关文件变化，只把受影响结果列为待复验。

已有 `RetryPlan(not_before, check_at)` 可直接交接，不另造一套退避常量。明确 I 应保存哪些字段、复查时如何重读额度；未获得核心文件 RELEASE 不修改它们。

先保存旧脚本与输出，使用带时间的结果文件。完成三项后等待对应修复，不反复重跑已通过的旧套件，也不持续轮询刷日志。

### C：等待具体新版本属于合理依赖

当前 `brain_life=35ba8843… / service=aa57cb2c…` 已有反例，无需再跑45项证明同一缺口。

I 新批交付后，只验证 resolve 后的撤权/DNA/活动/过期、stayed 的版本复核、正常路径成功和所在地时区注入。保留“模型耗时已修”和“解析期间仍需复核”的区别；冻结旧证据，避免覆盖 `evidence.json`。不新增付费 DNA 对照，不扩展可选“明确延后”字段。

### Q：先写合同，再按交付版本验收

立即在原领取范围编写缺失合同。需要新文件时先登记精确路径并查冲突，不把用例继续塞进已接近定义数上限的文件。

第一批优先：Q-C10 生图未知/恢复，Q-C12 最终提交（含正式撤权接口、stayed、正常对照），Q-C14 实际业务入口租约，Q-C16～C18 调度。后续补 C11 同事务回滚、C13 真实装配的编号/调用次数/账本恢复、C15 部分发送计量。

缺陷反例可显式运行并如实 FAIL，不提前进入已验收集合；修复交付时纳入必跑回归。已有15条合同全绿不覆盖这批新增项。C3d 当前已按实际执行者判定，不重复修改。

### 前端：交付一条完整手机体验

保持用户已明确的视觉连续性，在自己的 feature 范围继续开发。I RELEASE 后接共享层，完成：

访客浏览公开居民 → 选择伙伴/上传自己的宠物 → 注册或登录 → 恢复此前选择/邀请 → 接待叮嘱 → 入住 → 查看自己的家和宠物。

接正式接口，验证刷新恢复、登录态变化、资源归属和必要的异常状态。保留未知结果语义；测试素材不能冒充用户专属宠物。

沿用本窗口已登记的 18764 / 5291 和独立数据目录，不碰18763。交付手机尺寸浏览器证据，区分假供应商本地联调与真实供应商/线上证明。暂不把地图、农场、集市等全部页面同时纳入这批。

## 共用执行边界

- 沿用原窗口 ID；读 AGENTS 和各日志的相关最新记录，再记 START。当前分配以用户发送本续工单为准；未 RELEASE 的他人文件不能接管。
- 普通已授权本地开发、一次性数据库和隔离测试直接做；不新增真实付费、生产写入或公开发布。
- 后端测试使用 `TZ=UTC`；证据保留完整输出和运行前后指纹。模块测试、独立验收、本地自然时间、线上证明分开。
- 18763/E2保持原安排；前端、黑板及其他窗口服务不能擅自停止。不新开工作副本或worktree，不整仓Git操作。
- 一次领取、一次实质交付即可，不要求逐句“已读确认”。有重叠只停重叠部分。无可独立推进事项就明确等待具体交付，别为保持活跃重复测试。

## 用户可发送给窗口的通用续工消息

```text
继续 PetSoul 下一批执行，不停在已读确认或上一轮总结。
仓库：E:/petsoul-audit/petsoul。
先读 AGENTS.md、最新协作日志，以及 docs/coordination/NEXT-BATCH-EXECUTION-2026-09-23.md，按你已有窗口 ID 执行对应任务。
I 请首先完成文档所列前端与生图范围的明确交接，再修核心提交和调度；Q 现在开始写新增反例，不等全部修复；其他窗口按各自任务与交接条件推进。
共享文件必须原持有人明确 RELEASE、接手方 CLAIM 后才写；不重启18763/E2、不新增付费、不部署。不要重复已完成的报告或旧测试；交付实际代码/定向证据，并分批写回自己的黑板日志。
```

此消息只需在需要启动下一轮的现有窗口发送一次。黑板用于共享结果，不充当自动消息队列。
