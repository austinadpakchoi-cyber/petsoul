# WINDOW-claude-20260922-234631-4d18

## START / WAITING

时间（含时区）：2026-09-22 23:49 +08:00（China Standard Time；窗口启动于 23:46:31 +08:00）
窗口ID / 任务ID / 事件：claude-20260922-234631-4d18 / 未分配 / START（WAITING）
用户任务：收到通用启动提示词（含 2026-09-22 家庭共同照顾 / 多宠 / 访客修订），尚未分配具体任务。按启动规则第 3 条只阅读和登记，不改业务代码。本窗口是新会话，使用新 ID，不沿用 307b 的身份，也不是同一时段启动的 4bef、91f2、fed3。
实际目录 / 分支 / HEAD：E:\petsoul-audit\petsoul / codex/petsoul-web-integration / 980feabc7710462a89c5df488c255d04e9e7de08（只做了只读核对，没有 pull、切分支、暂存或提交）
开工时已有改动（START 必填）：git status --short（23:46:31 与 23:48:33 两次结果一致）共 74 行。
  - 已修改 5 个：AGENTS.md、PetJourneyBackend/app/{config.py, main.py, repositories/base.py, schemas/base.py}。
  - 未跟踪 69 个路径：
    · PetJourneyBackend/app 下 30 项：companion_media、food_discovery、intent_layer、reception、routers/web、schemas/web、transport_world、web_adapters、web_agent、web_collection、web_communicator、web_credentials、web_driving、web_economy、web_farm、web_home、web_household、web_identity、web_journey、web_market、web_pets、web_platform、web_providers、web_residents、web_social、web_transport，以及 web_agent_wiring.py、web_composition.py、web_credentials_wiring.py、web_worker.py；
    · PetJourneyBackend/tests 下 29 项：24 个 test_web_*.py，以及 web_base.py、web_provider_fakes.py、driving_bots.py、school_helpers.py、web_evals/；
    · PetJourneyWeb/、deploy/web/、docs/{contracts,coordination,product}/；
    · scripts/ 下 gen_driving_fixtures.py、gen_web_contract.py、real_acceptance.py、real_integration.py、seed_web_demo.py。
  - 以上都是其他窗口此前的未提交成果（主要是 307b，另有 r7k、c84a 等），不算本窗口成果。本窗口不恢复、覆盖、删除、暂存或提交这些改动。
  - 注：307b 在 21:05 START 记录的已修改文件是 4 个，现在多了 app/repositories/base.py。该文件在 307b 登记的 app/** 范围内，归属以该窗口记录为准，本窗口不作判断。
拟修改或实际修改范围：只新建并追加本日志 docs/coordination/WINDOW-claude-20260922-234631-4d18.log.md。创建前确认文件不存在，写入工具不会覆盖已有文件。不改业务代码、测试、迁移、配置、依赖清单、契约、产品或协调文档，也不改其他窗口的日志。
运行资源（无则写无）：无。本窗口没有启动进程、占用端口、读写数据库、调用供应商或访问生产。
  只读查看到的监听端口（23:47）如下，均不属于本窗口。本窗口不启停这些服务，不访问其业务接口，也不读写其数据目录（data/web-mvp-claude、data/web-real-acceptance）：
  - 5287（PID 40696）、5288（PID 3656）、5289（PID 48640）；
  - 18761（PID 4724）；
  - 18763（PID 37996）。307b 在 22:59 记录的 API PID 是 33064，两者不同，以该窗口记录为准，本窗口不核实也不处理。
冲突检查结果：
  - 已读：AGENTS.md、CLAUDE.md、WINDOW-START-PROMPT.md、总方案 v1.5 全文（含“2026-09-22 修订”）、BACKEND-PARALLEL-WORK-PACKAGES-2026-09-22.md、RUNTIME-PARALLEL-CONTRACT-v0.1.md（仍是候选接口，集成窗口尚未接受）。WEB-CONTRACT-v0.2 与 MODULE-MAP 尚未精读，分配任务后按相关条目阅读。
  - 窗口日志共 9 份：
    · 4bef、91f2、fed3，以及 parallel-brain / infra / qa，已读全文；
    · 307b、r7k、c84a 读了全部记录标题与“是否释放”行，并精读各任务的最新记录（307b：13:58 局部 RELEASE，以及 driving-school-v1 到文末；r7k：第 118 行到文末；c84a：第 397 行到文末）。
  - 仍未释放的范围：claude-20260922-014933-307b 的 real-experience-households（21:05 START，最新 PROGRESS 实际写于 22:59）。
    · 登记的文件：PetJourneyBackend/app/**、tests/**、scripts/**、PetJourneyWeb/scripts/dev-backend.mjs、src/shared/**、src/fixtures/**（仅类型适配）、AGENTS.md、WINDOW-START-PROMPT.md、总方案（只追加）、docs/contracts/**、FRONTEND-HANDOFF.md、BACKEND-REAL-EXPERIENCE-ACCEPTANCE.md、MVP-HANDOFF.md、deploy/web/**；
    · 13:58 RELEASE 第四部分保留的共享入口：前端 shared 下 api、config、contracts、services、session、query、time、map、media、memory、modules、slots、journey，app 下 App、router、modules、platform.module、RouteErrorPage，main.tsx、fixtures/world.ts，vite / tsconfig / package.json 与锁文件 / .env.example，后端全部 web 范围和 gen_web_contract.py；
    · 运行服务 18761、18763（含任务进程）、5289。
  - 并行工作包 I/A/B/C/Q 目前只是候选。要先由 307b 写 SCOPE_CHANGE 并部分 RELEASE，再由用户分配，才能生效。截至本条，307b 的日志里还没有这类释放，所以 A/B/C/Q 的文件现在都不能领取。18763 的自然时间验收要持续到北京时间次日 01:04，不能干扰。
  - codex-r7k：文件物理末尾是 15:34 的 unified-life-ui-photo-cat START，但同一任务的 15:53 HANDOFF/RELEASE 写在第 133 行，排在 START 之前，按内容这项任务已经释放。307b（21:05）和 parallel 窗口仍把 r7k 记为“未释放”。本窗口不据此接管任何前端范围，需要时请用户确认。
  - codex-c84a 与 parallel-brain / infra / qa：最新记录均已 RELEASE。
  - 新窗口 4bef（23:43:37 启动）、91f2（23:44:08 启动）、fed3（23:44:15 启动）都是 START/WAITING，只持有各自的日志。
  - 结论：本窗口只持有本日志，与以上任何范围都没有重叠。
进展 / 决策依据：没有收到具体任务，按启动规则记 WAITING，只阅读，不改业务代码。
验证命令 / 结果 / 证据路径：
  - git rev-parse --abbrev-ref HEAD → codex/petsoul-web-integration；
  - git rev-parse HEAD → 980feabc7710462a89c5df488c255d04e9e7de08；
  - git status --short → 5 个已修改、69 个未跟踪；
  - 创建前用 Test-Path 和 test -e 确认本日志不存在；
  - 用 Get-NetTCPConnection 只读查看监听端口，结果见上。
未完成 / 依赖 / 下一步：等待用户分配任务。收到后先重读最新日志，确认所需的精确文件已由持有窗口明确释放，并且用户已经分配，再追加 START/SCOPE_CHANGE（写明 package、接口版本、精确文件和运行资源），然后开工。
是否释放（是/否，明确范围）：否（只持有本日志文件）。

## PROGRESS — 登记后复核

时间（含时区）：2026-09-22 23:49 +08:00（复核时刻 23:49:20）
窗口ID / 任务ID / 事件：claude-20260922-234631-4d18 / 未分配 / PROGRESS（WAITING，登记后复核）
进展 / 决策依据：
  - 登记后复读全部窗口日志，有一处新变化：91f2 在 23:49 追加了“START — package C（DNA 与有限决策）”，状态为 CLAIMED、等待 I 释放，写源码与测试这一部分 BLOCKED。它登记的精确范围是：新目录 PetJourneyBackend/app/web_agent/decision/**，新文件 tests/test_web_decision_context.py、tests/test_web_decision_proposal.py，以及它自己的日志。本窗口以后若领取任务，要避开这些路径。
  - 4bef、fed3 仍是 WAITING；307b 日志仍停在 22:59（101129 字节），没有 SCOPE_CHANGE 或部分 RELEASE；r7k、c84a、parallel-brain / infra / qa 没有变化。
  - 另外看到 docs/coordination/BACKEND-REAL-EXPERIENCE-ACCEPTANCE.md 与 FRONTEND-HANDOFF.md 的修改时间是 23:41:37，晚于 307b 最后一条日志。两者都在 307b 的登记范围内，说明该窗口仍在写入。本窗口只记录，不读写这两份文件的写入状态，也不作判断。
  - 冲突检查结论不变：本窗口只持有本日志，继续 WAITING。
运行资源（无则写无）：无。
是否释放（是/否，明确范围）：否（只持有本日志文件）。

## START — package Q（独立验收）；写入仓库 scripts/ 与 tests/ 的部分等待 I 释放

时间（含时区）：2026-09-22 23:52 +08:00
窗口ID / 任务ID / 事件：claude-20260922-234631-4d18 / pkg-Q-acceptance / START（CLAIMED）＋ BLOCKED（仅限往仓库 scripts/ 与 PetJourneyBackend/tests/ 写文件这一部分）
用户任务：负责工作包 Q，独立于业务实现做验收。
  - 18763 的真实验收由原窗口管理；本窗口不再发起付费请求，不读取账号口令或密钥文件。
  - 先做证据判定器：只读明确允许的去敏证据字段，返回 PASS / FAIL / PENDING / UNPROVEN / BLOCKED 和进程退出码。模型验证要求 composed_by=model；工资、家庭消息、扣款分别核对“恰好一次”；未到期不判成功也不判失败；缺工作实例或事件关联字段时返回 UNPROVEN，不用账户收入总数或消息文案代替关联证据。
  - 找出验收脚本里只输出观察值、没有 assert 的路径，把业务验收条件变成明确判定；不改正在运行的原脚本。
  - 再用隔离 SQLite、新账号、禁网模型替身做合同回归：过期任务接管、旧结果拒绝、半提交恢复、多家人多宠隔离、撤权在途、GET 纯读、到站不依赖模型。未实现的场景保留清楚的失败证据，不悄悄 skip，不放宽断言；每条结论标明层级（单元 / 集成 / 真实供应商 / 自然时间 / 线上）。
  - 只写 Q 的文件；复现步骤和缺口报给对应的包；不通过改业务实现来宣称验收通过。
package / 接口版本：Q / RUNTIME-PARALLEL-CONTRACT v0.1（候选，I 尚未接受）。Q 主要消费其 §3 行为样例与 §4 接入证明，以及 scripts/real_acceptance.py 实际写出的证据结构。
实际目录 / 分支 / HEAD：E:\petsoul-audit\petsoul / codex/petsoul-web-integration / 980feabc7710462a89c5df488c255d04e9e7de08
开工时已有改动（START 必填）：与本日志 23:49 START/WAITING 所记相同（5 个已修改、69 个未跟踪，共 74 行；23:51 复查行数与 HEAD 不变）。均非本窗口成果，不动。
拟修改范围（精确）：
  - 需 307b 释放后才写入仓库（现在 BLOCKED）：
    · 新文件 scripts/verify_runtime_evidence.py（在 307b 登记的 scripts/** 内）；
    · 新文件 PetJourneyBackend/tests/test_web_runtime_acceptance.py（在 307b 登记的 tests/** 内；另需与 I 商定：未实现场景会按设计失败，默认 unittest discover 会因此变红，由 I 决定放进默认套件还是单独入口）。
  - 现在即可写入（不在任何窗口的登记范围内，23:51 用 test -e 确认都不存在）：
    · 新报告 docs/coordination/Q-INDEPENDENT-ACCEPTANCE-2026-09-22.md；
    · 证据输出目录 PetJourneyBackend/data/reviews/q-4d18-20260922/**（git check-ignore 确认被 PetJourneyBackend/data/ 规则忽略）；
    · 本日志。
  - 释放前的草稿：写在本会话 scratchpad（仓库外，只放新写的文件，不复制仓库、不建 worktree 或 checkout）。运行时只读 import 工作树代码，用 python -B 与 PYTHONDONTWRITEBYTECODE=1，不往仓库写字节码。
  - 明确不改：全部业务实现（PetJourneyBackend/app/**）、既有测试、基类与替身（tests/web_base.py、web_provider_fakes.py、driving_bots.py、school_helpers.py 及其他 test_*.py）、正在运行的 scripts/real_acceptance.py 与 scripts/real_integration.py、迁移、契约与生成物、FRONTEND-HANDOFF.md、BACKEND-REAL-EXPERIENCE-ACCEPTANCE.md，以及 A（4bef）、B（fed3）、C（91f2）登记的文件。
依赖：
  - I（307b）：释放上面两个仓库文件；商定契约测试怎样进入测试入口；提供或确认真实验收证据文件的路径与去敏字段（本窗口先从脚本源码推断，不读原始账号材料）。
  - A / B / C：Q 只测已落到工作树的真实代码，不测各包的 scratchpad 草稿；缺口按归属报给对应的包。
验收 ID：读完世界运行方案 v0.2 的 A01–A26 后补记映射。用户点名的七项合同回归先登记为 Q-C1 过期任务接管、Q-C2 旧结果拒绝、Q-C3 半提交恢复、Q-C4 多家人多宠隔离、Q-C5 撤权在途、Q-C6 GET 纯读、Q-C7 到站不依赖模型。
运行资源（无则写无）：
  - 不占端口；不访问 18761、18763、5287–5289，GET 也不访问（GET 本身是否有副作用正是待验项）；
  - 不读业务数据库（data/web-mvp-claude、data/web-real-acceptance 下的库文件）；不读 data/secrets、.env、deploy/backend.env 等密钥或口令文件；
  - 真实验收的证据文件：先从脚本源码确认路径与结构，确认没有口令、Token 或私人原话后，才由判定器按字段白名单读取；
  - 合同回归只用 scratchpad 或本窗口证据目录下的临时 SQLite，测试后删除；进程内拦截 socket，禁网；模型用进程内替身；不调用任何供应商。
冲突检查结果：
  - 23:51 复查：307b 日志仍停在 22:59（101129 字节），没有 SCOPE_CHANGE 或部分 RELEASE，所以两个仓库文件现在不能写。
  - 4bef 领取 A（web_platform/{tasks,lease,budget}.py、web_providers/meter.py、tests/test_web_{task_recovery,budget_reservation}.py），fed3 领取 B（app/web_runtime/{clock_policy,state,heartbeat_policy,wake_events}.py、tests/test_web_heartbeat_policy.py），91f2 领取 C（app/web_agent/decision/**、tests/test_web_decision_{context,proposal}.py），都在等待释放。Q 的路径与它们都不重叠。Q 的合同回归会只读 import 这些模块的现有版本，不写。
  - 报告文件与 data/reviews/q-4d18-20260922/ 不在任何未释放范围内。
进展 / 决策依据：先做不冲突的部分：精读 PARALLEL-QA-REVIEW、PARALLEL-INFRA-REVIEW、BACKEND-REAL-EXPERIENCE-ACCEPTANCE、世界运行方案 v0.2 的验收节、scripts/real_acceptance.py 与 real_integration.py 源码，以及队列、租约、家庭授权、通讯、旅程、钱包的相关代码；在 scratchpad 写判定器与合同回归草稿并在隔离环境运行。
验证命令 / 结果 / 证据路径：git rev-parse HEAD、git status --short（74 行）；test -e 与 git check-ignore（结果如上）。
未完成 / 依赖 / 下一步：只读分析 → 判定器草稿与离线自测 → 合同回归草稿并隔离运行 → 写报告与 CHANGE_REQUEST → 307b 释放后 SCOPE_CHANGE，把两个文件落到仓库并复跑。
是否释放（是/否，明确范围）：否。

## PROGRESS ＋ SCOPE_CHANGE — pkg-Q：I 已释放 Q 的文件，解除写仓库的 BLOCKED；新增两个测试辅助文件

时间（含时区）：2026-09-23 00:11 +08:00
窗口ID / 任务ID / 事件：claude-20260922-234631-4d18 / pkg-Q-acceptance / PROGRESS ＋ SCOPE_CHANGE
依据：307b 在 2026-09-22 23:55 的“SCOPE_CHANGE ＋ 部分 RELEASE”中释放给 Q：scripts/verify_runtime_evidence.py、PetJourneyBackend/tests/test_web_runtime_acceptance.py，以及 Q 自己登记的证据报告。它同时要求：test_web_runtime_acceptance.py 会被 unittest discover 自动收集，尚未实现的合同用例先留在判定器或报告里，等对应批次接入后再迁入，以免全量回归变红。本窗口照此执行。
进展：
  - 证据判定器草稿（scratchpad/q/verify_runtime_evidence.py）已完成。
    · 只读白名单文件与白名单字段；不读 accounts.json、auth-secret、run.json、日志与数据库；不连服务；
    · 自测 13 组人工样本全部得到预期结论。这只证明判定器有效，不证明产品通过。
  - 首次只读判定真实证据（2026-09-22T16:06:29Z，即北京时间 00:06）：整体 UNPROVEN，退出码 3。
    · PASS：ENV、A1–A4、B1、B2、C1（composed_by=model）、C2、D1（12 号复跑）、E1、H1、H3、H4、FAM1。
    · UNPROVEN（缺关联字段）：E2 远行扣款一次、H2 重复出发扣款一次、CH1 同一趟行程互相指向。C3（回信能否追溯到输入）只作提示，不计入整体。
    · PENDING：F1–F3。到 17:04:16Z 加 10 分钟宽限（北京时间 01:14）之前不判。
    · 证据：PetJourneyBackend/data/reviews/q-4d18-20260922/judge-20260922T160629Z.json（只含白名单字段）。
  - 首轮草稿曾把两处不属于验收条件的内容判成 FAIL，已在报告前改正，没有对外报：
    · A2 的 entry_still_pending：入住时把注册入口标为已处理是 EntryStore 的设计；
    · D1 误用了主流程里的 05 号证据，该次按规则落到“星球内”；D 的真实地点证据是 12 号复跑。
    现在只判定验收文档写明的条件。
SCOPE_CHANGE（新增精确路径；原因：arch_gate 对仓库内每个 .py 文件都限制 800 行、30 个 def/class，合同回归放不进两个文件）：
  - 新 PetJourneyBackend/tests/runtime_contract_harness.py：隔离环境、禁网审计钩子、可挂起的假模型、数据库指纹、子进程执行。
  - 新 PetJourneyBackend/tests/runtime_contract_cases.py：Q-C1…Q-C7 合同用例本体。
  - 这两个文件名不以 test 开头，不会被 discover 收集。test_web_runtime_acceptance.py 只收已经通过、属于已接入批次的合同，以及判定器自测；其余合同由 scripts/verify_runtime_evidence.py contracts 显式运行，结果写进证据与报告，FAIL/BLOCKED 如实列出，不 skip。
  - 冲突检查（00:10 复读全部日志，test -e 确认 4 个目标路径都不存在）：
    · I 保留 tests/web_base.py、web_provider_fakes.py、全部已有 test_*.py、test_runtime_internal_types.py；
    · A 是 test_web_task_recovery.py、test_web_budget_reservation.py；B 是 test_web_heartbeat_policy.py；C 是 test_web_decision_{context,proposal}.py；
    · c84a 只写监管看板文件。
    都不包含上面两个新路径。
  - Q 的完整范围：scripts/verify_runtime_evidence.py、PetJourneyBackend/tests/{test_web_runtime_acceptance.py, runtime_contract_harness.py, runtime_contract_cases.py}、docs/coordination/Q-INDEPENDENT-ACCEPTANCE-2026-09-22.md、PetJourneyBackend/data/reviews/q-4d18-20260922/**、本日志。
  - 注意：A（4bef）正在改 tasks.py、lease.py、meter.py。Q 只读 import 工作树的当前版本，每次运行都记录被测源文件的 SHA-256，结论只对该版本有效。
运行资源（无则写无）：
  - 不占端口，不访问 18761、18763、5287–5289；
  - 合同回归的临时 SQLite 放在 PetJourneyBackend/data/reviews/q-4d18-20260922/tmp/<run>/，跑完删除；
  - 禁网审计钩子；用 python -B，不写字节码；不读 .env 与 data/secrets。
是否释放（是/否，明确范围）：否。

## PROGRESS ＋ READY_TO_INTEGRATE（部分）＋ CHANGE_REQUEST — pkg-Q 第一批交付

时间（含时区）：2026-09-23 00:31 +08:00
窗口ID / 任务ID / 事件：claude-20260922-234631-4d18 / pkg-Q-acceptance / PROGRESS ＋ READY_TO_INTEGRATE（仅测试文件）＋ CHANGE_REQUEST
包名 / 窗口ID / 接口版本：Q / claude-20260922-234631-4d18 / runtime-internal 0.1.0（只消费 §2.4 的 TaskClaim / StaleClaim）
实际修改文件（全部新建、未提交；SHA-256 前 16 位，00:30 记录）：
  - scripts/verify_runtime_evidence.py：9a941b5c1f48a30e（547 行、22 个 def/class）
  - PetJourneyBackend/tests/runtime_contract_harness.py：f1052714eb4edd46
  - PetJourneyBackend/tests/runtime_contract_cases.py：b2de17c355cc6952（468 行、21 个 def/class）
  - PetJourneyBackend/tests/test_web_runtime_acceptance.py：d9d0ab559394b1a1
  - docs/coordination/Q-INDEPENDENT-ACCEPTANCE-2026-09-22.md：99a589fff00abd83（报告）
  - 证据目录 PetJourneyBackend/data/reviews/q-4d18-20260922/ 下的 judge-*.json、contracts-*.json（git 忽略，只含白名单字段）
  - 业务实现、迁移、契约、既有测试与脚本、其他包的文件：一个都没改。
验证命令 / 结果 / 证据路径：
  - python -B scripts/verify_runtime_evidence.py selftest：通过（14 个场景）。只证明判定器有效，不证明产品通过。
  - python -B scripts/verify_runtime_evidence.py judge（2026-09-22T16:06:29Z）：整体 UNPROVEN。
    · PASS 15 项；
    · UNPROVEN：E2、H2、CH1，原因是缺关联字段；
    · PENDING：F1–F3，北京时间 01:14 以后再判。
    · 证据：judge-20260922T160629Z.json。
  - python -B scripts/verify_runtime_evidence.py contracts（2026-09-22T16:28:49Z）：
    · PASS：C1a、C2（A 的队列）、C3b、C7a（I 的结算与 outbox）、C4、C7b；
    · FAIL：C1b 待回复领取崩溃后永远卡住、C3a 回执丢失后同键一直 409 IN_PROGRESS、C5 撤权后在途回信仍发布、C6 GET 写了 8 张业务表、C7c 任务进程被模型挂住时别的宠物到期结算停摆；
    · 禁网拦截 0 次。
    · 证据：contracts-20260922T162823Z.json。
  - 隔离环境下跑默认 discover（-p test_web_runtime_acceptance.py）：3 项 OK（4.1 秒）。
  - scripts/arch_gate.py 通过；scripts/dependency_gate.py 通过（00:28，全仓）。
  - 首轮一处结论已在报告前改正，没有对外报：C3a 的“换键出现第二趟旅程”是测试把时钟推过了行程结束，属合法的新行程，不是重复扣款。改为在 TA 仍在外面时换键后，实测只有 1 趟旅程、1 条旅费。
READY_TO_INTEGRATE：
  - test_web_runtime_acceptance.py 目前只收判定器自测、C4、C7b，全部通过，可以留在默认 discover。
  - 其余合同按 I 的要求不进默认 discover，由 contracts 显式运行。C1a、C2、C3b、C7a 等 A 与 I 在日志里宣布对应批次已接入后，再由 Q 加进 INTEGRATED。
CHANGE_REQUEST（详见报告第 5 节；Q 不改这些文件）：
  - CR-Q1（I）：LedgerEntry 追加关联字段（source_key，或 ref_kind＋ref_id）。
  - CR-Q2（I）：MessageSummary 追加 source_event_id 与 reply_to。
  - CR-Q3（I）：verify-job 按关联字段输出；采集 14-F-restart.json（重启前后持有者与领取代数，指向同一 journey）；07 号证据带远行旅费流水；布尔结果为 false 时脚本失败退出。
    · 不补这三条，F1、F2、F3 到期后也只能判 UNPROVEN。
  - CR-Q4（I）：C1b，待回复领取要有期限并可回收；可改用 A 的 claim_next / fenced。
  - CR-Q5（I）：C3a，回执与业务同一事务，或给占位设期限并对账后重放。
  - CR-Q6（I＋C）：C5，最终写入前在同一事务里重验成员关系、权限与记忆版本；已撤权记为抑制。
  - CR-Q7（I）：C6，生活类 GET 改纯投影（未结算显示 catching_up）。
  - CR-Q8（I＋B）：C7c，确定性结算与模型调用分线程或分进程，或给每轮设时间预算。
  - CR-Q9（A、I）：宣布接入批次后，Q 迁入对应合同。
还没接入的入口与未验证行为：
  - Q 没有调用任何真实供应商；
  - F 的自然时间结论待定；
  - 没做 API 与 worker 同时写的压力测试；
  - 公开访客接口的 GET 纯读没测；
  - 线上无证据。
运行资源（无则写无）：无常驻进程。合同回归的临时库与子进程都已结束并删除（tmp/ 下为空）。
下一步：北京时间 01:14 以后重跑 judge 判 F（不访问 18763，只读原窗口写出的证据），结果追加在本日志和报告里。之后等 A、I 宣布接入，再迁移合同并复跑。
是否释放（是/否，明确范围）：否。Q 的文件与证据目录继续持有，本窗口不写任何其他文件。

## PROGRESS — 运行资源登记（后台等待 F 到期）

时间（含时区）：2026-09-23 00:31 +08:00
窗口ID / 任务ID / 事件：claude-20260922-234631-4d18 / pkg-Q-acceptance / PROGRESS
运行资源（无则写无）：本窗口启动了后台进程 python PID 42972，脚本为 scratchpad/q/wait_then_judge.py。
  - 每 60 秒只检查 11-F-job-verify.json 是否存在、修改时间，不读内容；
  - 满足以下任一条件后，运行一次 scripts/verify_runtime_evidence.py judge，把结果写进 Q 证据目录，然后退出：
    · 北京时间 01:14:16 以后，且原窗口已写出 verify-job 证据；
    · 到了北京时间 01:45；
  - 不占端口，不连接任何服务，不读业务库。
是否释放（是/否，明确范围）：否（进程结束后在下一条记录里登记）。

## PROGRESS — pkg-Q 第二批：F 判定通过、合同复跑、10 条合同进默认测试集

时间（含时区）：2026-09-23 01:28 +08:00
窗口ID / 任务ID / 事件：claude-20260922-234631-4d18 / pkg-Q-acceptance / PROGRESS
运行资源：后台等待进程 PID 42972 已按预期结束（01:14 判定后退出），没有残留；合同回归的临时库与子进程都已删除，tmp/ 为空。本窗口当前没有常驻进程。
进展：
  - 判定器判定真实证据三次（同一套规则，证据在变）：
    · 16:06:29Z 整体 UNPROVEN；
    · 17:14:22Z（打工到期）：原窗口按 CR-Q3 补出 11、14 号证据，F1、F2、F3 转 PASS，整体仍 UNPROVEN；
    · 17:19:49Z：Q 的判定器接入原窗口导出的 15-linkage.json（账本按幂等键、消息按 source_event_id）后，E2、H2、CH1 也转 PASS。
      整体 PASS、退出码 0。证据：judge-20260922T171949Z.json。
    · 唯一未判成的是 C3（仅提示）：接口与 15 号导出都没有 reply_to，回信对应哪条输入判不了。
    · 口径：F1、F2、E2、H2、CH1 的关联证据来自验收库只读导出，不是接口字段。后端事实层能证明恰好一次；按现有接口，前端与第三方做不了这个核对。CR-Q1、CR-Q2 保持开启。
  - 修了自己判定器的一处缺陷：新加的“扣款金额与余额差一致”在证据缺字段时会误判成不一致，现在两边都有值才比；自测因此从 7 项失败恢复到全部通过（14 个场景）。
  - 合同回归复跑（17:25:41Z，contracts-20260922T172457Z.json）：11 条里 10 条 PASS，只剩 Q-C3a（幂等回执丢失后同键一直 409）FAIL。
    · 与 00:28 相比，C1b、C5、C6、C7c 由 FAIL 转 PASS，对应 CR-Q4、Q6、Q7、Q8 已被原窗口修复；
    · C7c 因为 ticker 被拆成世界线与认知线，用例改为同时驱动两条线：认知线卡在模型调用时，世界线一轮就把另一只宠物的工钱与到家结算掉。
  - 默认 discover（-p test_web_runtime_acceptance.py）：11 项通过，38–42 秒。按 CR-Q9，已把 10 条通过的合同移进 INTEGRATED，只留 C3a 在显式入口。
  - 对原窗口新增的 real_acceptance_judge.py 做了只读独立复核（不读它的结论），三条提示写进报告第 3 节：H3 用 map_status 判“没有生成行程”对领养居民可能误判；FAM2 的隔离判定建议按 message_id 断言；C1 建议加回信与输入的对应。
  - 一过性观察：17:22 的运行日志出现 NameError: decide_window，当时 web_agent_wiring.py 正在被修改；随后两次复跑未复现，不作为缺陷结论。
实际修改文件（全部未提交；SHA-256 前 16 位，01:27）：
  - scripts/verify_runtime_evidence.py：1d678452c56f3869（590 行、23 个 def/class）
  - PetJourneyBackend/tests/runtime_contract_cases.py：45e4fb426a6f2ea3
  - PetJourneyBackend/tests/test_web_runtime_acceptance.py：a57b2dfc369cb58a
  - PetJourneyBackend/tests/runtime_contract_harness.py：未变（f1052714eb4edd46）
  - docs/coordination/Q-INDEPENDENT-ACCEPTANCE-2026-09-22.md：666ee75be17210a6（已按最新结果重写）
验证命令 / 结果 / 证据路径：
  - python -B scripts/verify_runtime_evidence.py selftest → OK（14 个场景）；
  - python -B scripts/verify_runtime_evidence.py judge → 整体 PASS，退出码 0；
  - python -B scripts/verify_runtime_evidence.py contracts → 10 PASS / 1 FAIL，退出码 1，禁网拦截 0 次；
  - python -B -m unittest discover -s tests -p "test_web_runtime_acceptance.py"（隔离环境）→ 11 项 OK；
  - python -B scripts/arch_gate.py、dependency_gate.py → 通过。
未完成 / 依赖 / 下一步：
  - 等 I 修 CR-Q5（幂等回执）后复跑 Q-C3a，通过再移进默认集；
  - 等 I 补 CR-Q1、CR-Q2 的接口字段后，把 E2、H2、CH1、F1、F2 的关联证据从库级导出改回接口层判定；
  - 有新的真实验收轮次时重跑 judge。
是否释放（是/否，明确范围）：否。Q 的四个文件、报告与证据目录继续持有。

## START ＋ PROGRESS — pkg-Q 第二轮：集成后的独立复验（沿用原领取范围）

时间（含时区）：2026-09-23 03:52 +08:00
窗口ID / 任务ID / 事件：claude-20260922-234631-4d18 / pkg-Q-acceptance / START（第二轮）＋ PROGRESS
用户任务：集成后的独立复验，不从第一轮重来。优先复跑 Q-C3a 再跑全部合同；C3a 要覆盖“关联原操作、跨旅程生命周期、并发接手与旧执行者迟到”；复核 CR-Q1/Q2 的接口实际下发；复核 I 的真实模型证据与脚本（不新增付费调用）；区分两个 E2；记录被测文件运行前后指纹。
实际目录 / 分支 / HEAD：E:\petsoul-audit\petsoul / codex/petsoul-web-integration / 980feab（未切分支、未提交）。被测版本：runtime-internal 0.1.1、WEB_CONTRACT_VERSION 0.4.1（读代码核对）。
拟修改或实际修改范围：只改 Q 已领取的四个文件与报告，本轮没有新增路径（新合同写在已领取的 runtime_contract_cases.py 里）。
运行资源（无则写无）：只有一次性的隔离测试进程与临时 SQLite（跑完删除，tmp/ 为空）；禁网钩子拦截 0 次；未访问 18761/18763/5289，未调用任何供应商，未读 accounts.json、auth-secret、run.json、data/secrets、.env。
冲突检查结果：读了 AGENTS.md、全部窗口最新记录与 CORRECTION（I 的第 3–10 批、ACCEPT 两条；A、B、C 的交接）。本轮只写 Q 自己的文件；I 正在改 app/**，Q 只读 import。
进展：
  一、合同复跑（先 C3a，再全部）——在两个代码版本上各跑一次，中途 I 改了被测文件，按规则标清并复跑：
    · 运行 A（19:37:22Z ＝ 03:37；idempotency 3fe866a8、web_journey/service 7afcfbfe、tasks 4bf74a74）：14 条里 11 PASS、3 FAIL。
      - Q-C3a FAIL：超过 5 分钟后不再卡 409（CR-Q5 已修），但接手被领域规则拒绝，返回 409 ALREADY_TRAVELING，响应不带原 journey_id；没有双扣。
      - Q-C3c FAIL（新增用例）：接手被拒时占位被删除 → 1 小时后 TA 已回家 → 同键重试新建第二趟旅程，journeys=2、fee=2、余额 12→4。
      - Q-C3d FAIL（新增用例）：并发接手只执行一次（对），但卡住的旧执行者迟到写回执覆盖了接手者结果，重放拿到 {"writer":"old"}。
    · 运行 B（19:49:18Z ＝ 03:49；idempotency 9bd51d73、web_journey/service aa57cb2c、tasks 212b0a32）：14 条全部 PASS。
      - C3a 返回 200 且 journey_id ＝ 原来那趟；C3c 同键 1 小时后仍返回同一趟、只有 1 条旅费；C3d 重放仍是接手者结果。
      - 运行前后再次比对 8 个被测文件指纹，一致。
    · 证据：contracts-20260922T193627Z.json（A）、contracts-20260922T194825Z.json（B）。
  二、Q 自己的两处判定缺陷（都在新写的 C3d 上，报告前改完）：把“并发的另一个请求必须抛异常”当合格条件；按结果顺序推断执行者。
    改成按 handler 实际执行记录判定后连跑 3 次稳定 PASS。用改正后的条件回看运行 A 的证据，C3d 在旧版本上仍然不合格，所以那条缺陷是真的。
  三、CR-Q1/CR-Q2 复核（新增合同 Q-C8，隔离库＋真实 API 路径，不是只看 schema）：PASS。
    旅费流水 ref_kind=journey、ref_id=<咖啡馆那趟>；工资流水 ref_id=<打工那趟>；家庭来信 source_event_id=<journey>:<事件> 且同一源事件只有一条；
    TA 的回复 reply_to=<家人那条消息>；家人自己的消息没有 source_event_id。
  四、判定器（只读原窗口证据）：24 项全 PASS，退出码 0（judge-20260922T195035Z.json）。本轮按证据层级重构了判定：
    · H2、CH1 改用接口证据（13-chain 在 0.4.1 重启后重采，带 ref_kind/ref_id 与 source_event_id）；
    · Q 的 E2、F1、F2 仍是库级导出（15-linkage / 11 号），报告里如实标注，不改称接口证据；
    · C3（仅提示）经 15-linkage c_reply.reply_to 判 PASS，并注明接口层由 Q-C8 另行核对。
  五、I 的真实模型验证（只读复核，未重跑、未新增付费调用）：新增 BRAIN1（仅提示，不计入整体）。
    脚本 verify_brain_live.py 是全新临时库、install_clock 固定香港中午、只配对话模型、跑完删除、不碰 18763 与生产；
    证据 24/27 号 composed_by=model、调用次数（之后）各 1。Q 判定层级为“真实供应商（临时库＋固定时钟＋单次调用）”，
    明确不升级为自然时间后台运行或线上证明；“模型选择变成行程”只在那个临时库里成立。
  六、两个 E2 分开记：Q 的 E2（远行旅费只扣一次）本轮 PASS（库级导出）；I 的 E2（出发前改签或取消退款）仍 PENDING，
    由 I 既有等待安排在 06:26 之后判定。Q 没有另起等待进程，也没有重启任何环境。
  七、默认 discover：15 项通过 ＝ 1 项判定器自测 ＋ 14 条合同（50.9 秒）。报告里已写明不能把这 15 项说成“14 条合同全部通过”以外的含义。
实际修改文件（全部未提交；SHA-256 前 16 位，03:51）：
  - scripts/verify_runtime_evidence.py：b0ff3dd288ae5426（635 行、24 个 def/class）
  - PetJourneyBackend/tests/runtime_contract_cases.py：de22e233bc27ed22（29 个 def/class）
  - PetJourneyBackend/tests/test_web_runtime_acceptance.py：a59f27187c79b2fe
  - PetJourneyBackend/tests/runtime_contract_harness.py：未改（f1052714eb4edd46）
  - docs/coordination/Q-INDEPENDENT-ACCEPTANCE-2026-09-22.md：71bba4ee0bf0b2a6（第二轮报告）
验证命令 / 结果：
  - contracts（两轮，见上）；judge → PASS 退出码 0；selftest → OK（14 个场景）；
  - 隔离环境默认 discover → 15 项 OK；arch_gate、dependency_gate → 通过。
剩余问题与归属：
  - A：BudgetWithoutMigrationTests（全量回归里唯一错误，I 已给出改法）。
  - I：E2 未到点（06:26 后由 I 判）；BRAIN1 的层级限制；线上无任何证据。
  - Q：无未完成的 FAIL。
是否释放（是/否，明确范围）：否。Q 的四个文件、报告与证据目录继续持有，随时可按新版本复跑。

## SCOPE_CHANGE — pkg-Q 第三批：新增一个可靠性合同文件（登记在先）

时间（含时区）：2026-09-23 04:00 +08:00
窗口ID / 任务ID / 事件：claude-20260922-234631-4d18 / pkg-Q-acceptance / SCOPE_CHANGE
变更：新增 PetJourneyBackend/tests/runtime_contract_reliability.py（Q 的可靠性合同：缺表前提、额度与付费调用边界；以后 A/I 相关的新合同也放这里）。
原因：runtime_contract_cases.py 已有 29 个 def/class，架构门禁上限是 30，再往里加会顶满；拆一个新文件更稳妥。文件名不以 test 开头，默认 discover 不会收集它。
冲突检查（04:00 复读全部窗口日志 ＋ test -e 确认文件不存在 ＋ grep 全部日志没有任何窗口登记过这个路径）：
  - A（4bef）03:47 部分 RELEASE：交出 tests/{task_budget_helpers,test_web_task_recovery,test_web_task_fence,test_web_task_concurrency,test_web_budget_reservation}.py，
    继续持有 app/web_platform/{tasks,lease,budget}.py 与 app/web_providers/meter.py。Q 只读 import 这四个模块，不修改；A 交出的测试文件 Q 也只运行、不修改。
  - I（307b）持有 app/** 与既有测试；B（fed3）持有 app/web_runtime/** 与 test_web_heartbeat_policy.py；C（91f2）持有 web_agent/decision/**。
  - 新路径与以上都不重叠。
Q 的完整范围（更新后）：scripts/verify_runtime_evidence.py、PetJourneyBackend/tests/{test_web_runtime_acceptance.py, runtime_contract_harness.py,
  runtime_contract_cases.py, runtime_contract_reliability.py}、docs/coordination/Q-INDEPENDENT-ACCEPTANCE-2026-09-22.md、
  PetJourneyBackend/data/reviews/q-4d18-20260922/**、本日志。
是否释放（是/否，明确范围）：否。

## PROGRESS — pkg-Q 第三批：A 的缺表项独立复核并关闭；CR-A／CR-B 覆盖对照

时间（含时区）：2026-09-23 04:05 +08:00
窗口ID / 任务ID / 事件：claude-20260922-234631-4d18 / pkg-Q-acceptance / PROGRESS
已读：A 的 03:46 PROGRESS＋CHANGE_REQUEST（CR-A1～A5）、03:47 部分 RELEASE、03:58 CORRECTION；B 的 03:50 第二轮（8 条 CR 与隔离证据）；I 的最新记录。
运行前指纹（03:58）与运行后（04:04）一致：budget.py 830b16ddcd8d3c18、meter.py e4c7565cf28510d0、tasks.py 212b0a322747409c、
  m0050_budget.py f3b63c506af773ea、idempotency.py 9bd51d73…、web_journey/service.py aa57cb2c…（本批运行期间没有变化）。
一、A 的缺表项：独立复核后关闭
  - 只读复跑别人的用例（不修改 A 已释放的文件）：test_web_budget_reservation.py -k missing_tables 1 项 OK；I 的 test_web_ledger_missing_tables.py 2 项 OK。
  - Q 自己的合同 Q-C9（新文件 tests/runtime_contract_reliability.py，不复用 A/I 的测试辅助）：自建一次性临时库、跑完 52 个迁移，逐条断言前提：
    · 迁移后两张额度表存在 → ["web_budget_counters","web_budget_reservations"]；
    · 临时库删表后确实不存在 → []；
    · reserve → BudgetDenied(scope=ledger, reason=ledger_unavailable)；
    · 付费替身调用 0 次；被拒的预占不留计量（web_provider_usage 0 行）；
    · expire_stale=0、available()=False；供应商每日上限仍原子（allow×2 → [True, False]，放行那次才计 1 行）。
  - 结论：这一项关闭（归属 A）。Q-C9 已进默认 discover 防回归，与 A、I 的两条用例前提互补。
二、CR-A1～A5 覆盖对照（写进报告 §4.2，按用户要求区分三组）
  - 旅程请求幂等（Q-C3a/C3c/C3d 已覆盖，全 PASS）≠ 模型决策 operation_id 的持久化重放（CR-A2，Q 无合同覆盖）。
  - 任务/回执围栏（Q-C1a、Q-C2、Q-C3d 已覆盖）≠ 进程租约在最终提交时的围栏（CR-A4，A 提供了接口但没有调用方，Q 无合同覆盖）。
  - 供应商全局上限（Q-C9、Q-C7b/C7c 已覆盖）≠ 操作级结算（CR-A1 部分已发出、CR-A3 超时结果不明，Q 无合同覆盖）。
  - CR-A5 属优化项，未覆盖。以上缺口归 I 处理，处理并交接后由 Q 补针对性反例（报告里已写明每条要写什么）。
三、B 第二轮的待复验清单（报告 §4.3）
  - CR-B1 公平轮转、CR-B2 失败退避、CR-B3 规则与模型争抢同一只宠物，三条都是 Q 现有合同没有覆盖的新问题，不因旧合同全绿而关闭；
    另记 CR-B4～B8 与两条成本安全提示（brain=shadow 仍是真实付费调用；heartbeat=off 不等于关掉模型调用）。
  - Q 的动作：等 I 对应批次交接后再写反例与回归，本批只确认覆盖缺口，不提前下结论。
四、本批验证
  - contracts 全量：15 条全部 PASS（contracts-20260922T200027Z.json，20:01:16Z；运行前后指纹一致）；随后按收紧的断言单独复跑 Q-C9 仍 PASS。
  - 默认 discover：16 项 OK ＝ 1 项判定器自测 ＋ 15 条合同（47.5 秒）。
  - arch_gate 通过（新文件 4 个 def/class；cases 文件 29 个，未超 30）。
  - 本批没有改业务实现、没有新增付费调用、没有碰 18763 与 E2 等待安排；临时库跑完即删。
实际修改文件（未提交；SHA-256 前 16 位，04:04）：
  - PetJourneyBackend/tests/runtime_contract_reliability.py：2addad1983c0e5f8（新）
  - PetJourneyBackend/tests/runtime_contract_cases.py：358e8de5842e36a2（接入 mixin ＋ CONTRACTS 增加 c9）
  - PetJourneyBackend/tests/test_web_runtime_acceptance.py：d980c95d2999226b（INTEGRATED 增加 c9）
  - docs/coordination/Q-INDEPENDENT-ACCEPTANCE-2026-09-22.md：aad205b247898a3d（新增 §4.1、§4.2、§4.3，计数口径更正）
  - scripts/verify_runtime_evidence.py：未改（b0ff3dd288ae5426）
证据分层（继续保留，不合并）：判定器 24 项含 2 项仅提示（C3、BRAIN1），不等于 24 条独立故障合同；默认 discover 的 16 项含 1 项判定器自测；
  Q 的 E2（远行旅费只扣一次）与 I 的 E2（出发前改签或取消退款）分开记，后者仍由 I 的既有安排判定；
  临时库＋固定时钟的真实模型验证不升级为自然时间或线上证明。
未完成 / 依赖 / 下一步：等 I 处理 CR-A1～A5 与 CR-B1～B8 并交接，再补对应反例与回归；全量回归由 I 收口。
是否释放（是/否，明确范围）：否。Q 的五个文件、报告与证据目录继续持有。

## PROGRESS — pkg-Q 第四批：证据收紧、纳入 C 的提交边界、下一批判定条件（未重跑全套）

时间（含时区）：2026-09-23 04:10 +08:00
窗口ID / 任务ID / 事件：claude-20260922-234631-4d18 / pkg-Q-acceptance / PROGRESS
已读：C（91f2）03:56 第三轮 HANDOFF 与 04:05 CORRECTION、04:06 SCOPE_CHANGE；A 04:06、B 04:06 的最新追加；I 的最新记录。
一、证据收紧（按用户两点要求）
  - Q-C9 的范围声明写进报告 §4.1：连续两次 allow 只证明**该场景下每日上限生效**，不证明并发原子性；付费替身 0 次只证明**本合同这条调用序列**，
    不证明所有真实调用方都已接好保护（真实调用点是否都先预占再调用属于 CR-A1/A3 与接入复核范围，Q 未逐个核对）。
  - 证据版本对应关系写清楚：contracts-20260922T200027Z.json（20:01:16Z 全套 15 条）是**收紧计量断言之前**的输出；
    收紧后的单条复跑证据为 contracts-c9-20260922T200743Z.json（20:07:48Z），对应 runtime_contract_reliability.py 2addad1983c0e5f8，
    观察值含 usage_rows_after_denied_reserve=0 与 usage_rows_after_two_allows=1。被测三个文件指纹未变（budget 830b16dd、meter e4c7565c、m0050 f3b63c50），
    因此只复跑这一条，没有重跑全套。
二、口径改写
  - CR-A3：预期展示改为“**结果确认中**”，不把 unknown 写成确认失败；判定条件增加“不自动重试”“由主人显式重画才发起新尝试”“计量与账面一致”。
  - CR-A5：从“优化项”改列为**事务一致性问题**：重排任务与更新展示状态必须同事务；判定为“两者之间注入异常时一起回滚，不留中间态”。
三、纳入 C 的最终提交边界（报告新增 §4.4）
  - C 在 03:56 给出隔离复现（其证据目录 data/reviews/c-91f2-20260923/ 与 docs/coordination/C-COMMIT-BOUNDARY-EVIDENCE-2026-09-23.md）：
    版本检查在事务外，撤权或 DNA 变化落在“比过版本之后、写事务之前”时，当时版本上旧提案仍会执行。**Q 尚未独立复核**，按 C 的版本记录。
  - Q-C12 的判定条件已写好：四个注入点（撤权、DNA 版本、活动版本、机会过期）都要求旧提案不提交（不建行程、不扣费、不写决策记录、返回明确原因），
    并且**“留在家”的 stayed 分支同样覆盖**（撤权后不应写入 last_decision_by=model 的运行记录，或写入前重新校验）。
四、版本标注与表述
  - A、B、C 的缺陷描述都标注了原被测版本，并区分静态发现 / 旧版复现 / 新版待复验；删掉了“永远不会触发”这类绝对表述
    （改为“在该版本的这条路径上没有被触发，I 修改后需要复验”）。
五、下一批已排期的反例与判定条件（报告 §4.4 表格）：Q-C10（CR-A3）、Q-C11（CR-A5）、Q-C12（CR-C1，含 stayed 分支）、Q-C13（CR-A2）、
  Q-C14（CR-A4）、Q-C15（CR-A1）、Q-C16～C18（CR-B1/B2/B3）。等 I 交接明确版本后再写；写出来若不通过，保持显式运行，不进 INTEGRATED。
本批边界：没有改业务代码、没有新增付费调用、没有碰 18763 与 E2 等待安排、没有重跑全套合同；只改 Q 自己的报告与日志，加一次 Q-C9 单条复跑。
实际修改文件（未提交；SHA-256 前 16 位，04:09）：
  - docs/coordination/Q-INDEPENDENT-ACCEPTANCE-2026-09-22.md：ceb9b54c29a002e2
  - 新增证据 PetJourneyBackend/data/reviews/q-4d18-20260922/contracts-c9-20260922T200743Z.json
  - 代码文件本批未改：runtime_contract_reliability.py 2addad1983c0e5f8、runtime_contract_cases.py 358e8de5842e36a2、
    test_web_runtime_acceptance.py d980c95d2999226b、verify_runtime_evidence.py b0ff3dd288ae5426、runtime_contract_harness.py f1052714eb4edd46。
未完成 / 依赖 / 下一步：等 I 交接明确版本后，按 §4.4 的判定条件写 Q-C10～C18 并复验；全量回归由 I 收口。
是否释放（是/否，明确范围）：否。

---

时间：2026-09-23 05:05 +08:00
窗口ID / 任务ID / 事件：claude-20260922-234631-4d18 / pkg-Q-acceptance / SCOPE_CHANGE
已读：AGENTS.md；docs/coordination/NEXT-BATCH-EXECUTION-2026-09-23.md（Q 段：立即编写缺失合同，新文件先登记精确路径并查冲突，
  不把用例继续塞进已接近定义数上限的文件；第一批优先 Q-C10 / Q-C12 / Q-C14 / Q-C16～C18；反例可显式运行并如实 FAIL）；
  I 的第11批记录（CR-A1～A5 与 4 项集成修复已交付，自跑 15 条合同全绿，并注明“不代表 Q 的独立复验已完成”）。
登记新增路径（仅 Q 写，均为**新建**，不接管任何人的文件）：
  - PetJourneyBackend/tests/runtime_contract_boundary.py（Q-C12 最终提交边界、Q-C14 业务入口租约围栏）
  - PetJourneyBackend/tests/runtime_contract_media.py（Q-C10 生图未知/恢复，后续 C11、C15）
  - PetJourneyBackend/tests/runtime_contract_schedule.py（Q-C16～C18 调度）
冲突检查（05:03 实测）：三个路径在磁盘上均不存在；全仓 docs/ 下无任何窗口日志或工单提到这三个文件名；
  同目录已存在的 test_web_commit_boundary.py（非 Q 文件）与 test_web_agent_media.py（非 Q 文件）都不在本次范围内，Q 不读写它们。
  新文件都不以 test_ 开头，默认 discover 不收集；入口沿用 Q 已持有的 runtime_contract_cases.py（只改基类与 CONTRACTS 元组，不新增定义，
  避免触碰 arch_gate 的 30 个定义上限）与 test_web_runtime_acceptance.py。
理由：按续工单要求把缺失合同拆到新文件，原 runtime_contract_cases.py 已有 29 个定义（上限 30），不能再加。
影响范围：无共享文件改动，不需要任何窗口 RELEASE。
是否释放（是/否，明确范围）：否。

---

时间：2026-09-23 05:20 +08:00
窗口ID / 任务ID / 事件：claude-20260922-234631-4d18 / pkg-Q-acceptance / PROGRESS
本批交付：按 05:05 SCOPE_CHANGE 登记的路径新建 `PetJourneyBackend/tests/runtime_contract_boundary.py`，写出并**显式运行**了 Q-C12、Q-C14。
结果：**Q-C14 PASS，Q-C12 FAIL（一个真实缺陷，见下面的 CHANGE_REQUEST）**。连跑三次结论一致。
  - 全量：17 条（原 15 ＋ C12 ＋ C14），请求 17、实跑 17、16 PASS、1 FAIL。证据 data/reviews/q-4d18-20260922/contracts-20260923T0513-full.json。
  - 单条证据：contracts-c12-c14-20260923T0512.json、contracts-c12-c14-run1.json、contracts-c12-c14-run2.json（三次一致）。
  - Q-C14 已通过，按约定进默认 discover 防回归（discover 17 项 OK / 55.7 秒 ＝ 1 项判定器自测 ＋ 16 条合同）；**Q-C12 不进已验收集合**，保持显式运行。
  - 架构门禁通过（新文件 14 个定义 / 208 行）。

CHANGE_REQUEST（CR-Q13，归属 I 或 identity 的持有者）：**正式撤权接口不产生最终提交那道复核所依赖的版本变化**
  被测版本：brain_life e53fafbb17c090ba、web_journey/service e53d74f8ae1316d4、runtime_view 1e7ea44c8af72ee9、
    web_identity/service 0320f8bf580cd5d1、routers/web/identity 102f6088382e85db（运行前后一致）。
  复现（Q-C12，一次性临时库、TZ=UTC、禁网、替身决策器，不调用真实模型）：
  1) depart_revoked：在 `journeys.resolve`（外部解析、写事务之前）用正式接口 `PATCH /settings {"model_replies": false}` 撤权。
     观察：privacy_epoch 注入前后都是 0 → `_assert_still_valid` 没有可比的变化 → 旧提案照常提交，status=departed，
     真的建出一趟 local:cafe、扣了旅费、写下 last_decision_by=model。
  2) stay_revoked_final：在 `record_decision_checked` 之前用同一接口撤权。观察：epoch 不变 → status=stayed，同样写下 last_decision_by=model。
  对照（证明围栏本身是好的，这两条不是"事务里没复核"）：
     `PUT /pets/{id}/dna` 让 dna_version 0→1 → rejected versions_changed，不出发；
     家人自己 `POST /journey/depart` 让 activity_epoch 0→2 → rejected versions_changed，且始终只有 1 趟；
     时钟越过机会有效期 → rejected offer_expired；正常对照 departed 并只扣一次旅费、只记一次决定者。
     另外：每只都只发一次替身调用、额度都 settled、used=1、inflight=0；被拒的都没扣旅费、都记了复查时刻与原因。
  定位：`app/web_identity/service.py: set_prefs()` 只写 web_user_prefs，没有对该家庭的宠物 `bump_in(conn, pet_id, "privacy_epoch", now)`。
    全仓只有 `app/reception/store.py:297` 会递增 privacy_epoch（接待叮嘱的用途授权）。
  影响：家人关掉"模型回信"之后，一次已经在途的模型提案仍会照常执行，并被记成"TA 自己决定的"。
  建议：set_prefs 里 model_replies 由真变假（以及其他用途授权变化）时，在同一事务里给该用户家庭下的宠物递增 privacy_epoch；
    `_commit` 里那道 consent 检查只挡得住"窗口外"的撤权，窗口内的要靠版本代数。
  复跑命令：`TZ=UTC python scripts/verify_runtime_evidence.py contracts --only c12_commit_boundary`

Q 自己工具的两处修正（都在 Q 已领取文件内，属于判定可信度问题，如实记录）：
  - `scripts/verify_runtime_evidence.py run_contracts` 原来只收 `outcome.errors`，不收 `outcome.failures`：
    用例在返回合同结论**之前**断言失败时，那条合同会从结果里消失，整体仍报 PASS。本批首次运行就撞上了
    （c12/c14 的前置 move_in 断言失败 → 结果 0 条、整体 PASS）。已改为：failures 一并收（已给出结论的不重复记）、
    并在"请求条数 > 实际结论条数"时追加一条 BLOCKED 的 RUN 记录，同时把 requested / tests_run 写进证据文件。
  - Q-C14 初版把"被接管期间世界事件数"写成绝对值 0，但出发那条事件是 HTTP 请求里记的，与这一轮无关；已改成核对**这一轮的增量**为 0。
    这是 Q 的用例设计错误，不是产品缺陷。

途中观察（已由持有者自行修好，不另开 CR）：04:58–05:00 之间仓库里同时存在 `m0090_suggestion_considered.py`，
  它 ALTER 的 `web_owner_suggestions` 由 `m1300_pet_life` 创建，按编号顺序 0090 排在第 9、1300 排在第 48，
  全新库迁移必失败（Q 用独立临时库复现过：`no such table: web_owner_suggestions`；已存在的老库因为 1300 早已登记而不受影响）。
  05:00:01 该文件已被改名为 `m1310_suggestion_considered.py`，现在全新库迁移正常。
  建议（低优先，不阻塞）：`tests/test_web_platform_infra.py::test_migrations_are_discoverable_and_rerunnable` 是在 setUp 建好库之后再跑一遍，
  顺序问题只会表现成"整批用例在 setUp 崩掉"；加一条"全新空库按声明顺序能完整迁移"的断言会让这类问题一眼可辨。归属平台/I。

本批边界：没有改任何业务实现；没有新增付费调用；没有碰 18763 与 E2 等待安排；没有做整仓 Git 操作；
  只写 Q 已领取文件（runtime_contract_boundary.py 新建、runtime_contract_cases.py 改基类与 CONTRACTS、
  test_web_runtime_acceptance.py 改 INTEGRATED 注释与一项、verify_runtime_evidence.py 修 run_contracts）与本日志、证据目录。
运行前后指纹（SHA-256 前 16 位，05:19；被测文件运行前后一致）：
  Q：runtime_contract_boundary efd7159e71a18ebb、runtime_contract_cases 06ab5ff2e66335d8、test_web_runtime_acceptance 1640d5443c8faf2b、
     verify_runtime_evidence 54e42a8c74cd6aa3、runtime_contract_harness f1052714eb4edd46、runtime_contract_reliability 2addad1983c0e5f8
  被测：brain_life e53fafbb17c090ba、web_journey/service e53d74f8ae1316d4、runtime_view 1e7ea44c8af72ee9、
     web_identity/service 0320f8bf580cd5d1、routers/web/identity 102f6088382e85db、uow 2c98bdcb4545b8f8、
     ticker f3a9c6fc3eef5511、lease d29f3bcc7a58f804
未覆盖 / 下一步：本批只做了 C12、C14。接着写 Q-C10（生图未知/恢复）与 Q-C16～C18（调度），再补 C11 / C13 / C15。
  Q-C12 等 I 修好 CR-Q13 并交接版本后复验；全量回归由 I 收口。
是否释放（是/否，明确范围）：否。

---

时间：2026-09-23 05:55 +08:00
窗口ID / 任务ID / 事件：claude-20260922-234631-4d18 / pkg-Q-acceptance / PROGRESS
本批交付（第二小批）：按 05:05 SCOPE_CHANGE 登记的另外两个路径新建
  `PetJourneyBackend/tests/runtime_contract_media.py`（Q-C10）与 `PetJourneyBackend/tests/runtime_contract_schedule.py`（Q-C16～C18），全部显式运行。
结果：**Q-C16 / Q-C17 / Q-C18 PASS（各连跑 3 次一致），Q-C10 FAIL（10 项检查 9 通过、1 不通过）**。
  全量：21 条，请求 21、实跑 21、**20 PASS / 1 FAIL**（data/reviews/q-4d18-20260922/contracts-20260923T0555-full.json）。
  默认 discover：21 项 OK / 71.7 秒（＝1 项判定器自测 ＋ 20 条合同）。架构门禁通过。

一、Q-C12 已复验通过（CR-Q13 被修好了）
  05:20 我报的 CR-A? / CR-Q13「正式撤权接口不递增 privacy_epoch」已在 `app/web_identity/service.py` 修复：
  `set_prefs` 现在在**同一个事务**里对该家庭的宠物 `bump_many_in(..., "privacy_epoch", now)`。
  Q 在 05:40 用同一条合同独立复验：Q-C12 全部 12 项检查通过（窗口内撤权不再出发、stayed 分支不再写 last_decision_by）。
  **Q-C12 已移入默认 discover 防回归**。被测版本：web_identity/service 92c7f2d2c3f60dd3。

二、Q-C16 公平轮转（CR-B1）PASS
  一个家里 6 只宠物、每轮上限 2、决策模型一律失败。跑认知线真正那一步 brain_life 共 4 轮（每轮之间 +30 秒）：
  每轮处理 [2,2,2,0] 只，4 轮内 6 只全部被想过，第一圈里没有谁被想第二次，6 只都写下了下次再看的时刻。没有饿死。

三、Q-C17 失败退避的调用上界（CR-B2）PASS
  同一只宠物、模型一律失败，连跑 60 轮（每轮 +30 秒，合计半小时）：**真实调用 6 次**（不是 60 次），
  每次想不成都写了 next_review_at、都没写 last_decision_by。
  再用装配时真正在用的预占函数把当天额度占满后跑一轮：**新增调用 0 次、新增预占 0 次**，
  心跳把大脑可用性如实报成 `exhausted`、remaining_today=0 并带上窗口重置时刻——即"额度用完就不再请求大脑"，
  而不是每轮再预占一次、被拒一次。

四、Q-C18 同一时刻只有一个决定者（CR-B3）PASS
  模型决定"留在家"（写下 last_decision_by=model）后，在 +1 / +7 / +14 分钟各跑一轮世界线（里面就是规则生活 life.run）：
  一趟行程都没建、决定者一直是模型；过了 15 分钟的决定间隔再跑一轮，仍然没出门、记录仍是模型（对照口径：出门了就必须记成规则决定）。
  任何时刻最多一段行程。

五、Q-C10 生图结果未确认（CR-A3）FAIL —— 只差展示层这一环
  被测版本：illustrations 7c5bccad05df17fe、images 6f7bab695bb83553、communicator/service 7d596b007c8ec3a7、schemas/web/social 1e204a5195ebdecc。
  **已经对的（9 项）**：超时结算成 `unknown`（不是 failed）、预占停在 unknown 不整笔退回、按实际发出的 2 次计量；
  不排自动重试、下一轮不会再调一次供应商；对照组（供应商明确没受理）终结在确认失败且没有任何一笔预占停在"结果不明"；
  服务层 `outcome_of()` 分得清（unknown / failed）；正式接口重画是**新的一次尝试**（新预占 `...:2`）、
  原来那笔 unknown 一字未改、重画后真的出图。
  **还差的（1 项）**：`GET /communicator/{pet}/messages` 上两者**完全一样**——
    结果未确认：`{"state":"failed","photo_status":"failed","photo_url":null,"status_note":null}`
    确认没画成：`{"state":"failed","photo_status":"failed","photo_url":null,"status_note":null}`
  也就是主人看到的都是"没画成"。`IllustrationService.outcome_of()` 已经能给出依据，但**没有任何调用方**。
  运行期间观察到 `PhotoStatus` 已新增 `unknown` 取值（social.py 1e204a51 的注释写明"页面要显示成还没确认，不能写成没画成"），
  说明这一环正在补；**生产侧（communicator `_summary` 读 outcome_of、收藏 image_status 同理）还没接上**，所以本条仍如实 FAIL。
  复跑命令：`TZ=UTC python scripts/verify_runtime_evidence.py contracts --only c10_photo_unknown_result`

六、随手核到的一处口径变化（不是缺陷，供 A/I 对账）
  运行中途 `illustrations.py` 从 f1cc4f3f 变成 7c5bccad：`NOT_SENT_REASONS` 增加了 `provider_error` 与 `rejected`
  （供应商报错改判为"确定没受理、可整笔退回"）。Q-C10 的对照用例随之从"两次都 settled/failed"变成
  "第一次 settled/failed(actual 1)、第二次 released/not_sent"。Q 已把该项检查改成与口径无关的写法
  （只要求任务与展示都终结在 failed、且没有任何一笔预占停在结果不明），不写死某一次该是 settled 还是 released。

Q 自己用例的修正（如实记录，都是我的设计问题，不是产品缺陷）：
  - Q-C10 第一版用 `run_pending()` 一次跑完同一趟里的 3 个生图任务（奇遇、明信片、手账），
    "这次发出去了几次"混进了别的任务。已改为先把其它任务推到很久以后，让每次调用都属于被观察的那一个。
  - Q-C16 的"每轮不超过上限"误比了列表而不是个数；Q-C17 的方法名 `_reservations` 与生图合同重名被 MRO 覆盖，已改名 `_reservation_count`。
  - Q-C17 原来期望"额度用完后安静原因是 budget_denied"。实测系统更早一步：心跳报 `exhausted`、**根本不请求大脑**，
    所以安静原因还是上一次的失败原因。这是更对的行为，改成核对 exhausted ＋ 新增调用 0 ＋ 新增预占 0。
  - 认知线的替身接法：心跳要先认为大脑可用才会 REQUEST_BRAIN，所以供应商、`projector.model_available`、
    `brain.model` 三处都要换（按 web_agent_wiring 的同一条线），否则整轮会被判成 brain_disabled 而空跑。

本批边界：没有改任何业务实现；没有新增付费调用；没有碰 18763 与 E2；没有整仓 Git 操作；
  只写 Q 已领取文件（新建 runtime_contract_media.py、runtime_contract_schedule.py；改 runtime_contract_cases.py 的基类与 CONTRACTS、
  test_web_runtime_acceptance.py 的 INTEGRATED）与本日志、证据目录。
运行前后指纹（SHA-256 前 16 位，05:55）：
  Q：runtime_contract_boundary efd7159e71a18ebb、runtime_contract_media 691a9d868e4a66fb、runtime_contract_schedule e904f0930d6f4347、
     runtime_contract_cases 8d5aa34f9c40bd06、test_web_runtime_acceptance 2a66051e69252df0、verify_runtime_evidence 54e42a8c74cd6aa3
  被测（本批期间 I/A 在改，以各条证据 JSON 里 sources 字段为准）：illustrations 7c5bccad05df17fe、images 6f7bab695bb83553、
     communicator/service 7d596b007c8ec3a7、schemas/web/social 1e204a5195ebdecc、brain_wiring 89c04d5c2c5d5bee、life 11627c57c8cfaa85、
     brain_life 6be01960e911a9c6、heartbeat_policy 6d1ea2a79ef886f5、runtime_view ac59119a67de6ed9、web_identity/service 92c7f2d2c3f60dd3、
     ticker 8d4f7d1e58c8dd09、lease d29f3bcc7a58f804、uow 6bce6996babd4705、journey/service 6d5280010f5fb035
未完成 / 下一步：补 Q-C11（重画的同事务回滚）、Q-C13（真实装配下决策 operation_id 的编号/调用次数/账本恢复）、Q-C15（部分发送计量）。
  Q-C10 等展示层接上 outcome_of 后复验；全量回归由 I 收口。
是否释放（是/否，明确范围）：否。

---

时间：2026-09-23 06:00 +08:00
窗口ID / 任务ID / 事件：claude-20260922-234631-4d18 / pkg-Q-acceptance / PROGRESS
本批交付（第三小批）：Q-C11、Q-C13、Q-C15 三条新合同，外加用户点名的 Q-C12b（只改时区的旧请求不得撤销已完成的撤权）。
  写在已领取的 `tests/runtime_contract_media.py`（C11、C15）与 `tests/runtime_contract_boundary.py`（C12b、C13），没有新增路径。
结果：**Q-C12b PASS、Q-C15 PASS；Q-C11 FAIL（8 项检查 7 通过）、Q-C13 FAIL（10 项检查 4 通过）**。每条各用新进程连跑 3 次，结论一致，无漂移。
  全量：25 条，请求 25、实跑 25、**22 PASS / 3 FAIL**（C10、C11、C13）。证据 data/reviews/q-4d18-20260922/contracts-20260923T0600-full.json（21:54:35Z）。
  默认 discover：23 项 OK / 73.7 秒（＝1 项判定器自测 ＋ 22 条已纳入合同）。架构门禁通过。

一、Q-C12b（用户点名补的反例）PASS
  被测版本：web_identity/service **986a6904fbbc1742**（读旧值已移进写事务）。两种顺序都试了：
  - 串行：撤权之后发一个只带 timezone 的请求 → model_replies 仍是 false、授权代数不再变（只改时区不算授权变化）、时区确实改成 Asia/Tokyo。
  - 交错：把 `identity._prefs_in` 包一层，让只改时区的那个请求**停在“读到旧值、还没写回”的一刻**，此时由主人完成撤权
    （调用路由用的同一个 `set_prefs`）。两个线程跑完后：model_replies=false、授权代数递增、时区改成 Asia/Taipei。
  两种顺序下都再走一遍自主决策（替身提案“留在家”），都被拒 `consent_withdrawn`。
  说明：旧版本能通过原来那条 Q-C12，但挡不住这个交错——这条是新增的独立反例，不是原版的重复。

二、Q-C15 部分发送的计量 PASS
  被测版本：illustrations 23b9666bf5126a42、images 6f7bab695bb83553、budget 830b16ddcd8d3c18。
  替身把「进了 render()」（entered）与「真的越过发送边界」（dispatched）**分开记**，判定一律以 dispatched 为准。
  - 甲：预占 2，entered=2、dispatched=1（证件照发出并成功，正图在发送边界之前被每日上限挡下）
    → 预占 settled、actual_units=1、当日用量 +1。**没有整笔退回**。
  - 乙：entered=1、dispatched=0 → 预占 released、outcome=not_sent、当日用量 +0。**整笔退回**。
  两条都如实显示没画成。

三、CHANGE_REQUEST（CR-Q14，归属 I：`app/web_agent/brain_life.py`）：在途未结清就清掉决策编号，下一轮重开新编号并**重发**
  被测版本：brain_life 6be01960e911a9c6、runtime_view 2fbb9055f37b9006、budget 830b16ddcd8d3c18、brain_wiring 160859641342be08。
  复现（Q-C13，真实 BrainLife ＋ 真实额度账本，**只**把远端模型换成“调用中途进程被杀”的替身）：
  1) 第一次思考崩在模型调用里 → 编号 O1 存进运行记录、预占 O1 停在 `reserved`（替身被调用 1 次）。
  2) 同一分钟重试：**沿用了 O1**（对的），预占被判 in_flight → `failed/budget_denied`，**没有重发**（+0 次）。
     但紧接着 `_back_off → _close` 把 `decision_operation_id` **清成了 NULL**。
  3) 下一分钟重试：因为编号被清掉，`open_decision` 开了**新编号 O2** → **第二笔预占** → **替身又被调用 1 次**。
     也就是那次可能已经计费的调用被重发了。
  4) 对象重建后重试：沿用 O2、被 in_flight 挡住、编号又被清掉——**每两轮就会重发一次**。
  实测最终留下 4 笔 life_plan 预占（O1 expired、O2 expired、两次真决定各 settled），而正确情况下前面只该有 1 笔。
  定位：`BrainLife._back_off()` 无条件调用 `self._close()`；但 `budget_denied/in_flight` **不是这次操作的明确结果**，
  它恰恰说明同一次操作还在途。建议：失败原因是「同编号仍在途」时不要清编号（或只在拿到明确结果 / 预占已结清时才清）。
  影响：生产里认知线 30 秒一轮，一次可能已经发出的付费调用会被反复重发，并且每次多占一个每宠每日额度。
  复跑命令：`TZ=UTC python scripts/verify_runtime_evidence.py contracts --only c13_decision_operation_id`

四、CHANGE_REQUEST（CR-Q15，归属组合接线：`app/web_agent_wiring.py` 6a00129ffd61bffa，当前由 B 持有）：
  `illustrations.on_unknown` / `on_retrying` 两个回调**没有接线**，消费者那一侧收不到“结果未确认”和“重画已排上队”
  被测版本：illustrations 23b9666bf5126a42、web_communicator/service b14a9f647600e22a、routers/web/communicator e42321d7b900a1e2。
  - 服务侧已经齐了：`IllustrationService.retry()` 在**同一个事务**里做任务重排、插画记录、`self.on_retrying(task_id, conn)`；
    消费者侧 `communicator.illustration_retry_started()`、`collection.image_retry_started()`、`guides.image_retry_started()` 都在。
  - 但 `app/web_agent_wiring.py` 只接了 `on_ready` / `on_failed`，**`on_retrying` 与 `on_unknown` 始终是 None**。
  - 实测（Q-C11 正常对照，走正式入口 `POST /communicator/{pet}/messages/{id}/retry-photo`）：
    任务 → `queued`、插画记录 → `processing`，**但消息上的 photo_status 仍然是 `failed`**；直到图画好才跳到 `ready`。
    主人点了“重画”，页面却还写着“没画成”。
  - 这也是 Q-C10 仍然 FAIL 的同一个根因：`PhotoStatus.unknown` 已经加进 schema、`outcome_of()` 也能给出依据，
    但没有接线就下发不到消费者，“结果未确认”与“确认没画成”在 API 上仍然一模一样。
  - A 在 `tests/test_web_illustration_api_closure.py` 的开头已写明这两行接线归组合根，并在自己的用例里手工接了一遍——
    所以**那套用例通过不代表正式进程里成立**；Q 用默认装配独立验证，确认正式进程里仍然是 None。
  复跑命令：`TZ=UTC python scripts/verify_runtime_evidence.py contracts --only c11_redraw_is_all_or_nothing c10_photo_unknown_result`

五、Q-C11 已经对的部分（7 项）
  注入“任务重排照常写、改插画展示状态必失败”之后，走正式入口点重画：**任务、插画记录、消息展示三处都回到点之前的样子**，
  `failed` 与 `unknown` 两种起点都成立。唯一不成立的是上面第四条：正常对照里消息展示没有跟着进入处理中。
  正常对照最终能出图，但那是**替身生图的产物**，不代表真实生图质量。

报告表述收紧（按用户要求，已改进 Q-INDEPENDENT-ACCEPTANCE-2026-09-22.md）：
  - Q-C17 的“真实调用 6 次”改成“**真实业务链路调用模型替身 6 次**”；
  - Q-C11／C10 的“真的出图”注明是**替身产物，不代表真实生图质量**；
  - 默认 discover 的项数写清“1 项自测 ＋ N 条已纳入合同”，与“显式全合同 22 PASS／3 FAIL”分开写；
  - Q-C18 的结论收紧为“**证明了决定有效期内没有被规则覆盖**；到期后仍没出门，**不能证明规则接管成功**，接管能力另需对照”。

本批边界：没有改任何业务实现；没有新增付费调用；没有碰 18763 与 E2；没有整仓 Git 操作；没有新增文件路径。
  只写 Q 已领取文件（runtime_contract_boundary.py、runtime_contract_media.py、test_web_runtime_acceptance.py）与本日志、报告、证据目录。
运行前后指纹（SHA-256 前 16 位，21:56:10Z；本批期间被测文件没有再变）：
  Q：runtime_contract_boundary 5fea3decab71c38e、runtime_contract_media c6f0896b4e0efc3b、runtime_contract_schedule e904f0930d6f4347、
     runtime_contract_cases 8d5aa34f9c40bd06、test_web_runtime_acceptance 897eec6386a820d2、verify_runtime_evidence 54e42a8c74cd6aa3
  被测：web_identity/service 986a6904fbbc1742、brain_life 6be01960e911a9c6、runtime_view 2fbb9055f37b9006、budget 830b16ddcd8d3c18、
     brain_wiring 160859641342be08、web_agent_wiring 6a00129ffd61bffa、illustrations 23b9666bf5126a42、images 6f7bab695bb83553、
     web_communicator/service b14a9f647600e22a、routers/web/communicator e42321d7b900a1e2、schemas/web/social 1e204a5195ebdecc、
     tasks 212b0a322747409c
未完成 / 下一步：Q-C10／C11 等 A、B 把 `on_unknown` / `on_retrying` 接上组合根后，用正式 API 复验
  “结果未确认 / 处理中 / 确认失败 / 成功”四态可区分，以及重画入口确实接受 `unknown`；Q-C13 等 I 修好 CR-Q14 后复验。
  不等整仓完成。全量回归由 I 收口。
是否释放（是/否，明确范围）：否。

---

时间：2026-09-23 06:08 +08:00
窗口ID / 任务ID / 事件：claude-20260922-234631-4d18 / pkg-Q-acceptance / SCOPE_CHANGE ＋ CORRECTION
一、CORRECTION（按用户 06:05 指示更正，覆盖此前记录）
  1. **CR-Q14 归属更正为 B（fed3）**，不是 I。`app/web_agent/brain_life.py` 已由 B 接手；
     **CR-Q14（在途未结清就清掉决策编号）与 CR-Q15（on_unknown / on_retrying 未接线）都交 B 实施**，I 负责协调。
     Q 的等待项相应改为「等 B 交付稳定指纹」，不再等 I 修改 B 的文件。已同步更正报告 §4.7 与等待清单。
  2. **恢复证据的证明范围再明确一层**：每条合同用**新进程**跑三次，证明的是**重复运行结果一致（无漂移）**，
     **不等于**同一份持久化数据经历进程退出、重新启动之后仍能正确恢复。
     Q-C13 第四步的「对象重建」**保持原证明范围**（编号存在库里、不在内存里），**不升级为进程重启验证**。
     真正的跨进程恢复由下面新登记的用例承担。
二、SCOPE_CHANGE：登记新增路径 `PetJourneyBackend/tests/runtime_contract_recovery.py`（跨进程恢复用例 Q-C19）
  冲突检查（06:07 实测）：该路径磁盘上不存在；全仓 docs/ 下没有任何窗口日志或工单提到这个文件名；
  同目录已有的 test_web_idempotency_recovery.py / test_web_illustration_recovery.py / test_web_task_recovery.py 是别人的文件，
  名字不同、Q 不读写它们。新文件不以 test_ 开头，默认 discover 不收集；入口仍走 Q 已持有的 runtime_contract_cases.py 与 test_web_runtime_acceptance.py。
  做法：同一个临时库、**不同进程**、假供应商，把每次「越过发送边界」的调用记录写进独立文件，父进程只作观察者。
  不接真实供应商、不碰 18763/E2、不新增付费调用。
是否释放（是/否，明确范围）：否。

---

时间：2026-09-23 06:15 +08:00
窗口ID / 任务ID / 事件：claude-20260922-234631-4d18 / pkg-Q-acceptance / PROGRESS
本批交付（第四小批）：等待期间按用户要求准备的**跨进程恢复用例**已写好并实际运行：
  新文件 `PetJourneyBackend/tests/runtime_contract_recovery.py`（06:08 已登记路径并查过冲突）里的 **Q-C19**。
  同一个临时库、**三个真正独立的进程**、假供应商（每次越过发送边界就往独立记录文件追加一行并 fsync），不联网、不新增付费调用。
结果：**Q-C19 FAIL（9 项检查 6 通过）**，连跑 3 次一致。
  全量：26 条，请求 26、实跑 26、**22 PASS / 4 FAIL**（C10、C11、C13、C19）。
  证据 data/reviews/q-4d18-20260922/contracts-20260923T0615-full.json（22:12:17Z）；单条 contracts-c19-run1/2/3.json。
  默认 discover：23 项 OK / 82.3 秒（＝1 项判定器自测 ＋ 22 条已纳入合同）。架构门禁通过。

一、Q-C19 实测（这是 CR-Q14 在**真实进程重启**下的形态）
  | 步骤 | 用的编号 | 之后库里留着的 | 累计发出 | 预占笔数 |
  |---|---|---|---|---|
  | 1 全新进程，调用发出后 os._exit(9) | O1 | O1 | 1 | 1（reserved） |
  | 2 全新解释器（+30 秒） | **O1（沿用）** | **被清成 NULL** | **1（没重发）** | 1 |
  | 3 全新解释器（+90 秒） | **O2（新编号）** | O2 | **2（重发了）** | **2**（第二笔 unknown） |
  - **重启恢复本身是对的**：第二个进程能从库里接着原来那次操作走，被判 budget_denied（同编号仍在途），没有重发。
  - 但它紧接着把编号清掉，于是**第三个进程重开新编号，把一次由已经死掉的进程发出去的调用又发了一遍**，
    第二笔预占落在 unknown（保守计入）——同一次决策记了两个单位。真实供应商下就是**可能付两次钱**。
  这条比 Q-C13 的同进程复现更严重，交 B 一并处理。

二、按用户 06:05 指示的两条更正已落到报告与日志
  1. **CR-Q14、CR-Q15 都归 B（fed3）实施，I 负责协调**；Q 的等待项改为「等 B 交付稳定指纹」，不再等 I 改 B 的文件。
  2. **恢复证据的证明范围**写成一张表：「同一条合同用新进程跑三次」＝**无漂移**；Q-C13 第四步「对象重建」＝**编号在库里不在内存里**，
     **不升级为进程重启验证**；跨进程重启由 Q-C19 承担。三者不能互相替代。

三、**本批存在版本漂移，按约定不作为稳定版本验收**
  运行结束后复核指纹发现 B、A 在本批**运行之后**又改了这些文件，因此四条 FAIL 的结论只绑定各自证据 JSON 里记录的版本：
  | 文件 | 运行时 | 现在（22:13:56Z） |
  |---|---|---|
  | app/web_agent/brain_life.py | 6be01960e911a9c6 | 8895554f7f33d5a3 |
  | app/web_agent/runtime_view.py | 2fbb9055f37b9006 | 1be73e7e00e6bde2 |
  | app/web_agent/brain_wiring.py | 160859641342be08 | 952cc4032503769d |
  | app/web_journey/illustrations.py | 23b9666bf5126a42 | a71f041542c62a8f |
  | app/web_providers/images.py | 6f7bab695bb83553 | 94de22e8359002a6 |
  **不追着改动反复重跑**，等 B 给出稳定指纹后做一次定向复验。

四、等 B 稳定指纹之后的定向复验条件（已排期，未执行）
  - Q-C13 / Q-C19：① 在途（预占未结清）时编号必须保留，不能被退避顺手清掉；② 连续重试与**跨进程重启**都沿用同一编号、不重发；
    ③ 预占到期或结清之后才允许开新编号；④ 假供应商的发出次数与账本的预占／结算笔数一一对得上。
  - Q-C10 / Q-C11：**用正式装配（create_app 出来的默认对象），不手动补回调**；保留「正常路径」与「故障注入后整体回滚」两组对照；
    核对正式 API 上「结果未确认 / 处理中 / 确认失败 / 成功」四态可区分，并确认重画入口确实接受 unknown 起点。
  - 不接真实供应商、不碰 18763/E2、不新增付费调用；不为此重跑已经通过的全部合同。

本批边界：没有改任何业务实现；没有新增付费调用；没有碰 18763 与 E2；没有整仓 Git 操作；子进程全程禁网（拦截计数 0）。
  只写 Q 已领取文件（新增 runtime_contract_recovery.py，改 runtime_contract_cases.py 的基类与 CONTRACTS）与本日志、报告、证据目录。
运行前后指纹（SHA-256 前 16 位，22:13:56Z）：
  Q：runtime_contract_recovery 6bf640462823a363、runtime_contract_boundary 5fea3decab71c38e、runtime_contract_media c6f0896b4e0efc3b、
     runtime_contract_schedule e904f0930d6f4347、runtime_contract_cases cb12b6189ac995bf、test_web_runtime_acceptance 897eec6386a820d2、
     verify_runtime_evidence 54e42a8c74cd6aa3
  被测：见上面第三条的漂移表；未漂移的有 budget 830b16ddcd8d3c18、web_agent_wiring 6a00129ffd61bffa、
     web_communicator/service d47a5a79e493e78e、web_identity/service 986a6904fbbc1742、routers/web/communicator e42321d7b900a1e2、
     schemas/web/social 1e204a5195ebdecc、tasks 212b0a322747409c
未完成 / 下一步：等 B 的稳定指纹，按第四条做 C13／C19／C10／C11 的定向复验。全量回归由 I 收口。
是否释放（是/否，明确范围）：否。

---

时间：2026-09-23 06:30 +08:00
窗口ID / 任务ID / 事件：claude-20260922-234631-4d18 / pkg-Q-acceptance / PROGRESS
本批只做**复验前的判定条件调整**（用户 06:20 的三点），没有跑全套合同。

一、修正「到期后可以换编号」这个默认（用户第 1 点）
  收紧口径并写进两条合同：**reserved / expired / unknown 都不能证明请求没发出去**——预占到期只说明
  「过了保留时限还没人来结清」，不说明对方没收到。因此**超过一轮决策保留时限（STALE_DECISION，15 分钟）之后的恢复
  同样不许换编号重发**。两条合同各加了一步专门跑这个场景：
  - Q-C13 第 5 步 `5_after_stale_window`（同进程 +16 分钟）
  - Q-C19 第 4 步 `4_after_stale_window`（**新进程** +20 分钟）
  同时各加**正向对照**，避免修成永久卡住：只有 `released / not_sent`（确定没离开本机）之后才允许重开编号并再发一次。
  - Q-C13：另一只宠物，替身模型明确 not_sent → 预占整笔 released → 第二次必须拿到**新编号**并允许再进一次调用。
  - Q-C19：另一只宠物，跨两个进程做同样的对照。
  替身模型现在把 `calls`（进了几次 complete）与 `dispatched`（真的越过发送边界几次）分开记，判定各取所需。

二、把「可重复」和「版本一致」分开（用户第 2 点）
  - Q-C19：**每个子进程自己**把被测指纹写进记录文件并 fsync——开始时一条、结束前一条；
    **被杀的那个在 `os._exit(9)` 之前先落盘**。父进程据此核对「各子进程实际执行的是同一个版本」，
    **不再用最后的磁盘指纹代表三个进程**。新增两项检查：「每个子进程都自报了被测指纹」「各子进程实际执行的是同一个版本」。
  - Q-C13：运行前后各记一次被测指纹，新增检查「本次运行期间被测版本没有变化」。
  - 报告与本条都写明：**三次结果一致只证明可重复**；运行结束后改代码也不自动推翻旧版结果。

三、在 B 的在建版本上自检了一次调整后的合同（**这不是验收，记「待复验」**）
  被测版本（由各子进程自报，三次运行完全一致）：brain_life **8895554f7f33d5a3**、runtime_view 1be73e7e00e6bde2、
  budget 830b16ddcd8d3c18、brain_wiring 952cc4032503769d。**B 尚未宣布稳定指纹**，所以下面只作观察，不作结论。
  - Q-C13：13 项检查 **10 通过**；Q-C19：16 项检查 **14 通过**。各连跑 3 次完全一致。
  - 在这一版上，**保留时限内的恢复已经是对的**：编号不再被顺手清掉、三次恢复都沿用同一个编号、一次都没重发、只有 1 笔预占。
    （旧版 6be01960 的 FAIL 证据原样保留，绑定旧指纹；**不凭代码变化提前关闭**，也不拿旧结果去判新版。）
  - 仍然不成立的就是用户点出的那条：**过了 15 分钟保留时限之后重开编号并重发**。
    Q-C13 第 5 步：新编号、`+sent=1`、预占变成 ['expired', 'reserved']；
    Q-C19 第 4 步：新进程拿到新编号、累计发出从 1 变 2、预占变成 ['expired', 'unknown']。
  - **正向对照三项全通过**（两条合同都是）：确定没发出（not_sent）之后允许重开编号、允许再进一次调用、预占整笔退回。
    说明要求 B 做的修改不会把流程修成永久卡住。

四、等 B 稳定交付后的定向复验范围（不变，只做这四条）
  Q-C13 / Q-C19（按上面收紧后的条件）、Q-C10 / Q-C11（**用正式装配，不手动补回调**，保留正常路径与故障回滚两组对照）。
  不重复跑已经通过的全部合同；不接真实供应商；不碰 18763/E2；不新增付费调用。

本批边界：没有改任何业务实现；没有新增付费调用；没有碰 18763 与 E2；没有整仓 Git 操作；没有跑全套。
  只写 Q 已领取文件（runtime_contract_boundary.py、runtime_contract_recovery.py）与本日志、报告、证据目录。
指纹（SHA-256 前 16 位，22:28:20Z）：
  Q：runtime_contract_boundary 9e1f4fd342685556、runtime_contract_recovery e2e8f3dfc416874c、runtime_contract_cases cb12b6189ac995bf
  自检时的被测版本：见第三条（由子进程自报，非事后磁盘快照）
证据：contracts-c13-adjusted-first.json、contracts-c19-adjusted-first.json、
  contracts-c13-c19-20260923T0630-run1/2/3.json（三次一致）。旧版证据保留不动。
未完成 / 下一步：等 B 宣布稳定指纹后做第四条的定向复验。
是否释放（是/否，明确范围）：否。

---

时间：2026-09-23 06:45 +08:00
窗口ID / 任务ID / 事件：claude-20260922-234631-4d18 / pkg-Q-acceptance / PROGRESS
本批按用户 06:35 的两点收紧判定口径，并做了一次定向复验（**不关闭任何 CR**）。

一、「账本不动」已限定范围，并补上「费用事实不得被抹掉」的正面断言
  口径：**「账本不动」只针对宠物的业务账本、钱包与世界状态**（工钱、旅费、行程、世界事件、运行记录）。
  **已经发出的模型/生图调用，其供应商用量、预占与结算必须保留或正确补记**，不能为了让断言变绿而抹掉费用事实。
  落到四条合同：
  - **Q-C14 新增 B 段**：认知线在「模型已经答完、还没写业务」的那一刻被接管（直接改 `web_worker_leases` 的持有者，不动时钟）。
    要求同时成立：不出发、不动钱包、不写生活决定、不写退避 ＋ **预占没有被回滚成 released/not_sent、供应商用量照记、决策编号留着给合法执行者复用**。
  - **Q-C14 新增 C 段（正常租约反向对照）**：第三只宠物走同一条认知线、租约正常 → 必须真的出发、真的记决定、也照常记费用。
    证明围栏不是「一律挡下」。三段都在这个隔离测试进程里做，**没有改任何共享业务源码**。
  - **Q-C11**：把「一起回滚」改写成「业务与展示一起回滚」，并把预占快照并进同一张比对表——
    既不能被抹掉，也不能被这次失败的重画重复记一笔；另加「正常对照：重画真的另起一笔预占（没有吞掉成本）」。
  - **Q-C13 / Q-C19**：各加一条「已发出那次的费用记录一直没被回滚」（跨进程那条按每个进程各查一次）。

二、Q 自己用例的一处修正（如实记录）
  Q-C14 的 C 段第一次是**我的度量问题**导致的红：一轮认知线会替这一轮里所有需要想一想的宠物各走一次，
  我把 `brain.calls` 的总数当成了「这只宠物被调了几次」。已改成只数决策编号里属于该宠物的那些次
  （编号形如 `life:<pet>:<时刻>:<随机>`）。修正后连跑 4 次全绿。

三、定向复验结果（**在 B 的在建版本上；B 尚未宣布稳定指纹，故记「待复验」，不关闭任何 CR**）
  被测版本（Q-C19 由各子进程自报，三次运行完全一致；Q-C13 运行前后自查无漂移）：
    brain_life **6cd250de877f1dfe**、runtime_view **209da22ce2f69638**、brain_wiring **7cd84c0e22f45228**、budget 830b16ddcd8d3c18，
    另：web_agent_wiring **454c60ba271d820e**、illustrations **9a3a90e1fb7402a8**、communicator/service d47a5a79e493e78e。
  | 合同 | 结果 | 次数 |
  |---|---|---|
  | Q-C10 生图结果未确认（正式装配，未手动补回调） | **PASS** 10/10 | 3 次一致 |
  | Q-C11 重画的业务/展示一起回滚 ＋ 费用不被抹掉 | **PASS** 10/10 | 3 次一致 |
  | Q-C13 决策编号与恢复（含过保留时限、正向对照） | **PASS** 14/14 | 3 次一致 |
  | Q-C14 租约围栏（A 世界线 / B 费用保留 / C 正常租约反向对照） | **PASS** 11/11 | 3＋4 次一致 |
  | Q-C19 跨进程恢复（含过保留时限、正向对照、逐进程指纹） | **PASS** 17/17 | 3 次一致 |
  观察到的两处变化（**只记录，不据此关闭**）：
  - `app/web_agent_wiring.py:245-246` 现在接上了 `illustrations.on_unknown` 与 `on_retrying`——CR-Q15 所指的缺口在这一版上不再出现；
  - 过了保留时限之后不再重开编号重发——CR-Q14 所指的缺口在这一版上不再出现。
  **按约定：不凭代码变化提前关闭，也不把旧版失败结论套到新版。** 旧版 FAIL 证据（绑定 6be01960 / 8895554f 等旧指纹）原样保留。
  这五条**暂不并入默认 discover**，等 B 宣布稳定指纹、Q 再复验一次之后再并入防回归。

四、边界与指纹
  没有改任何业务实现；没有新增付费调用；没有碰 18763 与 E2；没有整仓 Git 操作；没有跑全套合同。
  只写 Q 已领取文件（runtime_contract_boundary.py、runtime_contract_media.py、runtime_contract_recovery.py）与本日志、报告、证据目录。
  Q 文件指纹（22:43:04Z）：runtime_contract_boundary eb18bbeb0b322d24、runtime_contract_media f5688e7cea04f505、
    runtime_contract_recovery 276a22b1c59c561c。架构门禁通过（boundary 30 个定义，正好在上限）。
  证据：contracts-c14-adjusted-third.json、contracts-scoped-20260923T0645-run1/2/3.json、
    contracts-c10-20260923T0645(.-run2/3).json。
未完成 / 下一步：等 B 宣布稳定指纹后，按同样五条做一次收口复验并决定是否并入默认回归；CR-Q14、CR-Q15 在那之前保持未关闭。
是否释放（是/否，明确范围）：否。

---

时间：2026-09-23 06:52 +08:00
窗口ID / 任务ID / 事件：claude-20260922-234631-4d18 / pkg-Q-acceptance / CORRECTION ＋ PROGRESS
本批按用户 06:50 的三点收尾；**到此停止追着编辑中的版本反复跑**。

一、CORRECTION：五条合同的状态写法（覆盖前文「新版失败」一类的笼统说法）
  下面五条在**各自证据里记录的指纹**下的结论统一写成 **「隔离合同通过，待稳定候选收口」**——
  既不是「新版失败」，也还不是「已验收」：
  | 合同 | 结论 | 绑定指纹（以各证据 JSON 的 sources / 子进程自报为准） |
  |---|---|---|
  | Q-C10 | 隔离合同通过，待稳定候选收口 | illustrations 9a3a90e1…、web_agent_wiring 454c60ba… |
  | Q-C11 | 隔离合同通过，待稳定候选收口 | 同上 ＋ communicator/service d47a5a79… |
  | Q-C13 | 隔离合同通过，待稳定候选收口 | brain_life 6cd250de…、runtime_view 209da22c…、brain_wiring 7cd84c0e… |
  | Q-C14 | 隔离合同通过，待稳定候选收口 | 同上 ＋ uow 5665d5b5…、lease d29f3bcc…、ticker 8d4f7d1e… |
  | Q-C19 | 隔离合同通过，待稳定候选收口 | 同上（由各子进程自报，三次一致） |
  旧版 FAIL 证据（6be01960 / 8895554f / 23b9666b 等）继续保留在各自指纹下，不被覆盖、也不外推到新版。

二、C10 / C11 等 A、B 两边都稳定（用户第 1 点）
  A 的 `app/web_journey/illustrations.py` 已经从被测的 **9a3a90e1…** 走到 **80269fef…**，本条记录时又到了 **1f9d8853…**。
  旧证据在 9a3a90e1 这一版下仍然有效，但**不覆盖新实现**。等 A、B 宣布稳定候选后，**只复验受影响项**
  （结果未确认的四态展示、重画入口接受 unknown、重画的业务/展示回滚与费用不被抹掉），**不必再整条连跑三次**。

三、Q-C14 补齐两处断言（用户第 2 点）——已完成，连跑 2 次 **12/12 全绿**
  1. **供应商层单独查、而且看本轮增量**：原来的 `provider_used_units` 实际查的是 `pet:<宠物>:life_plan`（每宠额度层）。
     现在分成两项：`pet_used_units`（每宠层）与 `provider_used_delta`（`provider:llm:life_plan` 的**本轮增量**，
     取前后两次快照相减）。实测这一点确有必要：被接管那轮的绝对值是 2→3 / 1→2，
     同一轮里别的宠物也在消耗，只看总数会把「这一次漏记了」盖过去。
  2. **「未写退避」两项一起核**：除了 `silence_reason` 为空，还要 `next_review_at` **与本轮之前完全一致**
     （不新增、不改变）。为此把 `_runtime_row` 的 `next_review_at` 由 bool 改回原值，并在每轮前后各取一份。

四、C 段按实际范围描述（用户第 3 点）
  C 段是**正常路径对照**：只说明「租约正常时这条路径本来就走得通」，因此 A、B 段的「写不进去」不是路径本身不通。
  它**没有故意关掉围栏**，所以**不能叫做对租约围栏的变异验证**。要证明「去掉围栏就会写进去」需要另做一次
  把围栏摘掉的对照——那要改业务源码，超出 Q 的范围，本合同没有做。合同标题、说明与复现步骤都已按这个范围改写。

五、下一步（按用户要求收口）
  停止追版本重复跑。等 A、B 各自宣布**稳定候选**后，Q 做一次收口复验：把**稳定实现指纹 ＋ 合同文件指纹 ＋ 证据指纹**
  三者关联写进报告；届时再由 Q 决定关闭 CR-Q14 / CR-Q15，并把这五条并入默认防回归。全量回归仍由 I 收口。

本批边界：没有改任何业务实现；没有新增付费调用；没有碰 18763 与 E2；没有整仓 Git 操作；没有跑全套合同。
指纹（22:49:04Z）：Q 文件 runtime_contract_boundary **372571beea950ed5**、runtime_contract_media f5688e7cea04f505、
  runtime_contract_recovery 276a22b1c59c561c；架构门禁通过（boundary 30 个定义，正好在上限）。
证据：contracts-c14-scoped-run1/2.json（12/12，两次一致）。
是否释放（是/否，明确范围）：否。

---

时间：2026-09-23 06:55 +08:00
窗口ID / 任务ID / 事件：claude-20260922-234631-4d18 / pkg-Q-acceptance / CORRECTION
更正一句（覆盖 06:52 那条的第四点）：**变异验证可以在独立测试进程里替换函数来做，并不需要改共享业务源码**。
  之前写成「要证明去掉围栏就会写进去得改业务源码、超出 Q 的范围」是不准确的。
  实际范围按这样记：**本批未做租约特定的变异验证**；Q-C14 的 C 段只是正常路径对照。
  **不为此另开一批测试。**
已同步改到：`tests/runtime_contract_boundary.py`（Q-C14 的说明与复现步骤）与报告 §4.11 第四节。
只改了叙述文字，**没有动任何断言、没有改被测范围**，因此不重跑；Q-C14 最近一次结论（12/12，两次一致）继续有效。
合同文件指纹：runtime_contract_boundary **372571beea950ed5 → cc296934d8ad72f6**（仅文案变化）。
状态不变：Q-C10 / C11 / C13 / C14 / C19 保持「隔离合同通过，待稳定候选收口」；停止追版本、停止重复跑。
等 A、B 宣布稳定候选后只复验受影响项，关联实现 / 合同 / 证据三组指纹，再由 Q 决定关闭 CR-Q14、CR-Q15 并并入默认回归。
全量回归由 I 收口。
是否释放（是/否，明确范围）：否。

---

时间：2026-09-23 07:47 +08:00
窗口ID / 任务ID / 事件：claude-20260922-234631-4d18 / pkg-Q-acceptance / PROGRESS
查过各窗口最新记录：**A 侧可以推进，B 侧还不能**。按分工只做 A 侧的收口复验。

一、可推进的判断依据
  - **A（4bef）**：07:10 起冻结 14 份，07:38 复核指纹逐个一致，并明确请 Q「按稳定候选的实际依赖指纹复验 C10／C11，独立决定是否关闭」。
    A 的解冻条件之一就是 Q 给出 C10／C11 结论——**Q 在关键路径上**。
    Q 逐个核对了 A 宣布的 9 份实现指纹，**与磁盘完全一致**：tasks b1d3fa67、lease d29f3bcc、budget 830b16dd、meter e4c7565c、
    images 94de22e8、illustrations 1f9d8853、guides 3ff5f201、communicator/service 97d3c52d、collection/service 343d31ea。
  - **B（fed3）**：07:18 自称「本批为稳定批次」，但 Q 复核发现**三份已经又变了**，所以 B 侧不作收口：
    | 文件 | B 宣布 | 磁盘（23:43:46Z） |
    |---|---|---|
    | app/web_agent/runtime_view.py | 4893EB5C07008307 | **1ba2b4f22f438490** |
    | app/web_agent/runtime_store.py | 3DBF12288E3621AA | **410695f2201f288f** |
    | app/web_agent_wiring.py | 471529A2CF9CF646 | **dbbbdf6768355ed0** |
    （brain_life 6cd250de、life 11627c57、ticker 8d4f7d1e、heartbeat_policy 5fb3c07d、brain_wiring 8c7651d8 与其宣布一致。）

二、A 侧收口复验结果（一次运行，不再连跑三次）
  证据 `contracts-A-stable-20260923T0745.json`（23:44:23Z）：**Q-C10 10/10、Q-C11 10/10、Q-C15 6/6，全部 PASS**。
  各条 `sources` 与 A 宣布的指纹逐个对上（illustrations 1f9d8853、images 94de22e8、communicator/service 97d3c52d、
  tasks b1d3fa67、budget 830b16dd、routers/web/communicator a992acfe、schemas/web/social 1e204a51）。
  **给 A 的结论：A 侧稳定候选下 C10 / C11 / C15 通过。** A 可据此推进解冻的第 ① 项条件。
  **范围声明**：C10 / C11 还依赖 B 的组合接线 `app/web_agent_wiring.py`（`on_unknown` / `on_retrying` 两行，
  实测在 **dbbbdf6768355ed0** 这一版上是接好的），**这一版不是 B 宣布的版本**。
  所以这两条里与接线相关的检查，等 B 指纹稳定后再快速复跑一次即可，A 侧的 9 份实现不需要再动。

三、CR 关闭决定
  - **CR-Q15（on_unknown / on_retrying 未接线）**：缺陷本身已消失（接线存在，且走正式装配、未手动补回调即可通过）。
    但它是 **B 的文件**、当前版本 B 未宣布，**故暂不关闭**，改记「已修复，待 B 指纹稳定后关闭」。
  - **CR-Q14（在途未结清就清编号 / 过保留时限重开编号重发）**：依赖 B 的 runtime_view / runtime_store，**保持未关闭**。
  - 这两条都不因代码变化提前关闭。

四、并入默认防回归的调整（Q 自己的判断）
  - **新并入：c10_photo_unknown_result、c11_redraw_is_all_or_nothing**（A 侧稳定候选下通过；
    若 B 的接线回退，默认回归会直接变红，这正是要的效果）。c15 早已在内。
  - **c14_lease_fence 保留在内**：在 B 的在建版本上仍然 12/12 全绿；等 B 稳定时连同结论重新绑定指纹。
  - **c13、c19 仍不并入**：结论必须绑定 B 的稳定指纹。
  默认 discover 实测：**25 项 OK / 92.2 秒**（＝1 项判定器自测 ＋ 24 条已纳入合同）。

五、顺手发现（交 B，不属于 Q）
  `python scripts/arch_gate.py` **当前失败 1 处**：`PetJourneyBackend/tests/test_web_runtime_projection.py`
  **384 行 / 37 个 def·class > 30**。这是 B 本批新增的文件；B 在 07:18 的记录里写的是「arch_gate 与 dependency_gate 均通过」。
  该文件磁盘指纹 **040ae633c57a6901**，与 B 宣布的 548BC809DD61583F 不同——应是宣布之后又加了用例。
  Q 不改别人的文件，只报告：**B 的稳定批次在门禁上目前是不成立的**，请 B 拆分后重出指纹。

本批边界：没有改任何业务实现；没有新增付费调用；没有碰 18763 与 E2；没有整仓 Git 操作；没有跑全套合同（只跑了受影响的三条 ＋ 一次默认 discover）。
Q 文件指纹（23:47:05Z）：test_web_runtime_acceptance **ccbabb933956dc28**、runtime_contract_boundary cc296934d8ad72f6、
  runtime_contract_media f5688e7cea04f505、runtime_contract_recovery 276a22b1c59c561c、runtime_contract_schedule e904f0930d6f4347、
  runtime_contract_cases cb12b6189ac995bf。
未完成 / 下一步：等 B 拆完门禁违规、重出稳定指纹，复跑 c13 / c19（必要时 c14 重新绑定），再决定关闭 CR-Q14 / CR-Q15。
是否释放（是/否，明确范围）：否。

---

时间：2026-09-23 07:52 +08:00
窗口ID / 任务ID / 事件：claude-20260922-234631-4d18 / pkg-Q-acceptance / NOTE（已直接同步 B）
就 07:47 那条第五点，补一次准确记录，并已把内容直接发给 B（世界时钟与心跳窗口）。

一、门禁：已自愈，只作记录
  - 23:47:05Z（07:47 +08）Q 跑 `python scripts/arch_gate.py` **失败**，违规文件是 B 本批新增的
    `PetJourneyBackend/tests/test_web_runtime_projection.py`（**384 行 / 37 个 def·class > 30**，磁盘指纹 040ae633c57a6901），
    与 B 在 07:18 记录的「arch_gate 与 dependency_gate 均通过」不符。
  - B 在 07:48:08 改过该文件；23:50:47Z Q 再跑**已通过**（该文件现在 **155 行 / 14 个定义**，指纹 **fa082842c5fc5734**）。
  - 结论：门禁这条不需要 B 再处理，**但它说明 B 07:18 的「稳定批次」在宣布时点上并不成立**。

二、真正要 B 处理的是指纹对不上（Q 不猜，等 B 重出表）
  | 文件 | B 07:18 宣布 | 磁盘（23:43:46Z，至 23:50Z 未再变） |
  |---|---|---|
  | app/web_agent/runtime_view.py | 4893EB5C07008307 | **1ba2b4f22f438490** |
  | app/web_agent/runtime_store.py | 3DBF12288E3621AA | **410695f2201f288f** |
  | app/web_agent_wiring.py | 471529A2CF9CF646 | **dbbbdf6768355ed0** |
  | tests/test_web_runtime_projection.py | 548BC809DD61583F | **fa082842c5fc5734** |
  与 B 宣布一致的：brain_life 6cd250de、brain_wiring 8c7651d8、life 11627c57、ticker 8d4f7d1e、heartbeat_policy 5fb3c07d。
  前三份自 23:43:46Z 起已静止约 7 分钟；**若这就是 B 要交的稳定值，B 在自己日志里重出一版表即可**。

三、同步方式
  已通过跨会话消息直接发给 B 所在窗口（「世界时钟与心跳」），内容＝上面一、二两节 ＋ Q 这边与 B 相关的进展
  （A 侧 C10/C11/C15 已通过并并入默认回归；C10/C11 依赖 B 的 `web_agent_wiring.py` 245-246 行的
  `on_unknown` / `on_retrying`，在 dbbbdf67 上是接好的；C13/C19 一直在等 B 的稳定指纹，现在没跑）。
  **Q 不写别人的日志**，B 的回执以 B 自己的记录为准；消息送达不等于 B 已读。
四、盯的条件（已架监视，每 60 秒一次）
  `arch_gate` 通过 ＋ B 的日志有新记录 ＋ 那三份指纹连续两次不变，三条同时成立就复跑 C13 / C19，
  并把 C14 的结论重新绑定一次指纹。CR-Q14 保持未关闭；CR-Q15 记「已修复，待 B 指纹稳定后关闭」。
是否释放（是/否，明确范围）：否。

---

时间：2026-09-23 07:56 +08:00
窗口ID / 任务ID / 事件：claude-20260922-234631-4d18 / pkg-Q-acceptance / HANDOFF（收口：关闭 CR-Q14、CR-Q15）
B 在 07:51 重出了稳定指纹表并宣布自 08:00 起冻结；A 自 07:10 起已冻结。两边都稳定，Q 做完收口复验。

一、三组指纹关联（实现 ／ 合同 ／ 证据）
  **实现（B 07:51 宣布 ＋ A 07:10 冻结，Q 于 23:52:29Z 逐个核对磁盘，16 份全部一致）**
    B：runtime_store 410695f2201f288f、runtime_view 1ba2b4f22f438490、brain_life 6cd250de877f1dfe、
       brain_wiring 8c7651d88cd75c64、web_agent_wiring dbbbdf6768355ed0、life 11627c57c8cfaa85、
       ticker 8d4f7d1e58c8dd09、heartbeat_policy 5fb3c07d012d3f93、
       tests/test_web_runtime_projection fa082842c5fc5734、tests/test_web_runtime_due_selection 1af8e4136328c912
    A：illustrations 1f9d8853a0cead1c、images 94de22e8359002a6、communicator/service 97d3c52d96dea5a8、
       tasks b1d3fa674a15dfec、budget 830b16ddcd8d3c18、lease d29f3bcc7a58f804
  **合同（Q 自己的文件，23:55:19Z）**
    test_web_runtime_acceptance c7ada8e1bbb6bb15、runtime_contract_cases cb12b6189ac995bf、
    runtime_contract_boundary cc296934d8ad72f6、runtime_contract_media f5688e7cea04f505、
    runtime_contract_recovery 276a22b1c59c561c、runtime_contract_schedule e904f0930d6f4347、
    runtime_contract_reliability 2addad1983c0e5f8、runtime_contract_harness f1052714eb4edd46、
    scripts/verify_runtime_evidence 54e42a8c74cd6aa3
  **证据**
    contracts-A-stable-20260923T0745.json（23:44:23Z，C10 10/10、C11 10/10、C15 6/6）
    contracts-B-stable-20260923T0755.json（23:53:00Z，C13 14/14、C14 12/12、C19 17/17）
    各条的 `sources` 与上面的实现指纹逐个对得上；C19 的六个子进程**自报**的版本也完全一致。

二、复验结果
  - **Q-C13 PASS 14/14**：保留时限内三次恢复沿用同一编号、一次不重发、只有 1 笔预占；
    **过了保留时限（+16 分钟）仍然沿用同一编号、仍然不重发、不因到期另开预占**；
    已发出那次的费用记录一直没被回滚；正向对照（确定 not_sent 之后允许重开编号并再进一次调用）成立。
  - **Q-C19 PASS 17/17**：三个全新解释器先后打开同一个库，含**过保留时限那一次**，累计只发出 1 次、只有 1 笔预占、编号始终不变；
    跨进程正向对照成立；逐子进程自报指纹一致。
  - **Q-C14 PASS 12/12**（结论重新绑定到这组指纹）：被接管时业务账本／钱包／世界状态写不进去，
    **已发出调用的费用事实保留**（每宠层 ＋ 供应商层本轮增量都照记），正常路径对照照常写入。
  - 默认 discover：**27 项 OK / 101.1 秒**（＝1 项判定器自测 ＋ **26 条合同**）。arch_gate、dependency_gate 均通过。

三、CR 关闭决定（Q 独立判断，不据他人自测）
  - **CR-Q14（在途未结清就清掉决策编号 → 下一轮重开新编号并重发）：关闭。**
    旧版复现证据保留在 brain_life 6be01960（同进程）与 8895554f（同进程＋跨进程）两组指纹下，不覆盖；
    本次在 6cd250de ＋ runtime_view 1ba2b4f2 上由 Q-C13、Q-C19 独立复验通过。
  - **CR-Q15（`on_unknown` / `on_retrying` 未接线，消费者收不到"结果未确认"与"重画已排上队"）：关闭。**
    接线现位于 `app/web_agent_wiring.py` 第 245-246 行（指纹 dbbbdf67）；
    Q-C10、Q-C11 走**正式装配、未手动补任何回调**通过，旧版 FAIL 证据保留在 illustrations 23b9666b 一组指纹下。
  - 其余仍开着的不因此关闭：CR-B6（B 自记"shadow 已接、正式切换未完成"）、CR-B7/B8/B11、
    A 的 `illustrations.py:395` 吞 LeaseLost、C 的 `settlement.py:133`。这些不在 Q 本轮范围内。

四、默认防回归已收齐
  **26 条合同全部进默认 discover，没有"显式运行才跑"的遗留项**（此前 c10/c11/c13/c19 是例外，现已并入）。
  以后任何一方回退这两个缺陷，Q 的默认回归会直接变红。

五、给各窗口
  - **A（4bef）**：Q 的 C10／C11 结论已给出（通过，绑定 A 的 9 份实现指纹）——A 解冻的第 ① 项条件满足；第 ② 项（I 的整仓回归）归 I。
  - **B（fed3）**：Q 的 C13／C19／C14 结论已给出（通过，绑定 B 07:51 那张表）——B 可据此推进下一批（CR-B7/B8/B11）。
  - **I（307b）**：Q 这边 26 条合同 ＋ 判定器自测在上述指纹下全绿，两道门禁通过；**全量回归仍由 I 收口**。
本批边界：没有改任何业务实现；没有新增付费调用；没有碰 18763 与 E2；没有整仓 Git 操作。
是否释放（是/否，明确范围）：否。

---

时间：2026-09-23 08:10 +08:00
窗口ID / 任务ID / 事件：claude-20260922-234631-4d18 / pkg-Q-acceptance / ACK COORD-0802-Q ＋ SCOPE_CHANGE
回执协调单 **COORD-0802-Q**。继续独立验收、不改业务实现；不重复跑全套、不重复三连跑。
已读 I 的 08:02：整仓回归**尚未开始**（还在等 B、P 给区间，窗口由六方交集定）。故本批可以写自己的测试文件；
若 I 在我写完之前真的开跑，我立即转只读，等本轮结束再动。

一、对照现有合同后的缺口（已覆盖的直接引用，不重复造）
  **CR-C8（租约失效中止本轮）**
    - 已覆盖（Q-C14，指纹见 07:56 收口条）：② 中止后无额外业务写入（行程／钱包／世界事件／运行记录都不动）；
      ③ 正常租约对照走完；以及协调单点名的**已发出供应商费用事实保留**（每宠层 ＋ 供应商层本轮增量都照记）。
    - **缺口**：① 的「**真正整轮中止**」没有可观察证据——Q-C14 只看了被接管那一只宠物／那一件事，
      **没有断言"同一轮里后续本应处理的宠物、后续本应执行的任务不再被处理"**。C 也写明它的 `round_stopped`
      只覆盖 `consider` 这一层，直接调 `consider` 的脚本证明不了整轮停止。→ 要补。
  **CR-C10（`lane_fence` 叠加语义）**：Q 现有合同**一条都不覆盖**（C 有自己的 9 项反例，但那是作者侧）。
    四条判定全缺：嵌套叠加、`None` 不解除、异常退出恢复、正常路径对照。→ 全补。
  **CR-C9（借车券同事务）**：Q 现有合同**不覆盖**。按协调单只补两条最关键的：
    真实 `collection.consume_in` ＋ **正式装配**下的正常用券出发（不扣租车费、券确实核销）；
    **核销之后任一步异常 → 券与行程一起回滚**。C 的 ③④⑤ 由 C 的最终接入验证覆盖，Q 本批不重复。

二、预计测试文件范围（**只写 Q 已领取的文件，不新增路径、不碰共享实现**）
  - `PetJourneyBackend/tests/runtime_contract_reliability.py`：新增 **Q-C20**（CR-C10 四条）、**Q-C21**（CR-C9 两条）
  - `PetJourneyBackend/tests/runtime_contract_schedule.py`：新增 **Q-C22**（CR-C8 整轮中止：后续宠物 ＋ 后续任务）
  - `PetJourneyBackend/tests/runtime_contract_cases.py`、`tests/test_web_runtime_acceptance.py`：只改登记与 INTEGRATED 元组
  手段：一次性临时库、替身、禁网；**变异只在测试进程内替换函数**（与 C 07:33、Q 06:55 同一做法），**不改任何共享实现**。

三、本批测试停写区间（已同步给 I）
  - **写入：2026-09-23 08:10 – 08:40 +0800**（只写上面四份 Q 自己的文件）
  - **08:40 起停写**，或**一旦 I 宣布全仓开跑就立即停手转只读**，以先到者为准；停写后在本日志明确宣布。
  - Q 的文件不在 I 的 15 份冻结清单里，也不在 A／B／C 的冻结清单里，不构成冲突。

四、状态同步（按协调单更正）
  - **不再以 Q 的通过作为 B 解冻的依据**。Q 07:56 给 B 的结论只说明「在 B 07:51 那张表的指纹上 C13／C19／C14 通过」，
    **不是准许 B 解冻**；B 08:02 起继续 B7/B8/B11、在改 runtime_view／runtime_store／brain_wiring，由协调方要求其 bounded 收批重发完整指纹。
  - **Q 原 26 条结果保留原指纹**（A 07:10 冻结的 6 份 ＋ B 07:51 宣布的 10 份），
    **不因 B 在建变化就说旧结论失效**；B 收批重发指纹后再按新指纹定向复验受影响项。
  - **I 已修 `settlement.py`**（吞 LeaseLost 那条）；对 A 的 `illustrations.py:395` 同类请求已按正式路径**撤为知情项**，
    Q 这边同步记为知情项，不作为 Q 的待办、也不计入任何合同结论。
是否释放（是/否，明确范围）：否。

---

时间：2026-09-23 08:17 +08:00
窗口ID / 任务ID / 事件：claude-20260922-234631-4d18 / pkg-Q-acceptance / DELIVERY ＋ **STOP WRITING（提前，08:17，早于承诺的 08:40）**
协调单 COORD-0802-Q 的三项缺口已补完并全部通过。**Q 从这一条起停止写入，直到 I 宣布整仓回归结束。**

一、交付：三条新合同，各跑两次，结论一致
  | 合同 | 结果 | 覆盖的 CR |
  |---|---|---|
  | **Q-C20** 围栏叠加 | **PASS 9/9**（单元：真实类＋隔离 SQLite） | CR-C10 四条判定 ＋ 两种进程内变异自证 |
  | **Q-C21** 借车券同事务 | **PASS 6/6**（集成：正式装配、真实收藏服务，不打替身） | CR-C9 的 ①② |
  | **Q-C22** 整轮中止 | **PASS 8/8**（集成） | CR-C8 缺的那一半：后续宠物 ＋ 后续任务 |
  证据：contracts-C-coverage-second.json（00:14:07Z）、contracts-C-coverage-20260923T0815.json（00:14:32Z）。

二、各条的实际观察
  - **Q-C20**：外层失效租约 ＋ 内层通过的检查 → 仍被挡下，且外层那道**确实执行过**（`seen=["lease"]`）；
    `lane_fence(None)` 不解除；内层块抛异常退出后外层照旧生效；两道都过则写得进去，内层退出后只剩外层也写得进去。
    **变异自证分两种，各判别不同判据**（都只在测试进程里替换 `lane_fence`，不改共享实现）：
    ①「内层覆盖外层」旧写法 → 嵌套与 `None` 两条变红，且 `seen=["version"]` 证明外层被顶掉；
    ②「装上不还原」旧写法 → 异常退出那条变红。两次变异下正常路径都仍然写得进去（排除"一律放行"的假阳）。
    **如实注明**：①那一种**判别不了第三条**（旧写法在块退出时同样会还原），所以才补了②。
  - **Q-C21**：真实 `collection.keepsake` 发券 → 走正式 `POST /journey/depart {local:drive_trip}`：
    出发成功、**0 扣费、0 旅费流水、券 consumed_at 已写**；故障组把 `collection.consume_in` 包一层
    （先调真实实现完成核销，紧接着抛异常）→ **0 行程、券仍可用（未核销）、钱一分没动**。
    C 的 ③④⑤ 由 C 的最终接入验证覆盖，Q 不重复。
  - **Q-C22**：认知线一轮里 4 只宠物要想，第一只的模型调用中途把 `cognition` 租约让给别人 →
    **只想了第一只**、0 行程、后面三只**一行运行记录都没写**；世界线在**本轮进行中**丢掉 `world` 租约 →
    队尾哨兵任务**没跑**、到期工钱**没入账**；正常租约对照下同一轮**一路跑到最后**、工钱照常入账。
    途中修正：第一版我在跑轮**之前**改租约行，结果整轮直接被跳过（或那一行还不存在），证明不了"中途丢了之后后面不再跑"；
    已改成队首插一步在轮内让出租约。这是 Q 的用例设计问题，不是产品缺陷。

三、默认防回归
  三条已一并纳入。默认 discover：**30 项 OK / 112.4 秒**（＝1 项判定器自测 ＋ **29 条合同**）。
  `arch_gate`、`dependency_gate` 均通过。

四、**停写宣布**
  Q 于 **08:17 +0800** 停止写入全部文件（比承诺的 08:40 提前 23 分钟），直到 I 宣布整仓回归结束。
  停写时的 Q 文件指纹：
    runtime_contract_reliability **ca81d00a4376d4f5**、runtime_contract_schedule **d504f9b71979c46d**、
    runtime_contract_cases **625048abf8850997**、test_web_runtime_acceptance **7860f258308c952c**、
    runtime_contract_boundary cc296934d8ad72f6、runtime_contract_media f5688e7cea04f505、
    runtime_contract_recovery 276a22b1c59c561c、runtime_contract_harness f1052714eb4edd46、
    scripts/verify_runtime_evidence 54e42a8c74cd6aa3
  本批全程：不改任何业务实现、不新增付费调用、不碰 18763 与 E2、不做整仓 Git 操作；变异只在测试进程内替换函数。

五、给 C（91f2）的结论
  **CR-C8 / CR-C9 / CR-C10 三项的独立验收：Q 这边全部通过**，绑定指纹见各证据的 `sources`
  （`uow.py 5665d5b5158c1bdd`、`journey/service.py 6d5280010f5fb035`、`collection/service.py f80f936ae1baf70c` 等）。
  是否关闭由 C 自己按其流程决定；Q 只给独立结论，不代为关闭。
是否释放（是/否，明确范围）：否。

---

时间：2026-09-23 08:23 +08:00
窗口ID / 任务ID / 事件：claude-20260922-234631-4d18 / pkg-Q-acceptance / CORRECTION（**只追加本日志，停写状态不变**）
停写期间**没有改动任何代码或用例文件**；本条只是把两处绑定指纹改准，并记下独立复核到的漂移。
（I 08:20 来信告知 B 的指纹又变、且 `test_web_runtime_projection` 有一项报错；Q 按自己的规矩复核了一遍，结果如下。）

一、更正我 08:17 那条里的两个指纹（以各证据 JSON 的 `sources` 为准，不以别人日志里的数字为准）
  1. **Q-C21 绑定的 `app/web_collection/service.py` 是 `343d31ea3d5a3826`**（A 07:10 冻结表里的值），
     我 08:17 写成了 C 日志里更早的 `f80f936ae1baf70c`，**写错了**。证据里两次运行都是 `343d31ea3d5a3826`。
  2. **Q-C22 绑定的 `app/web_agent/brain_wiring.py` 是 `7f160f1701087e2f`**——
     也就是说 00:14Z 跑那两次时，B 早已改过 07:55 宣布的 `8c7651d8`。
     **Q-C22 的结论绑的是 B 未宣布的在建版本**，等 B 收批重发指纹后需要重新绑定一次（内容不必重跑，只核指纹与受影响项）。

二、独立复核到的漂移（Q 于 00:21:47Z 实测，**与 I 08:03 的数字又不一样**，说明 B 仍在快速改）
  | 文件 | Q 原绑定 | I 08:03 实测 | Q 00:21:47Z 实测 |
  |---|---|---|---|
  | app/web_agent/runtime_view.py | 1ba2b4f22f438490 | 7e575374fefa4c0f | **ab95ef8a65af3c46** |
  | app/web_agent/brain_wiring.py | 8c7651d88cd75c64 | 7f160f1701087e2f | **1488b54adfb9fc19** |
  未漂移（与 Q 绑定一致）：brain_life 6cd250de、runtime_store 410695f2、web_agent_wiring dbbbdf67、
  uow 5665d5b5、journey/service 6d5280010f5fb035、illustrations 1f9d8853。
  **按既定规矩**：Q 的既有结论**保留在各自绑定的指纹上**，不因 B 在建变化就作废；也不拿旧结论去判新版。
  受影响待重新绑定的是：C13 / C19 / C14（原绑 runtime_view 1ba2b4f2、brain_wiring 8c7651d8）与 C22（原绑 brain_wiring 7f160f17）。
  不受影响：C10 / C11 / C15（A 侧冻结指纹未变）、C20 / C21（uow、journey/service、collection、credentials_wiring 均未变）。

三、I 提到的那一项报错，Q 复核结果是**已经不复现**
  `tests/test_web_runtime_projection`（B 的文件）Q 于 00:22:24Z 只读复跑：**16 项全过 / 23.4 秒**，
  没有出现 `AttributeError: 'HeartbeatShadow' object has no attribute 'monitor'`。
  与上面第二节的指纹变化一致——B 应该已经在 08:03 之后把 CR-B7 那半截接上了。
  **这条只是 Q 的只读观察，不构成对 B 本批的验收**；B 的收批仍以 B 自己重发的完整指纹为准。
  已把这一条回告 I（它把该报错列为等待 B 收批的阻塞事实之一）。

四、契约 0.4.4 的知情项
  `/ops/runtime` 新增可选字段 `next_review_at`（CR-B11 的接口配合，归 I）；路由/模型/枚举数量未变（117/210/109）。
  与 Q 现有 29 条合同**无关**（没有一条断言 `/ops/runtime` 的字段集合），记为知情项，不触发复验。
是否释放（是/否，明确范围）：否。停写状态不变，等 I 宣布整仓回归结束。

---

时间：2026-09-23 08:45 +08:00
窗口ID / 任务ID / 事件：claude-20260922-234631-4d18 / pkg-Q-acceptance / NOTE（COORD-Q-NEXT 回执；**只追加本日志，停写不变**）
停写期间**没有改动任何代码或用例文件**（本条与前一条同理：协调日志不在冻结工具监控的 `app` ＋ `tests` 之内）。

一、更正 I 本轮回归的起跑事实（以协调方给的实测为准，不以日志里的估计为准）
  - `candidate-0829` **实际起于 08:29:59 +0800**（PID 48648 / 23872），**不是** I 日志里写的 08:35 / 08:37。
  - **实际六方区间到 09:45 +0800**（不是 OPEN 那条写的交集 09:10）。I 已收到更正指令。
  - 我 08:37 那条转述（"08:37 开跑、交集 08:35–09:10"）据此作废，以本条为准。
  - Q 的动作不变：**停写到 CLOSED**；只按**真实 verdict 与依赖变化**收口，**不追在建文件、不连跑旧全套**。

二、登记一个**新的独立缺口**（生图在途撤权），待 A 稳定交付后由 Q 独立复验
  来源：协调方在**保留的候选副本**上独立跑出的反例，证据在
  `E:/petsoul-audit/coordination-audit/image-revocation-20260923/`（FINDING.md、probe.py、
  evidence-20260923T004057923672Z.json、second-run-full.txt、first-run-full.txt）。
  **它自己写明"Coordinator probe only, not Q independent acceptance"**，Q 按这个范围记录，不当成 Q 的结论。
  现象（4 个一次性临时应用、真实 HTTP 家庭设置、假生图供应商、外部连接 0 次）：
  | 场景 | 假生图调用 | 每次调用时的授权 | 结果 |
  |---|---:|---|---|
  | 正常许可（对照） | 2 | true, true | ready、任务 succeeded、媒体 GET 200 |
  | **开工前撤权** | 0 | — | 任务 superseded，**但消息与插画一直停在 processing** |
  | **证件照响应后、场景图调用前撤权** | 2 | true, **false** | **仍然发出第二次调用**，并 ready、GET 200 |
  | **场景图响应后、最终写事务前撤权** | 2 | true, true | **新结果仍被发布为 ready** |
  三次撤权都走正式家庭设置 PATCH 且断言成功，`privacy_epoch` 由 1 进到 2。费用侧：成功调用的场景各留一笔
  settled、`actual_units=2`；开工前撤权那次没有预占。**没有发生也没有验证任何真实供应商费用。**
  Q 的只读旁证（读代码，不是运行证据）：`IllustrationService.opted_in` 只在**排队时**（`request` / `request_image`）
  与**领取前**（`precheck`）被问到，`_render` 在两次调用之间、以及提交发布之前**都没有再问一次**——与上面的现象一致。

三、范围声明（重要，防止把旧结论放大）
  **这不推翻 Q-C10 / Q-C11 的既有范围**（它们验的是"结果未确认的展示与重画"与"重画的业务/展示回滚 ＋ 费用不被抹掉"），
  但**也绝不能把那两条的 PASS 扩大成"家庭生图在途撤权已受保护"**——那是本条新发现的缺口，Q 从没验过。
  已把这句写进本条，后续报告引用 C10/C11 时一并带上。

四、Q 的后续动作（**现在不写合同**，避免破坏当前冻结）
  待 **A 稳定交付**（A 负责生图执行与消费者状态；**B 配合正式权限回调**）后，Q 独立走**正式装配 ＋ 家庭接口**补一条合同，覆盖四个时点：
  ① 发送之前 ② 两次调用之间 ③ 最终发布之前 ④ 正常许可的正向对照。
  断言口径（按协调单，已定死）：
  - **已发出的调用其成本不被抹掉**（本地用量／预占照实结算）；
  - **未发送的不消耗**（开工前撤权不占额度）；
  - **不留永久 processing**，也**不自动重试**；
  - **不要求远端已经发出的调用可被撤销或退款**——只要求"撤权之后不再发下一次""迟到的结果不得以 ready 面世"。
  历史照片不在本条范围，修复不得顺带删除。
五、照片导演接入的验收层级（一并记下，避免以后混用）
  后续照片导演接入仍按**真实世界事件 ＋ 网页 API** 验证；**fixture 图**与**8 张真图**分开标层级，不互相替代。
是否释放（是/否，明确范围）：否。停写到 I 写 CLOSED。

---

时间：2026-09-23 08:50 +08:00
窗口ID / 任务ID / 事件：claude-20260922-234631-4d18 / pkg-Q-acceptance / PROGRESS（**解冻**；C13 / C14 / C19 / C22 重新绑定指纹）
I 于 08:46 写 CLOSED：整仓 **853 项全过（OK, skipped=1）**、退出码 0、可归属、两道门禁绿，各持有人可解冻。
Q 自 08:17 起的停写到此结束。**下面每一条都是 Q 自己在快照里复核的，不是转述 I 的数字。**

一、Q 的独立复核（对象是快照 `E:\petsoul-audit\snapshots\candidate-0829\`，不是工作区）
  1. **Q 的 8 份用例在 manifest 里与停写指纹逐个一致**，且 `manifest-before` 与 `manifest-after` 对这 8 份完全相同：
     harness f1052714、cases 625048ab、reliability ca81d00a、boundary cc296934、media f5688e7c、
     schedule d504f9b7、recovery 276a22b1、test_web_runtime_acceptance 7860f258。
  2. `verdict.json` 实读：`attributable=true`、`drift=[]`、`fingerprint_before == fingerprint_after == e59ed76174679de5`。
  3. **Q 的合同确实在这 853 项里跑过**。`run.log` 是非 verbose 的（0 行 " ... ok"），所以查不到用例名——
     **不能据此说没跑**。改查 Q 合同独有的运行痕迹，都在：
     `disk I/O error (injected by Q-C11)` ×8、`q-c21 注入` ×4、`Q 注入` ×6、
     `world ticker stopped mid-round` ×9、`brain round stopped` ×5。
     （Q-C20 不打日志，所以没有痕迹属正常；它的结论由下一条的"零失败"覆盖。）
  4. 853 项**零失败零错误** → Q 的 30 项（1 判定器自测 ＋ 29 合同）在候选源码上全部通过。
     唯一 `skipped=1` 是 `tests/test_place.py:495` 的 `time.tzset()` 平台限制，与 Q 无关。

二、**重新绑定**（这次比 Q 自己单跑更强：整段运行零漂移、前后 manifest 一致，绑的是同一组源码）
  候选里 Q 依赖的实现指纹（Q 从 `manifest-before.json` 实读）：
    runtime_view **ab95ef8a65af3c46**、brain_wiring **1488b54adfb9fc19**、brain_life 6cd250de877f1dfe、
    runtime_store 410695f2201f288f、web_agent_wiring dbbbdf6768355ed0、uow 5665d5b5158c1bdd、
    journey/service 6d5280010f5fb035、collection/service 343d31ea3d5a3826、illustrations 1f9d8853a0cead1c、
    lease d29f3bcc7a58f804、ticker 8d4f7d1e58c8dd09、budget 830b16ddcd8d3c18
  据此：
  - **Q-C13 / Q-C19 / Q-C14**：原绑 runtime_view 1ba2b4f2、brain_wiring 8c7651d8 → **重新绑定到上面这组**，结论仍是通过。
  - **Q-C22**：原绑 brain_wiring 7f160f17（B 未宣布的在建版本）→ **重新绑定到 1488b54a**，结论仍是通过。
  - C10 / C11 / C15 / C20 / C21 的依赖在候选里与原绑定**一致**，无需重新绑定。
  - **旧指纹上的结论一律保留、不覆盖**；重新绑定是"又多了一组指纹上的证据"，不是把旧的改写掉。

三、这次运行**没有**证明什么（照抄 I 的边界，Q 认同并补一条）
  没有证明真实环境、真实供应商费用、Linux 复现、视觉质量；18763 仍在跑第 8 批。
  **Q 补一条**：它也**没有**证明 08:45 那条登记的「家庭生图在途撤权」缺口已被修——那条反例跑在保留副本上、
  针对的是当前实现，853 全过只说明**现有用例**没覆盖到它。该缺口仍为待修（A 主修，B 配合正式权限回调），
  Q 的合同待 A 稳定交付后再写。

四、解冻后的 Q 状态
  - 29 条合同 ＋ 1 项判定器自测，全部在默认 discover 里；CR-Q14 / CR-Q15 已关闭（07:56）。
  - C 交来的 CR-C8 / CR-C9 / CR-C10 三项独立结论已给出（08:17，Q-C22 / Q-C21 / Q-C20），是否关闭由 C 决定。
  - 待办只有一项：**家庭生图在途撤权**的合同，等 A 稳定交付。
  - 后续定向复跑**从快照跑**（工作区各家解冻后会变），这一点采纳 I 的建议。
是否释放（是/否，明确范围）：否。

---

时间：2026-09-23 09:05 +08:00
窗口ID / 任务ID / 事件：claude-20260922-234631-4d18 / pkg-Q-acceptance / DELIVERY（COORD-Q-REVOCATION：Q-C23 四时点合同，**PASS 17/17**）
按协调单立即建立独立合同，不等 A 的照片导演大批；只跑这一条新增合同（含其内置的正常许可对照），
**没有重跑旧 29 条，没有触碰 candidate-0829**，四个正式场景一律走当前装配、**没有手工补任何回调**。

一、被测版本（各条以证据 `sources` 为准，两次运行完全一致）
  A：illustrations **b95c65faf289e73d**（A 09:00 更正后的值；其用例文件 dc52ff62 → **b818ba04b3012daf**，Q 只读核对一致，未运行 A 的用例）
  其余：images 94de22e8359002a6、communicator/service 97d3c52d96dea5a8、runtime_epochs 89b3488db6bb4511、
       **web_agent_wiring ec95758120cc4686**、**web_household/access 406b6c96e0fe507e**
  合同文件：runtime_contract_media **4399cd0cb56bd2a2**

二、结果：**Q-C23 PASS 17/17**，两次运行一致（01:03:12Z、01:03:56Z）
  | 场景 | 发出 | 插画 | 展示 | 结算 | 当日用量 | privacy_epoch |
  |---|---:|---|---|---|---:|---|
  | 正常许可（对照） | 2 | ready | ready | settled/succeeded/2 | +2 | 1→1 |
  | 开工前撤权 | **0** | failed | failed | **无预占** | **+0** | 1→2 |
  | 两次调用之间撤权 | **1** | failed | failed | settled/succeeded/**1** | +1 | 1→2 |
  | 最终写入前撤权 | 2 | failed | failed | settled/succeeded/2 | +2 | 1→2 |
  | 最终写入前撤权（只留版本围栏） | 2 | failed | failed | settled/succeeded/2 | +2 | 1→2 |
  逐条成立：撤权后不再发下一次；迟到的结果**不以 ready 面世**；已发出的**按实际次数结算、不退成 not_sent**；
  没发出的**不占额度**；三组撤权都**不留永久"处理中"**、展示如实 failed（不冒充"结果未确认"）；
  时钟 +10 分钟再跑一轮**不自动重试**、任务不停在 queued/running。
  **没有断言**已经发出的远端调用可以撤销或退款——协调单明确不要求，Q 也没要求。

三、**单列**正式 consent_in 那一层的门槛（按协调单要求，不与版本围栏混算）
  - 「正式装配里接上了 consent_in」→ **成立**。
  - 「正常路径下 consent_in 真的被问到」→ **成立**（计数 1；只包一层计数，不改行为）。
  - 「只留版本围栏那一层也挡得住」→ **成立**：把 consent_in 在本测试进程里**摘掉**（减一层，不是补替身），
    三项观察不变（2 发、不发布、展示 failed），consent_in 被问 0 次。
  **由此得到的分工结论**：三个撤权场景实际是**被 privacy_epoch 版本围栏挡下的**——
  `run_claimed` 里代数一变就短路，consent_in 在那几条路上根本没被问到（计数 0）。
  consent_in 只在**代数没动**时多加一道。两层各自的作用范围因此是分开的，不能互相代替。

四、**更正 A 的一处事实**（Q 两次独立实测，已回执 A）
  A 在 09:00 说「B 目前仍未接 consent_in（web_agent_wiring / web_composition 里都没有这个赋值）」——**与实测不符**：
  - `grep`：`app/web_agent_wiring.py:246  illustrations.consent_in = lambda conn, user_id, pet_id: households.generated_photos_in(conn, pet_id)`
  - 真装配实测：`create_app()` 之后 `web.illustrations.consent_in is None` → **False**
  时刻 09:01:30，`web_agent_wiring` 指纹 **ec95758120cc4686**（B 在短时间内从 dbbbdf67 → 6b2792d0 → ec957581 连改三轮，
  A 大概读到的是更早一版）。所以 A 自述的"最终复核只有一道"这个限定**比实际更严**；两道都在，且 Q 已分别验过。
  `households.generated_photos_in(conn, pet_id)` 确实存在（access.py 406b6c96，全仓一份实现）。

五、版本已取定，A 可以继续下一小批（已回执）
  Q 的结论绑定上面第一节那组指纹。**A 不必等 Q 结束再动**：后续 A/B/I 若交新版本，Q 按新指纹分别绑定、只复验受影响项。
  该合同已纳入默认 discover（第 30 条合同）。
是否释放（是/否，明确范围）：否。

---

时间：2026-09-23 09:15 +08:00
窗口ID / 任务ID / 事件：claude-20260922-234631-4d18 / pkg-Q-acceptance / ACK COORD-Q-PHOTO-NEXT ＋ SCOPE_CHANGE
本批**只登记与准备判据，不写合同代码**（各拥有者还在改，按协调单"不在编辑中追跑"）。也**没有重跑整套**。

一、SCOPE_CHANGE：登记新增路径 `PetJourneyBackend/tests/runtime_contract_photo.py`
  冲突检查（09:13 实测）：该路径磁盘不存在；全仓 docs/ 下无任何窗口提到这个文件名。
  为什么要新文件：`runtime_contract_media.py` 已 29 个定义（门禁上限 30），塞不下四条新合同。
  新文件不以 test_ 开头，默认 discover 不收集；入口仍走 Q 已持有的 runtime_contract_cases.py 与 test_web_runtime_acceptance.py。

二、四条合同与判据（先写死口径，待各拥有者给出可运行指纹后再实现）
  **Q-C24 同事务（拥有者 C ＋ A）**
    - `photo_taken` 事件、`visit`、`web_illustrations` 记录、`web_tasks` 任务**在同一个写事务里**；
      在其中任意两步之间注入异常 → **四者一起回滚**（不留"有事件没任务""有插画没事件"的中间态）；正常对照四者一起出现。
    - `captured_at` **等于主人点击的那一刻**：不等于到店时刻、不等于 worker 执行时刻。
      判法：点击后把时钟推进若干分钟再跑 worker，payload 里的 `captured_at` 必须仍是点击时刻。
    - `source_key` **重试不换**：同一次请求重试（同 Idempotency-Key）指向同一个 `dedupe_key = illustration:<source_key>`，不新建第二条。
  **Q-C25 正式 worker 只用导演编译结果（拥有者 P ＋ A）**
    - 传给图像适配器的 **prompt 文本、参考图 bytes 的哈希、尺寸 2048** 三样，必须**逐一等于** P 编译结果里的对应项
      （判法：包一层记录适配器实参，与导演编译产物逐项比对，不是"看起来像"）。
    - 目标场景**缺事实 / 事实过期 / 授权被撤** → **hold**：**0 次图片调用、0 笔预占**，
      且**不得回落旧模板**（断言 prompt 不等于旧 `build_*_prompt` 的产物）。
    - 规则导演**0 次文本调用**（P 自述），单独断言。
  **Q-C26 场景事实与幂等（拥有者 B ＋ I）**
    - `home` / `train` 必须是**真实状态**：不在家 / 不在列车上 → **409，连事件都不写**（与导演的 hold 区分开，两种拒绝不混算）。
      `train` 只认封闭词表（`TransportMode.train`），认不出一律不算。
    - `flight_adventure` **明确虚构**：不写世界事件、不进旅程历史、不扣旅费、不发勋章；但**留可恢复的插画记录**。
    - 同 Idempotency-Key：**跨分钟**、**对象重建**、**并发**三种情况都不得多登记一条。
  **Q-C27 图片额度（拥有者 I ＋ B）**
    - **每宠上限与全局上限并列**（两层都查，取更紧的一层），`used + inflight` **都计入**；
    - **单宠耗尽不吃光其他宠物**：A 宠打满之后 B 宠仍可正常出图；
    - 09:13 实测：`web_agent_wiring.py` 里目前**还没有**每宠图片上限（只有 `provider:image:daily` 一层），属待交付。

三、我需要的签名与事实（已直接发给各拥有者，不经用户传话）
  已读到的（不必再问）：`illustrations.request_photo_in(conn, user_id, pet_id, source_key, *, place, city, scene, captured_at=None, scene_key=None, **extras)`；
  `tasks.enqueue_in(conn, kind, dedupe_key, payload, *, ...)`；`photo_command` 的 `SCENE_FACTS` / `FACT_BASIS`；
  路由 `/pets/{pet_id}/photo-request` 已有 409 与 hold 的区分说明，并走 `idempotent(...)`。
  仍需确认：C 的 `photo_request_in(conn, visit, journey, *, captured_at, source_key)` 返回什么、`captured_at` 落在哪张表哪一列；
  P 的编译产物对外暴露的确切字段名（prompt / 参考 bytes / size）与 hold 的表示法；
  I/B 的每宠图片上限的 scope_key 命名与默认值。

四、边界
  不等全仓冻结、不在编辑中追跑；待各拥有者交出**具体可运行指纹**后**只验新增/受影响项**，不为准备重复跑整套。
  浏览器首条链路（5293→18765）由协调方复验，Q 不碰；**0 新增付费**；旧候选 candidate-0829 与 18763 一概不动。
  缺的是产品判断而非事实时，向协调方报，不自行替产品决定。
是否释放（是/否，明确范围）：否。

---

时间：2026-09-23 09:20 +08:00
窗口ID / 任务ID / 事件：claude-20260922-234631-4d18 / pkg-Q-acceptance / NOTE（按 C 09:1X 的答复修正 Q-C24 判据；并因 journey/service 变更做受影响项重绑）
C 答复了我 09:15 问的两点，还主动给了一条会让合同空转的前提。判据按其答复修正如下（**仍未写合同代码**）。

一、Q-C24 判据修正（C 的答复，Q 采纳）
  1. **`photo_request_in` 返回 `str | None`**，`None` **不是错误**，是"这次不生成照片"（没开启／供应商不可用），
     C 在同一个 `act()` 里转走**纸质卡片**分支（写 `web_postcards`，事件 data 变成 `{"photo_url": ...}`）。
     → 判据改成**两条分支分开断言**，不把 `None` 当失败。
  2. **`captured_at` 在 C 这侧落在 `web_world_events.occurred_at`**（`photo_taken` 那一行，值＝`act()` 收到的 `now`），
     **不写 `web_visits`**。A 那侧的落库（任务 payload / `web_illustrations`）另算。
     → C 侧等价断言：`web_world_events.occurred_at == 点击时刻`；"推进时钟再跑 worker"那条验的是 A 侧。
  3. **会让合同空转的前提（最重要）**：`act()` 里还有只读判定 **`journeys.photo_generation_on(visit, journey) -> bool`（默认 False）**。
     为假时**根本不会调用** `photo_request_in`，直接走纸卡片。
     → Q 的装配必须**同时**设这两个；只装 `photo_request_in` 会"全绿但什么都没测"。
     → 另加一条：它为真却没装 `photo_request_in` 时，应在动任何业务数据之前抛 `JourneyError("photo_not_wired")`。
  4. **注入点与两个坑**（C 踩过，Q 采纳）：
     - 建议用 `repo.update_visit` 返回 False（事务内造版本冲突、不争锁），或 `lane_fence` 造租约失效；
     - **租约那条容易空转**：围栏装在整段外面时，更早的世界推进会先被挡下，**根本走不到拍照**。
       → Q 会按调用栈只对 `act` 那段生效，并**断言围栏确实是在拍照那段拦下的**，否则这条判据无效。
     - **纸卡片的 SVG 文件写在事务外**（文件系统没有事务）。回滚后会留下一个没人引用的 SVG，
       这是 C 刻意选的方向（反过来"库里有卡片、文件不在"才是用户看得见的坏结果）。
       → **不得把残留 SVG 判成原子性失败**；原子集合就是那几张表。这条写进判据，免得以后误判。
  5. **幂等还有更早一层**：同一 visit 的 take_photo 活动一旦 `state == "done"`，`act()` 在最开头原样返回、
     **根本不再调钩子**；之后才轮到 `source_key = photo:<visit_id>` → A 的 `dedupe_key = illustration:photo:<visit_id>`。
     → 两层都断言。

二、C 对 Q 08:17 三项结论的处理（记录，Q 不代为关闭）
  - **CR-C10：C 已关闭**（`uow.py` 仍 `5665d5b5158c1bdd`，与 Q-C20 绑定完全一致）。
  - **CR-C8：C 已关闭**（Q-C22 补的正是它够不到的那半；实现归 B，状态由 B／I 收口）。
  - **CR-C9：C 暂不关闭**——Q-C21 绑的是 `journey/service.py 6d5280010f5fb035`，C 为照片接入又改过两次，
    现在是 **`24cb6ba86e6dcc24`**（Q 09:18 实测一致）。C 请 Q 在新指纹上重绑。

三、受影响项重绑（正在跑，结果另记）
  `journey/service.py` 是中心文件，Q 有 12 处合同把它记进证据（C3a/C3c/C3b/C5/C6/C7a/C7b/C7c/C8/C12/C14/C21 等）。
  **逐条挑着 `--only` 跑反而比一次跑完贵**，所以这次用一次完整合同回归拿到每条自己的 `sources`，
  一次把全部受影响项重绑到当前指纹。**这不是"为准备重复跑整套"**，是 C 的实现变更牵连面广所致；
  照片四条合同仍未开写，等各拥有者的可运行指纹。
  C 当前可运行指纹（Q 只核对、未运行 C 的用例）：service `24cb6ba86e6dcc24`、postcards `3fb4910b402b6792`、
  uow `5665d5b5158c1bdd`、tests/test_web_photo_commit `62fb0e88568b880e`、tests/test_web_postcard_commit `764371a54659573a`。
是否释放（是/否，明确范围）：否。

---

时间：2026-09-23 09:22 +08:00
窗口ID / 任务ID / 事件：claude-20260922-234631-4d18 / pkg-Q-acceptance / NOTE（按 P 09:2X 的答复修正 Q-C25 判据；含一处会打空的断言）
P 答复了我 09:15 问的三点，并指出我一条断言会打空。判据修正如下（**仍未写合同代码**）。

一、编译产物的确切名字（P 给的，Q 按此写）
  编译结果是独立对象 **`DirectedPhoto`**（**不是** `SceneDraft`），由 `PhotoDirector().direct(context, access)` 返回。
  `SceneDraft` 只装创作选择（recipe / camera / composition / expression / visible_facts），**上面没有 prompt、没有 size、没有参考图**，挂在 `photo.draft`。
  要断言的三样：`photo.prompt`（str）、`photo.size`（str，当前恒为 `"2048x2048"`）、
  `photo.references`（`tuple[ReferenceSlot]`，首批长度 1）；`ReferenceSlot = (position, role, reference_id, sha256)`，
  首批 `[0].position == 0`、`[0].role == "pet_identity"`。

二、**"参考图 bytes"那条要改写法**（Q 原判据在这一点上无对象可比）
  `app/web_photo_director/**` **全程不碰图片字节**，只带摘要元数据，字节由 A 的媒体层自己取。
  → 改成等价且更强的命题：`hashlib.sha256(适配器实际收到的 bytes).hexdigest() == photo.references[0].sha256`
    （P 保证 `photo.identity_sha256 == photo.references[0].sha256`）。
    这条能抓到"取错宠物的参考照"和"参考照换了但提示词没换"两类真错，比逐字节比对更有用。

三、hold 的表示法：**结构化对象**，既不是 None、也不只是异常
  推荐入口 `readiness(context, access) -> Readiness(ready, reason, missing_required, optional_absent)`，`__bool__` 即 `ready`，不抛异常。
  `reason` 是封闭取值：`None`（ready）／`scene_not_supported`／`hold_missing_required`／`hold_validation_failed`／`hold_no_shot_is_possible`。
  纵深防御：`direct()` 在 hold 条件下抛 `PhotoDirectorError`（`ValueError` 子类），message 就是稳定原因码。
  **本包没有任何入口返回 None** → Q 不写"返回 None 即 hold"这类断言。

四、**三种情况要分清，不是两种**（Q 原来只分了两种，这里补第三种）
  1. **409**：命令根本不成立、事件没落库 → **导演从未被调用**；
  2. **hold**：事件已存在、worker 跑了、导演判定不出图 → 导演被调用且 `readiness().ready is False`（或 `direct()` 抛 `PhotoDirectorError`），
     同时 **0 次图片调用、0 次预占、不回落旧模板**；
  3. **路由不走导演**：A 没拿到 `scene_key`（明信片邮筒自拍、爪爪驾校证件照这类映射不到四场景的）→ 继续走旧模板，
     **属正常路径，不是 hold**。Q 不得把它判成缺陷。

五、**Q 原有一条断言会打空**（P 指出，Q 已自己核实）
  `build_prompt` 是**同名双存**：
    `app/web_journey/photo_prompts.py:14  def build_prompt(*, species, name, personality, title, story, with_reference)`  ← 旧模板
    `app/web_photo_director/compiler.py:168 def build_prompt(context, draft)`                                            ← 导演自己的
  按名字导入很可能拿到导演包那个，断言就变成"导演的提示词 ≠ 导演自己编的提示词"，恒假或毫无意义。
  → **必须写全路径**：`from app.web_journey.photo_prompts import build_selfie_prompt`。
  另外旧模板**已不在** `web_journey/illustrations.py`，全在 `web_journey/photo_prompts.py`
  （`build_selfie_prompt:32`、`build_portrait_prompt:45`、`build_journal_prompt:53`）。Q 09:21 自己 grep 核实，与 P 所述一致。

六、文本调用的账（Q 原理解正确，P 确认）
  `TextCallRecord.outcome ∈ {skipped, rule_only, refused, sent_ok, sent_unknown, invalid_output}`，
  **只结算导演自己的文本调用**，与 A 的图片预占是两本账，不共用额度、不互相抵扣。
  首批规则导演实跑值：`photo.directed_by == "rule"`、`text_call.outcome == "skipped"`、`text_call.reserved is False`，
  `reason ∈ {text_director_not_permitted, text_director_not_wired}`。
  Q 的"0 次文本调用用计数替身钉死"仍然写，P 说零注入时在碰 chat 与预算**之前**就 return，计数替身会看到 0。

七、**再避一条白写的断言**（P 主动给）
  `photo.negative_prompt` **有值但首批不下发**（方舟 `/images/generations` 官方参数表里没有这个字段）。
  → 正确断言不是"负面提示词被传下去了"，而是
    `plan_delivery(photo, sink=CURRENT_WEB_SINK, strict=False).dropped == ("negative_prompt",)`
    ——即**丢的只有它，没有静默丢别的**。
P 自述本包仍冻结、与 candidate-0829 逐字节一致、159 项自测通过（作者自测，不替代 Q 的验收）、自 8 张真图批次以来供应商调用 0 次。Q 未运行 P 的用例。
是否释放（是/否，明确范围）：否。

---

时间：2026-09-23 09:25 +08:00
窗口ID / 任务ID / 事件：claude-20260922-234631-4d18 / pkg-Q-acceptance / PROGRESS（受影响项重绑完成：**30 条全 PASS**）
证据 `contracts-rebind-20260923T0920.json`（01:20:38Z）：请求 30、实跑 30、**30 PASS / 0 FAIL**，禁网拦截 0。

一、重绑结果
  把 `app/web_journey/service.py` 记进证据的共 **10 条**（Q-C3a、C3b、C3c、C6、C7a、C7b、C7c、C12、C14、C21），
  现在**全部绑定到 `24cb6ba86e6dcc24`**（C 为照片接入改后的当前版本），无一条仍绑旧的 `6d5280010f5fb035`。
  - **Q-C21 PASS 6/6** → 绑定 `service.py 24cb6ba86e6dcc24`、`collection/service.py 343d31ea3d5a3826`、
    `credentials_wiring c1c7704cc3ab7da1`。**CR-C9 的独立结论在新指纹上重新成立**，是否关闭由 C 决定。
  - Q-C14 PASS 12/12、Q-C12 PASS 12/12，均已带上新的 service 指纹。
  - 另注意：Q-C12 这次绑到的 `web_identity/service.py` 是 **3aa3dc1d07fad3e0**（不再是 986a6904），I 又改过；
    结论在新指纹上仍成立。
  旧指纹上的结论一律保留、不覆盖；重绑是多一组证据，不是改写旧的。

二、为什么一次跑完而不是逐条挑
  service.py 是中心文件，牵连 10 条；逐条 `--only` 要跑 10 次、每次都要重建应用，比一次完整回归更贵。
  这次是**实现变更导致的受影响项复验**，不是"为准备重复跑整套"。照片四条合同仍未开写。

三、当前 Q 状态
  30 条合同全部在默认 discover 里（第 31 项是判定器自测）。待办：照片四条合同，等各拥有者的可运行指纹。
  已收到并写进判据的答复：C（09:1X，见 09:20 那条）、P（09:2X，见 09:22 那条）。仍等 I／B 的每宠图片上限 scope_key。
是否释放（是/否，明确范围）：否。

---

时间：2026-09-23 09:28 +08:00
窗口ID / 任务ID / 事件：claude-20260922-234631-4d18 / pkg-Q-acceptance / DELIVERY（COORD-Q-PHOTO-C24-START：**Q-C24 PASS 14/14**）
新文件 `PetJourneyBackend/tests/runtime_contract_photo.py`（09:15 已登记路径）落地第一条：**Q-C24**。两次运行一致。

一、结果与绑定指纹（以证据 `sources` 为准）
  证据：contracts-c24-run1.json（01:26:06Z）、contracts-c24-run2.json（01:26:14Z），**PASS 14/14**。
  绑定：`journey/service.py 24cb6ba86e6dcc24`、`web_agent/photo_wiring.py 63437f0d539c735b`、
        `web_journey/illustrations.py **0d8b3d99e21d3ca5**`、`web_journey/postcards.py 3fb4910b402b6792`、
        `web_agent_wiring.py e6b82b47bb26d44e`。
  **注意漂移**：协调单转述 A 的 `illustrations e1aa7677159e3190`，我 09:23 实测时还一致，运行时已变成 `0d8b3d99e21d3ca5`
  （A 仍在写 worker 导演）。结论绑运行时那一版，不绑转述值。tasks 81cd33e7、photo_prompts 4d7a7140、
  test_web_photo_atomic fd8b7154（文件名是 `test_web_photo_atomic.py`，协调单写的 `test_photo_atomic` 少了前缀）均与转述一致。

二、十四项检查（全过）
  - **接线由正式装配提供**：`photo_request_in`、`photo_generation_on` 都在（`web_agent_wiring.py:253/257`）。**没有手补任何回调**。
  - 正常分支：到访活动 done、`photo_taken` 事件、`web_illustrations`、`web_tasks` **四者一起出现**；
    事件带的是 `photo_task_id`（不是照片地址）；去重键就是 `illustration:photo:<visit_id>`。
  - **拍摄时刻＝点击那一刻**：事件 `occurred_at` 等于点击时刻；再推进 37 分钟跑 worker，**仍不被执行时刻盖掉**。
  - **幂等**：趁 TA 还在店里再点一次同一活动 → 200 且事件/任务都仍是 1 条（`act()` 在最开头原样返回）。
  - **注入版本冲突**（把 `repo.update_visit` 包成返回 False，事务内造冲突、不争锁）：**四者一个都没写进去**，活动也没变 done。
  - **纸卡片分支**（不开"生成照片"）：留下卡片与事件、事件带的是 `photo_url`；**不排队、不写插画**。
  - **守卫**：该生成却把 `photo_request_in` 摘掉时（减一层，不是补回调），在动任何业务数据之前就被拒绝，四者全 0。

三、途中两处修正（都是 Q 的用例问题，不是产品缺陷）
  1. `web_world_events` 表里**没有 data 列**（只有 journey_id/event_key/kind/occurred_at/applied_at）。
     事件带的数据随 outbox 一起落库（`settlement._record_in` 写 `payload={"key":…, "data":…}`），
     所以"事件带了什么"要读 `web_outbox.payload_json`。已改。
  2. 幂等那条原先排在"推进 37 分钟"之后，那时 TA 已经离店，会先被 `not_in_venue` 挡下——**测不到幂等这一层**。
     已把再点一次挪到推进时钟之前。这正是 C 09:1X 提醒的"空转"那一类，换了个位置又踩了一次，记下来。

四、边界
  只跑这一条新增合同（两次），**没有重跑旧 30 条**；零付费调用；未碰 candidate-0829 与 18763。
  Q-C24 已并入默认 discover。Q-C25 等 A 的 worker 导演；Q-C26／C27 见下一条（I 刚交付）。
  Q 文件指纹：runtime_contract_photo **338b85c0d40cd924**、runtime_contract_cases **b90a3a279433cd5d**。
是否释放（是/否，明确范围）：否。

---

时间：2026-09-23 09:40 +08:00
窗口ID / 任务ID / 事件：claude-20260922-234631-4d18 / pkg-Q-acceptance / DELIVERY（**Q-C27 PASS 9/9**）＋ 一条给 B 的观察
按 COORD-Q-C26-C27 继续。Q-C27 写在 `tests/runtime_contract_photo.py`，两次运行一致（01:40:15Z、01:40:22Z）。

一、结果与绑定
  **PASS 9/9**，绑定 `brain_wiring b66ef7d0562832ee`、`budget 830b16ddcd8d3c18`、
  `illustrations 64c0aee1787813e0`、`config 078248ac5208eb38`（每宠 6、全局 20）。
  九项：两层上限并列（一笔预占同时记进 `provider:image:daily` 与 `pet:<id>:illustration`）；
  **一次无参考照请求占 2 个单位**（单位不是张数）；每宠上限来自 `web_image_per_pet_daily_cap`；
  **inflight 也算**（那几笔从未结算，`inflight=6 / used=0` 就已占满）；用完后被拒**且拒在这只宠物那一层**；
  被拒不新增预占行；被拒时**替身供应商一次都没被调用**、插画如实落 failed；
  一只打满**不吃掉别的宠物**；**全局那层独立生效**——把全局占满后，一只每宠额度从没动过的新宠物照样被拒在 `provider:image:daily`。
  做法：一律用**装配时真正在用的** `web.illustrations.reserve`，不自己拼 `BudgetLimit`。

二、**给 B 的观察：中间版本 `bff9438e7fe5a995` 上每宠那一层不生效**
  同一条合同在三个 brain_wiring 版本上跑过，证据里都记了运行时指纹：
  | 运行 | brain_wiring | `pet:<id>:illustration` 是否进 scope_pairs |
  |---|---|---|
  | 01:37:15Z | `b66ef7d0562832ee` | **在**（那次失败只因为 Q 自己的断言漏算了第三条 `usage:image:illustration`） |
  | 01:37:52Z / 01:38:06Z | **`bff9438e7fe5a995`** | **不在**；该宠物计数 `used=0 / inflight=0`，额度形同没有 |
  | 01:40:15Z / 01:40:22Z | `b66ef7d0562832ee`（已回到这一版） | **在**，9/9 全过 |
  也就是说 B 在 09:37–09:40 之间短暂交出过一版**每宠上限不起作用**的接线，现在已回到有效那版。
  **这不是对 B 本批的判决**（B 未宣布该中间版为稳定），只作观察记录；B 收批时请确认交出的是含每宠那一层的版本。
  这条也说明协调单"不要追在建文件"的道理：同一条合同在 3 分钟内因他人改动给出过相反结果。

三、Q 自己的两处断言修正（都不是产品问题）
  1. 账本除两道上限外还会自己加一条不设限的 `usage:image:illustration` 日用量，所以"两层并列"要用**包含判断**，不能写等于。
  2. "占满每宠额度"原来多占了一笔（第一笔已经占过 2 个单位），最后那笔本来就该被拒——已按 `per_pet_cap // 2 - 1` 计算补占几笔。

四、边界
  只跑这一条新增合同（共 5 次，其中 2 次是版本漂移期间的失败记录，原样保留）；**没有重跑旧 30 条**；
  未改业务实现；零付费调用；未碰 candidate-0829 与 18763。Q-C27 已并入默认 discover。
  下一条：**Q-C26**（含协调方点名的组合边界：首单 home 登记 → 只丢幂等回执 → 离家 +6 分钟同 key 仍取回原 task／原 captured_at／原 place／narrative）。
是否释放（是/否，明确范围）：否。

---

时间：2026-09-23 09:43 +08:00
窗口ID / 任务ID / 事件：claude-20260922-234631-4d18 / pkg-Q-acceptance / DELIVERY（**Q-C26 PASS 12/12**，含协调方点名的组合边界）
两次运行一致（01:42:49Z、01:42:54Z）。绑定：`routers/web/pets.py a4381ddf68dd854d`、
`photo_command.py 91bda7a473c4e25e`、`schemas/web/pets.py 6292f085b005821f`、`idempotency.py 9bd51d739d789bcc`、
`illustrations.py ab53d78495d04676`。**注意**：路由与 schema 的指纹都已不是协调单转述的 `1efd48e8` / `f7385841`（I 还在改），
结论绑运行时这一版。

一、十二项检查（全过）
  - 在家时命令成立并留下**一条**登记。
  - **组合边界（协调方在 I 旧版 1efd48e8 上实跑到 409 的那条）现在成立**：
    只删掉 `web_idempotency_keys` 里那一行（任务与插画记录都不动）→ 出门去 local:cafe → 时钟 +6 分钟 → 同 key 再请求：
    **200**（不是 409）、取回的是**原来那个任务**、拍摄时刻是**首次按下那一刻**、地点与叙事都按首次写下的、**不新增第二条登记**。
  - 不在家却要 `home`：**409 且一条登记都不写**；在家却要 `train`：同样 409 不留记录。
  - `flight_adventure`：默认叙事 **422**（必须显式选 `fictional_adventure`）；显式选了之后成立并留**可恢复的登记**，
    同时**世界事件 0 / 行程 0 / 勋章 0 / 旅费 0**——不碰真实旅行。
  - 同 key **跨宠不串号**：另一只宠物复用同一个 Idempotency-Key，拿到的是自己的登记，不是前一只的任务。

二、**未覆盖（如实写明）**
  `train` 的**正向**场景需要一段真实列车行程（封闭词表只认 `TransportMode.train`），本合同只验了它的**拒绝侧**。
  正向那一侧等有真实列车段的构造手段再补，**不拿拒绝侧的通过冒充正向已验**。

三、边界
  只跑这一条新增合同（3 次）；没有重跑旧 30 条；未改业务实现；零付费；未碰 candidate-0829 与 18763。
  Q-C26 已并入默认 discover。架构门禁通过。
  Q 文件指纹：runtime_contract_photo **64af82128fc24a39**、test_web_runtime_acceptance **270547b1502bc97e**、
  runtime_contract_cases b90a3a279433cd5d。
四、当前照片四条的状态
  **Q-C24 PASS 14/14**、**Q-C26 PASS 12/12**、**Q-C27 PASS 9/9**（含给 B 的中间版本观察）；
  **Q-C25 等 A 的真实 worker 接入**（P 正在只读审桥接；按 P 的口径，要比对的是
  `photo.prompt` / `photo.size` / `sha256(适配器实收 bytes) == photo.references[0].sha256`，
  hold 走 `readiness()`，且**不能用测试私补字段**冒充正式入口 payload）。
是否释放（是/否，明确范围）：否。

---

时间：2026-09-23 09:47 +08:00
窗口ID / 任务ID / 事件：claude-20260922-234631-4d18 / pkg-Q-acceptance / DELIVERY（**Q-C26 补 train 正向 → PASS 18/18**）＋ 登记 C 的 CR-C12
两次运行一致（01:46:57Z、01:47:03Z）。绑定：`routers/web/pets.py 38198fb5bfd112cd`、`photo_command.py 91bda7a473c4e25e`、
`schemas/web/pets.py 6292f085b005821f`、`idempotency.py 9bd51d739d789bcc`、`illustrations.py ab53d78495d04676`。
（路由指纹这一轮又变了，I 仍在改；结论绑运行时这一版。）

一、train 正向的做法（**不照抄魔数、不改库伪造**）
  真的买票出发 `POST /journey/depart {"destination_key":"tokyo_flight"}`，然后从 **`web_journey_legs` 里查出**
  `mode='train'` 的那一段，把时钟推到**该段的中点**。这一趟实际落出的 mode 是 `['flight','taxi','train','walk']`，
  列车段是 sequence 4（09:41Z–10:21Z），时钟取其中点。时刻表改了也不会失真——区间是从库里读的，不是写死的分钟数。
二、train 正向实测
  - 坐在真实列车段上要 `train`：**200**，留下**一条**登记。
  - 登记里带的已核验事实是 `['on_train','train_seat','train_window']`，且**每条 `verified` 都是 True**
    （结构是 `{fact_id, token, pet_id, household_id, event_id, verified}`，键名是 **token** 不是 key）。
  - 拍摄时刻**就是此刻**（下发的是按 TA 所在城市换算的本地时刻，所以比的是**同一瞬间**不是字符串），地点是「列车上」。
  - **对照**：同一段上要 `home` → **409**；在 `flight` 段上要 `train` → **409**。证明 train 通过不是"一律放行"。
三、Q 自己的三处修正（都不是产品问题）
  1. 事实的键名是 `token`（我原来写 `key`）；2. `captured_at` 按当地时区下发，必须比瞬间不能比字符串；
  3. `train_pos` 忘了放进证据 observed，以及一条断言返回的是任务号字符串而不是布尔。
四、登记 **CR-C12**（C 09:4X 交来，归 A；Q 待 A 修好后独立复验，本轮不写）
  **与 Q-C24 不是同一件事**：Q-C24 验的是 `act` 那一刻四表同事务（**已成立**）；CR-C12 在**提交之后**——
  任务进程与 FAST 投递是两个执行者，顺序不定。生图先到终态、家庭消息后插时，
  `communicator/service.py:376` 的 `illustration_ready` 是 `UPDATE … WHERE photo_task_id = ?`，消息行还没插就命中 0 行；
  而 `:329/340` 的 `on_world_event` 又**无条件** INSERT `photo_status='processing'` → **消息永久停在处理中**，无纠正路径。
  C 给的注入点（Q 采纳，待写时直接用）：**包住真实 `communicator._insert_event`**——此刻 `on_world_event` 已走完、
  任务号已有、消息还没插；先断言前提（任务 queued、插画已登记），再驱动真实 worker ＋ 假供应商到终态，最后委托回原方法。
  **正式回调不动、不手补字段、不手工造终态**。投递不在写事务里（`settlement._deliver_one` 在事务外调 sink），
  所以**不会争锁**——不要把 SQLite 锁异常当成保护。
  C 点出的两个坑（记下来免得重踩）：① 别用"扣住整个 sink"的办法——冒险插画那条路的图**正是消息 sink 自己请求的**，
  扣住 sink 就根本没有任务，那一档会空转变成假 FAIL；② 想拿"已 ready"的最强形态目前只能走**冒险插画**，
  目标 `take_photo` 会被导演挡在 `director_hold:inputs_missing`（C 实测 `missing=()` 空清单），只能拿到 failed 那一档——**这是范围不是漏测**。
五、边界
  只跑这一条（3 次）；没有重跑旧 30 条；未改业务实现；零付费；未碰 candidate-0829 与 18763。
  Q 文件指纹：runtime_contract_photo **07532cfaa4cfc8fd**。
是否释放（是/否，明确范围）：否。

---

时间：2026-09-23 09:53 +08:00
窗口ID / 任务ID / 事件：claude-20260922-234631-4d18 / pkg-Q-acceptance / DELIVERY（**Q-C28 PASS 14/14**：只读可见闭环与显式重画）
按 I 09:48 给的签名写成，两次运行一致（01:52:45Z、01:52:51Z）。
绑定：`routers/web/pets.py 38198fb5bfd112cd`（与 I 宣布一致）、`schemas/web/pets.py 6292f085b005821f`、
`photo_command.py 91bda7a473c4e25e`、`illustrations.py 64bbeae64890d417`（A 仍在改，绑运行时这一版）。

一、十四项检查（全过）
  - 列表是**裸数组**、新请求有**稳定编号**；排队中＝`processing`／无图／不可重画；
    画好＝`ready`／`image_url` 就是 `/api/v1/web/media/illustrations/{request_id}`／不可重画。
  - **GET 是纯读**：连读三次，替身调用次数、预占条数、任务状态一个都没动。
  - **四态分得清**：超时那条显示 `unknown`（可重画、无图），当场被拒且次数用完那条显示 `failed`，**两个取值不同**。
  - **显式重画**：回 200 ＋ 整个列表、任务排回队、**原先那笔已发出的成本没被退回**（不是 `not_sent`）、**另起一张票**。
  - **已经画好的那条重画**：回 200 ＋ 当前状态，**不重排**（任务仍 `succeeded`）、再跑一轮 worker **一次调用都不发**。
  - **跨家庭一律 404**：看别人的列表 404、拿别人的编号重画 404（不泄露存在与否）。
二、**一处与我预判相反的现状，记清楚**
  我原以为"真实上传过照片的宠物会被导演挡下"。**实测相反**：
  - **真实上传过照片** → 参考照来源明确 → 导演放行 → 1 次调用、`actual_units=1`、`ready`；
  - **没上传过照片** → 参考照来源无从得知 → **被导演挡下，一次调用都不发、连预占都没有**（`photo_status=failed`）。
  `reference_origin_of` 在正式装配里确实**没接**（Q 实测 `web.illustrations.reference_origin_of("x") is None`，
  组合根里 grep 不到赋值 → A 的 CR-A15 未落地）。所以本合同用**真实上传**跑通四态，
  用**没上传**那一侧钉住当前的 hold 行为——**没有手补 `reference_origin_of`**。
  这一侧"不花钱"是安全的，但"新伙伴（没上传照片）永远拍不出主动照片"是**产品可见的缺口**，归 A／B／I 闭环（见下）。
三、Q 自己的三处修正（都不是产品问题）
  1. 四态场景原先用"没上传照片"的宠物，结果全被 hold 成 failed，`ready`/`unknown` 根本造不出来——已改用真实上传。
  2. 有参考照时一次请求只发一次调用，替身剧本要跟着改（原来按两次写）。
  3. "已画好还再发"那条原先把**给 b 重画跑的那一轮**算到了 a 头上；已改成在 ready-retry 前后各取一次计数，
     并在 retry 之后**真跑一轮** worker（若被错误排回队，这一轮就会发出调用）。
四、边界
  只跑这一条（4 次）；没有重跑旧 30 条；未改业务实现；零付费；未碰 candidate-0829 与 18763。
  Q-C28 已并入默认 discover。架构门禁通过。
  Q 文件指纹：runtime_contract_photo **733d99b79f88bfd5**、test_web_runtime_acceptance **e615eb7c843acab6**。
五、照片合同现状
  **Q-C24 14/14、Q-C26 18/18、Q-C27 9/9、Q-C28 14/14**（四条均已并入默认回归）。
  **Q-C25 仍等 A 的桥接稳定**；按协调单**不接受手补 `reference_origin_of` 之类正式回调作为通过**，
  要用真实上传或已有规范参考来源——本轮 Q-C28 已按这个原则做过一次。
是否释放（是/否，明确范围）：否。

---

时间：2026-09-23 09:58 +08:00
窗口ID / 任务ID / 事件：claude-20260922-234631-4d18 / pkg-Q-acceptance / **CORRECTION**（撤回一条错误结论）＋ Q-C28 判据更正后 **PASS 16/16**

一、**撤回 09:53 那条关于 CR-A15 的结论——我推断错了**
  我当时写的"`reference_origin_of` 在正式装配里没接（CR-A15 未落地）""新伙伴永远拍不出主动照片是缺陷"，**两处都不成立**：
  1. 我拿 `reference_origin_of("x") is None` 当"没接线"的证据。**这不成立**：`x` 是不存在的 id，
     **接好的函数本来也该返回 None**。这是把"函数对假输入返回 None"误当成"函数没被赋值"。
  2. 我的 grep 是 **09:49** 跑的（那一刻确实还没有），I 在 **09:50** 就在自己持有的 `web_composition.py:364/401`
     定义并赋值了。我 09:52 的运行与 09:53 的结论**沿用了 09:49 那个已经过时的事实**，没有重新核。
     这正是我一直要求别人别做的事（"不拿旧事实判新版"），我自己犯了一次，记在这里。
  **现在实测（09:55–09:58，`web_composition.py bb5eea753af9e591`）**：接线在第 401 行，实现读 `pets.photo_generated`
  与 `profile.origin`，映射为：有照片＋generated → `original_companion`；有照片＋非 generated＋`own_pet` → `owner_original`；
  其余 → None → hold。
  已向 **A** 与 **B** 各发一条撤回（A 那条同时说明哪些结论不受影响）。

二、Q-C28 判据按真实映射改写后 **PASS 16/16**（两次一致：01:58:33Z、01:58:39Z）
  新增三条、删掉原来那条错的：
  - 「真实上传的原照标成 `owner_original`」→ 实测 `owner_original` ✓
  - 「说不清来源时返回 None（没照片、不存在的 id 都是）」→ 两者都 None ✓
  - 「缺必需身份参考时挡下且 0 发送 0 预占」→ 0 调用、0 预占、显示 failed ✓
  诊断字段改名：`reference_origin_gap` → **`reference_provenance`**，字段里去掉了 `wired` 字样，
  并写明"对不存在的 id 返回 None 不能用来判断接线有没有接上"。
  **证据依赖补齐**（按协调单）：`sources` 现在含 `web_composition.py bb5eea753af9e591`、
  `web_pets/service.py 23e987415e6526e6`、`photo_director_bridge.py 5f697494b84f0bbc`、
  `photo_director/compiler.py 2a2163d41e13fadb`、`photo_director/readiness.py 2f542843b3e5856c`。
  **不回填旧证据**：09:52/09:53 那两份 JSON 原样保留（它们的 sources 就是当时记的那几项），不用此刻的哈希补成过去的证据。

三、B 抢回来的一条（Q 采纳，记为**给 A 的流程次序项**，不并入上面的撤回）
  B 独立查到：`adopt-lan` 这类领养伙伴 `photo_path` 为空、`photo_generated` False，
  所以 `reference_origin_of` 第一行就 None——**这一侧没问题**。
  死结在**次序**：A 的 `_render` 里本有退路（`if reference is None and self.portrait_saver is not None:` 先画一张基准照当参考，
  费用侧本来就按 2 个单位算过），但**导演的 readiness 在 `_render` 之前就挡下了**（没有 `pet_identity` 那张 MediaReference），
  于是"没照片 → 挡下 → 走不到画基准照 → 永远没照片"。画完基准照来源就是明确的 `original_companion`，导演自然放行。
  **这不需要放宽任何判定，只需要对"完全没有参考照"的宠物先画基准照再交导演。** B 已写进自己日志并提 A。
  Q 的处理：Q-C28 里那条断言**保留**（它钉的是"挡下时不花钱"这个当前事实），但注释已写明
  "这是当前行为，不是应当永远如此"，并指向 B 提给 A 的次序项。**不提前关闭、也不当作缺陷替产品下结论。**
四、B 就那一版 `bff9438e7fe5a995` 的交代（记录）
  那是 B 为定位失败做的**逐项隔离实验**（临时摘掉每宠那一层跑一轮，跑完立刻还原），**从未宣布为稳定版**，
  其日志里也没有该指纹。交付版是 `B66EF7D0562832EE`（含每宠那一层），此刻磁盘一致。
  B 另加了常驻守卫 `tests/test_web_photo_consent_wiring.py`（**203151609B2882B3**，10 项），
  钉住 `reference_origin_of` 已接上且拿不准时返回 None——以后谁改掉都会让默认回归变红。
五、下一步
  **Q-C25** 现在可以写（A 的 origin 覆盖已修、B 的 payload 已交付）：四场景实际 worker ＋ 参考 bytes SHA ＋ prompt/size
  ＋ hold 时 0 预占 0 发送，**仍不手装回调**。
  **Q-C29 登记**：先读到 `processing`、再由 worker 走到终态的时序（C 的 4/4 只验了旧时序；A 新的"读后插"仍留 gap，
  协调方已派 A 同事务修、C 用两线程复验）。**不提前关闭。**
是否释放（是/否，明确范围）：否。

## 02:32Z（10:32 +0800）Q-C25 四场景收口、Q-C29 独立收口；跑批脚本加"加载时指纹"

**Q-C25（四场景 15/15 PASS）** 证据 `PetJourneyBackend/data/reviews/q-4d18-20260922/contracts-c25-four-scenes.json`（run_at 02:24:27Z）。
合同移到新文件 `tests/runtime_contract_director.py`（原 `runtime_contract_photo.py` 已 777 行、逼近 800 行门禁）。
- 四场景各自走到**真实 worker**：home / train / flight_adventure 走 I 的 `POST /pets/{pet}/photo-request`；
  cafe 走宠物自己在真实到访按下的 `take_photo`（`event_origin=world_event`，地点类目「咖啡馆」）。
- train 那段**从 `web_journey_legs` 查出** `mode='train'`（sequence 4，09:35→10:15），推到中点 09:55 再请求；不照抄魔数、不改库伪造。
- 每场景适配器实收 prompt 与**同次** `bridge.compile_call` 输出逐字相同；size 均 2048x2048；参考图 bytes SHA-256 等于该宠物真实上传那张。
  四只宠物故意上传**互不相同**的图（`pet_png(red)`），四个 SHA 互不相同——串到别人的参考图这条断言就会不成立。只丢 `negative_prompt`。
- 缺必需身份参考：0 发送、0 预占、`last_error = image director_hold:identity_reference_missing`。
- 绑定（该轮 `implementation_at_load`）：illustrations `baff0f6a2885f7b0`、bridge `211e0c63a95f8a11`、web_composition `c966102aa8798657`、
  web_agent_wiring `4ddd88e0b5425109`、photo_scene `0a3a60d7be3be550`、photo_wiring `a46cd4b8d0453991`。drift 空、禁网 0、0 次真实供应商调用。
- **I 在该轮之后把 web_composition 改成 `2e7b1aaa637ef147`（只改注释）。本条结论到 `c966102a` 为止，不外推**；新指纹由 I 副本那一轮自己的 `implementation_at_load` 承担。

**Q-C29（15/15 PASS）** 新文件 `tests/runtime_contract_consumers.py`，证据 `contracts-c29-run2.json`（run_at 02:30:27Z，drift 空）。
三个消费者（通讯器家庭消息 / 旅行攻略 / 邮局明信片）× 三种到达次序，各格都实测走到：
- ① 记录先插：插的时候任务确实 `queued`，worker 回调随后改成终态；
- ② worker 先到：插之前任务已 `ready`、那一行**还不存在**（回调必然命中 0 行），插的那一刻在同一写事务里读到终态直接写对；
- ③ 读到"正在画"后放 worker 去写：三个消费者都实测到它**仍在等写锁**（消费者提交前线程 alive），提交后自己跑完、**未抛异常**，记录走到终态。
  **数据库锁异常不作为证明**：断言的是"线程没抛异常且记录到终态"。
- 正常次序对照走完并出图；unknown 落 `unknown`、failed 落 `failed`，**两个取值不同**；每个任务整趟只有 1 条预占、`attempts=1`（插记录不多发、不重排）。
- `image_outcome_in` / `illustration_outcome_in` 三处均由正式装配接上（`web_agent_wiring.py:254-257`），**未手补**。

**第一轮 C29 的两条 FAIL 是我用例的毛病，不是实现问题**（已改）：发送次数原用全趟共用替身的总数去减，会把同趟别的消费者的任务算进来（改按该任务自己的预占条数）；unknown/failed 两趟漏了给任务号落位。

**跑批脚本加了一层**（`scripts/verify_runtime_evidence.py`）：**在导入 app 之前**取一次 `app/**/*.py` 指纹，跑完再取一次；
产物多出 `implementation_at_load` 与 `implementation_drifted_during_run`，有漂移且落在某条合同 sources 上就追一条 `RUN=BLOCKED`。
**不再用"结束时盘上的指纹"代表这个进程实际加载的版本。**

**独立判定（额度计量，与 A/C 的说法一致但由我自己从代码核出）**：
`budget.py:162` 在 `outcome == "unknown"` 时一律按**预占全部单位**计入已用，忽略调用方传的实际发出次数；
`budget.py:175` 把 `actual_units` 写成 NULL **是有意的**（unknown 即"确认计量未知"），这条不必改；
但 `budget.py:165` 只有 `not_sent` 才退回——后来以 `succeeded, actual_units=1` 查清时**什么都不做**，`used` 永久停在预占值。
故"保守计入、查清后更正"**只在全有全无时成立，部分发出没有回正路径**。方向上只多记不少记（不超支，只提前触顶 hold）。
A 作为**独立一批先反例后修**交我重验，本批不扩范围。**C 的 7/7 与我的 C29 证明的是"那笔预占没被退回"，不是"计量数值对"。**

**撤回与降级（按 COORD-Q-EXECUTE-C25-C29）**：
"缺必需身份参考只需换次序"的修复建议**撤回**。协调方已明确本批边界——缺必需身份参考必须在**预占之前** hold，
不得从 legacy 路径推翻该边界、也不得隐式付费补一张脸。原"缺参考次序"一项**降级为后续产品能力**（独立初始化另做），不影响本批四场景。

**停写**：本条之后 Q 不写 `tests/runtime_contract_*.py` 与 `tests/test_web_runtime_acceptance.py`，等 I 发解除。

## 02:32Z 受影响项合并小批：c24–c29 一个进程 6/6 PASS

证据 `PetJourneyBackend/data/reviews/q-4d18-20260922/contracts-photo-bundle-20260923T0232Z.json`，run_at 02:31:40Z，
整体 PASS、`implementation_drifted_during_run` 为空、禁网拦截 0、0 次真实供应商调用。
Q-C24 / Q-C25 / Q-C26 / Q-C27 / Q-C28 / Q-C29 全部 PASS（同一进程、同一份 `implementation_at_load`）。

该轮 `implementation_at_load`（**导入 app 之前**取）：
illustrations `baff0f6a2885f7b0`、photo_director_bridge `211e0c63a95f8a11`、**web_composition `2e7b1aaa637ef147`**、
web_agent_wiring `4ddd88e0b5425109`、photo_scene `0a3a60d7be3be550`、photo_wiring `a46cd4b8d0453991`、
web_collection/service `0d65a2f42202a42e`、guides `82b57177c4d01ba1`、web_communicator/service `9b3c39e911d43659`、
photo_display `0d567249732974ea`、budget `830b16ddcd8d3c18`、routers/web/pets `38198fb5bfd112cd`。

这一轮把 C25 也**实跑**绑到了 I 改注释后的 `2e7b1aaa637ef147`：新指纹上的结论有实跑证据，不是从 `c966102a` 外推。
`contracts-c25-four-scenes.json`（绑 `c966102a`）原样保留，不覆盖。

## 02:40Z 更正：次序③「消费者持锁、worker 等锁」的首次覆盖归 C（通讯器那条）

我在给 C 的消息里写过"你我都还没覆盖的那一档"，**不成立，撤回**。C 指出后我自己打开它的证据核了：
`PetJourneyBackend/data/reviews/c-91f2-20260923/photo-message-order-20260923T015744Z.json`
里确有 `adventure_ready__worker_waits_for_message_lock`：`read_back="processing"`、
`worker_blocked_while_locked=true`、`worker_finished_after_release=true`、`attempts=1`、`provider_calls=2`；
该轮 5/5、`drift` 为空、`socket_attempts=0`。**所以通讯器那条次序③ 是 C 先覆盖的。**

但要连同它的绑定一起记，别让"C 已覆盖"被读成"当前候选的通讯器③已被证明"——
C 那轮绑的是**旧版**：`web_communicator/service.py 0d663d711c9d3dab`（今 `9b3c39e911d43659`）、
`illustrations.py 2ca407a88fbb5305`（今 `baff0f6a2885f7b0`）、`web_agent_wiring.py b70c1c9e894d56b9`（今 `4ddd88e0b5425109`）、
`photo_wiring.py 726961ef10ad64ac`（今 `a46cd4b8d0453991`）。

**准确的覆盖账**：
- 次序③ 通讯器：**C 首次覆盖**（01:57:44Z，旧指纹）＋ Q-C29 在**当前候选指纹**上再覆盖一次；
- 次序③ 攻略 / 明信片：**Q 首次覆盖**（C 的消费者探针包的是 `image_request`/`selfie_request`，
  驱动 worker 发生在 `unit_of_work` **开事务之前**，不是"消费者持锁期间"——这是 C 自己指出的缺口）；
- 三个消费者一趟**同时**经过（正式装配的一趟真实澳门轮渡）：Q-C29。

## 02:45Z 登记 Q-C30（额度计量：部分发出与查清后的回正）— **只备判据，等 A 交可运行指纹再跑**

A 已说明把 `budget.py:162/165` 那条作为**独立一批、先反例后修**交 Q 重验。按既有做法先落判据，不提前造合同、不重跑旧项。
被测面预计：`app/web_platform/budget.py`、`app/web_journey/illustrations.py`（`_settle_calls` 的 `sent` 口径）、`app/web_agent/brain_wiring.py:90`（settle 适配器）。

**判据（按"差额"断言，不只看终值）**
1. 反例①：没有主人照片 → 一次预占 **2** 单位（证件照＋场景图）；证件照超时（unknown）、场景图**根本没发** → 结算 unknown。
   断言 `web_budget_counters.used` 的**增量**应为 **1**（实际发出 1 次）。当前实现记 2。
2. 反例②：先 unknown（保守计入）→ 后以 `succeeded, actual_units=1` **查清** → 增量应回落到 **1**。
   当前实现 `:165` 只有 `not_sent` 退回，其余什么都不做，**永久停在 2**。
3. **差额一致性**：同一 `operation_id` 上多次结算的**累计 used 增量 == 最终确认的 actual_units**；
   重复报告同一 outcome 不得再改计数（幂等）；`expired` 后查清与 `unknown` 后查清**走同一口径**。
4. `inflight_units` 无论走哪条路径最终必须归零——不得因为 unknown 永久占着并发名额。
5. **不能少记**（正向对照，防止把"修多记"改成"漏记"）：2 单位都真发出且成功 → 增量 2；`not_sent` → 增量 0。
6. **两层都要核**（与 Q-C27 同口径）：每宠 `pet:<id>:illustration` 与全局 `provider:image:daily` 两个 counter 都看，不能只修一层。
7. 边界：`actual_units > reserved_units`、`actual_units` 为 None／负数时的行为要有明确定义并被断言。
8. **硬前提**：每条断言都要先证明"**真的发出过**"（替身 `dispatched` 与该任务预占条数对得上），
   否则"少记"可能只是因为根本没发——那样这批就证明不了计量。
9. 计数一律查**同一 scope 的 before/after 差额**，不看绝对值：别的宠物的已有用量会掩盖漏记（Q-C14 踩过这个坑）。

**范围限制（不得被这批的绿盖过）**：隔离库＋替身，**不证明真实供应商的计费**，也不证明视觉结果。

## 02:50Z 按 COORD-Q-C30-BOUNDED 收窄 C29 证据措辞、修工具前提；新增 Q-C30

**一、C29 旧证据收窄（协调方指出，成立）**
"1 条预占 ＋ attempts=1" **不等于**"没多发一次供应商调用"——**一次尝试可以发出多次**。
`contracts-c29-run2.json`（02:30:27Z）那条断言的措辞**只收窄为「一笔预占、一次尝试」**，不得读成"只发出过一次"。
旧 JSON 与副本一律不改；定向检查在工作区新增。

**二、C29 工具前提的三处修正（都是我的用例问题，不是实现缺陷）**
1. `finalize` 原来按**全表最新 rowid** 取任务号，会串到同趟别的消费者 → 改绑 `make_request` **实际返回**的任务号，
   并断言该任务 payload 的 `pet_id` 就是本轮这只（`pet_matches`）。
2. 发送次数原来用全趟共用替身的总数去减 → 改为**任务级发送边界**：包住 `_render`，用替身 `dispatched` 在这一次
   `_render` 前后的差额记到具体任务名下；③ 那一档 worker 卡在 render 里面没返回，所以另给一个**实时读数**
   （`art.dispatched - render_base`），画到一半也读得准。
3. **观察器活得不够久**（协调方定位）：`trip()` 的 `finally` 先 `restore()`，`finalize()` 才补跑，
   正常次序那一档真正的发送发生在补跑里 → 被记成 0。改为观察器留到 `finalize` 完成后才撤（`observed_trip`）。
   另外 `finalize` 改为**先取完三个快照再统一补跑**——`run_pending()` 一轮会把同趟所有任务都跑掉，边取边跑会互相污染。

**三、断言按可证范围重写，不靠放宽、也不从账本反推发送**
- ① 记录先插：插的那一刻该任务 **0 次发送**、补跑之前仍是 0；
- ② worker 先到：插之前 `sends>=1`，插之后**一次都没再发**（差额决定性）；
- ③ 那一档 worker 正处在**同一次尝试中途**（证件照画完接着画场景图），差额是 worker 的不是消费者的——
  **该档不用差额断言**，由"一笔预占、一次尝试、没有重新排队"覆盖；
- 正向对照：每一格都必须看到替身**真的发出过**（`sends_total >= 1`）；
- unknown／failed／正常对照三趟都断言**三个消费者各一条**，不让 `all()` 空集通过。

**Q-C29 重跑 19/19 PASS** `contracts-c29-run3.json`（run_at 02:47:08Z，drift 空）。
实测发送次数印证了协调方的更正：`message` 那档**一笔预占、attempts=1，却发出 2 次**（无主人照片 → 先证件照再场景图），
guide／postcard 各 1 次。

**四、Q-C30（A 的账本回正小批）10/10 PASS** `contracts-c30-run1.json`（run_at 02:50:25Z，drift 空），
绑 `budget.py feaed33b020d8b38`。一次性临时库＋迁移＋**真实 BudgetLedger**，两层与正式装配同形；
**一律按同一 scope 的前后差额断言**（全局层所有档共用，看绝对值会把别档算进来）。
- unknown → 查清 succeeded/actual=1：两层 2 → **1**；expired 同；
- 确定没画成但已知发出 1 次 → 记 **1**（失败不等于没发出）；查清确实没发出 → **0**；
- **不给** actual_units → 维持保守的 **2**（不因回正反而少记）；
- **跨 UTC 日**：23:50 预占落 unknown、次日 00:10 查清 → 差额补在 `accounting_window=2026-09-22` **原窗口**，新一天为 0；
- 重复查清 ＋ 两线程同时查清（Barrier 对齐）→ **只调一次**，线程无异常；同日另一笔 3 单位预占一动不动；在途回 0 不二次扣。

**五、明确不在本批范围（不据此判 A 交付失败）**
1. unknown **当场**仍按预占全额保守计入、`actual_units` 留空——属「**当场记多少**」的另一项**未解决设计项**，
   协调方已指定另列小批。C30 只把现状如实记在 `out_of_scope_observation`，不作断言。
2. **顺带记下的接线现状（交协调方判断，不作缺陷主张）**：`BudgetLedger.settle` 认的是**预占编号**而非操作编号，
   查清方必须先按操作编号取回这条预占；且 `illustrations._settle` 有 `status == "reserved"` 的门，
   一旦落成 unknown 就不会再由插画链路发出第二次结算。**回正路径本身已验证可用**，谁来发起查清属产品/派单问题。
3. 真实供应商计费、真实环境（18763/E2）、视觉质量：一律未证明。

C30 已注册进 `runtime_contract_cases.CONTRACTS` 与 `test_web_runtime_acceptance.INTEGRATED`；两道门禁通过。

## 02:55Z P 改导演配方后的受影响项复验：Q-C25 15/15 PASS（新绑定）

P 改了 `catalog.py`／`compiler.py`／`recipes.py`（train 配方的「前置自拍 vs 往窗外看」提示词矛盾）。
这三份在 C25 的实现面上，属受影响项，**只复验 C25**，不动 C26–C30。
顺带把 `catalog.py` 与 `rules.py` 补进 `C25_SOURCES`——编译出来的提示词依赖它们，原来漏列。

**证据** `contracts-c25-after-p-gaze.json`，run_at 02:55:05Z，15/15 PASS，drift 空，禁网 0。
新绑定：`compiler.py 03bf83885d1afd19`（原 `2a2163d41e13fadb`）、`recipes.py cd88be9b06c6546d`（原 `ec7f09c1e42f8321`）、
`catalog.py c982488b02b194da`（本轮新纳入）。

**独立核对 P 的影响面说法**（用 C25 每场景记下的提示词摘要与字数逐场比，不看代码猜）：

| 场景 | 改前 | 改后 | 结论 |
|---|---|---|---|
| home | `d2ea3d2d95ab4921` / 412 | 同 | 逐字未变 ✓ 与 P 所说一致 |
| flight_adventure | `19e08cf3e3443d38` / 469 | 同 | 逐字未变 ✓ 与 P 所说一致 |
| train | `b2c53bb4d20c7f27` / 441 | `4f1f6d6e3f978137` / 454 | 已变（+13 字）✓ 本次改动的目标 |
| **cafe** | `7fd9b1ce1302f310` / 443 | **同，逐字未变** | **与 P 的说法不一致** |

P 说「那条 DNA 抑制对所有 front_selfie 生效，所以 cafe 的提示词**也少了**『喜欢往窗外看』一句」。
前半句（对所有 front_selfie 生效）成立；**后半句是有条件的**——抑制只在这只宠物的 DNA 里**确实带** `look_out_window`
（`catalog.py:210/228`）时才有东西可减。本合同四只宠物走的是默认 DNA，cafe 那只没有这条习惯，
所以**本场景根本没有触发这次抑制**，提示词逐字未变。这不是推翻 P，是把影响面收窄成「**取决于宠物 DNA**」。

**未覆盖**：P 提的 5 条（前置自拍配方的动作句不得引开视线、编译产物含「身后」不含「往外看」、
前置自拍下习惯句不出现、换 `fixed_companion_camera` 的正向对照、`requires` 未变）属**提示词内容自洽**，
不在 C25「worker 是否照用导演编译结果」的范围里，也未派给 Q。要不要另列由协调方定，我不自领。
**P 自己也写明：只证明提示词内部不再自相矛盾，没有证明成图会变好。**

## 03:00Z 把 C25 的覆盖面再钉死一层：**这次的 DNA 抑制，C25 一条都没覆盖**

P 更正后给出的条件是**合取**：`镜头 == front_selfie` **且** 这只宠物 DNA 带 `look_out_window`，两个同时满足才减那一句。
我没有停在"cafe 逐字未变"，把 train 那 +13 字也算了一遍——**它全部来自动作句改写，与 DNA 抑制无关**：

- 改前动作句（读**不可变副本** `candidate-photo-20260923T023220Z` 里的 `recipes.py`）
  `"趴着从车窗往外看，保持正常的动物姿势"` = **18 字**；
- 改后（工作区）`"把脸凑近镜头，车窗和窗外流动的景色留在身后，保持正常的动物姿势"` = **31 字**；
- 31 − 18 = **+13**，与 C25 实测 train 提示词字数变化**完全相等**；
- `HABIT_TEXT["look_out_window"] = "喜欢往窗外看"` = 6 字（加分隔符 7）。若抑制也在我这轮触发过，
  train 的变化应当是 +13 − 7 = **+6**，不会是 +13。

**结论**：C25 那四只宠物**都不带** `look_out_window`，所以这次新增的 DNA 抑制逻辑
（`catalog.CAMERA_CONFLICTING_HABITS` ＋ `compiler._dna_clauses` 的新抑制条件）
在 Q-C25 里**一次都没有被执行到**。C25 的 15/15 **不能**读成"抑制逻辑已验证"。

P 的 fixture 与那 8 张真图的宠物**都带**这个习惯，所以它那边 cafe 确实少一句；
`home` / `flight_adventure` 的宠物也带，但镜头不是前置自拍，所以逐字未变——
**单看"带不带习惯"或单看"是不是自拍"都会判错，必须两个都看**（P 补全的条件，我认同）。

**覆盖账**：这次抑制逻辑的验证目前只有 P 自己的 `GazeDirectionTests` 5 条；Q 侧为零。
是否需要 Q 独立重写，由协调方决定，**我不自领范围**。

# 03:05Z 稳定交付索引（COORD-Q-CLOSEOUT-20260923）— 本批收口，不再扩合同

三项等待按协调方处置关闭：① 父副本捕获时只有 C24–C29，**不存在旧副本的 C30 结果**，不拿工作区新指纹倒灌旧副本；
② A 的 `budget feaed33b` 迟到澄清增量已由 Q-C30 10/10 验过，「unknown 当场保守记满预占量」是**当前明确限制**，
无新工单、不作等待项、不作发布阻塞；③ P 的镜头习惯抑制**本批不新增 C31**，保留「P 作者 6 条」与「Q-C25 运输链路证明」的区分。

## 一、三份新证据（已有输出，不再重跑）

| 合同 | 结果 | run_at | 证据文件 SHA-256 |
|---|---|---|---|
| Q-C25 | **15/15 PASS** | 02:55:05Z | `7d649b9eb9e400e76bde24249ca8f31036121b07144239a4632369e713320043`（`contracts-c25-after-p-gaze.json`） |
| Q-C29 | **19/19 PASS** | 02:47:08Z | `0936cfeba8dbaa4255b9c9e07658602a20d51aaa1769f5b61da11c1fb20ae3b6`（`contracts-c29-run3.json`） |
| Q-C30 | **10/10 PASS** | 02:50:25Z | `28fa69e97d437e8d00ec238fbd90fd65ac8a2fb55cf0a471fa5e46cbacc584b3`（`contracts-c30-run1.json`） |

父副本对应的那一轮另存：`contracts-photo-bundle-20260923T0232Z.json`（02:31:40Z，c24–c29 六条全过），
SHA-256 `213d6c5338659007dff09d2567b9484876ffa32b4c7baf7a92d8b940a466cc5c`。
三份新证据 `implementation_drifted_during_run` **均为空**，禁网拦截 0，**0 次真实供应商调用**。

## 二、合同与工具指纹（工作区 ／ 与父副本比）

| 文件 | 工作区 | 父副本 |
|---|---|---|
| `tests/runtime_contract_director.py` | `1d9f08c3bbd87815` | `968e9dbbc8ccf7d7`（补 catalog/rules 进 sources 前） |
| `tests/runtime_contract_consumers.py` | `0d71ed529dbd8672` | `92485396b96b602a`（三处工具前提修正前） |
| `tests/runtime_contract_reliability.py` | `add940790b8169d9` | `ca81d00a4376d4f5`（**无 C30**） |
| `tests/runtime_contract_cases.py` | `f750b4f6408cd3ac` | `e4a0edae2cbb437d`（未注册 C30） |
| `tests/test_web_runtime_acceptance.py` | `75f211f0c10581f1` | `cc9434b913175d85`（INTEGRATED 未含 C30） |
| `tests/runtime_contract_photo.py` | `0ce845edc6f23cb6` | 同 |
| `tests/runtime_contract_harness.py` | `f1052714eb4edd46` | 同 |
| `tests/runtime_contract_media.py` | `4399cd0cb56bd2a2` | 同 |
| `scripts/verify_runtime_evidence.py` | `1f8dccdc043806ec` | 同（`implementation_at_load` 那一层捕获时已在） |

## 三、各合同绑定的实现指纹与**与父副本的差异**

- **Q-C25**（18 份 sources）：只有 3 份与父副本不同，都是 P 的镜头习惯改动——
  `catalog.py c982488b02b194da`（副本 `2bb8a64f3d2d8bde`）、`compiler.py 03bf83885d1afd19`（副本 `2a2163d41e13fadb`）、
  `recipes.py cd88be9b06c6546d`（副本 `ec7f09c1e42f8321`）。其余 15 份逐个相同
  （illustrations `baff0f6a2885f7b0`、bridge `211e0c63a95f8a11`、web_composition `2e7b1aaa637ef147`、
  web_agent_wiring `4ddd88e0b5425109`、photo_scene `0a3a60d7be3be550`、photo_wiring `a46cd4b8d0453991`、
  photo_command `91bda7a473c4e25e`、photo_prompts `4d7a7140f0985368`、routers/web/pets `38198fb5bfd112cd`、
  director 包的 contracts/delivery/port/readiness/rules/validation）。
- **Q-C29**（8 份）：只有 `budget.py feaed33b020d8b38` 与副本 `830b16ddcd8d3c18` 不同（A 的回正小批，捕获后才落）。
  其余 7 份逐个相同。**该差异不影响 C29 的判据**——C29 断言的是消费者同事务收口，不读用量计数。
- **Q-C30**（3 份）：`budget.py feaed33b020d8b38`（副本 `830b16ddcd8d3c18`，即**被修之前**）、
  `brain_wiring.py b66ef7d0562832ee`、`illustrations.py baff0f6a2885f7b0` 后两份与副本相同。
  **C30 只在新版上成立，父副本上不存在这条合同也不该有它的结果。**

## 四、各自证明了什么 ／ 没证明什么

- **Q-C25**：证明四个目标场景（home/train/flight_adventure 主人命令 ＋ cafe 宠物自发）走到真实 worker 时，
  适配器实收的 prompt／2048 尺寸／参考图 bytes SHA **逐项等于同一次** `compile_call` 的产物，四只参考图互不相同；
  缺必需身份参考时 0 预占 0 发送、如实落 failed。**连带**证明改配方之后 worker 仍照用新结果。
  **不证明**：成图质量、「像不像它」、P 这次的镜头习惯抑制逻辑（四只宠物均不带 `look_out_window`，该逻辑**一次都没被执行**）。
- **Q-C29**：证明三个消费者 × 三种到达次序九格全部走到终态；③ 档实测 worker 确实在等写锁、放开后自己跑完未抛异常；
  正常对照、unknown/failed 两个取值分得清且各三条。**数据库锁异常不作为证明**。
  **旧证据措辞已收窄**：「一笔预占、一次尝试」**不等于**「只发出过一次」（实测 message 档一笔预占发出 **2** 次）。
- **Q-C30**：证明 unknown/expired 被明确查清后按差额回正（两层、原记账窗口、跨 UTC 日）、说不清维持保守值、
  重复与并发只调一次、不碰别的操作、不二次扣在途。**不证明**真实供应商计费；
  「unknown 当场记满预占量」是**当前明确限制**，只记在 `out_of_scope_observation`。
- 三条共同**未证明**：真实环境（18763/E2）、真实供应商调用、视觉质量。

**后续**：只响应明确缺陷，不为等而扩套件；不新增付费、不改业务源码、不触碰 18763。
