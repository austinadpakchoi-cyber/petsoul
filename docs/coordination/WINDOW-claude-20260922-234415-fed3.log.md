# WINDOW-claude-20260922-234415-fed3

## START / WAITING

时间（含时区）：2026-09-22 23:46 +08:00（China Standard Time；窗口启动于 23:44:15）
窗口ID / 任务ID / 事件：claude-20260922-234415-fed3 / 未分配 / START（WAITING）
用户任务：收到通用启动提示词（含 2026-09-22 家庭共同照顾 / 多宠 / 访客修订），尚未分配具体任务。按启动提示词第 3 条只阅读，不改业务代码，等待用户分配。
实际目录 / 分支 / HEAD：E:\petsoul-audit\petsoul / codex/petsoul-web-integration / 980feabc7710462a89c5df488c255d04e9e7de08
开工时已有改动（START 必填）：
  - 已修改 5 个：AGENTS.md、PetJourneyBackend/app/{config.py, main.py, repositories/base.py, schemas/base.py}。
  - 未跟踪 69 项：PetJourneyBackend/app/ 下 companion_media、food_discovery、intent_layer、reception、routers/web、schemas/web、transport_world、web_adapters、web_agent、web_agent_wiring.py、web_collection、web_communicator、web_composition.py、web_credentials、web_credentials_wiring.py、web_driving、web_economy、web_farm、web_home、web_household、web_identity、web_journey、web_market、web_pets、web_platform、web_providers、web_residents、web_social、web_transport、web_worker.py；PetJourneyBackend/tests/ 下 24 个 test_web_*.py 及 web_base.py、web_provider_fakes.py、driving_bots.py、school_helpers.py、web_evals/；PetJourneyWeb/、deploy/web/、docs/{contracts,coordination,product}/、scripts/{gen_driving_fixtures,gen_web_contract,real_acceptance,real_integration,seed_web_demo}.py。
  - 以上均为其他窗口（主要是 claude-307b、codex-r7k、codex-c84a）的未提交成果，不算本窗口成果；本窗口不恢复、覆盖、删除、暂存或提交。
  - 注：307b 在 21:05 START 记录已修改文件为 4 个，未含 app/repositories/base.py。这个文件是之后才改的，推断出自仍持有 app/** 的 307b，本窗口未核实。
拟修改或实际修改范围：仅本日志文件。不改业务代码、测试、迁移、配置、依赖清单、契约、其他文档或他人日志。
运行资源（无则写无）：无。未启动进程，未占用端口或数据库，未调用供应商。
冲突检查结果：
  - 已读：AGENTS.md、docs/product/PETSOUL-2.0-MASTER-PLAN.md（v1.5 全文，含 2026-09-22 修订）、WINDOW-START-PROMPT.md、全部 6 份 WINDOW 日志、docs/contracts/WEB-CONTRACT-v0.2.md（代码契约 0.4.0）、docs/contracts/MODULE-MAP.md、RUNTIME-PARALLEL-CONTRACT-v0.1.md、BACKEND-PARALLEL-WORK-PACKAGES-2026-09-22.md。
  - 未释放范围：claude-20260922-014933-307b 的 real-experience-households（21:05 START，最新一条是 22:59 PROGRESS）仍未释放。它持有后端 PetJourneyBackend/app/**、tests/**、scripts/**，前端 src/shared/** 及共享入口，docs/contracts/**，FRONTEND-HANDOFF / BACKEND-REAL-EXPERIENCE-ACCEPTANCE / MVP-HANDOFF，deploy/web/** 等。并行工作包 I/A/B/C/Q 目前只是候选：307b 尚未 SCOPE_CHANGE 收窄范围，也未释放 A/B/C/Q 的文件，所以这些文件现在不能领取。
  - codex-r7k：按时间最新的一条是 15:53 unified-life-ui-photo-cat HANDOFF/RELEASE。这条在文件里排在 15:34 START 之前，容易被读漏。307b（21:05）和三个 parallel 窗口（23:3x）都写“r7k 未释放”，与这条记录不一致。本窗口不据此接管任何前端范围，留给用户或相关窗口确认。
  - codex-c84a、parallel-infra / qa / brain：全部已 RELEASE。
  - 运行资源只读检查（23:44）：
    · 18761：python PID 4724 在监听，属 307b。
    · 18763：python PID 37996 在监听，属 307b 的真实验收；它 22:59 记录的 API PID 是 33064，可能已重启，未核实。
    · 5287 / 5288 / 5289：node PID 40696 / 3656 / 48640，均为 307b 登记。
    · 18762、4287：空闲。
    · 本窗口不停止、不访问、不写入以上服务及其数据目录。
  - 结论：本窗口 WAITING，只持有本日志，与其他窗口无重叠。
进展 / 决策依据：未收到具体任务，所以记 WAITING，只阅读。本窗口是新会话，用新 ID，不沿用 307b 的身份。
验证命令 / 结果 / 证据路径：git rev-parse --abbrev-ref HEAD、git rev-parse HEAD、git status --short；Get-NetTCPConnection 只读检查端口监听（结果如上）。
未完成 / 依赖 / 下一步：等待用户分配任务。收到后先重读最新日志，确认所需的具体文件已由持有窗口明确释放，再追加 SCOPE_CHANGE 并按任务开工。
是否释放（是/否，明确范围）：否（仅持有本日志文件）。

## PROGRESS — 登记后复核

时间（含时区）：2026-09-22 23:48 +08:00
窗口ID / 任务ID / 事件：claude-20260922-234415-fed3 / 未分配 / PROGRESS（WAITING，登记后复核）
进展 / 决策依据：
  - 23:46:45 复读 docs/coordination，发现两个与本窗口几乎同时启动的新窗口：claude-20260922-234337-4bef（日志写于 23:45）和 claude-20260922-234408-91f2（日志写于 23:46）。两者都是 START/WAITING，只持有各自日志，与本窗口无重叠。
  - 307b、r7k、c84a 和三个 parallel 子窗口的日志，大小与修改时间都和开工阅读时一致，没有新的领取或释放记录。
  - 冲突检查结论不变：本窗口只持有本日志，继续 WAITING。
运行资源（无则写无）：无。
是否释放（是/否，明确范围）：否（仅持有本日志文件）。

## START — package B（世界时钟与心跳）；仓库内实施等待 I 释放

时间（含时区）：2026-09-22 23:51 +08:00
窗口ID / 任务ID / 事件：claude-20260922-234415-fed3 / pkg-B-heartbeat / START（CLAIMED，待 I 释放）＋ BLOCKED（仅限在仓库内写源码与测试这一部分）
用户任务：负责工作包 B。
  - 实现可注入时钟、RuntimeState 和纯 heartbeat policy。输入真实状态投影与授权事件；输出继续活动、执行确定性规则、请求大脑、延期或恢复，以及 next_check、原因和唤醒条件。函数本身不写库、不联网。
  - 要处理：真实 UTC 与地区时间、到期承诺优先、空闲复查、正常静默与依赖错误的区分、事件合并、零延迟空转。
  - 多主人不复制同一只宠物；公共居民不因没有 owner 被排除；活动进行中不能持续要求模型再思考。
  - 只改 B 自己的新模块和测试。用 FakeClock 验证睡眠、旅途、到期工作、48 小时离线、未知时区、新消息和版本变化。
  - 新模块未装配时明确写“未接入”，并给 I 输入映射和调用方式。不接管旧 ticker / life / moment 或组合根。
package / 接口版本：B / RUNTIME-PARALLEL-CONTRACT v0.1（候选，I 尚未接受）。涉及 §2.1 Clock、§2.2 Versions / RuntimeState、§2.3 WakeEvent / HeartbeatDecision 和 §3 行为样例。
实际目录 / 分支 / HEAD：E:\petsoul-audit\petsoul / codex/petsoul-web-integration / 980feabc7710462a89c5df488c255d04e9e7de08
开工时已有改动（START 必填）：与本日志 23:46 START/WAITING 所记相同（5 个已修改、69 个未跟踪，共 74 行；23:50 复查行数不变）。均非本窗口成果，不动。
拟修改范围（精确；I 释放后才写入）：
  - 新文件 PetJourneyBackend/app/web_runtime/clock_policy.py：Clock 协议、SystemClock、FakeClock，地区钟点经 app/city_timezones.py。
  - 新文件 PetJourneyBackend/app/web_runtime/state.py：Versions、RuntimeState、WakeEvent、HeartbeatDecision 等纯数据类型。
  - 新文件 PetJourneyBackend/app/web_runtime/heartbeat_policy.py：纯函数 evaluate 与 HeartbeatPolicy 参数。
  - 新文件 PetJourneyBackend/app/web_runtime/wake_events.py：授权事件的去重、合并与排序，只供 B 使用。
  - 新文件 PetJourneyBackend/tests/test_web_heartbeat_policy.py。
  - 本日志。
  - 23:50 用 Test-Path 确认：app/web_runtime/、上述测试文件、app/schemas/runtime_internal.py 都不存在。
  - 明确不改：
    · app/web_runtime/__init__.py（工作包 §2 规定由 I 维护）；
    · web_agent/ 下 ticker、life、moment、proactive、reply_policy、profile 等现有文件，web_agent_wiring.py、web_composition.py、web_worker.py、main.py、config.py；
    · 迁移目录、app/schemas/**（含 runtime_internal.py）、tests/web_base.py 与已有测试；
    · A 的 tasks / lease / budget / meter，C 的 web_agent/decision/** 及其两个测试文件，Q 的文件。
依赖：
  - I：
    · 释放上述文件；
    · 创建 app/web_runtime/__init__.py（可为空），或确认 B 先以不带 __init__ 的命名空间包交付；
    · 接受或最小修订接口 v0.1，并决定 RuntimeState / WakeEvent / HeartbeatDecision 最终放在 schemas/runtime_internal.py 还是 web_runtime/state.py（B 把类型集中在 state.py，只需改一处）；
    · 装配阶段：从真实库生成 RuntimeState 投影、提供授权事件来源、执行 HeartbeatDecision（task_intents 进 A 的队列；REQUEST_BRAIN 先经 A 预占预算，再调 C）。
  - C（91f2）：brain_purpose 的取值要与 C 的 DecisionContext.purpose 对齐，B 会以 CHANGE_REQUEST 提交候选取值。
  - A：无直接依赖。B 的 task_intents 只是意图，不入队。
验收 ID：读完世界运行方案 v0.2 的 A01–A26 后补记 B 负责的离线机制部分。
运行资源（无则写无）：无。
  - 释放前：只读分析现有代码；设计与原型写在本会话 scratchpad（仓库外），用 python -B（不写字节码）跑纯内存测试。
  - 不写仓库，不读业务库，禁网，不访问 18761 / 18763 / 5287–5289，不调用供应商。
冲突检查结果：
  - 23:49 复查：307b 日志仍停在 22:59（101129 字节），没有 SCOPE_CHANGE 或部分 RELEASE。
  - 工作包 §2 写明新目录也在 307b 的 app/** 占用内，所以 web_runtime/** 和 B 的测试文件现在不能写入。仓库内写源码与测试这一部分 BLOCKED，等 307b 释放、用户分配后实施。
  - 91f2 已领取 C（web_agent/decision/**、tests/test_web_decision_{context,proposal}.py，同样等待释放），与 B 的路径不重叠。4bef、4d18 为 WAITING。
  - BACKEND-REAL-EXPERIENCE-ACCEPTANCE.md 与 FRONTEND-HANDOFF.md 23:41 有更新，属 307b 范围，B 不写。
进展 / 决策依据：先做不冲突的部分。
  - 只读分析 ticker / life / moment / proactive / reply_policy / profile、city_timezones、web_worker、任务队列、旅程、居民、家庭与通讯的状态来源，整理 I 需要的输入映射。
  - 在 scratchpad 写设计与原型测试。
验证命令 / 结果 / 证据路径：git rev-parse HEAD、git status --short（74 行）；Test-Path（结果如上）。
未完成 / 依赖 / 下一步：只读分析 → scratchpad 原型与 FakeClock 测试 → 在本日志写输入映射与 CHANGE_REQUEST → 307b 释放后追加 START（实施），把文件落入仓库并在仓库内复跑测试。
是否释放（是/否，明确范围）：否。

## SCOPE_CHANGE — pkg-B-heartbeat（拟修改文件增加一个；仓库内仍未写入）

时间（含时区）：2026-09-22 00:16 +08:00（按实际应为 2026-09-23 00:16，见下方更正）
窗口ID / 任务ID / 事件：claude-20260922-234415-fed3 / pkg-B-heartbeat / SCOPE_CHANGE
变更：拟修改范围增加新文件 PetJourneyBackend/app/web_runtime/decision.py，放心跳输出类型（HeartbeatDecision、TaskIntent、动作 / 安静原因 / 原因码枚举）。
原因：架构门禁规定每个文件 def 与 class 合计不超过 30 个。输入类型与输出类型放同一个 state.py 会超限，所以拆成 state.py（输入）和 decision.py（输出）。
B 的完整拟修改范围（I 释放后才写入）：app/web_runtime/{clock_policy,state,decision,wake_events,heartbeat_policy}.py、tests/test_web_heartbeat_policy.py、本日志。
冲突检查：23:5x 复查，decision.py 不存在，其他窗口也没有登记这个路径；91f2 的 C 包在 web_agent/decision/** 下，是不同目录，不重叠。307b 仍未释放，仓库内写入继续 BLOCKED。
是否释放：否。
CORRECTION：本条标题时间写错。实际写入时间约为 2026-09-23 00:1x +08:00（已跨过午夜）。
CORRECTION（2026-09-23 00:03 +08:00）：上面两处时间都不准。用 Get-Date 核对，本条 SCOPE_CHANGE 实际写入时间是 2026-09-23 00:03:08 +08:00。

## START（实施）— pkg-B-heartbeat：I 已释放，开始写仓库

时间（含时区）：2026-09-23 00:08 +08:00
窗口ID / 任务ID / 事件：claude-20260922-234415-fed3 / pkg-B-heartbeat / START（CLAIMED → 实施），解除上一条的 BLOCKED
依据：
  - 307b 在 2026-09-22 23:55 的“SCOPE_CHANGE ＋ 部分 RELEASE”中释放给 B：app/web_runtime/ 下除 __init__.py 外的新模块（含 B 在日志登记的包内辅助模块），以及 tests/test_web_heartbeat_policy.py。
  - 307b 在 00:02 ACCEPT：共享类型 runtime-internal 0.1.0 落在 app/schemas/runtime_internal/，app/web_runtime/__init__.py 为空包（I 维护）。
接口版本：runtime-internal 0.1.0。B 一律从 app.schemas.runtime_internal 导入 Clock、Versions、ActivityRef、RuntimeState、WakeKind、WakeEvent、HeartbeatAction、TaskIntent、HeartbeatDecision、BrainPurpose、CognitionStatus，不定义同名类。
最终拟修改范围（替换此前列表；decision.py 不再建，改名为 reasons.py，避免与共享类型的 decision 子模块混淆）：
  - PetJourneyBackend/app/web_runtime/clock_policy.py：SystemClock、FakeClock（实现共享 Clock 协议）、ClockMonitor（单调时钟校时检测）、地区钟点工具（严格 IANA，不回退默认时区）。
  - PetJourneyBackend/app/web_runtime/state.py：B 自己的输入事实（到期事项、认知可用性、自主复查时刻），名字都不与共享类型重复；以及从这些事实推导共享 RuntimeState 字段的函数。
  - PetJourneyBackend/app/web_runtime/reasons.py：任务意图种类、原因码、安静原因（正常 / 延期 / 故障）词表。
  - PetJourneyBackend/app/web_runtime/wake_events.py：唤醒事件的去重、合并与生效判断。
  - PetJourneyBackend/app/web_runtime/heartbeat_policy.py：HeartbeatPolicy 参数、纯函数 evaluate / evaluate_all、next_review_at。
  - PetJourneyBackend/tests/test_web_heartbeat_policy.py。
  - 本日志。
明确不改：app/web_runtime/__init__.py、app/schemas/**（含 runtime_internal）、迁移目录、组合根与 web_worker、web_agent/**（ticker / life / moment 等）、A / C / Q 的文件、已有测试。B 不需要任何新表或新列（纯函数），没有 DDL 需求。
运行资源（无则写无）：无。只跑纯内存单元测试和只读门禁：python -B（不写字节码），TZ=UTC；不连数据库，不联网，不启停 18761 / 18763 / 5289，不调用供应商。
冲突检查结果（00:07 复读全部窗口日志）：4bef = A（tasks / lease / budget / meter 与两份测试），91f2 = C（web_agent/decision/** 与两份测试），4d18 = Q（verify_runtime_evidence.py 与 test_web_runtime_acceptance.py），c84a = 监管看板文档；都没有登记 web_runtime/ 或 test_web_heartbeat_policy.py。无重叠。
是否释放：否。

## HANDOFF — pkg-B-heartbeat：READY_TO_INTEGRATE（LOCAL_VERIFIED，未接入，未提交）

时间（含时区）：2026-09-23 00:26 +08:00
包名 / 窗口ID / 接口版本：B / claude-20260922-234415-fed3 / runtime-internal 0.1.0（从 app.schemas.runtime_internal 导入 Clock、Versions、ActivityRef、RuntimeState、WakeKind、WakeEvent、HeartbeatAction、TaskIntent、HeartbeatDecision；没有定义同名类）
实际目录 / 分支 / HEAD：E:\petsoul-audit\petsoul / codex/petsoul-web-integration / 980feab（未提交、未暂存）
一、实际修改文件（全部新建；SHA256 前 16 位）
  - C540AA15531D29F3  PetJourneyBackend/app/web_runtime/clock_policy.py（176 行）：SystemClock、FakeClock（实现共享 Clock）、ClockMonitor（单调时钟校时：回拨 / 前跳后一直报异常，直到墙上时钟回到推算值或 reanchor）、ensure_utc、resolve_zone（严格 IANA，认不出返回 None）、local_wall、in_local_window、next_local_occurrence（跨午夜、夏令时：跳过的钟点顺延到跳变时刻，重复的钟点取第一次）。
  - 97A38251820BA94E  PetJourneyBackend/app/web_runtime/state.py（155 行）：B 自己的输入事实 HeartbeatFacts（DueItem、CognitionFacts / InFlightBrain / BrainAvailability、AutonomyFacts、last_evaluated_at、maintenance）；推导函数 commitment_deadline、cognition_status（供 I 填共享 RuntimeState 的两个字段，与心跳看到的一致）、facts_from_state（只有共享状态时的最小事实：不启用大脑）。
  - B1A11B9870AB6EE4  PetJourneyBackend/app/web_runtime/reasons.py（125 行）：IntentKind（settle_due / deliver_reply / brain_decide / rule_life_plan / cancel_brain / recover_task / reconcile）、ReasonCode、SilenceReason 与 SilenceKind（normal / deferred / fault）、silence_of。
  - E1E0A27E072D2B65  PetJourneyBackend/app/web_runtime/wake_events.py（78 行）：merge_events：别的宠物的事件不处理也不消费；同 id 去重；同 (kind, source_ref) 只留 sequence 最大的；未生效的保留、不消费。
  - 1087899460833FBA  PetJourneyBackend/app/web_runtime/heartbeat_policy.py（416 行）：HeartbeatPolicy 参数、evaluate、evaluate_all（同一 pet_id 出现两份状态直接报错）、next_review_at。
  - FECAE7576E335169  PetJourneyBackend/tests/test_web_heartbeat_policy.py（513 行，21 项）。
  - 本日志。没有改 app/web_runtime/__init__.py、schemas、迁移、组合根、web_agent/**、其他包的文件或已有测试。
二、策略要点（evaluate 是纯函数：不写库、不联网、不调模型、不预占额度、不读宿主机时区）
  - 每只宠物一份状态、一次评估：不按家人复制；待领养居民（household_id 为 None）照样评估，默认用规则生活（resident_brain=False，沿用“居民不触发模型”）。
  - 优先级：维护中 DEFER（连事件也不消费）→ 时钟不可靠 RECOVER（不结算、不请求大脑、只消费 clock_unhealthy 事件）→ 收集意图（有恢复类意图时为 RECOVER，否则为 APPLY_RULE）→ 未入住 CONTINUE → 活动进行中 CONTINUE（不请求大脑，建议先记着）→ 在家 / 在驿站空闲时按当地钟点判断。
  - 收集意图包括：到期事项（承诺排最前）；task_expired 事件；认知操作超时 → 恢复；认知操作依据的版本变了 → 作废；主活动未知或早该结束却没有到期事项 → 对账。
  - 到期承诺优先：睡着、大脑关闭、额度用完都不影响兑现；连续失败交给恢复；行程版本对不上先对账；每次最多 max_intents 项。
  - 空闲时：时区认不出 → RECOVER（不套用香港或上海）；睡着 / 不在出门时段 / 今天出去够了 / 没到复查时间 / 离上次决定不到 15 分钟 → CONTINUE；该复查了 → 大脑可用就 REQUEST_BRAIN（purpose=life_plan，意图 brain_decide 带 deadline），不可用时按 brain_fallback 走规则生活（APPLY_RULE rule_life_plan，并标 rule_fallback，不冒充模型）或 DEFER。
  - 安静原因分三类：normal（生活中、睡着、暂无新决定……）、deferred（额度用完、维护）、fault（依赖不可用、任务卡住、时钟异常、时区未知、数据不一致）。写成 "silence:<原因>" 放进 reason_codes，silence_of() 读回。
  - 下一次检查：永远 ≥ now + 1 秒、≤ now + 6 小时（看门狗）；过去的时刻交给到期事项处理，不会当成“马上再查”；执行规则或恢复后 60 秒复查。
三、给 I 的输入映射（I 在同一个读事务里构建，不在 GET 里推进）
  - RuntimeState：
    · versions：runtime_epoch 与 activity_epoch 由 I 的每宠运行记录提供；dna_version 取 web_pet_dna.version；privacy_epoch 在接待确认 / 更正 / 撤回、用途授权变化时递增；membership_epoch 在家庭成员变化时递增（居民为 0）；itinerary_version 取当前行程版本。
    · household_id：households.household_of_pet(pet_id)；驿站居民为 None。
    · timezone：TA 此刻所在地的 IANA 时区，严格取值。在家取 HomePlaceStore 的 timezone，在驿站取 residents.timezone_of，在交通段取 leg.origin 的 timezone，在店里取 visit.place 的 timezone。不能用 moment._zone（回退香港）或 city_timezones.timezone_for_city（回退上海）；取不到就给 None。
    · primary_activity：由只读的 journeys.peek 与入住状态推出：
      未入住 → not_activated；
      在家、行程已定但还没到出门时间 → at_home，ref 为 journey:<id>，ends_at 为出门时刻，interruptible=False；
      在家或在驿站空闲 → at_home、ref=None；
      在交通段 → travel，ends_at 为该段结束；
      在店里 → visit，打工则为 work，ends_at 为 visit 结束；
      附近散步 / 喝一杯可以整段记为 local_activity；
      驾校正式考局 → exam；
      判断不了 → unknown（心跳会对账，不去猜）。
    · sleep_window：profile_of_pet 画像的 (sleep_start, wake)。
    · pending_commitment_deadline 用 state.commitment_deadline(facts)，cognition_status 用 state.cognition_status(facts, now)。
  - HeartbeatFacts：
    · due_items：
      进行中的旅程给一项 DueItem("journey:<id>", JOURNEY, 最早一个未生效事件的时刻, commitment=是否含工钱, itinerary_version=当前)；需要 I 在 web_journey 提供不推进的只读查询。
      待回复承诺按（家人，宠物）会话各给一项 DueItem("reply:<稳定的会话引用>", REPLY, 最早未兑现的 due_at, commitment=True, input_high_watermark=会话内最新输入序号)。
      attempts / retry_after 取自对应任务记录（A 的队列）。
    · cognition：
      enabled 取 AI 生活规划开关与家庭设置；
      availability 取 readiness、计量与 A 的额度预占结果：未配置为 DISABLED；额度被拒为 EXHAUSTED，带 UTC 窗口重置时刻；供应商故障为 UNAVAILABLE，带 retry_after；
      operation 取 I 的认知操作记录；remaining_today 取 A 的额度数据。
    · autonomy：active_window 取 life.decide_window(profile)；outings_today 为当地当天已出门次数；outings_per_day 取画像；next_review_at / last_decision_at 取 I 的运行记录；latest_suggestion_at 取 24 小时内还在考虑的 web_owner_suggestions 的最新时刻。
    · last_evaluated_at、maintenance 取 I 的运行记录。
  - WakeEvent 来源：
    · message_received：家人发消息；suggestion_received：家人给建议；activity_due：到期事件被应用，或交通段、到访结束；
    · versions_changed：任一版本递增时，包括提交大脑提案、作废操作、切换维护状态之后；
    · dependency_recovered / budget_restored：供应商与额度恢复；clock_unhealthy：ClockMonitor 报异常；task_expired：A 的过期回收。
    · 浏览、媒体暂停、地图缩放都不是唤醒事件：暂停一起听不会改变任何检查时刻。
四、调用方式（示意）
  reading = monitor.sample(clock)  # 进程级 ClockMonitor 与 SystemClock
  state, facts = 读事务投影(pet_id, reading.now_utc)
  decision = evaluate(state, 已授权事件, POLICY, reading.now_utc, facts=facts, clock_health=reading.health)
  写事务（UoW）里：
  - 先复核 runtime_epoch 与版本未变；
  - 按 intent.dedupe_key 入队：
    · settle_due → 补齐旅程；deliver_reply → 兑现该会话；
    · brain_decide → 先经 A 预占，再建认知操作（id 为 dedupe_key，source_versions 为 state.versions，截止为 deadline_at），交给 C；
    · rule_life_plan → 现有 LifeEngine 对这一只宠物做一次决定，并写 last_decision_at 与 next_review_at(now, POLICY)；
    · cancel_brain → 作废操作（提交围栏拒收旧结果）；recover_task → A 的恢复流程；reconcile → 重建投影、告警；
  - 写 next_check_at、silence_of(decision)、last_evaluated_at；
  - 确认 consumed_event_ids。
  一轮多只宠物用 evaluate_all（传每只一份状态；不要用 member_pets 那种按家人展开的列表）。
  建议先 shadow：与现有 ticker 并行只比较结论、不执行，再逐只切换。
五、CHANGE_REQUEST（给 I，均为兼容追加，接受与否由 I 定）
  1. HeartbeatDecision 增加 silence_reason: str | None = None。B 现在把它编码在 reason_codes 里（"silence:<原因>"），采纳后 B 同时写字段，并保留编码一个版本。
  2. WakeKind 没有“规则已提交 / 大脑结果已提交 / 维护切换”。B 目前靠 versions_changed 与 activity_due 覆盖，要求 I 在这些时刻发出其一。若 I 愿意加显式类别，B 同步加进 wait_event_kinds。
  3. HeartbeatFacts / DueItem / CognitionFacts / AutonomyFacts 目前归 B 所有，名字都不与共享类型重复。若 I 希望 evaluate 严格只收契约里的 4 个参数，可把它们并入 runtime_internal（0.1.x），B 改为从那里导入。
  4. 时区严格性（A02）：填 RuntimeState.timezone 时不能用 HK / 上海回退。如需要严格版的 city_timezones 函数，由 I 在自己的文件里加。
  5. 名字重叠：clock_policy.FakeClock 与 tests/web_base.FakeClock（替换 utils.utcnow 的测试工具）同名但用途不同。arch_gate 会合并统计同名类，目前远低于上限。要改名请告诉 B（用户任务里写的是 FakeClock）。
  B 本身没有 DDL 需求。建议 I 的每宠运行表至少有：next_check_at（带索引）、silence_reason、last_evaluated_at、next_review_at、last_decision_at、maintenance、runtime_epoch。
六、验证命令 / 结果 / 隔离环境（D:\python\python.exe 3.12.2，-B 不写字节码，禁网、无数据库）
  - TZ=UTC `python -B -m unittest discover -s tests -p "test_web_heartbeat_policy.py"` → 21 项 OK；不设 TZ 复跑 → 21 项 OK（与宿主机时区无关）。
    覆盖：FakeClock 驱动的睡眠 / 夜猫子 / 起床；旅途六种活动与活动中每分钟唤醒；到期工钱在大脑关闭、额度用完、睡着时照样结算且去重键不变；承诺优先与批量上限；48 小时离线只补一次；未知时区；新消息不触发也不作废思考；五种版本变化与行程改签；认知超时 / 任务过期 / 卡住事项；复查间隔与多位家人建议只合成一次决定；大脑不可用时规则或延期；居民与领养途中；evaluate_all 防复制；事件合并；48 小时调度循环有界（两天恰好 72 次规则决定）；时钟回拨 / 前跳 / 事件；活动超时对账；维护；最小状态与推导函数、参数校验。
  - TZ=UTC `python -B -m unittest discover -s tests -p "test_runtime_internal_types.py"`（I 的测试，只读运行）→ 9 项 OK。
  - `python -B scripts/dependency_gate.py` 通过；`python -B scripts/contract_diff.py` 通过。
  - `python -B scripts/arch_gate.py`：B 的 6 个文件无违规（定义数最多 29，行数最多 513）。整体仍失败，原因是 A（4bef）在建的 tests/test_web_budget_reservation.py（32 个定义）与 tests/test_web_task_recovery.py（58 个定义）；00:10 时还有 C 的 test_web_decision_proposal.py，后来已消失。都不是 B 的文件，B 不改。
  - 没有跑后端全量回归：B 的模块没有被任何现有模块导入，全量回归由 I 在接入时跑。
七、未接入 / 未验证
  - 未接入：没有任何 worker、ticker 或路由导入 app.web_runtime；18763 / 18761 进程不会加载它（307b 23:55 已核对无 reload）。
  - 未验证：真实库投影、与现有 ticker 的 shadow 对照、公平调度与容量（A13 属 I 与 A）、真实供应商。参数（6 小时看门狗、15 分钟复查、90 秒认知截止等）是首轮建议值。
  - 夏令时只用纽约规则测过（香港没有夏令时）。
  - heartbeat_policy.py 416 行，略超 AGENTS 对平铺模块 400 行的建议；它在包内，门禁上限 800，暂不拆。
八、需要 I 做的事（B 不改这些文件）
  - 投影与装配放在 I 的范围：web_agent_wiring.py（或 I 另登记的新文件）；web_journey 提供不推进的“最早未生效事件”查询；web_communicator 提供按会话的待回复摘要；每宠运行表迁移；worker 注册心跳任务，逐步替换 life.run 与 deliver_due 的全量扫描。
  - 覆盖的验收项（离线机制部分）：A01、A02、A04、A23，以及 A03、A15、A18、A20 中属于 B 的部分；契约 §3 的第 1–5 行与“收到后来消息”一行中属于心跳的判断。
运行资源（无则写无）：无。没有启动进程，没有数据库、端口或供应商调用；web_runtime 下没有生成 __pycache__。
是否释放（是/否，明确范围）：否。保留 B 的 6 个文件，用来回应 I 的接入反馈和 Q 的验收发现。I 接入确认或用户另行安排后再 RELEASE。
CORRECTION（2026-09-23 00:28 +08:00）：第六部分“00:10 时还有 C 的 test_web_decision_proposal.py”时间写错。实际是本窗口第一次运行 arch_gate（约 00:21）时看到它，00:23 复跑时已不在违规列表里。

## PROGRESS ＋ CHANGE_REQUEST — pkg-B-heartbeat 第二轮：集成后的运行行为复核

时间（含时区）：2026-09-23 03:50 +08:00
窗口ID / 任务ID / 事件：claude-20260922-234415-fed3 / pkg-B-heartbeat / PROGRESS（复核集成后的实际行为）＋ 给 I 的 8 条 CHANGE_REQUEST
实际目录 / 分支 / HEAD：E:\petsoul-audit\petsoul / codex/petsoul-web-integration / 980feab（未提交）
CORRECTION（对本窗口 00:26 交接）：那条写的“未接入：没有任何 worker、ticker 或路由导入 app.web_runtime”已经过期。
  I 在 01:35（第 4 批）之后把心跳接进了三处真实调用：世界线的 `heartbeat_shadow`、认知线 `brain_life` 的准入判断、以及 `GET /ops/runtime/{pet_id}`。本条按现状重新核对。

一、B 自己改的（只改 B 的文件）
  - 共享类型 0.1.1 已按 B 的 CR-1 加了 `HeartbeatDecision.silence_reason`，但 B 还只写原因码。现在 `_Draft.done` 同时写字段与 `silence:<原因>` 原因码，
    `reasons.silence_of` 首选字段、字段为空时回落到原因码（兼容旧结论）。这样 I 的 `record_evaluation` 不必再靠回落分支。
  - 文件与 SHA256 前 16 位：heartbeat_policy.py 41047FE3618FCB08、reasons.py 0E3DAB783B0AA10D、tests/test_web_heartbeat_policy.py EB30264D2519530D。
  - 验证：B 21 项、I 的 test_web_runtime_shadow 8 项、test_web_brain_life 10 项、test_runtime_internal_types 9 项全部 OK；arch / dependency 门禁通过（arch 本轮已全绿，A 的两个测试文件已修）。

二、输入映射差异表（逐项核对真实来源，不只看字段在不在）
  | B 的输入 | I 的真实来源 | 结论 |
  |---|---|---|
  | 所在城市时区 | `web_agent/runtime_view.py::RuntimeProjector._where`：店里 `visit.place["timezone"]` → 当前交通段 `leg.origin["timezone"]` → 家/驿站 `home_places.get(...).timezone`；都没有则 None | 合格。没有香港/上海兜底。实测驿站居民拿到 `Asia/Hong_Kong`（与 `residents.timezone_of` 一致）。与 B 交接的唯一差异：POI 没有时区字段时回落到“家的时区”而不是 None——I 在 01:35 已说明，B 接受 |
  | 活动 | 同上 `_where`：到店 → visit/work、在途 → travel、行程已定未出门 → at_home＋ends_at=出门时刻＋不可打断、其余 at_home | 基本合格。缺口：驾校正式考局没有投影成 `exam`；散步/进城也走 travel/visit（`local_activity` 从不出现），ops 上看不出“在考试” |
  | 到期承诺 | `web_journey/settlement.py::due_summary`（整段旅程一项，work_done/returned_home 记为承诺，带 itinerary_version）＋ `web_communicator/service.py::pending_summary`（按会话，只给条数不给正文） | 关键映射正确、不带正文。差异：due_summary 只给“已经到期”的（`_due_events` 按 now 过滤），未来边界靠 `primary_activity.ends_at` 覆盖；`attempts/retry_after/deadline_at` 永远是默认值 → B 的“连续失败转恢复”“退避”分支在生产里不会触发 |
  | DNA 作息 | `RuntimeProjector.state`：`profile_of(pet).sleep_start/wake`；出门时段由装配注入 `life.decide_window(profile)` | 合格，与规则生活同一份画像 |
  | 授权版本 | `RuntimeProjector.versions`：activity/privacy/membership 取 `web_entity_runtime`（第 7 批已在业务事务里递增）、dna 取 `web_pet_dna.version`、itinerary 取当前行程 | 合格。`runtime_epoch` 恒为 0（I 说明只有切换推进者时才用） |
  | 认知状态 | `RuntimeProjector.facts`：`enabled = 家庭“模型回信”开关 ∧ providers.chat.available`，availability 只有 AVAILABLE / DISABLED | **不合格**。没有 EXHAUSTED / UNAVAILABLE / retry_after / remaining_today / operation / last_requested_at → B 的额度退避、依赖故障退避、进行中操作超时与作废四类分支全是死代码（反例见 CR-B2） |
  | next_check_at | `record_evaluation` 写进 `web_entity_runtime`，`GET /ops/runtime` 展示 | **没有任何调度器读它**（grep 全仓只有写与展示）。两条线仍是每 30 秒全量扫描；迁移 0060 已建 `idx_web_entity_runtime_due` 备用 |
  | next_review_at | 只有 `brain_life._commit` 的 departed / stayed 会写，且传的恒为 None | **不合格**。失败、拒绝、跳过都不写；B 的 `next_review_at()` 与大脑的 `suggested_review_after_seconds` 都没被使用 |
  | 唤醒事件 | 三处调用都传 `()` | 现阶段靠 30 秒全量扫描可接受；改成按 next_check_at 调度时必须补事件来源（B 的 wake_events 已就绪） |
  | 时钟健康 | 没有人构造 `ClockMonitor`，`evaluate` 也没收到 `clock_health` | 回拨能靠 `last_evaluated_at` 发现（实测 silence=clock_unhealthy，下一轮自动恢复）；“墙上时钟大幅前跳”这类只有 ClockMonitor 能发现的异常测不到 |

三、实际调度流程（按代码与实跑，不是设计意图）
  - 世界线（租约 world，30 秒一轮）：`journeys.advance_all`（到期结算与 outbox 同事务）→ `deliver_outbox(FAST)` → `refresh_scheduled`（出门前复核，会真的调高德）→ `life.run`（规则生活，可能出发，会调地图）→ outbox 回执清理 → `heartbeat_shadow`（每只宠物评估一次并写运行记录）。
  - 认知线（租约 cognition，30 秒一轮、租期 5 分钟）：`deliver_outbox(SLOW)` → `communicator.deliver_due`（到点回复）→ `proactive.run`（主动消息）→ `brain_life`（只对心跳说 REQUEST_BRAIN 的宠物做一次决策，一轮最多 5 只）。
  - 心跳一轮被评估两次（世界线 shadow 一次、认知线准入一次，各用各的 now），外加 `/ops/runtime` 的只读评估。
  - REQUEST_BRAIN 之后：`brain_life.consider` → 规则给 offers → A 的 `BudgetLedger.reserve(operation_id)` → C 的 `Brain.propose`（模型）→ `settle` → live 模式复核（版本、机会有效期、是否已在外面）→ `journeys.depart` → `record_decision(last_decision_by)`。
  - 下一次唤醒＝固定的下一轮（30 秒）。心跳算出来的 next_check_at 不参与。
  - 两个开关组合的真实行为（这条最容易被误解）：
    · heartbeat=shadow ＋ brain=off（默认）：心跳评估并记录，一次模型都不调用，宠物由规则生活推进；
    · heartbeat=shadow ＋ brain=shadow：**仍然会真的调用模型**（只是不出门），属于付费调用；
    · heartbeat=shadow ＋ brain=live：模型决策会真的成行，同时规则生活照常在世界线独立决定 → 两个决定者并存；
    · heartbeat=off ＋ brain=live/shadow：不写运行记录，但 `brain_round` 仍然内联评估心跳并调用模型——**把心跳关掉不等于关掉模型调用**。

四、隔离证据（临时库、假模型、FakeClock；不碰 18763，未产生任何真实付费调用）
  探针在本会话 scratchpad：`probe_fairness.py`（驱动真实 `brain_wiring._round`，注入替身）、`probe_integration.py`（I 的测试基类 `web_base.WebPlatformTestBase`，每个探针一个临时库）。
  1. 公平性：8 只宠物、每轮上限 5、决策持续失败 → 12 轮里 pet-01..05 各被处理 12 次，pet-06..08 一次都没轮到。对照组（决策成功并写运行记录）：2 轮覆盖全部 8 只。
  2. 失败节流：模型每次失败，16 轮（8 分钟）里 12 次真实模型调用把每宠每日 12 次额度打满，之后 4 轮仍每轮尝试预占；`last_decision_at` 始终为 None。
  3. 对照：模型正常返回“留在家”时，8 分钟只调用 1 次，下次检查落在 15 分钟后 → 记录写下了就能限流。
  4. 竞争：brain=live、模型每次都选“留在家”；12 小时里模型被调用 4 次、运行记录 `last_decision_by=model / 04:45`，而世界线的规则生活在 05:00 仍然让它出门（local:cafe）。48 轮心跳结论分布：request_brain 5、apply_rule 2、continue/living 1、continue/enough_today 14、continue/outside_active_hours 24、continue/asleep 2。
  5. 活动中不误合并：出门后心跳＝continue/living（不请求大脑），主人发消息后 6 分钟一轮，TA 的回复照常发出，生活规划的模型调用 0 次——承诺兑现走认知线的 replies 步骤，与“活动中不规划”互不影响。
  6. 确定性结算：模型一直失败、额度被消耗、宠物在打工 → 只跑世界线，工钱恰好 1 笔（24 星币）、世界事件 7 条、生活规划模型调用 0 次。
  7. 离线 48 小时：第一轮只调用 1 次模型，没有成倍补发。
  8. 时钟回拨 10 分钟：`silence_reason=clock_unhealthy`，再跑一轮恢复为正常（不会永久卡住）。
  9. 待领养居民：3 只抽样，时区取到驿站的 `Asia/Hong_Kong`，心跳结论 apply_rule ＋ `resident_rule_life` ＋ `rule_fallback`——没有因为没有 owner 被排除。

五、CHANGE_REQUEST（都在 I 的文件里；B 不改）
  - CR-B1 公平性（`app/web_agent/brain_wiring.py::_round.brain_round`）：`done` 只在 `consider` 返回后 +1，而失败不写运行记录，下一轮排序又从 pet_id 最小的开始 → 超过一轮上限时后排宠物饿死（证据四·1）。
    建议：①改成游标轮转（与 `web_journey/settlement.py::advance_all` 的分页同一做法），或按 `web_entity_runtime.last_evaluated_at / last_decision_at` 升序取；②无论结果如何都记一次“这轮想过了”。
  - CR-B2 失败后的节流（`app/web_agent/brain_life.py::consider/_commit` ＋ `runtime_view.py::RuntimeProjector.facts`）：failed / rejected / skipped 都不写运行记录，于是每 30 秒重试一次，直到把每宠每日 12 次额度打满（证据四·2）。
    建议：①每条返回路径都写一次尝试时间戳；②把失败原因投影进 `CognitionFacts`（availability=UNAVAILABLE / EXHAUSTED、retry_after、remaining_today、last_requested_at），B 的 `_brain_blocked` 已有对应分支和测试，投影补上就能自己退避；③或直接改用心跳给出的 `brain_decide` 意图去重键（`heartbeat_policy.py::_slot_key`，已按 15 分钟时段＋语义版本生成），交给 A 的队列按 dedupe_key 去重。
  - CR-B3 规则生活与模型生活竞争同一只宠物（`app/web_agent_wiring.py` 的 `("life", life.run)` 与 `app/web_agent/life.py::LifeEngine.consider`）：brain=live 时两条线各自决定，世界线还先手；模型决定“留在家”15 分钟后规则仍把它送出门，运行记录里的决定者与实际出门者不一致（证据四·4）。
    建议：brain_mode != off 时，规则生活只处理心跳结论不是 REQUEST_BRAIN 的宠物；或统一由心跳的 `rule_life_plan` 意图驱动规则生活，保证一只宠物同一时刻只有一个决定者。
  - CR-B4 出门次数口径不一致（`runtime_view.py::RuntimeProjector._outings_today` 对 `life.py::LifeEngine.consider`）：前者统计最近 24 小时的 `web_journeys`，后者统计“当地当天 0 点以来”。跨午夜会出现心跳说 `enough_today` 不再规划、规则却允许出门。建议按 `state.timezone` 换算当地当天 0 点。
  - CR-B5 共享 RuntimeState 两个字段没填（`runtime_view.py::RuntimeProjector.state`）：`pending_commitment_deadline` 恒为 None、`cognition_status` 恒为 "quiet"。B 已提供 `app/web_runtime/state.py::commitment_deadline / cognition_status` 两个推导函数，直接调用即可；否则 `/ops/runtime` 与任何按共享类型读状态的消费者看不到“有承诺待兑现 / 大脑不可用”，最小模式（`facts_from_state`）也会丢掉承诺。
  - CR-B6 next_check_at 未参与调度：现阶段 30 秒全量扫描可接受，但每轮每只要做 8–10 次查询且被评估两次。切换成“按 next_check_at 取到期宠物”时，请同时补 WakeEvent 来源（消息、建议、活动边界、版本变化、额度恢复），B 的 `wake_events.merge_events` 与 `evaluate_all`（同一只宠物重复登记会报错）都已就绪。
  - CR-B7 时钟健康没接（`runtime_view.py::WorldClock` 旁）：建议每轮采样一次 `ClockMonitor.sample(clock)`，把 `reading.health` 传给 shadow 与 brain_round 的 `evaluate`；现在只能发现回拨，发现不了大幅前跳。
  - CR-B8 三个小项：①驾校正式考局没投影成 `exam`（考试期间心跳把宠物当空闲）；②`versions/state/facts` 每次评估分别开三次连接读 `web_entity_runtime`，可能不是同一时刻的快照；③`pending_summary` 的 ref 形如 `reply:<user_id>:<pet_id>`，会经 B 的意图与 `/ops/runtime` 输出，建议换成不含家人编号的稳定引用。

六、仍缺的自然时间证明
  - 18763 上跑过的只有 heartbeat=shadow ＋ brain=off；brain=live 只在临时库里由假模型（I 的 10 项）与两次真实模型脚本（I 的 03:10 / 03:13 ACCEPT，临时库、跑完即删）验证过，没有在真实环境里连续跑过一个昼夜。
  - 失败退避、公平轮转（修复后）、心跳接管调度，这三件在真实环境的自然时间证据都是 0。
  - E2（出门前按作息复核）是 I 的自然时间项，本窗口不重复建立等待进程。

七、启用下一阶段前的具体条件（建议给用户与 I）
  1. 打开 brain=live（不限于脚本）之前：CR-B1、CR-B2、CR-B3 修复并各有回归用例；`/ops/status` 能看到每宠决定者与安静原因分布；确认每宠每日上限与供应商上限两层都生效。
  2. 让心跳接管调度之前：CR-B5、CR-B6 落地（填字段 ＋ 按 next_check_at 取到期 ＋ 事件来源），并与 shadow 对照跑满一个完整昼夜。
  3. 公开发布之前：CR-B4 口径统一、CR-B7 时钟健康接入、CR-B8 三个小项。
运行资源（无则写无）：无。探针都在临时目录建库、跑完即删；没有启停 18763 或任何共享服务，没有联网、没有真实供应商调用。
是否释放（是/否，明确范围）：否。B 的 6 个文件继续持有，用来回应 I 对上述 CHANGE_REQUEST 的处理。

CORRECTION（2026-09-23 04:05 +08:00）：上条第四节证据 2 写的“12 次真实模型调用”不准确，容易被读成真实供应商调用。
准确说法：**12 次假模型调用（ScriptedModel），但走的是真实业务调用链——真实的 BrainLife、真实的 A 额度预占与结算、真实的运行记录**；
每次调用都真实消耗了那只宠物当天 12 次的额度计数。边界不变：全程未联网、未调用任何真实供应商、未产生任何费用。
同节证据 3（“8 分钟只调用 1 次”）、证据 6（“生活规划模型调用 0 次”）中的“模型调用”同样都是假模型调用次数，口径一并更正。

## SCOPE_CHANGE — pkg-B-heartbeat：登记证据目录（写文件前先登记）

时间（含时区）：2026-09-23 04:06 +08:00
窗口ID / 任务ID / 事件：claude-20260922-234415-fed3 / pkg-B-heartbeat / SCOPE_CHANGE
新增领取：`PetJourneyBackend/data/reviews/b-fed3-20260923/**`（探针源码与去敏证据 JSON）。
原因：上一轮探针只在本会话 scratchpad，I 与 Q 无法复验。data/reviews 已被 git 忽略，且是本项目既有的评审证据惯例
（claude-fixes-20260922、codex-progress-20260923、codex-direction-20260922、q-4d18-20260922 各一份）。
冲突检查（04:05）：全部窗口日志里没有人登记过 `data/reviews/b-*` 或 `b-fed3`；该目录此刻不存在。
边界：只写这个目录，不碰 tests/、scripts/、app/ 下 B 以外的文件。探针文件名不以 test_ 开头，不会被 unittest discover 收走。
如果 I 希望把其中的用例纳入默认回归套件，请在 I 自己的 tests/ 下新建文件引用或复制，B 不代为创建。
是否释放：否。

## PROGRESS — pkg-B-heartbeat 第三轮：按 I 第 11 批的新代码复跑反例（不沿用旧结论）

时间（含时区）：2026-09-23 04:06 +08:00
窗口ID / 任务ID / 事件：claude-20260922-234415-fed3 / pkg-B-heartbeat / PROGRESS（复验 ＋ B 侧一处改动）
一、本轮复验针对的代码指纹（SHA256 前 16 位；全部未提交）
  - I 第 11 批（B 只读引用，不修改）：brain_wiring.py 3CAB668971CD0CA0（04:00 版，含游标轮转）、brain_life.py E62313018F991F60（含 _back_off）、
    runtime_view.py 82F439399EB95200（record_backoff / decided_within）、life.py 9C2152CDF6EF70A4（规则线接共用决策账）、
    web_agent_wiring.py 2DB0EE844865B992（注入 recently_decided / record_decision）。
  - B 自己的：heartbeat_policy.py 1A094047ED12EFD2（本轮新增 next_attempt_at）、reasons.py 0E3DAB783B0AA10D、state.py 97A38251820BA94E、
    clock_policy.py C540AA15531D29F3、wake_events.py E1E0A27E072D2B65、tests/test_web_heartbeat_policy.py 91667FEFA455F4B7。
  - 证据与探针：data/reviews/b-fed3-20260923/{probe_fairness.py 12F80720002AA88B, probe_runtime.py C804607F12497E59,
    evidence-fairness.json A3DD07FC85E39967, evidence-runtime.json 37606F0B26276ED8}。
  - 复验口径：所有“模型调用次数”都是假模型（ScriptedModel）调用次数，走真实 BrainLife、真实额度预占与结算、真实运行记录；未联网、无费用。
    临时库随用例创建与删除；没有重启或访问 18763，没有碰 E2。

二、B 侧这轮改的一处（只改 B 的文件）
  - `heartbeat_policy.next_attempt_at(now, policy, *, dependency=False, retry_after=None)`：一次没想成之后最早什么时候再想。
    依赖故障按 dependency_recheck（5 分钟），其余按 min_decision_interval（15 分钟）；**账本或供应商给了恢复时刻就尊重它**，
    最长不超过 max_review_interval（6 小时），保证额度被人工调高时仍会重新看一眼。退避只推迟“再想一次”，不推迟到期承诺与确定性结算。
  - 目的：把退避口径收回 B 一处，替换 brain_life 里自写的 BACKOFF / DEPENDENCY_BACKOFF 两个常量（见 CR-B9）。
  - 验证：B 21 项测试通过（新增 5 条断言覆盖四种取值与“恢复时刻已过”）；arch / dependency 门禁通过。

三、复验结果（对照 03:50 那轮的旧结论，逐条重判）
  1. 公平性（探针 1，真实 `_round`，8 只 / 每轮上限 5 / 12 轮）：
     · 决策失败（provider_error）：8 只全部轮到，各 1 次；单轮最多 5 只 → **已修复**（旧结论：后 3 只 12 轮一次都没轮到）。
     · 额度被拒：同上 → **已修复**。
     · **未预料到的异常仍绕过上限**：8 只每轮都被 consider 一次、12 轮各 12 次，**单轮处理了 8 只（上限 5）**。
       原因：`brain_wiring._round` 的 `except ... continue` 不增加 done，也不会写退避。若异常发生在模型调用之后（例如提交阶段出错），
       一轮的真实调用次数就不再受 limit 约束。→ CR-B10。
     · 换进程（第 2 轮换一个 `_round` 闭包，游标回到开头）：退避在库里，8 只仍然各 1 次 → **通过**。
  2. 退避七种情况（探针 2，真实服务 + 临时库）：
     | 情况 | 结果 | 判定 |
     |---|---|---|
     | live 成功（出门） | last_decision_by=model、last_decision_at 有值、next_review_at 未写 | 通过（有效期＝心跳 15 分钟间隔，但不是显式写出来的，见 CR-B11） |
     | **shadow 成功** | 16 轮（8 分钟）里调用 12 次，直到把每宠每日 12 次额度打满才因 budget_denied 退避 | **未修复**：成功路径不记录任何尝试 → CR-B9 第 2 条 |
     | 模型失败 | next_review_at=+5 分钟、silence=brain:provider_error、last_decision_at 仍为 None；8 分钟内只调用 2 次 | 通过（失败没有被写成决定） |
     | 额度耗尽 | 账本给了 `retry_after=2026-09-23T00:00Z`（下一个 UTC 记账日），brain_life 仍固定退避 15 分钟 | **未按要求**：没有用账本的恢复时间 → CR-B9 第 1 条 |
     | 没有可行机会 | 退避 15 分钟、silence=brain:no_offers、0 次调用 | 通过 |
     | 提案失效（思考中撤权） | status=failed / reason=revoked、退避 15 分钟、没有行程、last_decision_at 仍为 None | 通过 |
     | 换进程后 | 退避写在 web_entity_runtime；换一个 RuntimeStore 读到同一时刻；2 分钟后心跳 continue / review_not_due | 通过 |
  3. 决定权：模型 04:00 决定“留在家” → 规则线 05:00（60 分钟后）才出门，**15 分钟有效期内没有被绕过**；出门后 last_decision_by 变成 rule → 通过。
     模型不可用时：心跳给 apply_rule ＋ brain_disabled ＋ rule_fallback、意图 rule_life_plan，规则线照常送它出门（local:cafe）、0 次模型调用 → 宠物没有停摆，通过。
  4. 并发：同一只宠物连续两次 depart，第二次被领域唯一约束挡住（already_traveling）。
     `decided_within` 是读后再做、不在事务里，只是门槛；真正的原子校验在提交处（`journeys.depart` 的 operation_key ＋ 一只宠物一段进行中旅程的唯一索引）。
     与 A 的进程租约围栏（I 第 11 批 CR-A4）、C 的提交边界复核属于同一件事，交 I 统一收口，B 不重复实现。
  5. 调度：第一轮想一次后，接下来 10 分钟（决定间隔内）不再调用；出门后心跳 continue/living，活动中再跑 5 分钟仍然是 0 次新增调用 →
     **当前 30 秒扫描已经尊重退避与活动边界**，通过。next_check_at 仍然只写不读、也没有事件唤醒，属于下一阶段的前置条件（CR-B6 保留）。

四、上一轮已通过、本轮保留的结论（版本与证明范围照旧，不重复复验）
  - 确定性结算不被模型失败或额度紧张阻塞（工钱恰好一次）、活动中回复照常兑现、离线 48 小时只补一次决策、时钟回拨后自动恢复、
    待领养居民照常评估：均在 03:50 那轮基于 03:39–03:48 的代码验证。本轮 I 的改动集中在 brain_wiring / brain_life / runtime_view / life，
    不涉及结算与投递链路，故保留原结论与其证明范围；若 I 之后改动结算链路，需要重跑。

五、CHANGE_REQUEST（I 的文件；B 不改）
  - CR-B9 退避口径（`app/web_agent/brain_life.py`）：
    ① 额度被拒时用账本给的恢复时刻——`BudgetDenied.retry_after` 实测为下一个 UTC 记账日；现在固定 15 分钟，会在额度真正恢复前多做约 80 次无用尝试。
       建议改为 `next_attempt_at(now, HeartbeatPolicy(), dependency=..., retry_after=getattr(denied, "retry_after", None))`，
       同时把 BACKOFF / DEPENDENCY_BACKOFF 两个常量删掉，口径只留 B 一处。
    ② shadow 成功后也要记一次尝试（至少写 next_review_at）：现在 shadow 模式每 30 秒调用一次模型，8 分钟就把一只宠物当天的额度打满。
       shadow 是“只记录不执行”，不该是“不记录还每轮调用”。
    ③ 建议顺带记一个 last_attempt_at（或复用 next_review_at 的写入时刻），让运维能区分“想过但没成”与“根本没想过”。
  - CR-B10 异常路径绕过上限（`app/web_agent/brain_wiring.py::_round.brain_round`）：`except ... continue` 不计入 done。
    反例见 evidence-fairness.json 的 behaviour=raises：一轮处理了 8 只（上限 5），12 轮每只各 12 次。
    建议：异常也 `done += 1`（一轮的尝试预算按“尝试”算，不按“成功”算），并对异常路径也写一次退避，避免同一只宠物每轮抛同样的错。
  - CR-B11 “留在家”的有效期与下一次复查（`brain_life._commit` 的 stayed 分支）：现在 `record_decision(..., next_review_at=None)`，
    有效期只能靠心跳的 15 分钟间隔隐含表达。建议写成显式时刻：`next_review_at = next_review_at_helper(at, policy, proposal.suggested_review_after_seconds)`
    （B 的 `heartbeat_policy.next_review_at` 已经做了夹取），这样 ops 与前端能直接回答“TA 决定在家待到什么时候”。
  - 仍然有效、本轮未变的：CR-B4（出门次数 24 小时 vs 当地当天）、CR-B5（共享 RuntimeState 两个字段没填）、CR-B6（next_check_at 未参与调度 ＋ 事件唤醒）、
    CR-B7（没接 ClockMonitor）、CR-B8（exam 活动未投影、三次分别读运行记录、会话引用含家人编号）。

六、给 I 的交接分类
  - **已复验通过（新代码，本轮证据）**：失败与额度被拒下的公平轮转；换进程后退避仍生效；模型失败 5 分钟退避且不写成决定；
    no_offers 与提案失效退避；“留在家”15 分钟内规则线不绕过、到期后换成 rule 并记录；模型不可用时规则后备照常运行；
    最终提交的原子校验挡住第二次出发；30 秒扫描尊重退避与活动边界。
  - **已有代码待复验（等 I 改完或等自然时间）**：shadow 模式的节流（CR-B9②）、额度恢复时间（CR-B9①）、异常路径的尝试上限（CR-B10）、
    “留在家”的显式有效期（CR-B11）；以及 CR-B4/B5/B7/B8 这些上一轮提出、本轮代码里还没动的项。
  - **仍需修改（B 判断必须在下一阶段前做掉）**：CR-B9①②、CR-B10。它们都直接关系到真实付费调用的上限与公平性：
    shadow 模式下一只宠物 8 分钟打满当天额度、异常路径一轮可以超过 5 只、额度耗尽后整天每 15 分钟重试一次。
七、仍缺的自然时间证明（与上一轮相同，未推进）
  - 18763 仍然是 heartbeat=shadow ＋ brain=off；退避、公平轮转、决定权这三件在真实环境的自然时间证据仍为 0，本轮全部是临时库里的确定性复现。
  - 心跳接管调度（按 next_check_at ＋ 事件唤醒）尚未开始。
  - E2 归 I，本窗口没有触碰，也没有新建等待进程。
运行资源（无则写无）：无。探针用临时库、跑完即删；未启动常驻进程、未联网、未调用真实供应商、未改共享配置。
是否释放（是/否，明确范围）：否。B 的 6 个源文件与新登记的证据目录继续持有。

## CORRECTION ＋ PROGRESS — pkg-B-heartbeat 第四轮：退避语义分开、证明范围更正、状态分类互斥

时间（含时区）：2026-09-23 04:10 +08:00
窗口ID / 任务ID / 事件：claude-20260922-234415-fed3 / pkg-B-heartbeat / CORRECTION ＋ PROGRESS（B 侧改动）

一、CORRECTION：更正上一轮的三处证明范围（结论本身不变，但不能当成更强的证明用）
  1. `case_commit_guard` 只证明了**顺序两次出发被领域唯一约束挡住**，不是并发证明，也不是“最终提交原子性”的证明：
     探针在单线程里先后调用两次 `journeys.depart`，没有两个进程或两个事务真的同时提交。
     正式的并发与恢复验收交 Q 收口；本窗口不再用这条支撑“并发安全”。
  2. 上一轮写的“换进程后退避仍生效”应当叫**“重建对象 / 重建轮询闭包后仍读到持久化的退避”**：
     探针新建的是 `RuntimeStore` 对象与新的 `_round` 闭包（游标回到开头），同一个进程、同一个临时库，没有真的换进程。
     它证明的是“退避写在库里、不依赖进程内状态”，不是进程重启恢复。
  3. A 的进程租约围栏、C 的事务内版本校验，本窗口都**没有验证**，上一轮也不该被读成已覆盖：它们仍待 I 接入后由 Q 验收。

二、B 侧改动（只改 B 的文件）：把“禁调下限”和“只读复查”分开
  - 上一轮加的 `next_attempt_at` 语义不对：它把硬性恢复时刻按 max_review_interval 截短，等于把“额度 20 小时后才恢复”变成“6 小时后可以再调”。
  - 现在改为 `retry_plan(now, policy, *, dependency=False, retry_after=None) -> RetryPlan(not_before, check_at)`：
    · `not_before`：最早允许再发起一次**可能付费**的尝试。账本或供应商给的硬性恢复时刻**原样保留，不被截短**；
    · `check_at`：下一次**只读复查**时刻，仍不超过 max_review_interval（6 小时）。到点只重新读一遍事实，条件没变就继续安静，
      既不调用模型，也不把 `not_before` 提前；
    · `RetryPlan.allows(now)` 给集成层判断“此刻是否已过禁调下限”；但它只是下限，不是许可——真正开闸取决于复查时读到的额度与可用性事实。
  - 额度被调高之后怎么重新评估：心跳的大脑可用性判断读的是投影出来的 `CognitionFacts`（availability / remaining_today / window_resets_at），
    不看任何“禁调到某时”的存量标记。所以额度一调高，下一次复查就会重新给出 REQUEST_BRAIN，不需要人工清标记。
  - 新增断言（都在原有用例里，未新增 def）：
    · `retry_plan(..., retry_after=+20h)` → not_before=+20h、check_at=+6h；`allows` 在 +6h/+19h 为 False、+20h 为 True；
    · 依赖故障 not_before=+5 分钟；`retry_after` 已经过去时回落到 +15 分钟；
    · 额度要等 20 小时时，在 +4 小时（当地仍在出门时段）复查：动作仍是 DEFER、安静原因 budget_deferred、**没有任何意图**；
    · 同一时刻把额度调高（remaining_today=3）：复查立刻回到 REQUEST_BRAIN。
  - 指纹（未提交）：heartbeat_policy.py 6D1EA2A79EF886F5、tests/test_web_heartbeat_policy.py 7ED53EFDB72FD5DC。
  - 验证：B 21 项测试通过；arch / dependency 门禁通过。没有联网、没有真实供应商调用、没有碰 18763 与 E2。

三、给 I 的三条阻塞项（按本轮要求重述；措辞即验收口径）
  - BLOCK-1 shadow 成功也要留痕，但不能冒充已执行的生活决定：
    记一次“尝试过了”与下一次复查（建议写 `retry_plan(...).check_at` 到 next_review_at），
    **不得**写 last_decision_at / last_decision_by——那两个字段的含义是“世界真的按这个决定走了”。
    现状反例：shadow 模式每 30 秒调用一次模型，8 分钟把一只宠物当天 12 次额度打满（evidence-runtime.json 的“退避·shadow 成功”）。
  - BLOCK-2 额度耗尽尊重真实恢复时间：`BudgetDenied.retry_after` 实测为下一个 UTC 记账日，现在固定退避 15 分钟。
    建议 `plan = retry_plan(now, policy, retry_after=getattr(denied, "retry_after", None))`，
    把 `plan.check_at` 写进 next_review_at、把 `plan.not_before` 作为禁调下限；复查时只读状态，额度没变就不要发起调用。
  - BLOCK-3 异常也要消耗本轮尝试名额并进入退避：
    · 名额必须在**可能调用模型之前**占用，覆盖“模型已经返回、随后提交出错”这条路径——否则一轮的真实调用次数不再受 limit 约束；
    · 异常路径同样写一次退避，避免同一只宠物每轮抛同样的错、每轮再调一次模型。
    现状反例：`behaviour=raises` 时一轮处理 8 只（上限 5），12 轮每只各 12 次（evidence-fairness.json）。

四、状态分类（互斥，同一项只出现在一处）
  - **仍需修改（当前反例仍然失败）**：BLOCK-1、BLOCK-2、BLOCK-3。
  - **新版待复验（I 已改动、B 尚未按新指纹验证）**：brain_life.py 已漂到 35BA884384D36371（04:01）、runtime_view.py 漂到 6D0D13ABD01D9F1E（04:00），
    均晚于本窗口复验所用的 E62313018F991F60 / 82F439399EB95200。按约定不追着编辑中的文件跑，等 I 宣布可复验批次后，只验受影响部分。
    另有上一轮提出、代码尚未动的 CR-B4（出门次数口径）、CR-B5（共享 RuntimeState 两个字段）、CR-B6（next_check_at 未参与调度 ＋ 事件唤醒）、
    CR-B7（未接 ClockMonitor）、CR-B8（exam 未投影 / 三次分读运行记录 / 会话引用含家人编号）、CR-B11（“留在家”的显式有效期）。
  - **已复验通过（有版本与证据）**：以下均基于 brain_wiring 3CAB668971CD0CA0、brain_life E62313018F991F60、runtime_view 82F439399EB95200、
    life 9C2152CDF6EF70A4，证据在 data/reviews/b-fed3-20260923/：
    · 失败与额度被拒时的公平轮转（8 只 / 上限 5 / 12 轮，全部轮到且单轮不超过 5）；
    · 重建对象与轮询闭包后仍读到持久化退避；
    · 模型失败 5 分钟退避，且不写成生活决定；
    · no_offers 与提案失效（思考中撤权）退避 15 分钟、不出门；
    · “留在家”15 分钟内规则线不绕过，到期后换成 rule 并记录新决定者；
    · 模型不可用时规则后备照常运行，宠物不停摆；
    · 30 秒扫描尊重退避与活动边界（决定间隔内不再调用、活动中不请求大脑）；
    · 顺序两次出发被领域唯一约束挡住（仅此范围，见本条 CORRECTION 一）。
    更早一版（03:39–03:48 代码）已通过、本轮未复验但未受影响的：确定性结算不被模型失败或额度紧张阻塞、活动中回复照常兑现、
    离线 48 小时只补一次决策、时钟回拨后自动恢复、待领养居民照常评估。
五、下一批（合并进行，不重复跑旧探针）
  - 等 I 宣布批次后，只跑：shadow 留痕、额度恢复时间、异常名额与退避这三条对应的探针分支，以及受 brain_life/runtime_view 改动影响的投影断言。
  - 正式并发与恢复验收交 Q；本窗口不再以顺序调用充当并发证明。
运行资源（无则写无）：无。
是否释放（是/否，明确范围）：否。B 的 6 个源文件与 data/reviews/b-fed3-20260923/** 继续持有。

## START ＋ PROGRESS — pkg-B-heartbeat 第五轮（续工单 NEXT-BATCH-EXECUTION-2026-09-23）：三项定向复验

时间（含时区）：2026-09-23 04:47 +08:00
窗口ID / 任务ID / 事件：claude-20260922-234415-fed3 / pkg-B-heartbeat / START ＋ PROGRESS（定向复验，不等 E2）
用户任务（续工单“B：三项定向复验”）：按实际磁盘指纹跑 shadow 成功、额度硬恢复时间、异常上限三个受影响分支；运行前后记指纹；
  交付三项结论 ＋ 当前指纹 ＋ 不可覆盖的输出；明确 I 应保存哪些字段、复查时如何重读额度；未获 RELEASE 不改核心文件。
已读增量：AGENTS.md 的 2026-09-23 运行层修订（纯读接口、两条线、到期结算同事务、三条硬规则、两个模式开关）、
  NEXT-BATCH-EXECUTION-2026-09-23.md、307b 第 11 批 PROGRESS 与 STATUS（04:5X / 05:1X）。
实际目录 / 分支 / HEAD：E:\petsoul-audit\petsoul / codex/petsoul-web-integration / 980feab（未提交）
修改范围：只写 data/reviews/b-fed3-20260923/probe_three_blocks.py 与带时间戳的证据文件，以及本日志。
  **没有修改 I 的任何文件**（brain_life / brain_wiring / runtime_view / life / web_agent_wiring 均未获 RELEASE，本轮也不需要改）。
运行资源：无。临时库、假模型、假时钟；未联网、未调用真实供应商、未碰 18763 与 E2、未新增付费。

一、被测指纹（运行前后各取一次，**跑动期间无变化**）
  - brain_life.py 35BA884384D36371（04:01）、brain_wiring.py 3CAB668971CD0CA0（03:59）、runtime_view.py 6D0D13ABD01D9F1E（04:00）、
    life.py 9C2152CDF6EF70A4（03:54）、web_agent_wiring.py 2DB0EE844865B992（03:54）、budget.py 830B16DDCD8D3C18（00:23）、
    heartbeat_policy.py 6D1EA2A79EF886F5（04:09，B 自己的）。
  - 结论有效范围仅限以上指纹；I 之后改动这些文件，对应结论自动回到“待复验”。

二、三项结论（证据：data/reviews/b-fed3-20260923/evidence-three-blocks-20260922T204607Z.json）
  - **BLOCK-1 shadow 成功仍然不留痕 —— 仍需修改。** 16 轮（每 30 秒一轮，共 8 分钟）里调用了 12 次模型：
    shadow 分支只调用 `_close(pet_id, now)` 关掉决策编号，既不写 next_review_at 也不写任何尝试记录，于是每轮都重来；
    直到把这只宠物当天 12 次额度打满，才因为 budget_denied 触发退避（silence=brain:budget_denied、next_review_at=+21 分钟）。
    也就是说：现在**唯一**能停下 shadow 的机制是把当天额度烧光。
  - **BLOCK-2 额度耗尽仍用固定 15 分钟，没用账本给的恢复时间 —— 仍需修改。**
    实测 `BudgetDenied(reason=limit_reached, retry_after=2026-09-23T00:00:00Z)`（下一个 UTC 记账日），
    `brain_life._back_off` 仍按 `BACKOFF=15 分钟` 记 next_review_at，于是额度真正恢复前会多做约 80 次无用复查与预占尝试。
  - **BLOCK-3 异常仍然绕过本轮名额 —— 仍需修改。** `behaviour=raises`：8 只宠物、上限 5，实测**单轮处理 8 只**，
    12 轮每只各被 consider 12 次。`brain_wiring._round` 的 `except ... continue` 在 `done += 1` 之前，
    异常既不占名额也不写退避；若异常发生在模型返回之后（提交阶段出错），一轮的真实调用次数就不再受 limit 约束。

三、给 I 的接入条件（B 已有实现，不要另造一套退避常量）
  1. 接口：`from app.web_runtime.heartbeat_policy import retry_plan, RetryPlan`
     `retry_plan(now, policy, *, dependency=False, retry_after=None) -> RetryPlan(not_before, check_at)`；`RetryPlan.allows(now)` 判断是否已过禁调下限。
     · `dependency=True` 用于 provider_error / timeout / deadline_passed（5 分钟）；其余用 15 分钟；
     · `retry_after` 传账本或供应商给的硬恢复时刻，**不会被 6 小时上限截短**；
     · `check_at` 是只读复查时刻（≤6 小时），到点只重读事实。
  2. I 需要保存的字段（建议随一支新迁移加在 `web_entity_runtime`，命名由 I 定）：
     · `next_review_at` ← `plan.check_at`（已有列，沿用）；
     · `brain_not_before` ← `plan.not_before`（**新列**：硬性禁调下限，可能远晚于 check_at）；
     · `last_attempt_at` ← 这次尝试的时刻、`last_attempt_outcome` ← 例如 `shadow_proposed` / `failed:provider_error` / `rejected:consent_withdrawn`（**新列**）；
     · **不可覆盖的输出**：`last_decision_at` / `last_decision_by` 只能由“世界真的按这个决定走了”写入（departed / stayed）。
       shadow 成功、失败、被拒一律不得写这两列，也不得把它们当成“想过了”的标记；`silence_reason` 可以写 `brain:<原因>`，但它不是决定。
  3. 复查时怎么重读额度（BLOCK-2 的正确做法）：
     · 到 `check_at` 只做只读判断，不直接发调用：用 A 的 `BudgetLedger.usage("pet:<pet_id>:life_plan")` 与 `usage("provider:llm:life_plan")`
       读回 `used_units / inflight_units`，和当轮上限比对，投影成 `CognitionFacts(availability=EXHAUSTED, remaining_today=剩余, window_resets_at=下一个 UTC 记账日)`；
       心跳据此继续 DEFER，不会给出 REQUEST_BRAIN。
     · 额度确实恢复（`used+inflight < 上限`）时才把 availability 投影回 AVAILABLE；此时心跳自然重新给 REQUEST_BRAIN，不需要清任何“禁调”标记。
     · **复查不等于允许调用**：`plan.allows(now)` 为真只说明过了额度这一项的下限；依赖故障的 5 分钟退避、两次决定的 15 分钟间隔、
       机会有效期各自独立，任何一项没过都不发起调用。额度恢复只解除额度这一项。
     · **真正发起调用前仍要重新校验并重新预占**：用已持久化的 `decision_operation_id` 调 `ledger.reserve(...)`，
       拿到 `BudgetReservation` 才能调模型；再次被拒就按 `retry_plan(now, policy, retry_after=denied.retry_after)` 重新退避。
       `BudgetDenied` 的两种 retry_after 都可直接用：`limit_reached` 给下一个 UTC 记账日、`in_flight` 给那笔预占的到期时刻。
  4. BLOCK-1 的最小改法：shadow 分支在 `_close` 之后按 `plan = retry_plan(now, policy)` 写 `next_review_at=plan.check_at`、
     `last_attempt_at=now`、`last_attempt_outcome="shadow_proposed"`，不写 last_decision_*。
  5. BLOCK-3 的最小改法：把名额占用移到**可能调用模型之前**——`decision.action is REQUEST_BRAIN` 成立、准备调用 `life.consider` 之前就 `done += 1`；
     `except` 分支同样写一次退避（`retry_plan(now, policy, dependency=True)` 即可），避免同一只宠物每轮抛同样的错、每轮再调一次模型。

四、不可覆盖的输出（证据侧）
  - 本轮新证据写成带时间戳的新文件 `evidence-three-blocks-20260922T204607Z.json`；
    上一轮的 `evidence-fairness.json` 与 `evidence-runtime.json`、两个旧探针脚本**原样保留未改**，供对照。
  - 新脚本 `probe_three_blocks.py` 只复用旧探针的分支，不重跑旧套件；JSON 内含运行前后指纹与 `drifted_during_run`（本轮为空）。

五、下一步
  - 等 I 对这三条给出修复批次后，**只跑**这三个分支与受影响的投影断言；其余已通过结论保留原版本与范围，不重复回归。
  - 本轮不轮询刷日志、不重跑旧套件；E2 与 18763 按原安排，不碰。
是否释放（是/否，明确范围）：否。B 的 6 个源文件与 data/reviews/b-fed3-20260923/** 继续持有。

## PROGRESS — pkg-B-heartbeat 第六轮：复验工具 v2（七项）＋ 自检结果；已读 CORE-DELEGATION，待 I RELEASE 后 CLAIM

时间（含时区）：2026-09-23 05:06 +08:00
窗口ID / 任务ID / 事件：claude-20260922-234415-fed3 / pkg-B-heartbeat / PROGRESS（工具完善 ＋ 自检）
修改范围：只写 data/reviews/b-fed3-20260923/probe_three_blocks_v2.py 与带时间戳的证据文件，以及本日志。**没有改 I 的任何文件。**

一、工具 v2 做了什么（v1 的两个探针与三份旧证据原样保留，作为旧版基线）
  - 留家路径改对新入口：撤权注入点从"模型调用时"移到 `RuntimeStore.record_decision_checked` 被调用的那一瞬间（先 bump privacy_epoch 再委托原方法），
    这样验的是 I 新加的同事务版本复核，而不是旧入口。
  - 每项都要确认三件事，任意异常不算通过：注入真的发生（计数器）、拒绝原因是指定的那一个（字符串比对）、
    没有新增行程 / 新增账本流水 / 已执行决策记录（`last_decision_at`、`last_decision_by` 必须仍为空；行程与流水按前后差值比）。
  - 额度项同时读宠物层 `pet:<pet>:life_plan` 与供应商层 `provider:llm:life_plan`，并分别看 used 与 inflight。
  - 异常项区分普通异常与租约失效：`LeaseLost` 单独一项，要求停住旧执行者。
  - 运行前后各记一次 10 个相关文件的指纹，写进结果文件；结果文件名带时间戳，不覆盖旧证据。
  - 工具指纹：probe_three_blocks_v2.py 1D23750EE1B4B6B5。

二、自检结果（**工具自检，不是验收**：I 仍在编辑，未宣布稳定批次；跑动期间指纹无变化）
  被测指纹：brain_life E53FAFBB17C090BA、brain_wiring 81AC4FBAF8F42CA0、runtime_view 1E7EA44C8AF72EE9、life 676AE322DF820415、
  web_agent_wiring 6A00129FFD61BFFA、budget 830B16DDCD8D3C18、lease D29F3BCC7A58F804、uow 2C98BDCB4545B8F8、ticker F3A9C6FC3EEF5511、
  heartbeat_policy 6D1EA2A79EF886F5。证据：evidence-v2-tool-selfcheck-20260922T210453Z.json。
  1. shadow 留痕与轮询不重复：PASS。首轮 1 次调用，之后 8 分钟 0 次新增，过了复查时刻才第 2 次；`silence_reason=brain:shadow`、
     `next_review_at=+15 分钟`、`last_decision_at/by` 仍为空、决策编号已清。
  2. 额度双层与在途：PASS。一次预占后宠物层与供应商层都是 `inflight=1`；结算后都变成 `used=1`；在途 11 笔未结算时第 13 次被拒
     （`limit_reached`，`retry_after=2026-09-23T00:00Z`）——在途确实计入上限。
  3. 复查不突破硬恢复时刻：PASS（判据已更正，见下）。额度耗尽后心跳自己就挡住（`brain_exhausted` ＋ `rule_fallback`，不再给 REQUEST_BRAIN），
     一轮下来 0 次模型调用；强行走一次决策得到 `failed/budget_denied`，退避写的 `next_review_at=+6 小时`，早于账本给的次日 00:00Z；
     到了复查时刻额度仍未恢复，心跳照旧挡住、仍是 0 次调用。
  4. 额度读取失败不算可用：PASS。预占抛异常时 0 次模型调用、写了退避、没有写成决定；另外确认"把失败映射成 None"这条误接法
     在当前实现下也没有导致调用（但契约上 None＝没接账本＝不限额，仍不应这样映射）。
  5. 异常占名额（含模型返回后提交出错）：PASS。假投影单轮最多处理 5 只（上限 5）；真实链路里注入"模型答完之后提交炸了"，
     本轮模型只被调用 1 次、写了退避、没有写成决定。
  6. **租约失效停住旧执行者：FAIL**。注入 `LeaseLost("cognition","taken_over")` 后，`brain_wiring._stall` 把它当普通异常处理：
     写了退避 `next_review_at=2026-09-22T01:05:00Z`、`silence_reason=brain:round_failed`、并且 `close_decision` 清掉了决策编号。
     被接管的旧执行者仍然在写库；应当立刻停住本轮、既不写退避也不关闭操作，交由新任期处理。
  7. 留家撤权注入到新入口：PASS。结论 `rejected`、原因 `versions_changed:privacy_epoch`、注入 1 次、新增行程 0、新增账本流水 0、
     `last_decision_at/by` 仍为空、退避 `brain:versions_changed`。

三、判据更正（属于工具，不是被测方的问题）
  - 第 3 项原先期望"认知线跑一轮会写退避"。实际上新投影已经把额度耗尽映射进 `CognitionFacts`，心跳在更早一步就挡住了，
    认知线根本不会进到决策——这是更好的行为。判据改为：心跳必须给出 `brain_exhausted` 且不是 REQUEST_BRAIN、全程 0 次模型调用，
    并另行强制走一次决策来检查退避时刻早于账本硬恢复时刻。
  - 因此"硬下限要不要单独存一列"这条不再是必须：每次复查都从账本重读（账本本身就是权威），比存一个可能过期的时刻更可靠。
    原 CR 里"新增 brain_not_before 列"的要求撤回；`last_attempt_at / last_attempt_outcome` 仍建议保留（运维要区分"想过没成"与"没想过"）。

四、状态（互斥）
  - **已复验通过（旧版基线，指纹 brain_life 35BA8843… / brain_wiring 3CAB6689… / runtime_view 6D0D13AB…）**：04:46 那轮三项失败的结论，保留原范围不动。
  - **新版待复验**：上面七项自检跑在 I 仍在编辑的版本上（brain_life 已从 1309BC3B 漂到 E53FAFBB），按约定不算验收结论；
    等 I 宣布稳定批次或按 CORE-DELEGATION 把范围交给 B 之后，再以固定指纹复跑并出正式结论。
  - **仍需修改**：第 6 项（租约失效被当普通失败）。这条与 I/Q 的租约验收口径相关，按 CORE-DELEGATION 属于 B 接手后的实现范围
    （`brain_wiring._stall`），交接前不改。

五、CORE-DELEGATION-2026-09-23 的回应
  - 已读。B 将接手的 10 份文件：app/web_agent/{brain_life,brain_wiring,runtime_view,life,ticker}.py、app/web_agent_wiring.py、
    tests/test_web_brain_{life,commit_fence,round_fairness,backoff_semantics}.py。
  - **在 I 的精确 RELEASE 出现之前，这些文件对 B 仍然只读**，本轮没有改动其中任何一个。
  - RELEASE 出现后，B 在本日志追加 CLAIM（接手文件、看到的指纹、本批要改什么、验证命令），然后：先用 v2 工具按固定指纹复验，
    再直接修自己范围内的剩余问题（第 6 项租约失效；以及 shadow / 退避 / 决定权 / 复查间隔的收尾），并交付定向测试。
  - 跨文件需求按新分工直接找对应负责人：留家提交要用 C 维护的 `unit_of_work` 与 `versions_in`，接口按现状固定，需要改签名先一次对齐。
运行资源（无则写无）：无。临时库、假模型、假时钟；未联网、未调用真实供应商、未碰 18763 与 E2、未新增付费。
是否释放（是/否，明确范围）：否。

---

## CLAIM ＋ CORRECTION — pkg-B-runtime-scheduling：接手 I 释放的 12 份文件；更正第六轮的一个小标题

时间（含时区）：2026-09-23 05:19 +08:00
窗口ID / 任务ID / 事件：claude-20260922-234415-fed3 / pkg-B-runtime-scheduling / CLAIM（接手）＋ CORRECTION（本窗口旧条目）
依据：docs/coordination/CORE-DELEGATION-2026-09-23.md；WINDOW-claude-20260922-014933-307b.log.md 的 “RELEASE（精确，逐包）— I 交出运行调度给 B”。
仓库状态：分支 codex/petsoul-web-integration，HEAD 980feab，工作区仍为未提交状态（与前几轮一致，本窗口不做整仓 Git 操作）。

一、CLAIM：接手 12 份文件（I 的交出清单，逐个核对磁盘指纹，12/12 与交接表一致）

| 文件 | 交接指纹 | 05:14 与 05:19 复核 |
|---|---|---|
| app/web_agent/brain_life.py | E53FAFBB17C090BA | 一致 |
| app/web_agent/brain_wiring.py | 81AC4FBAF8F42CA0 | 一致 |
| app/web_agent/runtime_view.py | 1E7EA44C8AF72EE9 | 一致 |
| app/web_agent/life.py | 676AE322DF820415 | 一致 |
| app/web_agent/ticker.py | F3A9C6FC3EEF5511 | 一致 |
| app/web_agent_wiring.py | 6A00129FFD61BFFA | 一致 |
| tests/test_web_brain_life.py | 6183858A0AA29C96 | 一致 |
| tests/test_web_brain_commit_fence.py | 4DCB75750D9EB61E | 一致 |
| tests/test_web_brain_round_fairness.py | 04A0D802521138FC | 一致 |
| tests/test_web_brain_backoff_semantics.py | 18F37B692B5AFDED | 一致 |
| tests/test_web_decision_operation_id.py | C025B7EE68333CB2 | 一致 |
| tests/test_web_suggestion_lifecycle.py | 4C9100DB4B6463CC | 一致 |

  自此这 12 份由 B 写入，I 不再编辑。B 原有范围（app/web_runtime/{clock_policy,state,reasons,wake_events,heartbeat_policy}.py、
  tests/test_web_heartbeat_policy.py、data/reviews/b-fed3-20260923/）继续持有。
  **不接手**：app/web_platform/{lease,uow,budget,runtime_epochs}.py（A/C 持有）、app/web_journey/*（C 持有）、身份与 PATCH /settings（I/identity 持有）。只读引用其接口。

二、第六轮自检的效力更新（不是重跑，是范围认定）
  第六轮七项自检跑在 brain_life E53FAFBB / brain_wiring 81AC4FBA / runtime_view 1E7EA44C / life 676AE322 / web_agent_wiring 6A00129F 上，
  与本次 RELEASE 的交接指纹**逐个相同**。因此那七项不再是“新版待复验”，就是**交接版本的复验结论**：六项 PASS、第 6 项（租约失效）FAIL。
  证据文件不变：data/reviews/b-fed3-20260923/evidence-v2-tool-selfcheck-20260922T210453Z.json。独立验收仍由 Q 做，本条只认定自测范围。

三、CORRECTION（更正本窗口第六轮“四、状态（互斥）”的第一条小标题）
  原文：“**已复验通过（旧版基线，指纹 brain_life 35BA8843… / brain_wiring 3CAB6689… / runtime_view 6D0D13AB…）**：04:46 那轮三项失败的结论，保留原范围不动。”
  标题写错了：那一轮的结论是**三项失败**，不是“已复验通过”。
  更正为：“**旧版基线结论：三项失败（已被新版取代，不再作为当前结论）**，指纹 brain_life 35BA8843… / brain_wiring 3CAB6689… / runtime_view 6D0D13AB…，
  保留原范围仅作历史对照。”其余文字与结论不变。

四、本批要改什么（已确认的缺陷，按优先级）
  1. **租约失效被当普通模型失败**（第一优先，第六轮第 6 项 FAIL）。当前整条链都会吞掉 `LeaseLost`：
     - `RuntimeStore` 的七个写方法直接用 `storage.connect()`，**根本没过 C 的 `lane_fence`**——租约丢了照样写 `web_entity_runtime`；
     - `brain_wiring.brain_round` 两处 `except Exception` ＋ `_stall` 把它变成退避并继续下一只宠物；
     - `brain_life._commit` 的 `except Exception as exc` 把 `journeys.depart` 抛出的 `LeaseLost` 变成 `rejected`＋退避；`_back_off` / `_close` 的兜底也会吞掉它；
     - `life.LifeEngine.run` / `consider`、`HeartbeatShadow.run` 同样按普通异常继续下一只。
     目标行为：租约失效**终止本轮**（由 `ticker._run_jobs` 统一收口记 `lease_lost`），不写退避、不清决策编号、不写生活决定、不继续处理后续宠物；
     已发出的模型调用记录（`decision_operation_id` 与账本预占／结算）保留给合法执行者对账。
  2. **留家提交的租约保护**：`record_decision_checked` 改用 C 的 `unit_of_work`（同事务先过围栏再 `versions_in` 比版本），与 C 的接口对齐，不改 C 的签名。
  3. **收紧额度的证明范围**：补“宠物额度尚有、供应商额度已满”与“`usage` 读取失败”两条定向用例（现有 PASS 只证明了账本双层计量与 `reserve` 抛异常）。
  4. Q-C12：B 只负责消费侧与留家保护（`privacy_epoch` 变化必须挡住旧提案），**不用手工 bump 的 PASS 关闭它**；正式 `PATCH /settings` 入口由 I/identity 修，Q 用真实接口独立复验。

五、验证命令（本批用；TZ=UTC）
  - 定向：`python -B -m unittest discover -s tests -p "test_web_brain_*.py"`、`... -p "test_web_decision_operation_id.py"`、`... -p "test_web_suggestion_lifecycle.py"`、`... -p "test_web_heartbeat_policy.py"`
  - 探针：`python -B data/reviews/b-fed3-20260923/probe_three_blocks_v2.py`（跑前跑后各记一次指纹，结果文件带时间戳不覆盖）
  - 门禁：`python scripts/arch_gate.py`（注意 runtime_view.py 已是 30 个 def/class 的上限，本批不得新增定义）
运行资源（无则写无）：无。临时库、假模型、假时钟；未联网、未调用真实供应商、未碰 18763 与 E2、未新增付费、未部署。
是否释放（是/否，明确范围）：否。本批完成后再按稳定批次说明。

---

## DELIVER — pkg-B-runtime-scheduling 第一批（稳定版本）：租约失效整条传播链 ＋ 留家租约保护 ＋ 额度证明收紧

时间（含时区）：2026-09-23 05:47 +08:00
窗口ID / 任务ID / 事件：claude-20260922-234415-fed3 / pkg-B-runtime-scheduling / DELIVER（LOCAL_VERIFIED，已接入，未提交）
范围：只改 B 已 CLAIM 的文件，外加 B 原有的 heartbeat_policy.py。**没有改 A/C/身份持有的任何文件。**

### 一、修改文件与指纹

| 文件 | 交接指纹 | 本批交付指纹 | 改了什么 |
|---|---|---|---|
| app/web_agent/runtime_view.py | 1E7EA44C8AF72EE9 | 2FBB9055F37B9006 | 七个写方法改走 C 的 `unit_of_work`；shadow 轮不再吞 LeaseLost；额度读取失败改判 UNAVAILABLE |
| app/web_agent/brain_life.py | E53FAFBB17C090BA | 6BE01960E911A9C6 | `_commit` / `_back_off` / `_close` 三处兜底放行 LeaseLost |
| app/web_agent/brain_wiring.py | 81AC4FBAF8F42CA0 | 160859641342BE08 | 轮内两处兜底与 `_stall` 放行 LeaseLost；`budget_facts` 改成两层额度取紧 |
| app/web_agent/life.py | 676AE322DF820415 | 11627C57C8CFAA85 | `run` / `consider` 放行 LeaseLost；`_record`、建议结算与过期改走 `unit_of_work` |
| app/web_agent/ticker.py | F3A9C6FC3EEF5511 | 8D4F7D1E58C8DD09 | 只改注释，把"整体回滚"的范围说准（行为未变，已由新测试证明本就正确） |
| app/web_runtime/heartbeat_policy.py | 6D1EA2A79EF886F5 | 5FB3C07D012D3F93 | 退回规则生活时，依赖故障如实写进安静原因，并允许被"依赖恢复/额度恢复"叫醒 |
| tests/test_web_brain_lease_fence.py | （新增） | 7171A2189BC355A5 | 8 条：真实装配的租约故障注入 |
| tests/test_web_brain_commit_fence.py | 4DCB75750D9EB61E | 22CD0D9556295322 | 修一条继承来的失败断言；新增 2 条（纵深防御、两次检查之间的撤权） |
| tests/test_web_brain_backoff_semantics.py | 18F37B692B5AFDED | A95E46CD574C0FE5 | 新增 3 条：供应商额度、在途、账本读取失败 |
| 其余 4 份测试与 app/web_agent_wiring.py | — | 未改 | web_agent_wiring 仍是 6A00129FFD61BFFA |

被依赖的他人文件（只读引用，本批期间未漂移）：uow 6BCE6996BABD4705、lease D29F3BCC7A58F804、budget 830B16DDCD8D3C18。
**注意**：uow.py 已从上一轮记录的 2C98BDCB 变成 6BCE6996，是 C 在本窗口交接后改的；本批结论绑定 6BCE6996 这一版。

### 二、租约缺陷：查到的真实范围比"只改 _stall"大得多

第六轮只看到 `brain_wiring._stall`。接手后逐层读代码发现**最根本的一层在别处**：
`RuntimeStore` 的七个写方法都直接 `storage.connect()`，**根本没经过 C 的 `lane_fence`**——
也就是说旧进程失去租约之后，照样能往 `web_entity_runtime` 写退避、写决定、清决策编号。
围栏只装在 `unit_of_work` 上，而运行记录从来没走过它。整条链上被吞掉 LeaseLost 的地方：

  1. `RuntimeStore.{record_evaluation,record_decision,record_decision_checked,record_attempt,record_backoff,open_decision,close_decision}`：没有围栏 → 现在全部改走 `unit_of_work`；
  2. `brain_wiring.brain_round` 两处 `except Exception` ＋ `_stall` → 放行，本轮立即终止；
  3. `brain_life._commit`（`depart` 抛出的）、`_back_off`（写退避时才发现的）、`_close` → 放行；
  4. `life.LifeEngine.run` / `consider`、`HeartbeatShadow.run` → 放行；
  5. `ticker._run_jobs` 本来就是对的（捕获后结束本轮），本批只把注释改准。

交付后的行为：租约失效 → 终止本轮 → 不写退避、不清决策编号、不写生活决定、不出发、不处理后面的宠物与后面的任务。
**已发出的模型调用记录保留**：额度账本不走租约围栏，预占与结算照常落库；决策编号留在 `web_entity_runtime` 里。
合法执行者下一轮用 `open_decision` 复用同一个编号 → `reserve` 返回那条已结算的预占 → 决策层 `_permit_problem`
判成 `reservation_not_active` → 不会二次调用模型。这条恢复路径是读代码确认的，已由第 2 条证据间接覆盖（预占状态 settled）。

### 三、修复前后的定向证据

跑前跑后指纹一致，无漂移；全部 TZ=UTC、假模型、临时库，零付费调用。

1. **新增 tests/test_web_brain_lease_fence.py（8 条，全绿）**。注入方式是真实的：直接改 `web_worker_leases` 的持有者，
   再让真实装配（WorldTicker → lane_fence → 认知线 → BrainLife → C 的 unit_of_work）跑下去。
   - 模型答完之后被接管：模型确实调用了 1 次；本轮只处理了 1 只宠物（不再往下排）；本轮后面的任务 0 次执行；
     没有行程、`next_review_at` 为空、`last_decision_at/by` 为空、`silence_reason` 不是 `brain:round_failed`、**决策编号仍在**。
   - 已发出的调用仍在账本上：按保留下来的编号查到预占，状态 `settled`。
   - **对照组**：普通模型失败仍然照旧——两只宠物都被处理、后面的任务照常跑、写了退避、清了编号。
   - 留家提交：`{"choice":"continue"}` 提案在被接管后同样写不进去。
   - 六个运行写入口在围栏下逐个必须抛 LeaseLost 且什么都没写（含 `open_decision` 不得开新编号）。
   - 对照组：租约还在自己手上时，同事务的版本复核照旧挡下旧提案（围栏没有把版本检查盖掉）。
   - 世界线规则生活被接管后 `run()` 直接抛出，不再跳到下一只；TA 没有被送出门。
   - `WorldTicker` 收口：三个任务里第二个抛 LeaseLost，第三个不再跑。
2. **v2 探针七项复跑（修复后）**：7/7 PASS（修复前 6 PASS ＋ 第 6 项 FAIL）。
   证据 `data/reviews/b-fed3-20260923/evidence-v2-after-lease-fix-20260922T214122Z.json`；
   第 6 项现在的观察值：注入 1 次、可感知的抛出 1 次、`next_review_at` 为 None、`silence_reason` 为 None、决策编号仍在、`last_decision_at` 为 None。
   旧证据一律保留，未覆盖。

### 四、额度：收紧证明范围之后补上的两个真实缺口

按要求不再用"账本双层计量 PASS"和"reserve 抛异常 PASS"充当已验证，重新查了投影侧，**两条都确实是缺口**：

1. **供应商额度已满、宠物额度尚有**：`budget_facts` 原来只读 `pet:<id>:life_plan`，供应商那层根本没看。
   已改成两层都读（都按"已用 ＋ 在途"），取更紧的一层。新用例：把 `provider:llm:life_plan` 占满之后，
   真实装配的投影返回剩余 0、心跳给出 APPLY_RULE、这一轮 0 次模型调用（模型替身一旦被调用就抛错）。
2. **账本读取失败被当成可用**：`_cognition` 原来在 `budget_facts` 抛异常时返回 `AVAILABLE`。
   已改成 `UNAVAILABLE ＋ retry_after = now + 5 分钟`。新用例：`BudgetLedger.usage` 抛错时，
   投影为 UNAVAILABLE、给出再看的时刻、心跳不是 REQUEST_BRAIN、原因码含 `brain_unavailable`、
   安静原因是 `dependency_unavailable`（运维能和"正常安静"分开）、0 次模型调用。
3. **在途计量**：补了真实预占不结算的用例（按配置的每宠上限预占满），投影剩余为 0、心跳不再说"去想一想"。
4. 连带改动：心跳退回规则生活时，若原因是依赖故障，安静原因如实写出，并把等待事件放宽到含"依赖恢复 / 额度恢复"，
   额度一恢复就能被叫醒，不必干等固定复查。21 条心跳策略测试全绿，未改其他分支语义。

### 五、Q-C12：**不关闭**，但交一条对 Q 有用的观察

按分工，正式 `PATCH /settings` 入口归 I/identity，Q 用真实接口独立复验。B 只做了消费侧与留家保护，并补了两条真实接口用例：
  - 观察（绑定本批指纹）：在当前代码上，真实 `PATCH /settings {"model_replies": false}` **确实把 privacy_epoch 从 1 推进到 2**，
    旧提案被决策层的授权围栏拦下（`revoked`），没有出发、没有写成生活决定。这与 Q-C12 的描述不一致，
    可能是 I 已经修过、也可能 Q 的场景不同（例如多成员家庭、家庭级开关、或别的入口）。**请 Q 按自己的场景复验，不要用这条结论关闭 Q-C12。**
  - 顺带修好一条**继承来的失败**：`test_web_brain_commit_fence.py` 里"撤权"那条在交接版本上就是红的
    （断言只允许 `consent_withdrawn`，实际是更早一步的 `revoked`）。已在 I 的交接指纹上复现确认是继承缺陷，不是本批引入。
    现在改成：两道围栏都算数，但必须先证明撤权真的发生（privacy_epoch 前进了一代、开关确实关了），且不能写成生活决定。
  - 另补两条：`_commit` 的 `consent_withdrawn` 分支单独覆盖（撤权但版本没动的纵深防御）；
    以及**两次检查之间**用真实接口撤权，必须由 `record_decision_checked` 在同一事务里挡下（`versions_changed:privacy_epoch`，未写决定）。

### 六、受影响回归（本批实际跑过的，全绿）

  - 定向：test_web_brain_*（43）、test_web_decision_operation_id（4）、test_web_suggestion_lifecycle（7）、test_web_heartbeat_policy（21）。
  - 受影响面：test_web_agent_life（13）、test_web_runtime_shadow（8）、test_web_lease_commit_fence（9）、test_web_commit_boundary（10）、
    test_web_consent_epoch（9）、test_web_consent_concurrency（3）、test_web_agent（15）、test_web_decision_chain（12）、test_web_intent（9）、
    test_world_guardrails（17）、test_web_reception_rules（5）。合计 185 条通过，0 失败。
  - 门禁：`python scripts/arch_gate.py` 通过（runtime_view.py 仍是 30 个 def/class 的上限，本批没有新增定义）。
  - **整仓回归交 I，独立验收交 Q**；本批没有跑全套旧测试，也没有重跑历史探针。

### 七、剩余问题与跨包请求

  - **给 C（web_journey）**：同一类缺陷还在世界线的下游投递里。`app/web_journey/settlement.py:133` 的 `_deliver_one`
    用 `except Exception` 兜住 `sink.on_world_event(...)`，而 `guides.py` 这类 sink 内部是走 `unit_of_work` 的；
    租约失效会被记成"这个下游失败了"——`self.outbox.failed(item, …)` 写了一条失败、白耗一次重试，然后继续投递下一条。
    `app/web_journey/illustrations.py:395` 的逐任务兜底同理。建议与本批一致：先 `except LeaseLost: raise`。B 不改 C 的文件。
  - **残留（已知、已记）**：`depart` 已提交、随后 `record_decision` 才撞上租约失效时，行程成立但 `last_decision_at` 没写上。
    行程本身是权威记录，新任期读到"已在外面"会跳过，不会重复出门；但运行记录会缺一行决定人。按事务边界这是可接受的，列为已知残留。
  - **未做（I 移交时列出的收尾项，下一批继续）**：CR-B4（出门次数按 24 小时还是按当地自然日）、CR-B5（RuntimeState 字段补齐）、
    CR-B6（next_check_at 与事件唤醒）、CR-B7（ClockMonitor 接入）、CR-B8、CR-B11；游标仍在进程内；shadow 仍每次都真的调用模型。
  - **不属于 B**：Q-C12 的正式入口（I/identity）、C 的 UoW 与旅程事务、A 的账本与租约原语。

运行资源（无则写无）：无。临时库、假模型、假时钟；未联网、未调用真实供应商、未碰 18763 与 E2、未新增付费、未部署、未做整仓 Git 操作。
是否释放（是/否，明确范围）：否。12 份文件继续由 B 持有，下一批做调度收尾。

---

## DELIVER — pkg-B 第二批（稳定小批 ①）：CR-Q14 / Q-C13 决策恢复；② CR-Q15 / CR-A8 生图正式接线

时间（含时区）：2026-09-23 06:42 +08:00
窗口ID / 任务ID / 事件：claude-20260922-234415-fed3 / pkg-B-runtime-scheduling / DELIVER（两个小批，LOCAL_VERIFIED，已接入，未提交）
范围：只改 B 已 CLAIM 的文件 ＋ B 原有的 heartbeat_policy.py。没有改 A/C/Q/身份持有的任何文件。

### 小批 ①：CR-Q14 / Q-C13 —— 没结清的操作不许换编号重发

**根因（两处，不只一处）**
  1. `BrainLife._back_off()` 无条件 `_close()`。`budget_denied/in_flight` 恰恰说明同一次操作还在途，
     清掉编号下一轮就开新编号、再预占、再发一次。`brain_wiring._stall()` 也一样无条件清编号。
  2. **按时间轮换编号**：`open_decision(stale_after=15 分钟)` 让"卡住太久"的编号换新。
     按 2026-09-23 06:20 收紧的口径，这条本身就是错的——**时间经过、预占过期都不能单独成为换编号重发的依据**。
  另外查出一个 Q 报告里没有、但同源的缺陷：`reserve` 把**上一次**的记录原样交回来时（settled/unknown/expired），
  旧代码仍会拿这一轮的 `settle_outcome()` 去结算它。对 `expired` 结算 `not_sent` 会**整笔退回**——
  把一笔可能已经计费的调用当成"没发出"退款了。

**改法**
  - `brain_life._resolved(permit, settled)`：只有**明确结局**才算结清。
    `succeeded / failed`（正常收场）与 `not_sent / released`（确定没离开本机）算；
    `in_flight / reserved / unknown / expired` 都不算，**过多久都不算**。
  - `_close(pet_id, now, operation_id)` 只在结清时调用，且**只清自己这一轮的那个编号**；
    `RuntimeStore.close_decision(..., operation_id=...)` 加了 `AND decision_operation_id = ?`。
  - `open_decision` 的 `stale_after` 改成可选，生活决策**不再传**：库里还存着编号就一直沿用。
  - `consider` 只结算 `permit.status == "reserved"` 的那一笔（这一轮真的占上的），不再动上一次的记录。
  - `_stall` 只写退避，不碰编号。

**正向对照的范围（按用户 06:35 的要求写清，不外推）**
  - `not_sent / released` 这一条只证明"**确定未发送之后的重试可以继续**"，**不是说新决策只能由这两种状态开启**；
  - `succeeded / failed`（一次决策正常做完）同样是明确结局：下一次本来就是**真正的新一次决策**，
    照常换新编号、照常允许再调用。收紧重发**没有**把常态路径挡住，这一条单独有用例。

**定向证据**（TZ=UTC，真实 BrainLife ＋ 真实额度账本，只换远端模型；零付费调用）
  - 新增 `tests/test_web_brain_decision_recovery.py`（10 条，全绿）：同一分钟重试 / 跨整分钟 / 连续 8 轮 30 秒 /
    对象重建 —— 都沿用同一编号、模型只被调用 1 次、始终只有 1 笔预占、编号不被清；
    **过了 10/20/60/1440 分钟仍然不换编号、不重发**，那笔只能停在 `expired` 不能退回成 `released`；
    轮内异常兜底不清编号；两条正向对照（确定未发送后重试可继续；正常做完后开启真正的新一次决策）。
  - 更正 `tests/test_web_decision_operation_id.py` 里一条**与新口径相反**的旧断言
    （原 `test_an_abandoned_decision_is_not_reused_forever` 断言"卡住太久要换新编号"）：
    改成 `test_time_alone_never_rotates_an_unresolved_decision`，并补 `test_a_decision_proven_unsent_is_released_and_can_start_over`。
    这是按新口径的更正，不是为了让测试变绿而放宽。
  - 同理更正 `tests/test_web_brain_lease_fence.py` 里"普通失败要清掉编号"那一句：模型抛异常时账本判 `unknown`，
    结果未明本来就该留着编号。

### 小批 ②：CR-Q15 / CR-A8 —— `on_unknown` / `on_retrying` 正式接线

  `app/web_agent_wiring.py` 原来只接 `on_ready` / `on_failed`，另两个回调始终是 None，
  所以"结果未确认"和"重画已排上队"下发不到消费者（Q-C10 / Q-C11 的同一个根因）。
  - `on_image_failed` 增加 `outcome` 参数并透传给三个消费者（communicator / collection / guides）；
    `illustrations.on_unknown` 接成 `outcome="unknown"`；
  - 新增 `on_image_retrying`，把 `illustration_retry_started` / `image_retry_started` 下发给同样三个消费者；
  - **同事务连接一路传下去**：两个回调都接 `(task_id, conn)`，与服务侧 `retry()` 的 `unit_of_work`、
    `_finish_failed()` 的领取围栏事务在同一个事务里提交。没有改 A 的 `illustrations.py`。

### 版本与证据

| 文件 | 上一批指纹 | 本批稳定指纹 |
|---|---|---|
| app/web_agent/brain_life.py | 6BE01960E911A9C6 | **6CD250DE877F1DFE** |
| app/web_agent/runtime_view.py | 2FBB9055F37B9006 | **209DA22CE2F69638** |
| app/web_agent/brain_wiring.py | 160859641342BE08 | **7CD84C0E22F45228** |
| app/web_agent_wiring.py | 6A00129FFD61BFFA | **454C60BA271D820E** |
| tests/test_web_brain_decision_recovery.py | （新增） | 5A8C7C91399A711E |
| tests/test_web_decision_operation_id.py | C025B7EE68333CB2 | 2F3B57D319AF9A4F |
| tests/test_web_brain_lease_fence.py | 7171A2189BC355A5 | E7AACB1E5500D78A |
| life.py / ticker.py / heartbeat_policy.py / 另两份测试 | 未再改动 | 11627C57C8CFAA85 / 8D4F7D1E58C8DD09 / 5FB3C07D012D3F93 / 22CD0D9556295322 / A95E46CD574C0FE5 |

**合同结果（作者自测，Q 的独立验收仍以 Q 自己的运行为准）**
  绑定合同指纹：boundary `EB18BBEB0B322D24`、recovery `276A22B1C59C561C`、media `F5688E7CEA04F505`、harness `F1052714EB4EDD46`。
  `--only c13 c19 c10 c11` **连跑 3 次全 PASS，跑前跑后合同指纹一致**（含 Q-C19 的真·跨进程 `os._exit` 重启）。
  全合同一轮 **27 PASS / 0 FAIL**——但那一轮期间 Q 仍在编辑合同文件（boundary 9E1F4FD3→EB18BBEB、
  recovery E2E8F3DF→276A22B1、media C6F0896B→F5688E7C），所以**全合同那一轮跨了版本，不作为结论**；
  作结论的只有上面那组绑定指纹的定向复跑。

**受影响回归（全绿）**：test_web_brain_*（53）、test_web_decision_operation_id（5）、test_web_heartbeat_policy（21）、
  test_web_suggestion_lifecycle（7）、test_web_agent_life（13）、test_web_runtime_shadow（8）、test_web_decision_chain（12）、
  test_web_illustration_api_closure（6）、test_web_illustration_settlement（10）、test_web_illustration_recovery（6）、
  test_web_collection_consume（6）、test_guide（6）、test_web_commit_boundary（10）、test_web_consent_epoch（9）＝ 172 条，0 失败。
  门禁 `scripts/arch_gate.py` 通过。

### 状态更正（按用户 06:35）

  1. **Q-C12 与 Q-C12b 已由 Q 分别报告通过**（C12b 绑定 identity/service.py 986A6904FBBC1742）。
     本窗口上一批"Q-C12 不关闭"的说法**作废**；也不把它外推成"生图撤权链整体通过"。
  2. **`illustrations.py:395` 的逐任务兜底吞 LeaseLost —— 改交 A**（该文件归 A），不是 C。上一批写错了接收人。
  3. **`settlement.py:133` 先核对归属再提**：该文件当前不在 B 手上，本窗口只读核对到
     `_deliver_one` 的 `except Exception` 会兜住走 `unit_of_work` 的 sink（`guides.py` 是其中之一）。
     **不并给 C**，由 I 按实际持有人分派后我再提供复现要点。

### 剩余与边界

  - 本批不含调度收尾（CR-B4/B5/B6/B7/B8/B11），按约定排在两项阻塞之后。
  - `app/web_agent_wiring.py` 与 `app/web_agent/runtime_view.py` 现在都是 **30 个 def/class 的门禁上限**，
    下一个动这两个文件的人需要先拆分。
  - `unknown / expired` 的编号要解开，得由账本那边**查实后再结算一次**（`not_sent` → 整笔退回）。
    这是设计内的人工/对账路径，不是计时器；目前没有自动对账任务，列为已知缺口。
运行资源（无则写无）：无。临时库、假模型、假时钟、假供应商；未联网、未调用真实供应商、未碰 18763 与 E2、未新增付费、未部署、未做整仓 Git 操作。
是否释放（是/否，明确范围）：否。**本批为稳定批次，请 Q 按上表指纹复验 C13 / C19 与正式装配的 C10 / C11。**

---

## DELIVER — pkg-B 第三批（稳定小批）：调度收尾 CR-B4 / CR-B5 / CR-B6

时间（含时区）：2026-09-23 07:20 +08:00
窗口ID / 任务ID / 事件：claude-20260922-234415-fed3 / pkg-B-runtime-scheduling / DELIVER（LOCAL_VERIFIED，已接入，未提交）
范围：只改 B 已 CLAIM 的文件。没有改 A/C/Q/身份持有的任何文件。

### 零、先拆文件（上一批留的前置条件）

`runtime_view.py` 上一批已经顶到门禁上限（30 个 def/class），B4/B5/B6 都落在这个文件里，所以先拆：
  - 新增 `app/web_agent/runtime_store.py`，把 `RuntimeStore` 整类搬过去（运行记录读写 vs 只读投影 vs shadow，本来就是三件事）；
  - `runtime_view.py` 里 `from .runtime_store import RuntimeStore` 再导出，**原导入路径全部不变**
    （Q 的 `runtime_contract_boundary.py` 就是 `from app.web_agent.runtime_view import RuntimeStore`，不受影响）；
  - 拆完：runtime_view 267 行 / 20 个定义，runtime_store 173 行 / 13 个定义。依赖方向门禁通过。

### 一、CR-B4 出门次数口径（跨午夜两处打架）

  `RuntimeProjector._outings_today` 按"最近 24 小时"数，`life.LifeEngine.consider` 按"当地当天 0 点以来"数。
  跨午夜就会出现心跳说"今天够了、不再规划"、规则生活却还允许出门。
  - 改成按 **TA 所在地的自然日**（用 B 自己的 `resolve_zone` / `local_wall`），和规则生活同一个口径；
  - `facts(pet_id, now, *, timezone=...)`：时区由 `state()` 算好传下来，不重复查；
  - 时区取不到时退回最近 24 小时——那是**偏保守**的一侧（算得多、更早说够了），宁可少出门也不要两处给相反结论。

### 二、CR-B5 共享 RuntimeState 的两个字段

  `pending_commitment_deadline` 恒为 None、`cognition_status` 恒为 "quiet"，
  于是任何按共享类型读状态的消费者都看不到"有承诺待兑现 / 大脑不可用"，
  `facts_from_state`（最小模式）更是把承诺整个丢掉。
  - `state()` 改用 B 自己的 `commitment_deadline(facts)` 与 `cognition_status(facts, now)` 填这两个字段，
    保证和心跳看到的是同一份事实；
  - `state(pet_id, now, *, facts=...)`：调用方已经算好就传进来，不重复算。

### 三、CR-B6 按到期取宠物 ＋ 唤醒来源

  新增 `RuntimeStore.due_pets(now, *, watchdog, roster, limit)`：一条 SQL 选出"现在该看谁"，并给出**为什么该看**。
  五个唤醒来源（任缺一个都会漏调度，所以宁可多叫醒、不可漏）：
    1. `due` —— 到了上一次心跳自己排的 `next_check_at`。可预测的边界（活动结束、承诺到期、额度重置、看门狗上限）
       心跳算 next_check_at 时已经算进去了，这一条把它们全兜住；
    2. `never` —— 还没评估过；
    3. `changed` —— 运行记录这一行在上次评估之后被动过（撤权、改 DNA、换成员、活动变化、退避、决定）。
       按 `updated_at > last_evaluated_at` 判，会**多叫醒**（写一次退避也算），这是安全的一侧；
    4. `reply` —— 有到点还没兑现的回复承诺（上一轮排好下次检查**之后**才进来的消息）；
    5. `suggestion` —— 上次评估之后家人提了新建议。
  再加**看门狗兜底**：超过 `max_check_interval`（6 小时）没被评估的一律叫醒。`maintenance=1` 的排除在外。

  **实现中自己踩到并修掉的一个真缺陷**：运行记录是"第一次写才建行"的，新来的宠物在 `web_entity_runtime` 里
  根本没有行——光查这张表会把它们整个看不见、**永远排不上**。所以 `due_pets` 接 `roster`（调用方知道的全部宠物），
  把"还没有行"的一并算成 `never`。这条是"不漏调度"用例先红出来的，不是事后想到的。

  **接线范围（按世界运行方案的前置条件，没有越过）**：
  - 已接：`HeartbeatShadow` 接上 `due_of`（`web_agent_wiring.py`），对照线这一轮只看该看的；没接时退回全量扫描，行为不变。
  - **未接**：世界线的规则生活与认知线的 `brain_round` 仍是全量扫描。真实调度切过来要等 shadow 对照**跑满一个完整昼夜**，
    这是方案里写明的前置条件，本批不擅自切换。
  - 顺带修掉 CR-B6 里点名的"每轮每只被评估两遍"：新增 `RuntimeProjector.snapshot()` 一次算出 state ＋ facts
    （`_where` 只查一次），`brain_round` 与 shadow 都改用它。

### 四、证据

  新增 `tests/test_web_runtime_projection.py`（17 条，全绿）：
  - CR-B4：跨午夜 0 次、午夜前 2 次、**三个时刻逐一比对"心跳数的"与"规则生活数的"必须相等**（Q 的验收口径）、未知时区保守退回；
  - CR-B5：承诺到期时刻等于最早那件承诺、最小模式仍留着承诺、`disabled / quiet / backoff` 三态分得开、传入 facts 与自算一致；
  - CR-B6：`never / due / watchdog / changed / reply / suggestion` 六种选中理由各一条，`maintenance` 排除一条，
    以及**不漏调度**：按到期取连跑 12 小时（每 30 秒一轮，跨过两个看门狗窗口），最长间隔不超过看门狗窗口，
    且总评估次数不到全量扫描的四分之一。
  - **反例确认**（不是"写完就绿"）：把 `_outings_today` 与那两个字段临时回退成修复前的实现，同一批用例 **6 条变红**，恢复后全绿。

| 文件 | 上一批指纹 | 本批指纹 |
|---|---|---|
| app/web_agent/runtime_view.py | 209DA22CE2F69638 | **4893EB5C07008307** |
| app/web_agent/runtime_store.py | （新拆出） | **3DBF12288E3621AA** |
| app/web_agent/brain_wiring.py | 7CD84C0E22F45228 | **8C7651D88CD75C64** |
| app/web_agent_wiring.py | 454C60BA271D820E | **471529A2CF9CF646** |
| tests/test_web_runtime_projection.py | （新增） | 548BC809DD61583F |
| brain_life / life / ticker / heartbeat_policy | 未改动 | 6CD250DE877F1DFE / 11627C57C8CFAA85 / 8D4F7D1E58C8DD09 / 5FB3C07D012D3F93 |

  **回归（全绿）**：test_web_runtime_projection（17）、test_web_brain_*（53）、test_web_runtime_shadow（8）、
  test_web_heartbeat_policy（21）、test_web_agent_life（13）、test_web_decision_operation_id（5）、
  test_web_suggestion_lifecycle（7）、test_web_decision_chain（12）、test_web_agent（15）＝ 151 条，0 失败。
  门禁：arch_gate 与 dependency_gate 均通过。
  **合同复跑（作者自测，Q 的独立验收仍以 Q 自己的运行为准）**：绑定 boundary `CC296934D8AD72F6`、
  recovery `276A22B1C59C561C`、media `F5688E7CEA04F505` —— C13 / C19 / C10 / C11 全 PASS；
  调度三条 C16 / C17 / C18 也全 PASS（本批动了调度选择，专门复跑确认没碰坏）。
  注：boundary 的指纹这一轮又从 EB18BBEB 变成 CC296934，Q 仍在编辑，结论绑定的是上面这一组。

### 五、剩余与跨包

  - **CR-B6 未完成的那一半**：真实调度（规则生活、brain_round）切到按到期取，要等 shadow 对照跑满一个完整昼夜再切。
    本批只把能力与来源做齐并接到对照线上，**不宣称 CR-B6 已关闭**。
  - `changed` 这一路按 `updated_at > last_evaluated_at` 判，会把"写了一次退避"也当成变化而多叫醒一次。
    要精确区分得在运行记录里存一份"上次评估时的版本代数"（`web_entity_runtime` 加列＝迁移，归 A/C）。
    当前这样是安全的一侧，先不提迁移请求，等真实调度切过去、确认多叫醒的量之后再提。
  - `web_agent_wiring.py` 仍是 30 个定义的门禁上限（本批没有新增定义）；下一个动它的人需要先拆。
  - 未做：CR-B7（ClockMonitor 接入）、CR-B8、CR-B11（"留在家"的显式有效期）。
  - 上一批留下的两条跨包仍然有效：`illustrations.py:395` 逐任务兜底吞 LeaseLost → **A**；
    `settlement.py:133` → 等 I 按实际持有人分派后我再给复现要点。
运行资源（无则写无）：无。临时库、假模型、假时钟、假供应商；未联网、未调用真实供应商、未碰 18763 与 E2、未新增付费、未部署、未做整仓 Git 操作。
是否释放（是/否，明确范围）：否。本批为稳定批次，可交 Q 复验 CR-B4 / CR-B5 的验收口径与 CR-B6 的"唤醒来源齐全、不漏调度"。

---

## DELIVER — pkg-B 第四批（稳定增量）：修两处定向问题 ＋ 收紧两处证明范围；附给 I 的冻结范围

时间（含时区）：2026-09-23 07:52 +08:00（原写 07:58 是笔误，已更正；当时实际时刻 07:52）
窗口ID / 任务ID / 事件：claude-20260922-234415-fed3 / pkg-B-runtime-scheduling / DELIVER（LOCAL_VERIFIED，已接入，未提交）
范围：只改 B 已 CLAIM 的文件。上一批的实现继续记为"已交付，待独立复核"。

### 一、旧评估抹掉新变化的唤醒信号（已修）

**确认复现**，而且比报告的更宽：不只 `record_evaluation`，运行记录的**七个写方法**都会把 `updated_at`
覆盖成自己那一轮的时刻，慢一轮的写入都能把别人刚立起的"这一行被动过"抹掉。

修法两条，缺一不可：
  1. `record_evaluation(..., expected=versions)`：写之前在**同一个事务**里再比一次语义版本，
     变了就**整个不写**并返回变了的字段名。旧评估的结论是用旧事实算的，
     让它写回去等于"确认消费了它没读到的新版本"。`HeartbeatShadow` 传 `state.versions`，
     作废的条数记进 `ShadowResult.stale`（不再混进 `unavailable`）。
  2. `updated_at` 一律 `max(updated_at, 本次时刻)`——**七个写方法全部**。它是唤醒标记，只能往前。

  这一条**不再**按"安全地多唤醒一次"留到正式切换后观察：它是**漏唤醒**，方向相反。

### 二、新宠物绕过 limit（已修）

**确认复现**：`limit=2` ＋ 五只没有运行记录的新宠物，旧实现返回五只——`fresh` 是在 SQL 的 LIMIT 之后直接拼上去的。

修法：SQL 不再截断（候选只取两列，本来就便宜），新旧候选**合并成一个列表**，
按 `pet_id` 排序后**从游标 `after` 之后取、到尾部绕回开头**，再统一截断到 `limit`——
和认知线的轮转是同一个办法（Q-C16）。`HeartbeatShadow` 持有游标，每轮停在最后一只身上。
简单截断会造成另一组饥饿，所以专门补了两条：五只新宠物三轮内全部轮到；
一堆新宠物**不能**把"已经到点"的老宠物一直挤掉。

  顺带修好一处**上一批没生效的改动**：`reply` 那一路本来想用 `claimed_until` 判断领取是否过期，
  但那次 `str.replace` 因为尾随空格没匹配上、静默没生效（我当时没加 assert）。现在已按 `OPEN_PENDING` 的口径改对。

### 三、收紧两处证明范围（按要求）

  - **12 小时用例是假时钟的隔离模拟**（单只宠物、无并发命令、无真实负载），只证明"选谁"这套规则自身不会饿死宠物；
    **不能替代完整自然昼夜的 shadow 对照**。用例里"评估次数远少于扫描轮数"也只是**评估次数之比**，
    **不等于**整轮数据库成本同比下降——每一轮仍要跑一次候选查询。两句都已写进用例文档。
  - **未知时区下的 24 小时计数是退化口径，不是"当地自然日"**，不能拿来当两处口径一致的证明。
    用例已改名为 `test_an_unknown_timezone_is_a_degraded_count_not_a_local_day`，
    并**额外断言心跳仍然给出时区不可用状态**（`ReasonCode.TIMEZONE_UNKNOWN`），不套用任何默认城市。

### 四、证据

  - 反例确认：把这两处临时回退成修复前实现，**4 条用例变红**
    （`stale_evaluation` / `same_interleaving_through_the_real_shadow_round` / `new_pets_do_not_bypass_the_round_limit` /
    `every_new_pet_gets_its_turn_across_rounds`），恢复后全绿。
  - 用例按门禁拆成两份（原文件 37 个定义已超上限）：
    `test_web_runtime_projection.py`（8 条，CR-B4/B5）、`test_web_runtime_due_selection.py`（15 条，CR-B6 ＋ 本批两项）。
  - 交错反例走了两层：store 层精确一条（注入点在 `record_evaluation` 之前，断言返回 `["privacy_epoch"]`），
    真实 shadow 一轮一条（注入点在 `record_evaluation` 被调用的那一瞬间，断言 `stale == 1`、
    `last_evaluated_at` 与 `next_check_at` 都没被旧结论改写、下一轮仍排得到它）。另有一条"没有交错就照常写下"的对照组。

| 文件 | 上一批指纹 | 本批指纹 |
|---|---|---|
| app/web_agent/runtime_store.py | 3DBF12288E3621AA | **410695F2201F288F** |
| app/web_agent/runtime_view.py | 4893EB5C07008307 | **1BA2B4F22F438490** |
| app/web_agent_wiring.py | 471529A2CF9CF646 | **DBBBDF6768355ED0** |
| tests/test_web_runtime_projection.py | 548BC809DD61583F | FA082842C5FC5734 |
| tests/test_web_runtime_due_selection.py | （本批拆出） | 1AF8E4136328C912 |
| brain_life / brain_wiring / life / ticker / heartbeat_policy | 未改动 | 6CD250DE877F1DFE / 8C7651D88CD75C64 / 11627C57C8CFAA85 / 8D4F7D1E58C8DD09 / 5FB3C07D012D3F93 |

  **回归（全绿）**：本批两份用例（8 ＋ 15）、test_web_brain_*（53）、test_web_runtime_shadow（8）、
  test_web_heartbeat_policy（21）、test_web_agent_life（13）、test_web_decision_operation_id（5）、
  test_web_suggestion_lifecycle（7）、test_web_decision_chain（12）、test_web_agent（15）、
  test_web_commit_boundary（10）、test_web_consent_epoch（9）＝ 176 条，0 失败。
  门禁：arch_gate、dependency_gate 均通过。
  **合同复跑（作者自测，Q 的独立验收仍以 Q 自己的运行为准）**：绑定 boundary `CC296934D8AD72F6`、
  recovery `276A22B1C59C561C`、media `F5688E7CEA04F505`、schedule `E904F0930D6F4347` ——
  C10 / C11 / C12 / C13 / C14 / C16 / C17 / C18 / C19 **九条全 PASS**。

### 五、给 I 的冻结范围与时段

  **请冻结（B 持有，本批之后不再改动，直到 Q 复验出结论）**：
    - `app/web_agent/runtime_store.py` 410695F2201F288F
    - `app/web_agent/runtime_view.py` 1BA2B4F22F438490
    - `app/web_agent/brain_life.py` 6CD250DE877F1DFE
    - `app/web_agent/brain_wiring.py` 8C7651D88CD75C64
    - `app/web_agent_wiring.py` DBBBDF6768355ED0
    - `app/web_agent/life.py` 11627C57C8CFAA85、`app/web_agent/ticker.py` 8D4F7D1E58C8DD09
    - `app/web_runtime/heartbeat_policy.py` 5FB3C07D012D3F93 及 B 的其余 web_runtime 模块
    - 上述两份新用例
  **时段**：自 2026-09-23 08:00 +08:00 起冻结。B 下一批（CR-B7/B8/B11）**不动这些文件**，
  等 Q 出结论或有定向缺陷再解冻；期间只在本日志追加，不改代码。
  **不需要全仓停写**：这只是 B 自己范围的冻结，其他窗口照常。

  **昼夜对照的边界（按用户口径写清）**：完整自然昼夜的 shadow 对照由 I 安排候选环境，
  它**只阻塞 CR-B6 的正式调度切换**，**不阻塞全仓离线回归**，也不该让其他窗口一起等一天。
  CR-B6 继续记为 **"shadow 已接，正式切换未完成"**。

### 六、剩余

  - CR-B7（ClockMonitor 接入）、CR-B8、CR-B11（"留在家"的显式有效期）放下一批。
  - `changed` 仍按 `updated_at > last_evaluated_at` 判，会把"写了一次退避"也算成变化而多叫醒一次
    （这是安全的一侧；真正的漏唤醒已由本批修掉）。要精确区分得在运行记录里存"上次评估时的版本代数"＝加列＝迁移，归 A/C，
    等正式切换后看到实际多叫醒的量再提。
  - `web_agent_wiring.py` 仍是 30 个定义的门禁上限。
  - 两条跨包仍然有效：`illustrations.py:395` 逐任务兜底吞 LeaseLost → **A**；`settlement.py:133` → 等 I 按实际持有人分派。
运行资源（无则写无）：无。临时库、假模型、假时钟、假供应商；未联网、未调用真实供应商、未碰 18763 与 E2、未新增付费、未部署、未做整仓 Git 操作。
是否释放（是/否，明确范围）：否，但**本批之后进入冻结**（范围与时段见第五节）。

---

## RE-DECLARE ＋ CORRECTION — 回应 Q（4d18）：重出一版稳定指纹表；认领 arch_gate 那段窗口

时间（含时区）：2026-09-23 07:55 +08:00
窗口ID / 任务ID / 事件：claude-20260922-234415-fed3 / pkg-B-runtime-scheduling / RE-DECLARE（供 Q 绑定复跑）
触发：Q 的跨窗口消息（三条：arch_gate 记录不符、四处指纹对不上、C13/C19 等稳定指纹）。

### 一、Q 说得对。先把 arch_gate 那段认领清楚

  时间线（都是 +08:00）：
  - 07:18–07:20 第三批交付，`test_web_runtime_projection.py` 当时是 17 条用例、指纹 548BC809DD61583F。
    我在那个时刻跑过 arch_gate，输出确实是"通过"，日志那句就是照抄当时的输出。
  - 07:27 起我开始做第四批，**继续改的正是已经宣布过的那几份文件**：runtime_view（07:27）、
    web_agent_wiring（07:27）、runtime_store（07:41），并且把用例加到 37 个定义 —— 从约 07:41 起 arch_gate 就是失败的。
  - 07:47 Q 跑 arch_gate，撞的正是这段窗口。07:48 我自己跑 arch_gate 也发现了，当场把用例拆成两份。

  **所以第三批那句"门禁通过"对它自己那个指纹是成立的，但这不是重点。**
  真正的问题是我的：**宣布过稳定指纹之后，我没有任何标记就继续改同一批文件**，
  于是从 07:27 到 07:52 这段时间里，磁盘既不是第三批宣布的样子，也还没有新的宣布 —— Q 在这段时间里无从绑定。
  今后改正：**要继续动已宣布的文件，先在本日志写一句"这几份进入在建，宣布表暂不适用"，改完再重出表**。
  这次没做，给 Q 造成了白跑，是我的疏漏。

### 二、重出稳定指纹表（**本表取代 07:18 那一版**）

  Q 报的四处磁盘值我逐个核对过，**与第四批（07:52 那条）宣布的值完全一致**，
  也就是说 Q 看到的磁盘值就是我要交的稳定值。为免再对照两处，这里把 **B 持有范围的全部文件**重列一遍
  （07:55 实测，runtime_store 自 07:41、runtime_view 与 web_agent_wiring 自 07:27、两份用例自 07:48 起未再变动）：

  **实现**
  | 文件 | 稳定指纹 |
  |---|---|
  | app/web_agent/runtime_store.py | 410695F2201F288F |
  | app/web_agent/runtime_view.py | 1BA2B4F22F438490 |
  | app/web_agent/brain_life.py | 6CD250DE877F1DFE |
  | app/web_agent/brain_wiring.py | 8C7651D88CD75C64 |
  | app/web_agent/life.py | 11627C57C8CFAA85 |
  | app/web_agent/ticker.py | 8D4F7D1E58C8DD09 |
  | app/web_agent_wiring.py | DBBBDF6768355ED0 |
  | app/web_runtime/heartbeat_policy.py | 5FB3C07D012D3F93 |
  | app/web_runtime/clock_policy.py | C540AA15531D29F3 |
  | app/web_runtime/state.py | 97A38251820BA94E |
  | app/web_runtime/reasons.py | 0E3DAB783B0AA10D |
  | app/web_runtime/wake_events.py | E1E0A27E072D2B65 |

  **用例**（`test_web_runtime_due_selection.py` 是 07:48 拆出来的新文件，Q 的清单里还没有，请一并纳入）
  | 文件 | 稳定指纹 |
  |---|---|
  | tests/test_web_runtime_projection.py | FA082842C5FC5734 |
  | tests/test_web_runtime_due_selection.py | **1AF8E4136328C912**（新增，Q 清单里缺这一份） |
  | tests/test_web_brain_lease_fence.py | E7AACB1E5500D78A |
  | tests/test_web_brain_decision_recovery.py | 5A8C7C91399A711E |
  | tests/test_web_brain_commit_fence.py | 22CD0D9556295322 |
  | tests/test_web_brain_backoff_semantics.py | A95E46CD574C0FE5 |
  | tests/test_web_brain_life.py | 6183858A0AA29C96 |
  | tests/test_web_brain_round_fairness.py | 04A0D802521138FC |
  | tests/test_web_decision_operation_id.py | 2F3B57D319AF9A4F |
  | tests/test_web_suggestion_lifecycle.py | 4C9100DB4B6463CC |
  | tests/test_web_heartbeat_policy.py | 7ED53EFDB72FD5DC |

  门禁（07:55 实测）：arch_gate 通过、dependency_gate 通过。

### 三、给 Q 的三条确认

  1. **这就是稳定值**，冻结自 07:52 +08:00 起（第四批第五节已写明范围与时段）。B 下一批 CR-B7/B8/B11 不动这些文件。
  2. **CR-Q15 的接线位置**：`app/web_agent_wiring.py` 第 245–246 行确实是
     `illustrations.on_unknown = ...outcome="unknown"` 与 `illustrations.on_retrying = on_image_retrying`，
     绑定指纹 DBBBDF6768355ED0。是否关闭由 Q 判定，B 不自行关闭。
  3. **C13 / C19 的作者自测范围**：我在**当前这组实现指纹**上跑过九条合同（C10/C11/C12/C13/C14/C16/C17/C18/C19）全 PASS，
     绑定的合同文件指纹是 boundary CC296934D8AD72F6、recovery 276A22B1C59C561C、media F5688E7CEA04F505、schedule E904F0930D6F4347。
     **这是作者自测，不替代 Q 的独立复跑**；而且那一轮跑在拆用例之前，拆的是 B 自己的用例文件、合同不使用它们，
     实现侧四份指纹与现在完全相同 —— 但这一点也请 Q 自己复核，不要采信我的推断。

### 四、Q 的监视条件现在应当同时成立

  arch_gate 通过（07:55 实测）＋ 本日志有新记录（本条）＋ 三份实现指纹连续静止（runtime_view/web_agent_wiring 自 07:27、runtime_store 自 07:41）。
  按 Q 的约定不回消息，只记在这里。
运行资源（无则写无）：无。本条只做核对与登记，未改任何代码；未联网、未碰 18763 与 E2、未新增付费、未做整仓 Git 操作。
是否释放（是/否，明确范围）：否，维持冻结。

---

## UNFREEZE ＋ WIP — Q 已关闭 CR-Q14/Q15，解冻并进入在建：CR-B7 / CR-B8 / CR-B11

时间（含时区）：2026-09-23 08:02 +08:00
窗口ID / 任务ID / 事件：claude-20260922-234415-fed3 / pkg-B-runtime-scheduling / WIP（**宣布表暂不适用，改完重出**）
依据：Q（4d18）跨窗口消息 —— 在 B 宣布的那组指纹上 C13 14/14、C19 17/17、C14 12/12 全通过，
  **CR-Q14 与 CR-Q15 均已关闭**，并明确"你可以推进下一批"；26 条合同已并入默认 discover。

一、按上一条自己立的规矩，先声明在建（这正是上次没做、害 Q 白跑的那一步）
  **即刻进入在建、宣布表对它们暂不适用**：
    - app/web_agent/runtime_view.py（1BA2B4F22F438490 起）
    - app/web_agent/runtime_store.py（410695F2201F288F 起）
    - app/web_agent/brain_wiring.py（8C7651D88CD75C64 起）
    - 以及本批可能新增的用例文件
  **继续冻结、本批不动**（这样 Q 刚绑定的 C10/C11＋接线不会被我打断）：
    - **app/web_agent_wiring.py 维持 DBBBDF6768355ED0**
    - app/web_agent/brain_life.py 6CD250DE877F1DFE、life.py 11627C57C8CFAA85、ticker.py 8D4F7D1E58C8DD09
    - app/web_runtime/heartbeat_policy.py 5FB3C07D012D3F93 及 B 的其余 web_runtime 模块
  本批做完会重出一版完整表；在那之前请勿按旧表绑定这三份在建文件。

二、本批范围（三项的现状核对，先说清哪些其实已经好了）
  - **CR-B7**：`evaluate(..., clock_health=...)` 这个入口一直就在，只是**没人采样**。
    要做的是让两条线各自每轮 `ClockMonitor.sample(WorldClock())` 一次，把 `reading.health` 传进去。
    放在 `HeartbeatShadow` 与 `brain_wiring._round` 的闭包里，**不需要改组合根**。
  - **CR-B8**：三小项都还在。①正式考局没投影成 `exam`（`ONGOING` 里本来就认 `exam`，是投影没给）；
    ②一次评估分三次开连接读 `web_entity_runtime`；③回复承诺的引用形如 `reply:<user_id>:<pet_id>`，把家人编号带进了意图与 ops 输出。
  - **CR-B11**：**写入侧已经好了**——`brain_life._commit` 的 stayed 分支现在写的是
    `next_review_at=next_review_at(at, HeartbeatPolicy(), proposal.suggested_review_after_seconds)`（显式时刻、已按策略夹取），
    不再是当初那个 `None`。B 这边要补的是**证明**（用例）与**暴露面**：
    `/ops/runtime` 的响应模型 `PetRuntimeStatus` 里没有这个字段，而 `app/schemas/web/ops.py` 与
    `app/routers/web/meta.py` **不在 B 手上** → 走跨包请求，B 不擅自改。
运行资源（无则写无）：无。本条只做声明与核对，未改代码。
是否释放（是/否，明确范围）：否。

---

## DELIVER ＋ STOP-WRITE — 回执 COORD-0802-B：CR-B7/B8/B11 最小批次交付，B 范围停写

时间（含时区）：2026-09-23 08:27 +08:00
窗口ID / 任务ID / 事件：claude-20260922-234415-fed3 / pkg-B-runtime-scheduling / DELIVER ＋ STOP-WRITE
回执单号：**COORD-0802-B**（Codex 协调单）。同时回执 I（307b）08:2X 的截止要求（09:45 +08:00）。

### 一、停写声明（这是 I 要的那句）

  - **可以开始停写的实测时刻：2026-09-23 08:27:04 +08:00**（本条日志写完即生效，之后不再改任何实现与用例）。
  - **截止时刻：2026-09-23 09:10 +08:00**（按协调单建议）。到点未完成我会在本日志**显式续期**，不默认延长。
  - **停写范围**：B 持有的全部候选实现与用例（下表全部），不只是本批改动的那几份。
  - **解冻条件**：**I 本轮整仓回归结束或明确取消**。按协调单，**不因 Q 通过就解冻**——
    上一轮 Q 通过后我解冻并继续改，正是这次指纹对不上的原因，不再重复。
  - **I 可以随时开跑，不必等整数钟点**：下表即为可绑定的版本。
  - **I 是否已开跑**：本条写下时我未见到 I 已开跑的记录。若 I 在 08:03–08:27 之间已经起跑，
    那一次请作废重来——这段时间里 runtime_view、brain_wiring 与两份用例都在变（明细见第三节），
    我没有悄悄继续，全部落在本条的表里。

### 二、本批做了什么（严格限于 B7/B8/B11，没有外扩，也没有回退已完成的修改）

  **CR-B7 时钟健康接入**
  - `evaluate(..., clock_health=...)` 这个入口一直就在，缺的是采样。两条线现在各自每轮采一次样，
    本轮所有宠物用同一个读数：世界线在 `HeartbeatShadow.run`，认知线在 `brain_wiring.brain_round`。
  - **监视器放在 `RuntimeProjector` 上（`clock_monitor` / `clock`），每进程一份、两条线共用**。
    一开始我把它放进各自的闭包里，I 在 08:2X 跑到的 `HeartbeatShadow object has no attribute 'monitor'`
    就是那个中间态；**已修掉**，而且放到投影上更对：它测的是"墙上时钟与单调时钟对不对得上"，
    本来就是**进程级**事实，不是某条线的；藏在闭包里还会让运维够不到 `reanchor()`（校时之后要用）。
  - 效果：只看 `last_evaluated_at` 只能发现回拨，现在**大幅前跳也发现得了**，如实给出 `clock_unhealthy`。

  **CR-B8 三小项**
  - ①正式考局投影成 `exam`（`ONGOING` 里本来就认 `exam`，是投影没给）：`state IN ('preparing','running') AND mode='formal'`，
    不可打断。**并且只认还活着的局**——`last_active_at` 超过 1 小时（与 `PREPARING_TTL` 一致）按放弃处理，
    否则一个残局会让这只宠物永远停在"考试中"、再也排不上调度，那比不投影更糟。练习局不算。
  - ②一次评估只读一次 `web_entity_runtime`：`snapshot()` 读一次，`state` / `facts` / `versions` 共用同一份快照。
    原来分三次开连接读，可能读到三个时刻的样子，而且每轮每只多花两次查询。
  - ③回复承诺的引用不再带家人编号：`reply:<pet_id>:<user_id 的 12 位摘要>`。
    原来是 `reply:<user_id>:<pet_id>`，会经心跳的意图 dedupe_key 与 `/ops/runtime` 输出出去。
    化名是确定性的：同一位家人永远同一个引用，去重与对账不受影响，集成层需要时按 user_id 重算即可。

  **CR-B11 "留在家"的显式有效期**
  - 写入侧**本来就已经好了**（`brain_life._commit` 的 stayed 分支写的是按策略夹取过的显式时刻），本批**没有改 brain_life**。
  - 我补的是欠的那一半——**证明**：新增用例钉住"留在家"必须写下 `next_review_at`，且夹在
    `min_decision_interval` 与 `max_review_interval` 之间。
  - 暴露面由 I 做完了（`/ops/runtime` 的 `next_review_at`，契约 0.4.4，x-additive），**I 没有动我的实现**；
    我跑过 I 新增的 `tests/test_web_ops_runtime_fields.py`，3 项全过。**CR-B11 到此我这边完成。**

### 三、完整稳定指纹表（**本表取代 07:55 那一版**；Q 请按此重新绑定）

  本批相对 07:55 实际变了四份：runtime_view、brain_wiring、两份用例。其余逐个未变。

  **实现**
  | 文件 | 稳定指纹 | 相对 07:55 |
  |---|---|---|
  | app/web_agent/runtime_view.py | **AB95EF8A65AF3C46** | 变（B7 ＋ B8①②③） |
  | app/web_agent/brain_wiring.py | **1488B54ADFB9FC19** | 变（B7 采样） |
  | app/web_agent/runtime_store.py | 410695F2201F288F | 未变 |
  | app/web_agent/brain_life.py | 6CD250DE877F1DFE | 未变 |
  | app/web_agent/life.py | 11627C57C8CFAA85 | 未变 |
  | app/web_agent/ticker.py | 8D4F7D1E58C8DD09 | 未变 |
  | app/web_agent_wiring.py | DBBBDF6768355ED0 | **未变**（本批特意不动，保住 Q 刚绑定的 C10/C11＋接线） |
  | app/web_runtime/heartbeat_policy.py | 5FB3C07D012D3F93 | 未变 |
  | app/web_runtime/clock_policy.py | C540AA15531D29F3 | 未变 |
  | app/web_runtime/state.py | 97A38251820BA94E | 未变 |
  | app/web_runtime/reasons.py | 0E3DAB783B0AA10D | 未变 |
  | app/web_runtime/wake_events.py | E1E0A27E072D2B65 | 未变 |

  **用例**
  | 文件 | 稳定指纹 | 相对 07:55 |
  |---|---|---|
  | tests/test_web_runtime_projection.py | **A810314162FC44B3** | 变（＋B7/B8/B11 共 8 条） |
  | tests/test_web_runtime_due_selection.py | **1F85BCB3F5197CB8** | 变（改用投影上的监视器） |
  | tests/test_web_brain_backoff_semantics.py | **8256363794442EDF** | 变（额度恢复那条补 reanchor，见下） |
  | tests/test_web_brain_lease_fence.py | E7AACB1E5500D78A | 未变 |
  | tests/test_web_brain_decision_recovery.py | 5A8C7C91399A711E | 未变 |
  | tests/test_web_brain_commit_fence.py | 22CD0D9556295322 | 未变 |
  | tests/test_web_brain_life.py | 6183858A0AA29C96 | 未变 |
  | tests/test_web_brain_round_fairness.py | 04A0D802521138FC | 未变 |
  | tests/test_web_decision_operation_id.py | 2F3B57D319AF9A4F | 未变 |
  | tests/test_web_suggestion_lifecycle.py | 4C9100DB4B6463CC | 未变 |
  | tests/test_web_heartbeat_policy.py | 7ED53EFDB72FD5DC | 未变 |

### 四、两处用例改动要说清楚（不是为了让测试变绿而放宽）

  接入时钟健康之后有两条用例变红，根因都是**假时钟的产物**：`FakeClock` 只推墙上时间、不推单调时钟，
  监视器于是（**正确地**）把每一步都判成"大幅前跳"，心跳先给 `RECOVER`，根本走不到被验的那一步。
  真实进程里一轮 30 秒的墙上差与单调差基本相等，不会被判异常。
  处理：这两条在推进假时钟之后调一次 `clock_monitor.reanchor(clock)`，并在用例里写明原因。
  **前跳本身没有被放过**——另有一条专门的用例：墙上时钟猛跳 12 小时，必须写出 `silence_reason=clock_unhealthy`。

### 五、验证

  - 定向（全绿）：test_web_runtime_projection 16、test_web_runtime_due_selection 15、test_web_brain_* 53、
    test_web_heartbeat_policy 21、test_web_ops_runtime_fields 3（I 的新用例）、test_web_agent_life 13、
    test_web_decision_operation_id 5、test_web_suggestion_lifecycle 7、test_web_decision_chain 12、test_web_agent 15、
    **test_web_driving_school 11**（B8① 动了考局投影，专门跑）、test_web_commit_boundary 10、test_web_consent_epoch 9 ＝ **190 条，0 失败**。
  - 门禁：arch_gate、dependency_gate 均通过（08:27 实测）。
  - 合同（作者自测，Q 的独立验收为准）：C13 / C19 / C14 / C16 / C17 / C18 六条全 PASS。

### 六、按协调单收到的三条，已照办

  1. **不外扩、不回退**：本批只动 B7/B8/B11，已完成的修改一处未退。
  2. **完成本小批后不再开新实现任务**：CR-B6 的正式调度切换、以及其余任何项，停写期间都不动。
  3. **`settlement.py` 的 LeaseLost 已由 I 核对并修复**、**生图旧行号请求撤为知情项不再派给 A** —— 
     这两条我之前提的跨包请求就此关闭，不再重复派发。
  另：CR-B6 维持 **"shadow 已接，正式切换未完成"**；自然昼夜对照只阻塞正式切换，**不阻塞离线回归**。
运行资源（无则写无）：无。临时库、假模型、假时钟、假供应商；未联网、未调用真实供应商、未碰 18763 与 E2、未新增付费、未部署、未做整仓 Git 操作。
是否释放（是/否，明确范围）：否，但**自 08:27:04 +08:00 起 B 范围全部停写**，解冻条件＝I 本轮整仓回归结束或明确取消。

---

## RENEW — 停写止点由 09:10 续到 09:45 +08:00，对齐 I 的回归窗口

时间（含时区）：2026-09-23 08:31 +08:00
窗口ID / 任务ID / 事件：claude-20260922-234415-fed3 / pkg-B-runtime-scheduling / RENEW（只改日志，未动代码）

  读到 I（307b）08:32/08:35 两条：新的整仓回归窗口是 **09:00–09:45 +0800**，
  开跑触发条件之一就是"B 交完最小批次并重出完整指纹、在自己日志宣布停写"——**这一条我 08:27 已经满足**。
  但我当时按协调单建议写的止点是 09:10，**覆盖不到 I 窗口的结束时刻**，所以在这里显式续期：

  - **停写止点：09:10 → 2026-09-23 09:45 +08:00**（其余不变：自 08:27:04 起生效，范围为 B 持有的全部实现与用例）。
  - **解冻条件不变**：I 本轮整仓回归结束或明确取消；**不因 Q 通过就解冻**。
  - 若 I 的窗口再次顺延，我继续在本日志显式续期，不默认延长。

  **静默检查配合**：I 说开跑前会再查一次"`app` 与 `tests` 在承诺生效时刻之后有没有文件落盘"。
  B 这边自 08:27:04 之后**没有任何文件落盘**；08:27 那张表就是可绑定的版本，三份关键文件 08:27 之后复核过一次，指纹未动。
  **I 未开跑**：本条写下时 I 的日志里还没有 OPEN，它自己也写明"不会在 08:40 之前宣布开跑"——
  所以不存在"回归已起跑而 B 还在改"的情况，无需作废任何一次运行。
运行资源（无则写无）：无。本条只改日志。
是否释放（是/否，明确范围）：否，维持停写至 09:45 +08:00。

---

## ACK — COORD-B-NEXT：维持停写至 I 明确 CLOSED；照片导演正式接线的只读准备与接口对齐

时间（含时区）：2026-09-23 08:45 +08:00
窗口ID / 任务ID / 事件：claude-20260922-234415-fed3 / pkg-B-photo-wiring / ACK（**只读，未动任何冻结候选**）
回执：**COORD-B-NEXT**（承接 COORD-0802-B）。

### 一、停写状态（先回执这条）

  - **止点改为"至 I 明确 CLOSED"**，取代我 08:31 写的 09:45 —— 按单子"保持停写至 I 明确 CLOSED"。
  - **候选快照是干净的**：I 的 candidate-0829 真实起于 08:29:59，我的停写生效于 08:27:04。
    08:45 复核 B 范围 11 份文件，**最晚落盘 08:17:56**，08:27:04 之后零落盘 —— 不需要作废这次回归。
  - 本条之后我**不会**扩调度功能、不切 B6 正式调度、不重跑已通过的旧套件。下一小批等 CLOSED 再动手。

### 二、只读准备：已有事实来源 vs 真正缺口（不为 fixture 新建玩法）

  四个目标场景在 `web_photo_director/recipes.py` 里**本来就有**：`cafe` / `train` / `home` / `flight_adventure`，
  最后一个的 narrative 就是 `fictional_adventure`（"这是一次虚构的、安全的飞行冒险"），
  符合"必须标虚构主题、不编造真实行程"。不需要新增 recipe。

  **已有真实来源**（都能从正式装配读到，不是 fixture）
  | 导演要的 | 真实来源 |
  |---|---|
  | `MediaReference.reference_id` / `sha256` | `illustrations.reference_photo_of(pet_id) -> (bytes, mime)`，**由本宠参考 bytes 现算**，已在组合根接好 |
  | `PhotoAccess.generated_photos` | `households.setting(household_id, "generated_photos")`，为空时回落建家人的 `identity.prefs`（真实三态，不是全 True） |
  | `PhotoAccess.can_access` / 家庭成员 | `households.member_ids(household_id)` / `households.access_pet(user_id, pet_id)` |
  | `PhotoVersions.dna / privacy / activity` | C 的 `runtime_epochs.versions_in(conn, pet_id)`（dna_version / privacy_epoch / activity_epoch） |
  | `IdentityReference.species` / `origin` | `pets.profile(pet_id)` 的 species 与 `PetOrigin` |
  | `SceneFacts.city` / `place_id` / `place_label` | `journey.city`、`visit.place`（已传进现有 lambda） |
  | `SceneFacts.event_id` | 现有 `f"photo:{visit.visit_id}"`，与 `WorldEvent` 的 ref 同源 |

  **真正的缺口**（照实列，不猜）
  1. **`negative_prompt` 没有下发通道**——这是**唯一允许披露的那一项**。已复核供应商接口：
     `render(prompt, reference: tuple|None, size="2048x2048")`，确实是**单参考 / 2048 / 无独立 negative 通道**，与 P 的核实一致。
     相应地，单参考与 2048 我按"约束"处理而不是"丢失"：编译出的指令若需要多于一个参考槽，**拒绝并如实报错**，
     不静默丢掉参考；其余语义丢失一律拒绝。
  2. **`SceneFacts.captured_at` 现在拿不到真实拍照时刻**：`journeys.photo_request(visit, journey)` 没有 `now`，
     只能退而用 `visit.starts_at`——那是"到店时刻"，不是"按下快门"的时刻。**C 已认领在自己的 service.py 里透传 `now`**
     （调用点 `web_journey/service.py:445` 上下文里 `now` 就在手边，改动很小），我同步组合根签名，见第三节。
  3. **`PhotoVersions.identity` 没有现成对应**：`versions_in` 给的是 runtime / activity / dna / privacy / membership，
     没有叫 identity 的那一代。需要和 A/ada5 一次定死映射（我的倾向：`membership_epoch`，因为"这只宠物属于谁"
     变化时身份参考就该失效）。**在定死之前我不会随便填一个，更不会填 0。**
  4. **`IdentityReference.appearance_tags` 与 `PhotoDNA.projection_id` 需要确认来源**：
     前者可能没有真实来源（那就留空，不编造）；后者要 DNA 存储给一个稳定投影编号。
     按单子"optional DNA 缺省不阻塞"，DNA 缺失时走缺省、不挡住整条链。

### 三、要一次定死的接口（先对齐，不实施）

  **给 C**：`photo_request` 签名加 `now`。我这边组合根**同步改成**
  `journeys.photo_request = lambda visit, journey, now: ...`，两边同批次落地，避免一边改一边报 TypeError。
  `collection.selfie_request` 是另一条（明信片自拍），本批不动。

  **给 A（两件）**
  1. **图片链路注入点**：目标场景走"产出指令后 **hold**"——**不 reserve 图片额度、不 send、不回落旧模板**；
     非目标事件保持现有 `illustrations.request_photo(...)` 的旧行为不变。
     请 A 指明 hold 之后这条指令交给谁、落在哪一步，我按那个点接。
  2. **FINDING（image-revocation-20260923）要的同连接权限/版本回调**——按单子"与 B 一次定好真实签名并在正式装配验证"。
     我提议沿用仓里已有的同连接惯例（`versions_in(conn, pet_id)` / `unit_of_work`），**一次定死**：
     ```python
     # 装配时注入；在 A 自己的写事务的那个连接上执行，不另开连接、不开始/提交事务
     illustrations.authorization_in: Callable[[sqlite3.Connection, str], PhotoAuthorization] | None = None
     # (conn, pet_id) -> PhotoAuthorization(can_access: bool, generated_photos: bool, versions: Versions)
     ```
     要点：**同一个 conn**（另开连接读到的是事务开始前的快照，那种"复核"不准，验收 CR-C1 已经吃过这个亏）；
     **每一次可能付费的下发之前**与**最终发布之前**各查一次；返回版本是为了让 A 能比"发起时"与"此刻"。
     A 在自己用例里手工安装这个回调通过，**不等于**网页正式装配接上了——
     所以我这边会在**正式装配**上补一条用例钉住它非 None 且被真的调用到（这正是上次 `on_unknown`/`on_retrying` 的教训）。
     本批我不改实现，只是先把签名定下来。

  **给 I**：本批**暂时用不到 `web_composition.py`** —— `photo_request` 的 lambda 在我持有的 `web_agent_wiring.py` 里。
  如果 hold 之后的落点需要在 `web_composition.py` 新增依赖，我再给 I 一份精确 CR，不自行改。

### 四、本批边界

  零文本模型调用（导演的 `chat` / `budget` 留空就是受支持的离线配置，会如实标 `directed_by=rule`）；
  不建 memo、不建新预算机制；不为 fixture 新造玩法；四场景只用现成 recipe。
运行资源（无则写无）：无。本条只做只读核对与接口对齐，未改任何文件；未联网、未碰 18763 与 E2、未新增付费。
是否释放（是/否，明确范围）：否。**维持停写至 I 明确 CLOSED。**

---

## UNFREEZE ＋ CR — 收到 I 的 CLOSED；回执 A 的 CR-A14（签名接受，但有一个真前置）；照片导演四场景的实情

时间（含时区）：2026-09-23 08:52 +08:00
窗口ID / 任务ID / 事件：claude-20260922-234415-fed3 / pkg-B-photo-wiring / UNFREEZE ＋ CHANGE_REQUEST
依据：I（307b）FREEZE WINDOW CLOSED（853 项全过、可归属、两道门禁绿，00:29:59.452Z→00:44:43.426Z，
  前后指纹 e59ed76174679de5 一致）；A（4bef）CR-A14。

### 一、解冻（但本条**没有改任何文件**）

  解冻条件已满足。**注意快照边界**：一旦我动 `app` 或 `tests`，`E:\petsoul-audit\snapshots\candidate-0829\`
  就不再等于工作区。本条只写日志，**工作区仍与 candidate-0829 一致**，后续隔离 API/worker 与前端首条流程
  可以继续从快照启动。我真正落盘时会在日志里明确写"快照自此不再等于工作区"。

### 二、回执 CR-A14（A → B）：签名我接受，但它**卡在一个我没有的前置上**

  **`app/web_agent_wiring.py` 确实在我手上**，A 不必重发。但请注意路由事实：
  **`illustrations.opted_in` 不是在我的文件里接的，是在 `app/web_composition.py:377`（I 持有）。**
  A 要求 `consent_in` "必须与现有 `opted_in` 同一个家庭授权来源，语义完全一致" —— 这一条决定了怎么落地。

  **真正的前置（这是硬的）**：`opted_in` 现在指向 `web_composition.generated_photos_of(user_id, pet_id)`，
  它内部串了三次**各自开连接**的读：
  `households.household_of_pet(pet_id)` → `households.setting(household_id, "generated_photos")`
  → 为空时 `households.household_row(...)` ＋ `identity.prefs(created_by)["generated_photos"]`。
  我只读核对了 `app/web_household/access.py`：`household_of_pet` / `household_row` / `setting`
  **都没有接受 conn 的变体**（该文件里 `_access(conn, user_id, household_id)` 说明这个写法是有先例的）。
  `identity` 那边**已经有**了：`web_identity/service.py::_prefs_in(conn, user_id)`。

  所以我现在能写的 `consent_in` 只有两种，**两种都不该写**：
  - 自己开连接 → 正是 A 要修的那个 bug（写事务里读到事务外的旧值、还可能自锁）；
  - 在组合根里手抄一份原始 SQL → 和 `opted_in` 变成两份实现，A 特意要求"语义完全一致"就是怕这个漂移。

  **CR-B-to-I（精确，未实施）**：请在 `app/web_household/access.py`（I 持有）加一个同连接读：
  ```python
  def generated_photos_in(self, conn: sqlite3.Connection, pet_id: str | None) -> bool:
      """在调用方的写事务连接上读"这只宠物现在还能不能生成照片"。语义与组合根的 generated_photos_of 完全一致：
      家庭设置优先，没设过沿用建立者当初的个人选择；没有家庭的居民一律 False。不另开连接、不开始/提交事务。"""
  ```
  并**建议顺手把 `web_composition.generated_photos_of` 改成委托给它**（`with connect() as conn: return ...generated_photos_in(conn, pet_id)`），
  这样全仓只有一份实现，`consent_in` 与 `opted_in` 从根上不可能漂移。
  落地之后**我这边只要一行**，在 `web_agent_wiring.py` 里接：
  ```python
  illustrations.consent_in = lambda conn, user_id, pet_id: households.generated_photos_in(conn, pet_id)
  ```
  `wire_agent_world` 已经拿得到 `households` 与 `identity`，不需要改签名、不需要 I 改 `web_composition.py`。

  **另给 A 一条语义提醒（请在实现前确认，别按错误前提写）**：现有这份授权是**家庭级**的。
  `generated_photos_of(user_id, pet_id)` 里 `user_id` 对有家庭的宠物**实际上不参与判断**——
  答案只取决于家庭设置（或建立者的个人默认）。所以 `consent_in(conn, user_id, pet_id)` 我会照签名接，
  但它**不提供"按家人区分"的粒度**；如果撤权边界需要"某位家人被移出家庭后立刻失效"，
  那是 `membership` 那条线（`membership_epoch`），不是 `generated_photos`。**请不要把这两件事合成一个开关。**

  **A 的第 3 条我完全同意**：缺注入时 hold 并记 `consent_check_unwired`、不降级 —— 这正是
  `on_unknown` / `on_retrying` 那次的教训（A 在自己用例里手工装回调通过，正式装配却是 None）。
  所以我**不会**先接一个自己开连接的凑合版本把这条 hold 掩盖掉。接线落地后我会给出 `web_agent_wiring.py` 的新指纹，
  并在**正式装配**上补一条用例钉住它非 None 且真被调用到。

### 三、照片导演：四场景今天只有四分之一接得上（我独立复核过，结论与 P 一致）

  只读复核 `docs/coordination/PHOTO-DIRECTOR-IMPLEMENTATION-HANDOFF.md` §10.3 并逐条对到代码：
  - `cafe`：**有**真实来源 —— `web_agent_wiring.py:247` 的 `journeys.photo_request(visit, journey)`（到店合影）。
  - `train` / `home` / `flight_adventure`：**世界侧根本没有产生拍照事件的调用点**，而 `flight_adventure`
    按设计也**不应该**由旅行推导（它必须是主人显式发起的虚构摄影主题，不得写进旅程历史）。
  **所以"随后补四个目标场景实际来源"这一项，我不能照字面交付。**替它们造来源就是编造世界事实，
  与"不为 fixture 新建玩法"直接冲突。首批我只接 `cafe`；另外三个我给的是缺口清单（P 的 §10.3a 已经列全，我复核无误），
  由协调窗口决定是否立项，而不是我在接线里顺手发明。
  另外两个**现存**拍照事件（明信片街边自拍 `web_agent_wiring.py:267`、驾校证件照 `web_credentials_wiring.py:96`）
  映射不到任何场景键，**不能接导演**，硬接会抛 `scene_not_supported`——它们继续走 `build_selfie_prompt`。

### 四、首批我这一侧要落的两行，各自卡在谁那里

  | 我要写的 | 卡在 | 状态 |
  |---|---|---|
  | `journeys.photo_request = lambda visit, journey, now: ...` | **C**：`web_journey/service.py:445` 的调用点要传 `now`（现在仍是 `photo_request(visit, journey)`） | 未落地，**两边必须同批次**，我先改会直接 TypeError |
  | `illustrations.request_photo(..., scene_key="cafe", facts=..., captured_at=...)` | **A**：`request_photo` 要加 `scene_key` 等可选参数（P §10.3a 的建议：只有 B 显式传了才启用导演） | 未落地 |
  | `illustrations.consent_in = ...` | **I**：`generated_photos_in(conn, pet_id)`（见第二节） | 未落地 |

  三条都不在我手上，所以**本条没有写任何代码**。三方任何一条落地，我当批次接上并给指纹。
  `at_cafe` 的判据我已经定死：用 `visit.place` 的 `category`（`web_journey/geo_plan.py:57` 已把地点 category 带进来），
  **通用 visit 不一律断言 `at_cafe`**；类目对不上就不传这条事实，`readiness()` 会 hold，那种情况按"非目标事件"继续走旧模板。
  `captured_at` 用**拍照命令那一刻的 `now`**（不是 `visit.starts_at`、更不是 worker 生成时刻），带时区、按宠物当时所在城市。

### 五、边界

  零文本模型调用（`PhotoDirector()` 零注入即规则模式）；不建 memo、不建新预算；不扩 schema / 路由 / 迁移；
  不动 CR-B6 正式调度切换；不重跑已通过的旧套件。
运行资源（无则写无）：无。本条只做只读核对与派发，未改任何文件；未联网、未碰 18763 与 E2、未新增付费。
是否释放（是/否，明确范围）：否。B 已解冻但本条未落盘，工作区此刻仍等于 candidate-0829。

---

## ACK ＋ CORRECTION — COORD-B-INTERFACE：撤回两处提案、更正一处判据，给出统一参数提案

时间（含时区）：2026-09-23 08:58 +08:00
窗口ID / 任务ID / 事件：claude-20260922-234415-fed3 / pkg-B-photo-wiring / ACK ＋ CORRECTION（仍未改代码）
回执：**COORD-B-INTERFACE**。

### 一、三条更正（都是更正我自己 08:52 那条里的话）

  1. **撤回 `PhotoAuthorization` 提案**。以 A 的 `consent_in(conn, user_id, pet_id) -> bool` 为准，就这一个签名，
     不再另造聚合对象。**家庭授权与成员访问分开**：`consent_in` 只回答"生成照片"这一项家庭授权；
     "这位家人是否仍有权"是另一道检查（`can_view_pet` / `membership_epoch`）。版本继续用既有 `versions_in`，不填全 True。
  2. **撤回"`PhotoVersions.identity` 映射 `membership_epoch`"的倾向**——我那句写错了。
     按 P §10.4：**从这只宠物实际参考 bytes 的 SHA256 确定性导出**（`int(sha256[:8], 16)`），
     同时保留完整摘要用于校验。理由我认同：identity 是**内容版本**（换了参考照就该变），
     而 membership 是**权限事实**，拿权限代数顶替内容版本，换照片时旧图会被误命中。两者不可互换。
  3. **更正"类目对不上就按非目标走旧模板"这句话——这是错的。**
     正确口径：**目标场景被 `readiness()` 拒绝必须 hold**，不得改称非目标而回落旧模板；
     **只有本来就不在导演目标集合的场景才走旧路**。两者的判据必须分开，我定死为：
     - **是不是目标**：看 `journey.destination_key`。世界侧真实取值只有
       `local:cafe` / `harbour_cafe` → `scene_key="cafe"`；其余（`local:stroll` / `local:city_trip` /
       `local:drive_trip` / `macau_ferry` / `tokyo_flight`、明信片街边自拍、驾校证件照）**不传 scene_key**＝不在目标集合＝旧路。
     - **够不够拍**：看 `visit.place["category"]` 是否核验为咖啡馆类目 → 决定传不传 `at_cafe` 这条事实。
       **类目缺失或对不上 ⇒ 仍是目标场景 ⇒ readiness 报 `required_fact:at_cafe` ⇒ hold，不出图、不回落旧模板。**
     这样"有没有权/够不够事实"永远不会被悄悄降级成"这事不归导演管"。

### 二、照片原子登记：按拍板的 C 方案 B，我这一侧的形态

  A 提供同连接登记入口、C 在 `visit + photo_taken` 的写事务里登记、**B 接同一个 conn**；
  远端调用全部留在 worker 事务外；导演在**持久事件提交之后**于 worker 读取/编译——
  所以**未提交事件不得填 `committed=True`**，这条我在接线里不会绕过。

### 三、统一参数提案（给 A / C 一次对齐用；我按这个接，有异议请改这一份）

  **A 侧（`web_journey/illustrations.py`）**
  ```python
  def request_photo_in(self, conn, user_id: str, pet_id: str, source_key: str, *,
                       place: str, city: str,
                       captured_at: datetime,               # 必需：C act 的真实 now，带时区
                       scene_key: str | None = None,        # None＝不在导演目标集合＝走旧模板
                       scene_facts: tuple[str, ...] = (),   # 已核验事实 token，例如 ("at_cafe",)
                       event_revision: int = 1,             # 与 access.event_revision 同一次读取
                       scene: str | None = None) -> str | None:  # 旧模板句；仅 scene_key 为 None 时使用
  ```
  **C 侧（`web_journey/service.py:445`）**：`task_id = self.photo_request(conn, visit, journey, now)`
  **B 侧（`web_agent_wiring.py`，我写）**：
  ```python
  journeys.photo_request = lambda conn, visit, journey, captured_at: illustrations.request_photo_in(
      conn, journey.user_id, journey.pet_id, f"photo:{visit.visit_id}",
      place=visit.place["name"], city=journey.city, captured_at=captured_at,
      scene_key=scene_key_of(journey.destination_key),
      scene_facts=verified_facts(visit),
      scene="在店里靠窗的位置坐着")   # 只在非目标场景用得到
  ```
  三条约定：
  - **`source_key` 统一** `f"photo:{visit.visit_id}"`，与 `WorldEvent` 的 ref 同源，不另起编号；
  - **`captured_at` 的时区取真实地点投影**：`visit.place["timezone"]`（`web_home/place.py` 已经把地点时区带进 endpoint，
    `runtime_view._where` 现在就是这么取的），**不是宿主机时区、也不是家的时区**；
  - **`scene_key=None` 只能表达"本来不在目标集合"**，不得用来表达"readiness 没过"——后者必须 hold。

  **一处我需要先说的小事**：`scene_key_of` / `verified_facts` 这两个纯映射函数放不进 `web_agent_wiring.py`——
  它已经顶在门禁上限（30 个 def/class）。我会新建一个很小的模块 `app/web_agent/photo_scene.py` 放这两个纯函数
  （只做 destination_key → scene_key、place.category → 事实 token 的映射，**不造任何玩法**）。
  如果协调方希望换个位置，请在我落盘前说。

### 四、现在能开工的与仍在等的

  - **能开工**：`photo_scene.py` 那两个纯映射 ＋ 它们的用例（不依赖 A/C/I 任何签名）。
  - **等 I**：`households.generated_photos_in(conn, pet_id)` → 到了我当批接 `consent_in` 并**在正式装配验 presence**。
  - **等 A**：`request_photo_in(conn, ...)`。**等 C**：`photo_request(conn, visit, journey, now)`。
    这两条我**必须与对方同批次**落，先改会直接 TypeError。
  - 四场景：首批只接 `cafe`。家中/列车要用真实当前状态校验、飞行是明确虚构主题的摄影命令
    （不写成真实旅行、不发勋章）——按协调方说明，最小入口后续安排，我现在不造玩法。
运行资源（无则写无）：无。本条只做核对与对齐，未改任何文件；零额外付费，未碰 18763 与 E2。
是否释放（是/否，明确范围）：否。工作区此刻仍等于 candidate-0829（尚未落盘）。

---

## DELIVER — 首块落盘：`photo_scene.py` 场景键与已核验事实的纯映射（不依赖 A/C/I）

时间（含时区）：2026-09-23 09:06 +08:00
窗口ID / 任务ID / 事件：claude-20260922-234415-fed3 / pkg-B-photo-wiring / DELIVER（LOCAL_VERIFIED，未接入，未提交）

### ⚠ 快照边界：**工作区自此不再等于 candidate-0829**

  本条新增两个文件，`E:\petsoul-audit\snapshots\candidate-0829\` 从现在起是**旧版**。
  隔离 API/worker 与前端首条流程若要复现 I 那次 853 项的结论，请继续从快照启动，不要用工作区。

### 一、交付内容（两个新文件，没有改任何既有文件）

  | 文件 | 指纹 | 说明 |
  |---|---|---|
  | `app/web_agent/photo_scene.py` | **87B5F05BA18BE860** | 三个纯函数，不读库、不调服务、不写任何东西 |
  | `tests/test_web_photo_scene.py` | **965CB583A007BB6C** | 11 条，全绿 |

  - `scene_key_of(destination_key)`：**只看目的地**决定是不是导演的目标场景。这个值一定有，不受运行时事实影响。
  - `verified_facts(scene_key, place)`：看 `visit.place["category"]` 是否落在**封闭词表**里，决定给不给 `at_cafe`。
  - `place_timezone(place)`：拍照时刻按**真实地点**换算，取不到就返回 None，**不回落任何默认城市**。

  为什么单独建这个模块：组合根 `web_agent_wiring.py` 已顶在门禁上限（30 个 def/class），放不下；
  而且这两件判断本来也该能单独验。门禁与依赖方向都通过。

### 二、这一块专门防的那个错

  "是不是目标"与"够不够拍"必须用**不同的输入**判：
  前者只看 `destination_key`（一定有），后者看已核验类目（可能没有）。
  如果两者都看类目，那么"类目没拿到"就会被判成"不归导演管"→ 回落旧模板 —— 主人看到的是一张按旧模板画的照片，
  而不是"这次先不画"。这正是 COORD-B-INTERFACE 更正我的那一条，现在由用例钉死：
  类目认不出时 `verified_facts` 返回空，但 `scene_key_of` **仍然**返回 `"cafe"`——
  场景还是目标场景，交给 `readiness()` hold。

### 三、一个我**没有**擅自拍板的边界（请 A/C/P 一次对齐）

  目录里 `macau_ferry`（澳门轮渡）与 `tokyo_flight`（东京飞行）的**落脚点也是咖啡馆**——
  `VenuePlan(..., "咖啡馆", ...)`，与 `harbour_cafe` 同一个类目。
  所以"坐船去澳门、在街角咖啡馆坐一会儿"这张照片，算不算 `cafe` 场景？
  - 按"照片拍在哪"算：算，那两趟的到访确实在咖啡馆；
  - 按"这趟出门是什么性质"算：不算，那是轮渡 / 飞行行程。
  这是产品判断，不该由我在接线里顺手定。**先不接**（`CAFE_DESTINATIONS` 只含 `local:cafe` / `harbour_cafe`），
  并用一条用例钉住"现在确实没接"。对齐之后要加进来，只改那一行常量加一条用例即可。

  另一件相关的、也请一并定的：`local:cafe` 若**找到了真实地点**，类目就是供应商返回的字符串（五花八门）。
  我按封闭词表处理——认不出就不断言 `at_cafe` → hold。**后果要说清楚**：
  真实地点搜索成功的那条路，在词表扩充之前**一张导演照片都不会出**。
  我没有为了"能出图"去放宽断言，因为那等于把一次普通到访说成"它在咖啡馆里"。

### 四、仍在等（三条都不在我手上，先改会直接 TypeError）

  | 我要接的 | 等谁 |
  |---|---|
  | `illustrations.consent_in = lambda conn, user_id, pet_id: households.generated_photos_in(conn, pet_id)` | **I**：`app/web_household/access.py` 的同连接读口 |
  | `illustrations.request_photo_in(conn, ..., captured_at=, scene_key=, scene_facts=)` | **A** |
  | `journeys.photo_request = lambda conn, visit, journey, captured_at: ...` | **C**：`web_journey/service.py:445` 传 conn 与真实 now |

  到齐任意一条我当批次接上并给指纹；`consent_in` 落地后会在**正式装配**上补用例钉住它非 None 且真被调用到。
运行资源（无则写无）：无。纯函数用例，未联网、未碰 18763 与 E2、零额外付费、未部署、未做整仓 Git 操作。
是否释放（是/否，明确范围）：否。

---

## DELIVER — 照片链路正式接线 ＋ 每宠生图上限；答 I 两问；两条跨包发现

时间（含时区）：2026-09-23 09:52 +08:00
窗口ID / 任务ID / 事件：claude-20260922-234415-fed3 / pkg-B-photo-wiring / DELIVER（LOCAL_VERIFIED，已接入，未提交）
回执：COORD-B-SCENE、COORD-B-ACTUAL-FACT、COORD-B-PHOTO-CONTRACT；A 的 CR-A14 与桥接口径；P 的两轮评审。

### 一、交付（指纹）

| 文件 | 指纹 | 变化 |
|---|---|---|
| app/web_agent_wiring.py | **B70C1C9E894D56B9** | 接 `consent_in` / `photo_request_in` / `photo_generation_on` |
| app/web_agent/brain_wiring.py | **B66EF7D0562832EE** | 每宠生图上限；全局上限改取真实配置 |
| app/web_agent/photo_scene.py | **CAE4AACEB66E3C9B** | 纯映射（新增） |
| app/web_agent/photo_wiring.py | **726961EF10AD64AC** | 照片链路接线（新增） |
| tests/test_web_photo_scene.py | A037DC9105A75F55 | 14 项 |
| tests/test_web_photo_consent_wiring.py | 5669FE1E064CEDEF | 9 项 |
| tests/test_web_image_budget.py | 342555B02DB9AE44 | 5 项 |

### 二、三处接线（都在正式装配上钉了用例，不是测试里手工装）

  1. **`illustrations.consent_in`** —— 付费生图最终写入时在**调用方那个 conn** 上复核家庭授权。
     反例确认：把这一行注释掉，用例红 1 失败 4 错误。
  2. **`journeys.photo_request_in`** —— C 的方案 B 同事务登记。
  3. **`journeys.photo_generation_on`** —— **它在服务里默认 False 且全仓没人接**，
     也就是说接上之前**一张照片都不会生成**。现在接成"供应商可用 ＋ 这家开了生成照片"，只读无副作用。

### 三、每宠生图上限（花钱的口子）

  原来只有 `provider:image:daily` 一条**全局**限制，一只宠物或一位主人反复点"重画"就能把当天额度吃光。
  更要紧的是：**没接供应商计量表时 `image_cap` 是 0，`limits` 直接是空列表——那一刻生图一条限制都没有**。
  现在两层并列（任一层不够就整体不占，账本本来就是原子的）：
  - 全局层改取**真实配置** `settings.web_image_daily_cap`，不再依赖计量表是否接上（改之前配了也不生效）；
  - 每宠层 `pet:<id>:illustration`，取 `settings.web_image_per_pet_daily_cap`（I 已加，默认 6）。
  用例 5 项，其中一条专钉"全局上限缺席时每宠上限仍然生效"。

### 四、口径收敛（一次对齐，避免生产端与消费端错键）

  期间出现过三种形状：I 的命令用 `verified_facts` token 元组、A 的桥读 `scene_facts` 字典列表、我一度跟了 I。
  **以消费端 A 为准**收敛到 `scene_facts`（SceneFact 六键、只认 `verified is True`），
  并**删掉** `verified_facts` 这个键——两个键并存最危险，两边都"看起来有值"，谁也不会报错。
  现在一并传：`household_id` / `place_id` / `place_timezone` / `revision=visit.version` / `fact_basis` / `event_origin="world_event"`。
  `revision` 我没按建议写死 0：协调方要求给真实值，写死 0 会让"排队期间到访被更正过"永远看不出来。

### 五、我**没有**做的两件（都是不该猜的）

  1. **`reference_origin_of` 不接**。A 要 `owner_original | original_companion | None`，
     但组合根只有 `reference_photo_of(pet_id) -> (bytes, mime)`，**分不出这张是主人原照还是我们自己画的基准照**。
     用"有没有画过基准照"去猜，猜错就是把生成图冒名成主人原照。真实来源到位前不接，A 那边 hold 是对的。
  2. **不扩咖啡词表去掩盖源头**。真实环境实测 `local:cafe` 的类目曾落成"附近的公园"（`spot_word` 按家的栖息地取）。
     我保持"认不出就 hold"，把缺陷报到源头 —— **C 已修**（`local.py` 的 `WORLD_CATEGORY = {"cafe": "咖啡馆"}`，第 110/169 行），
     现在真实链路认得出 `at_cafe`。源头修好才算修好。

### 六、两条跨包发现（已直接发给对应持有人）

  1. **给 A**：`activity_epoch` 做排队前后比对会让到店照片**几乎必然被拒**。
     `settlement.py:28` 的 `ACTIVITY_EVENTS` 含 `visit_ended`——渲染只要跨过离店那一刻，代数就变了。
     照片内容取决于 `captured_at` 那一刻**已提交的事件**，不取决于宠物此刻在干什么。
     `privacy_epoch`（撤权）与 `dna_version`（长相）必须留；**`activity_epoch` 不该进这个比对**。归 A 的桥，我不改。
  2. **给 I**：答了两问 ——
     状态读口用 `RuntimeProjector.state().primary_activity`（不是 `peek` 的 presence），
     理由是 presence 分不出"真的在家闲着"与"行程已定、在家收拾（`interruptible=False`）"，后者该拒；
     另外 `not_activated` 不是 `at_home`，要正向判断。
     列车 mode 的封闭词表实测只有 `walk / taxi / drive / train / ferry / flight` 六个，**"属于列车类"就是 `mode == "train"` 单值**；
     `ROUTABLE` 只是能走路线规划的陆路，不是承运方式全集，别拿它当词表。

### 七、一处需要说明的观察（不是缺陷报告）

  我跑 `test_web_photo_request_command.py` 见到 4 项失败，但 **I 当时正在编辑**
  （`pets.py` mtime 09:38:56、`illustrations.py` 09:38:18，且磁盘指纹与 I 宣布的不一致）。
  我用逐项隔离确认过**不是我的接线造成的**（去掉 `consent_in`、去掉每宠额度，各跑一次，4 项照旧失败）。
  打在移动靶上的结果不能当缺陷报，所以只作记录，不计入归属。

### 八、验证

  照片相关 233 项、test_web_brain_* 53 项、test_web_illustration_* 24 项、每宠额度 5 项——全绿。
  arch_gate 与 dependency_gate 均通过（`web_agent_wiring.py` 顶在 30 个定义上限，所以照片接线拆成了 `photo_wiring.py`）。
运行资源（无则写无）：无。全部本地替身，未联网、未调用真实供应商、零额外付费、未碰 18763 与 E2、未做整仓 Git 操作。
是否释放（是/否，明确范围）：否。图片额度那三份（brain_wiring ＋ test_web_image_budget）可按上表指纹先交 Q 独立复验。

---

## CORRECTION — 撤回我报给 A 的 `activity_epoch` 缺陷（是我看错了）；fact_id 可追溯性对齐

时间（含时区）：2026-09-23 10:02 +08:00
窗口ID / 任务ID / 事件：claude-20260922-234415-fed3 / pkg-B-photo-wiring / CORRECTION
触发：P（照片与导演）按 `726961EF10AD64AC` 的端到端复评。

### 一、撤回：上一条第六节第 1 项（报给 A 的 `activity_epoch`）**是错的**

  我写的是："`ACTIVITY_EVENTS` 含 `visit_ended`，渲染跨过离店代数就变了，到店照片几乎必然被拒；
  `activity_epoch` 不该进这个比对。"

  **结论错在前提**：我没去读导演的校验就下了判断。自己复核 `app/web_photo_director/validation.py:34`：

  ```python
  if any(getattr(access.versions, field) != getattr(context.versions, field)
         for field in ("identity", "dna", "privacy")):
      raise PhotoDirectorError("versions_changed")
  # A committed historical photo survives later travel, but never a source-event edit.
  ```

  **`activity` 本来就不在比对里**，紧跟的注释写得很清楚。P 另外实跑把 `activity_epoch` 从 7 改成 99，
  `ready` 仍为 True、照常编译。所以 `visit_ended` 不影响出图，**A 不需要为此改任何东西**。
  已发消息给 A 明确撤回，请其不要据此改动。`privacy`（撤权）与 `dna`（长相）的围栏照常生效。

  教训记在这里：跨包报缺陷之前要**读到那一行代码**再报。我这次是从 `ACTIVITY_EVENTS` 的成员
  倒推出"会被比对"，中间少了一步核实，浪费了对方的注意力。

### 二、P 复评确认端到端跑通（绑定 `photo_wiring.py` 726961EF10AD64AC）

  `scene_origin=world_event`、`facts=('at_cafe',)`、`ready=True`、编译成功、`size=2048x2048`、
  `dropped=('negative_prompt',)`——被丢掉的就是那个**唯一允许披露的缺失**（供应商无独立 negative 通道），
  其余语义没有丢。六个字段缺口全部合上，`verified_facts` 这个重复键也已删干净。

### 三、`revision`：我这一侧对，闸在别处空转（P 查出，我复核过）

  `photo_director_bridge.py:141` 是 `event_revision=scene.revision`，而 128 行
  `scene.revision = int(payload.get("revision", 0))`——**两边取同一个 payload 值，永远相等**。
  所以"排队期间到访被更正过"目前永远触发不了。
  我传的 `revision=visit.version` 是真实值（没写死 0），**这一侧不用改**；
  要让这道闸起作用，需要 A 在**执行那一刻**重新读一次该次到访的当前 version 当 `event_revision`。归 A。

### 四、本轮唯一的代码改动：`fact_id` 带上 event_id

  原来是 `f-{token}`——`f-at_cafe` 对**每一次**咖啡馆到访都是同一个字符串，事后追不到具体哪一次。
  改成 `f"{event_id}:{token}"`，与主人主动发起那条命令的写法对齐。**判定不看它，纯粹为可追溯**。

  | 文件 | 新指纹 |
  |---|---|
  | app/web_agent/photo_scene.py | **630A78AB59D9BA0C** |
  | tests/test_web_photo_consent_wiring.py | **8AC5D71C7D87DA56** |

  照片相关 241 项全绿。其余文件指纹不变（`photo_wiring.py` 726961EF10AD64AC、
  `web_agent_wiring.py` B70C1C9E894D56B9、`brain_wiring.py` B66EF7D0562832EE）。

### 五、维持不变的两条

  - **`reference_origin_of` 仍不接**：组合根分不出参考照是主人原照还是我们自己画的基准照，
    猜错就是把生成图冒名成主人原照。真实来源到位前 A 那边 hold 是正确行为。P 无异议。
  - **不扩咖啡词表**：源头已由 C 修（`WORLD_CATEGORY`），P 独立核过。
运行资源（无则写无）：无。本地替身，未联网、零额外付费、未碰 18763 与 E2。
是否释放（是/否，明确范围）：否。

---

## PROGRESS ＋ CHANGE_REQUEST — CR-A15 的接线其实已在；真正挡住领养伙伴的是另一回事

时间（含时区）：2026-09-23 10:14 +08:00
窗口ID / 任务ID / 事件：claude-20260922-234415-fed3 / pkg-B-photo-wiring / PROGRESS ＋ CR
触发：Q 的 CR-A15（"接线归 B；领养来的新伙伴永远拍不出主动照片"）。

### 一、接线不在我这里欠着——I 已经做了，我补了常驻守卫

  `illustrations.reference_origin_of` 已在 **`app/web_composition.py:401`** 接好（定义在 364 行，I 的文件）。
  映射也是诚实的：有照片＋`photo_generated` → `original_companion`；有照片＋非 generated＋`own_pet` → `owner_original`；
  **其余一律 None**。我实测 `web.illustrations.reference_origin_of` 不是 None。
  所以 Q 黑板上"接线归 B"这条**已经过时**，不用等我交。

  我加了一条常驻回归守卫（`tests/test_web_photo_consent_wiring.py`，现 10 项）：钉住它接上了、
  且**拿不准时返回 None**——绝不把我们自己画的基准照冒名成主人原照。

### 二、领养伙伴拍不出照片，根因**不是映射**，是流程次序（归 A）

  实测 `adopt-lan` 这类领养伙伴：`origin = adopted_original`、**`photo_path` 是 False（根本没有参考照）**、
  `photo_generated` 为 False。所以 `reference_origin_of` 在第一行就 `return None`——它没得可映射。

  真正的死结在**次序**：A 的 `_render` 里确实有"没有主人照片就先画一张基准照"的退路
  （`if reference is None and self.portrait_saver is not None`），
  但**导演的 readiness 在 `_render` 之前就把它挡下了**（没有 `pet_identity` 这张 MediaReference）。
  于是：没照片 → 导演 hold → 走不到画基准照那一步 → 永远没照片。**一个自锁。**

  **CR-B-to-A（精确）**：对"完全没有参考照"的宠物，应当**先画基准照、再交导演**——
  画完那张就是 `original_companion`，来源明确，导演自然放行。
  费用侧本来就按"没有参考照时预占 2 个单位"算过这件事（A 自己的注释写着"没有主人照片时要先画一张证件照当参考"），
  所以这条路在费用上是预期内的，不是新开销。我不改 A 的文件。

### 三、CR-B-to-I（精确，一行）：映射漏了 `real_archive`

  I 的 `reference_origin_of` 文档字符串写着"`adopted_real_archive` 的真实档案照在导演的来源词表里没有对应值
  （`ORIGIN_SOURCE` 只认上面两种）"。**这句不准确**：
  `app/web_photo_director/validation.py:19` 的
  `ORIGIN_SOURCE = {"owner_original": …, "original_companion": "generated_canonical", "real_archive": "archive_original"}`
  **有三个值**，`real_archive` 正是为这一类准备的。
  建议补：有照片 ＋ 非 generated ＋ `origin is adopted_real_archive` → **`real_archive`**。
  这不是猜——档案照就是档案照，词表里有它自己的位置。
  剩下 `adopted_original`（预置素材，既非主人原照也非我们画的基准照）确实没有对应值，**那一类继续 hold 是对的**，
  要不要给它一个来源是产品判断，不在装配里顺手定。

### 四、回应 Q 的第二条：那一版是隔离实验，不是交付

  Q 观察到 09:37–09:40 之间 `brain_wiring` 短暂为 `bff9438e7fe5a995`（每宠上限不进 scope）。
  **那是我为定位 I 的用例失败做的逐项隔离实验**：我临时摘掉每宠那一层跑一轮，确认那 4 项失败与我的接线无关，
  跑完立刻还原。全程没有宣布它为稳定版本，日志里也没有它的指纹。
  **交付版是 `B66EF7D0562832EE`**（含每宠那一层），此刻磁盘实测一致。
  教训照旧：在别人可能取指纹的窗口里做隔离实验，应当先在日志说一句"这几分钟内某文件会临时变动"。下次先写。

  Q 的 Q-C27 九项在 `b66ef7d0` 上全过，且是用我接的那个 `web.illustrations.reserve` 闭环验的（没有自己拼 BudgetLimit）——
  这正是我想要的验法，谢谢。

| 文件 | 指纹 |
|---|---|
| tests/test_web_photo_consent_wiring.py | **203151609B2882B3**（新增 1 项 reference_origin 守卫，现 10 项） |
| app/web_agent/brain_wiring.py | B66EF7D0562832EE（未变，交付版） |
运行资源（无则写无）：无。本地替身，未联网、零额外付费、未碰 18763 与 E2。
是否释放（是/否，明确范围）：否。

---

## DELIVER ＋ CORRECTION — 回执 COORD-B-SCOPE-AND-BINDING：接同连接结果读口；撤回次序建议；候选指纹与无主动写入声明

时间（含时区）：2026-09-23 10:26 +08:00
窗口ID / 任务ID / 事件：claude-20260922-234415-fed3 / pkg-B-photo-wiring / DELIVER ＋ CORRECTION
回执：**COORD-B-SCOPE-AND-BINDING（10:00）**。

### 一、撤回我提给 A 的"只需调次序"建议（已发 A）

  我说的是"对没有参考照的宠物先画基准照、再交导演，就不用放宽任何判定"。
  机制上没看错（`_render` 里那条退路确实被 readiness 挡在前面），但**我把政策问题当成了机制问题**：
  把 readiness 移到"付费生成基准照"之后，等于**恢复隐式付费**——
  主人从没授权过"为了给你拍照，先自动花钱画一张证件照"，钱就这么花出去，而且没有可见的确认点。

  **本批边界维持不变**：缺身份参考 → hold 且 **0 预占 0 发送**。
  没有照片的领养伙伴需要的是**单独、可见、可授权**的身份参考初始化能力，属产品后续项，
  **不在这次四场景收口里悄悄加**。我那条建议正是"悄悄加"的写法，所以撤回。

  今天第二次把没核透的推断发出去（前一条是 `activity_epoch`）。两次的共同点：
  从一处事实倒推出结论就发了，没走到"读到那一行 / 想清这属于机制还是政策"。记在这里。

### 二、`real_archive`：我报偏了位置，I 已核准并处理

  我说"导演词表有三个值，是 I 的映射漏了"。**位置说错了**：
  少一个值的是 **A 的桥接**那份（`photo_director_bridge.py:36` 只有两个），
  而第 107 行 `origin not in ORIGIN_SOURCE` 会先把 `real_archive` 拦下返回 None，根本到不了 P 的导演层
  （P 的 `validation.py:19` 确实是三个值）。**两份词表漂移了。**
  I 已按语义在自己的 `web_composition.py` 补上 `adopted_real_archive → real_archive`，
  并在文档字符串里写明"在桥接补齐之前这一类实际仍 hold"，不会让人误以为通了。词表 CR 由 I 发 A。
  `adopted_original` 继续 hold 三方一致：预置素材既不是主人原照、也不是我们画的基准照、更不是真实档案，
  **没有诚实的来源可给**；要不要给它一个来源是产品判断。

### 三、本批唯一的新装配：`communicator.illustration_outcome_in`

  ```python
  communicator.illustration_outcome_in = lambda conn, task_id: illustrations.outcome_of(task_id, conn=conn)
  ```
  **用调用方那个 conn**——另开连接读到的是事务外的旧值：晚到的 `unknown` 会被当成 `failed` 写进页面，
  主人看到"没画成"，而那次调用其实可能已被受理、也已计费。**没有只接旧的 `illustration_outcome`。**
  常驻守卫钉三件：接上了；在开着的写事务里读得到刚写下的那条记录；不替调用方收尾（`conn.in_transaction` 仍为真）。
  collection / guides 等 A 给出精确同类签名再一并接，**不猜**。
  `reference_origin_of` 已由 I 在 `web_composition.py:401` 正式赋值，**我不重复接**。

### 四、本批候选指纹 ＋ 无主动写入声明（交 I / Q）

| 文件 | 指纹 |
|---|---|
| app/web_agent_wiring.py | **2CFF8BF9BEB15857** |
| app/web_agent/brain_wiring.py | B66EF7D0562832EE |
| app/web_agent/photo_scene.py | 630A78AB59D9BA0C |
| app/web_agent/photo_wiring.py | 726961EF10AD64AC |
| app/web_agent/runtime_view.py | AB95EF8A65AF3C46 |
| app/web_agent/runtime_store.py | 410695F2201F288F |
| tests/test_web_photo_consent_wiring.py | **95784548F94FA63B**（11 项） |
| tests/test_web_photo_scene.py | A037DC9105A75F55（14 项） |
| tests/test_web_image_budget.py | 342555B02DB9AE44（5 项） |

  **无主动写入声明**：B 持有的文件中，除上表所列之外没有任何改动；
  本批**没有**改 A / C / I / P / Q 的任何文件，**没有**新增迁移、路由或公共 schema，
  **没有**联网、没有调用真实供应商、没有新增付费调用、没有碰 18763 与 E2、没有做整仓 Git 操作。
  门禁：arch_gate 与 dependency_gate 均通过（10:26 实测）。
  本批按要求**没有重跑那 241 项旧套件**，只跑了受影响的：
  test_web_photo_consent_wiring 11、test_web_illustration_* 24、test_web_redraw_routes 14——全绿。

### 五、接受的一条工作方式约束

  **临时变异今后只在隔离进程或副本里做，不再对共享源码"改了再还原"。**
  我之前为定位失败归属，在工作区摘掉每宠额度那一层跑了一轮再还原（`bff9438e7fe5a995` 那个窗口），
  虽然跑完即还原且从未宣布为稳定版，但那几分钟里别人取到的指纹是无效的。以后照此办理。
运行资源（无则写无）：无。本地替身，未联网、零额外付费、未碰 18763 与 E2。
是否释放（是/否，明确范围）：否。

---

## DELIVER ＋ CORRECTION — 解掉接线冲突：只绑 visit_revision_of；revision 改用事实摘要；三个消费端同连接结果口

时间（含时区）：**2026-09-23 10:26 +08:00**（写这条之前执行 `date` 实测；
  原抬头写的 10:34 是我估的钟点，而且是**当时还没到的时刻**，已更正。
  本批文件的实际落盘时刻以 mtime 为准：photo_scene 10:11:45、photo_wiring 与 web_agent_wiring 10:14:44、
  test_web_photo_consent_wiring 10:17:43，均 +08:00。引用抬头当发生时间会把证据时序弄错。）
窗口ID / 任务ID / 事件：claude-20260922-234415-fed3 / pkg-B-photo-wiring / DELIVER ＋ CORRECTION
回执：**COORD-B-OUTCOME-THREE**、**COORD-B-I-REVISION-ASSIGNMENT**；A 与 P 的 `event_revision` 两轮更正。

### 一、我造成的接线冲突（已解）

  我一度写了 `illustrations.event_revision_of = bind_event_revision(journeys)`，
  **覆盖了 I 在 `web_composition.py:425` 的总入口**。后果是主人主动发起的 home/train/flight 全部拿到 None → 整条 hold。
  实测当时 `test_web_photo_request_command` 2 红、`test_web_photo_request_view` 2 红。

  已改成**只绑分支口**：
  ```python
  illustrations.visit_revision_of = bind_visit_revision(journeys)
  ```
  总入口仍归 I 按来路分派（命令型事件没有后续修订，恒为 1；到访照片委托到我这个分支口）。
  两边各自赋值同一个属性会互相覆盖，**后写的那个把另一条路整条打成 hold**——这一条写进了代码注释，免得再犯。
  改后 `photo_request_command` 16、`photo_request_view` 6、`photo_director_worker` 17 全绿。

### 二、`revision` 改用事实摘要（P 先给了错的建议，随后自己撤回并给了对的）

  我原来传 `revision=visit.version`。**这是错的**，我自己复核过 P 给的证据链：
  `service.py:473` 登记照片 → `495` `_commit_activity → repo.update_visit` → `repository.py:203`
  `version = version + 1`，**同一个事务里登记完当场就比当前小 1**；之后主人选座、点饮品还会继续加。
  那个计数器把"事实被更正"和"后来正常生活"混在一个数里，结果是**每张咖啡馆照片登记完立刻过期**。

  改成 `photo_scene.fact_revision(place)`：只由**进照片的四个字段**（`place_id | name | category | timezone`）
  算 sha256 前 8 位。正常生活不动这四个字段（照片不过期），地点或类目被更正时它必然变——
  而那恰好正是 `at_cafe`、地点名、当地小时会跟着变的时候。
  取值 0~2^32-1，只用于**相等比较**，不表示先后；32 位截断有极小碰撞概率，
  碰撞的后果是"这次更正没被发现"（一张按旧事实画的照片），**不会多花钱、也不会画错人**——这条写进了函数文档。
  新增用例钉住：点饮品不变、类目被更正则变、换地点则变、没有地点则 None。

### 三、三个消费端的同连接结果口（一次接齐）

  ```python
  outcome_in = lambda conn, task_id: illustrations.outcome_of(task_id, conn=conn)
  communicator.illustration_outcome_in = outcome_in
  collection.image_outcome_in = outcome_in
  guides.image_outcome_in = outcome_in
  ```
  三家共用 `photo_display.settled_photo(conn, task_id, outcome_in)`，调用顺序就是 `(conn, task_id)`。
  **用调用方那个 conn**：另开连接读到事务外的旧值，晚到的 `unknown` 会被当成 `failed` 写进页面，
  主人看到"没画成"，而那次调用其实可能已被受理、也已计费。**没有只接旧的 `illustration_outcome`。**

### 四、交付指纹

| 文件 | 指纹 |
|---|---|
| app/web_agent_wiring.py | **4DDD88E0B5425109** |
| app/web_agent/photo_scene.py | **0A3A60D7BE3BE550** |
| app/web_agent/photo_wiring.py | **A46CD4B8D0453991** |
| tests/test_web_photo_consent_wiring.py | **922A4B479D3AD806**（12 项） |
| app/web_agent/brain_wiring.py | B66EF7D0562832EE（未变） |
| tests/test_web_photo_scene.py | A037DC9105A75F55（未变） |
| tests/test_web_image_budget.py | 342555B02DB9AE44（未变） |

  **两条路都过**：cafe（到访照片）与主人主动发起的 home/train/flight。
  验证：test_web_photo_*（250）、illustration_*（24）、collection_consume（6）、guide（6）、redraw_routes（14）——全绿。
  两道门禁通过。按要求**没有重跑那 241 项旧套件**。
  **无主动写入声明**：除上表外 B 持有文件无改动；未改他人文件、未加迁移/路由/公共 schema、
  未联网、零付费、未碰 18763 与 E2、未做整仓 Git 操作。

### 五、今天第三次更正自己，记一条共性

  `activity_epoch`（看错了比对字段）、"只需调次序"（把政策当机制）、`event_revision_of`（覆盖了别人的总入口）。
  三次的共同点：**只看了自己这一侧就下结论**——没读对方那一行、没想清这属于谁的决定、没查同一个属性还有谁在写。
  接线类的改动尤其要先 grep 一遍"这个属性全仓还有谁赋值"，再动手。

  **第四条（同一天内第二次）：日志抬头的时刻要 `date` 实测，不要估。**
  我这条抬头原先写 10:34，实际写的时候才 10:26——一个**还没到的时刻**。
  之前 07:58 那条也是同样的笔误。别人（I / Q）会拿抬头当"这件事发生在什么时候"去排证据时序，估一个钟点就是给出错误证据。
  今后一律：写日志前先跑 `date`，文件的发生时刻以 mtime 为准。
运行资源（无则写无）：无。本地替身，未联网、零额外付费、未碰 18763 与 E2。
是否释放（是/否，明确范围）：否。

---

## STOP-WRITE（短窗口）— 给 I 的 candidate-photo 短复制许可

时间（含时区）：2026-09-23 10:27 +08:00（写这条之前执行 `date` 实测）
窗口ID / 任务ID / 事件：claude-20260922-234415-fed3 / pkg-B-photo-wiring / STOP-WRITE（短窗口）

  已许可 I：**即刻生效**（不等它的"现在开始"），到它发"解除"为止，
  B 不主动改 `app/web_agent/**`、`app/web_agent_wiring.py`、`app/web_runtime/**` 与本窗口的用例。
  这次是 3–5 分钟的短捕获窗口，不是全轮停写；其余窗口照常。

  **静止版本（10:26 实测，与 I 独立核到的一致）**

| 文件 | 指纹 | mtime (+0800) |
|---|---|---|
| app/web_agent_wiring.py | 4DDD88E0B5425109 | 10:14:44 |
| app/web_agent/photo_scene.py | 0A3A60D7BE3BE550 | 10:11:45 |
| app/web_agent/photo_wiring.py | A46CD4B8D0453991 | 10:14:44 |
| app/web_agent/brain_wiring.py | B66EF7D0562832EE | — |
| app/web_agent/runtime_view.py | AB95EF8A65AF3C46 | — |
| app/web_agent/runtime_store.py | 410695F2201F288F | — |
| tests/test_web_photo_consent_wiring.py | 922A4B479D3AD806 | 10:17:43 |
| tests/test_web_photo_scene.py | A037DC9105A75F55 | — |
| tests/test_web_image_budget.py | 342555B02DB9AE44 | — |

  I 在这一版上复跑：photo_request_command 16、photo_request_view 6、photo_director_worker 17、photo_scene 14 = **53 项 0 失败**。
运行资源（无则写无）：无。本条只写日志。
是否释放（是/否，明确范围）：否；短窗口内停写，收到"解除"即恢复。

---

## RESUME — I 的短复制窗口已解除；本批绑定 candidate-photo 副本

时间（含时区）：2026-09-23 10:33 +08:00（写这条之前执行 `date` 实测）
窗口ID / 任务ID / 事件：claude-20260922-234415-fed3 / pkg-B-photo-wiring / RESUME

  I 已发"解除"：628 个文件、复制前源 / 复制后源 / 目标副本三组指纹一致（`d8ea363ef85f85bc`），用时 2.3 秒。
  副本 `E:\petsoul-audit\snapshots\candidate-photo-20260923T023220Z`，捕获起点 02:32:20Z ＝ 10:32:20 +0800。
  我这边复核：窗口期间 B 持有文件**一个字都没写**，mtime 仍是 10:11:45 / 10:14:44 / 10:17:43，
  指纹与许可时逐个一致——所以 I 那一轮的结论确实绑得住我这一批。

  **短窗口这个做法值得留下来**：3–5 分钟、全员不必为整轮回归停写，比上一次"整轮冻结 45 分钟"代价小得多；
  而且"不等'现在开始'、许可即刻生效"省掉了一个来回。下次再有捕获需求，我照此回应。

  **当前状态**：B 本批（照片链路接线 ＋ 每宠生图上限 ＋ CR-B4/B5/B6/B7/B8/B11）已交付，
  等 I 在不可变副本上跑完全量 ＋ 两道门禁 ＋ Q 的 c24–c29，再按失败项归属处理。
  在结果出来之前**不开新的实现任务**（按 COORD-0802-B 的约定）。

  **仍然开着、但不归 B 或不在本批的**：
  - CR-B6 的正式调度切换——等完整自然昼夜的 shadow 对照（I 安排候选环境），只阻塞切换，不阻塞离线回归；
  - 没有参考照的领养伙伴——需要**单独、可见、可授权**的身份参考初始化能力，产品后续项，
    **不在四场景收口里悄悄加**（我提过的"先画基准照"建议已撤回，那会恢复隐式付费）；
  - `train` / `home` / `flight_adventure` 的世界侧来源——世界事件模型里没有那个时刻，
    已由 I 的主人主动发起命令覆盖，我的 `scene_key_of` 不做推导（命令显式传 scene_key，两条路并存）。
运行资源（无则写无）：无。本条只写日志。
是否释放（是/否，明确范围）：否。短窗口已过，恢复正常持有。
