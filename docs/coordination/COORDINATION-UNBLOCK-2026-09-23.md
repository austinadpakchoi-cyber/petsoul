# 各窗口等待关系与收口建议

核查时点：2026-09-23 07:55 +0800；07:57 补入 Q/I 最新交接并核对 Q 的两份证据。记录者：codex-20260922-product-refresh-c84a。

本文件回应用户“检查互相等待，判断是否需要调整方向”的请求。它是可供用户和 I 执行的建议，**不是已发送的派单，不替代各窗口回执，不改变文件所有权，也不宣布共同冻结已经开始**。

## 已核实的变化

- **B 的旧门禁阻塞已解除。** 本窗口实际运行 `PYTHONIOENCODING=utf-8 python -B scripts/arch_gate.py`，退出码 0；核对下表十份文件，与 B 最新 RE-DECLARE 一致。Q 可以按最新候选继续，不能再让 B 重修旧的 37 定义问题。其余文件由 Q/I 在自己的完整清单中核对。
- **Q 已完成 A/B 稳定收口。** `contracts-A-stable-20260923T0745.json` 与 `contracts-B-stable-20260923T0755.json` 实际读取均为 `overall=PASS`。Q 最新 HANDOFF 关闭 CR-Q14、CR-Q15，默认测试为 27 项（1 自测 + 26 合同）。先前“Q 仍等 B 指纹”的建议已被这条取代。合同通过不等于整个后端验收完成。
- **P 已承诺 08:00–08:40 +0800 暂停写入其列出的实现、测试、fixture、工具和规格。** 这是 P 的新承诺，不等于旧的已撤销共同窗口自动恢复。
- **I 已声明路由等 13 份稳定，并追加 settlement 实现和测试，现为 15 份。** I 已自行认领并修复 settlement 租约异常尾项。其最新段落仍称“B 没更新稳定表”，已落后于 B 的 RE-DECLARE 和 Q 的通过交接，需刷新。
- **C 明确允许 Q 独立推进。** C 的验证工具补清单不再是 Q 的前置任务；C 当前待命不表示卡死。

| B 文件 | 本次实际读取的 SHA-256 前 16 位 |
|---|---|
| `app/web_agent/runtime_store.py` | `410695f2201f288f` |
| `app/web_agent/runtime_view.py` | `1ba2b4f22f438490` |
| `app/web_agent/brain_life.py` | `6cd250de877f1dfe` |
| `app/web_agent/brain_wiring.py` | `8c7651d88cd75c64` |
| `app/web_agent/life.py` | `11627c57c8cfaa85` |
| `app/web_agent/ticker.py` | `8d4f7d1e58c8dd09` |
| `app/web_agent_wiring.py` | `dbbbdf6768355ed0` |
| `app/web_runtime/heartbeat_policy.py` | `5fb3c07d012d3f93` |
| `tests/test_web_runtime_projection.py` | `fa082842c5fc5734` |
| `tests/test_web_runtime_due_selection.py` | `1af8e4136328c912` |

上述路径相对 `PetJourneyBackend/`。这是当前内容核对，不补作早先运行的版本证据。

## 各窗口下一步

| 窗口 | 等待是否必要 | 建议的具体动作 |
|---|---|---|
| **I / 307b** | 等完整候选和共同停写区间合理；继续等旧门禁或旧 B 表不合理 | 收取 Q 已交的 26 条合同与最终测试清单，确认 B/P 新记录，安排有共同区间的整仓回归。使用含 settlement 的新候选，核对全部实际输入和各窗口停写承诺。 |
| **B / fed3** | 当前需要保持候选稳定，不再扩功能 | 最新表已可供复验。把冻结止点明确改为覆盖 Q 收口与 I 整仓运行，不能“Q 一通过就恢复改动”。补一个明确截止/续期规则。CR-B7/B8/B11 留下一批。 |
| **Q / 4d18** | 本轮 26 条已收齐，可以交 I；无需重跑这一批 | 保持最新合同文件稳定，修正“B 可立即推进下一批”的交接：仍须尊重 I 的整仓回归区间。另列尚未独立关闭的 CR-C9 券核销、CR-C10 围栏叠加、CR-C8 后续宠物/后续任务停止，核对是否已有覆盖，不重复造套件，也不凭本次 26 条自动关闭。需要新增测试则与 I 约定在全量之前或之后做。 |
| **A / 4bef** | 等 I 回归期间保持稳定合理 | Q 侧第一条件已满足，继续遵守“Q 完成且 I 结束”的双条件。不为照片导演的后续扩展提前改冻结文件。只处理经确认的具体残留，见下节。 |
| **C / 91f2** | 合理待命，不必制造新任务 | 当前实现及测试保持稳定，响应 Q 的具体失败即可。不要重复跑旧工具、盯目录或把补工具清单变成别人前置条件。若后续有新实现任务，仍需精确范围和原持有人释放。 |
| **P / ada5** | 真实出图的配置阻塞与代码冻结分开 | 保持四场景、同一只宠物、规则导演、零文本调用的首批目标。与 I 核对实际使用的供应商配置来源，当前进程环境变量为空不能证明项目从未配置过。按 P 窗口已确认的授权执行；不要回交用户填写报价、解释内部预算或索要明文密钥。真实成图和正式网页接入仍需完成，不把 159 项作者测试当作成图验收。 |
| **前端 r7k** | 不需要等全后端收口 | 继续已领取的家园/当前家庭和宠物上下文批次；优先交付浏览器里可连续操作的“入住→家园→出发→来信/照片”，再扩次要页面。使用自己的 18764/5291 环境，保持真实当前宠物身份，不用通用猫替代。 |

Q 对已通过且依赖未变化的撤权合同，不因 I 再次交接相同指纹而重复全套；新覆盖和受影响项另列。整轮停止按后续工作没有执行判定，允许 ticker 捕获后停止；已发出调用的费用事实必须保留。

## 两条旧租约请求需要纠正位置和状态

1. **`settlement.py` 不应再简单写“等 I 分派”。** 当前 `app/web_journey/settlement.py` 已有 `except LeaseLost: raise`，当前指纹 `c543c099b19b189f`；`test_web_settlement_chain.py` 已出现对应定向用例。I 最新交接也已确认归属、作者测试和进程内变异结果，并将两份文件补入候选。本次只读确认存在，没有运行该用例，不替 Q 宣布独立验收。B/Q 更新旧待办即可，不再按旧代码重复派单。
2. **生图侧旧行号已失效，原工单需要落实到实际异常链。** 当前 `IllustrationService.run_pending()` 调 `web_platform.tasks.run_once()`。本窗口用真实 `run_once`、内存队列和抛出真实 `LeaseLost` 的 handler 做了方法级检查：函数正常返回，并调用 `fail_claim(..., 'LeaseLost')`。没有数据库或供应商调用。这证明该方法会吞掉租约异常，**不证明当前独立生图 worker 实际持有什么进程围栏、也不证明线上已发生**。

建议 A（`tasks.py` 持有人）和 I 在原租约尾项下核对正式 worker 的调用及围栏范围：确实可达就先补正式路径反例，再定向修复；不在该路径的保护范围就写清边界，不凭旧行号扩成全局故障。若需要改候选，I 先协调运行窗口，A 不在正在执行的整仓回归中偷偷改。无需为这项另建重复工单或回收 A 的文件。

## 建议的收口顺序

1. **现在：** Q 最新交接已经完成本轮收口；I 接收候选并完成上述残留的归属/范围确认。**B 不因 Q 的“可以下一批”而改动当前候选。** A/C 保持稳定。前端照常推进；P 处理配置定位和既定规则出图准备。
2. **确认 Q 不再修改本轮合同后：** I 核实 I/A/B/C/Q/P 的实际停写范围与共同区间。Q 的默认纳入改动已交付，要以最终文件为准；测试文件会改变运行输入，不能当成冻结例外。
3. **共同区间内：** I 跑一次完整候选回归，保留完整输出、实现/测试/fixture/工具指纹与真实承诺。当前 08:00–08:40 只是 P 提供的可用区间；不够覆盖准备和运行就明确重约或续期，不能把单方承诺当作六方已就绪。
4. **完成后：** 结束本轮冻结，再按明确候选开始照片导演网页接入和同版本前后端/worker 连续运行。离线回归通过不能省略这一段。

CR-B6 的完整自然昼夜对照，只阻塞真实调度切换，不阻塞当前离线回归。模型导演的持久化备忘/并发恢复，只阻塞启用该文本模型路径，不阻塞首批规则导演。真实成图的密钥、费用与人审，不应让前端和现有后端合同一起等待。

## 证据与执行边界

读取的主要交接：I `WINDOW-claude-20260922-014933-307b.log.md`、A `...234337-4bef...`、B `...234415-fed3...`、C `...234408-91f2...`、Q `...234631-4d18...`、P `WINDOW-claude-20260923-055300-ada5.log.md`、r7k `WINDOW-codex-20260922-131657-petsoul-visual-r7k.log.md`，均位于 `docs/coordination/`。

本次实际读取的 Q 证据摘要：A-stable SHA-256 `14ab043080fc6973b04242e62fcd311c48420f74fdc58f43e161f33267f05022`；B-stable SHA-256 `3d3923cb12634702a6c4090656043fd41e69be324fe1bbd720ce9ea0a299eae3`。位于 `PetJourneyBackend/data/reviews/q-4d18-20260922/`。本窗口没有重跑 Q 的合同。

本轮仅做只读核对、一次架构门禁和一个无数据库的内存方法探针；初次门禁因 GBK 控制台打印警告符号报编码错误，设置 UTF-8 后退出码 0。未运行全套测试，未生成图片，未调用真实供应商，未动 18763/E2，未改业务源码或其他窗口文件，未关闭任何 CR，未创建新 agent，未向 Claude 窗口发送消息。

