# PetJourneyWeb 环境变量示例。VITE_* 会被打进客户端 bundle：这里只能放公开配置，禁止放任何密钥。
# 地图/模型/生图等供应商密钥只存在于后端 PetJourneyBackend 的环境变量中。

# 数据模式：fixture（显式演示数据，不访问后端）| live（只走 /api/v1/web，未接入的能力显示“尚未接入”，不混入 fixture）
VITE_PETSOUL_DATA_MODE=fixture

# API 基址：默认同源 /api/v1/web（生产由反向代理同源转发）。开发时由 Vite 代理到下面的本地后端。
VITE_PETSOUL_API_BASE=/api/v1/web

# 仅 vite dev/preview 使用（不进入 bundle）：本地 FastAPI 地址。默认 127.0.0.1:18761，避免与其他窗口的 8000 冲突。
PETSOUL_DEV_API_TARGET=http://127.0.0.1:18761
